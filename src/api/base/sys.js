/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/
import * as $protobuf from "protobufjs/minimal";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;

// Exported root namespace
const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const PlaceBean = $root.PlaceBean = (() => {

    /**
     * Properties of a PlaceBean.
     * @exports IPlaceBean
     * @interface IPlaceBean
     * @property {string|null} [id] PlaceBean id
     * @property {string|null} [name] PlaceBean name
     * @property {string|null} [address] PlaceBean address
     * @property {number|null} [lat] PlaceBean lat
     * @property {number|null} [lng] PlaceBean lng
     */

    /**
     * Constructs a new PlaceBean.
     * @exports PlaceBean
     * @classdesc Represents a PlaceBean.
     * @implements IPlaceBean
     * @constructor
     * @param {IPlaceBean=} [properties] Properties to set
     */
    function PlaceBean(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * PlaceBean id.
     * @member {string} id
     * @memberof PlaceBean
     * @instance
     */
    PlaceBean.prototype.id = "";

    /**
     * PlaceBean name.
     * @member {string} name
     * @memberof PlaceBean
     * @instance
     */
    PlaceBean.prototype.name = "";

    /**
     * PlaceBean address.
     * @member {string} address
     * @memberof PlaceBean
     * @instance
     */
    PlaceBean.prototype.address = "";

    /**
     * PlaceBean lat.
     * @member {number} lat
     * @memberof PlaceBean
     * @instance
     */
    PlaceBean.prototype.lat = 0;

    /**
     * PlaceBean lng.
     * @member {number} lng
     * @memberof PlaceBean
     * @instance
     */
    PlaceBean.prototype.lng = 0;

    /**
     * Creates a new PlaceBean instance using the specified properties.
     * @function create
     * @memberof PlaceBean
     * @static
     * @param {IPlaceBean=} [properties] Properties to set
     * @returns {PlaceBean} PlaceBean instance
     */
    PlaceBean.create = function create(properties) {
        return new PlaceBean(properties);
    };

    /**
     * Encodes the specified PlaceBean message. Does not implicitly {@link PlaceBean.verify|verify} messages.
     * @function encode
     * @memberof PlaceBean
     * @static
     * @param {IPlaceBean} message PlaceBean message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PlaceBean.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.id != null && Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
        if (message.name != null && Object.hasOwnProperty.call(message, "name"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
        if (message.address != null && Object.hasOwnProperty.call(message, "address"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.address);
        if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.lat);
        if (message.lng != null && Object.hasOwnProperty.call(message, "lng"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.lng);
        return writer;
    };

    /**
     * Encodes the specified PlaceBean message, length delimited. Does not implicitly {@link PlaceBean.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PlaceBean
     * @static
     * @param {IPlaceBean} message PlaceBean message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PlaceBean.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a PlaceBean message from the specified reader or buffer.
     * @function decode
     * @memberof PlaceBean
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PlaceBean} PlaceBean
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PlaceBean.decode = function decode(reader, length, error) {
    PlaceBean.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.PlaceBean();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.id = reader.string();
                    break;
                }
            case 2: {
                    message.name = reader.string();
                    break;
                }
            case 3: {
                    message.address = reader.string();
                    break;
                }
            case 4: {
                    message.lat = reader.int32();
                    break;
                }
            case 5: {
                    message.lng = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a PlaceBean message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PlaceBean
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PlaceBean} PlaceBean
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PlaceBean.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PlaceBean message.
     * @function verify
     * @memberof PlaceBean
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PlaceBean.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.id != null && message.hasOwnProperty("id"))
            if (!$util.isString(message.id))
                return "id: string expected";
        if (message.name != null && message.hasOwnProperty("name"))
            if (!$util.isString(message.name))
                return "name: string expected";
        if (message.address != null && message.hasOwnProperty("address"))
            if (!$util.isString(message.address))
                return "address: string expected";
        if (message.lat != null && message.hasOwnProperty("lat"))
            if (!$util.isInteger(message.lat))
                return "lat: integer expected";
        if (message.lng != null && message.hasOwnProperty("lng"))
            if (!$util.isInteger(message.lng))
                return "lng: integer expected";
        return null;
    };

    /**
     * Creates a PlaceBean message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PlaceBean
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PlaceBean} PlaceBean
     */
    PlaceBean.fromObject = function fromObject(object) {
        if (object instanceof $root.PlaceBean)
            return object;
        let message = new $root.PlaceBean();
        if (object.id != null)
            message.id = String(object.id);
        if (object.name != null)
            message.name = String(object.name);
        if (object.address != null)
            message.address = String(object.address);
        if (object.lat != null)
            message.lat = object.lat | 0;
        if (object.lng != null)
            message.lng = object.lng | 0;
        return message;
    };

    /**
     * Creates a plain object from a PlaceBean message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PlaceBean
     * @static
     * @param {PlaceBean} message PlaceBean
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PlaceBean.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.id = "";
            object.name = "";
            object.address = "";
            object.lat = 0;
            object.lng = 0;
        }
        if (message.id != null && message.hasOwnProperty("id"))
            object.id = message.id;
        if (message.name != null && message.hasOwnProperty("name"))
            object.name = message.name;
        if (message.address != null && message.hasOwnProperty("address"))
            object.address = message.address;
        if (message.lat != null && message.hasOwnProperty("lat"))
            object.lat = message.lat;
        if (message.lng != null && message.hasOwnProperty("lng"))
            object.lng = message.lng;
        return object;
    };

    /**
     * Converts this PlaceBean to JSON.
     * @function toJSON
     * @memberof PlaceBean
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PlaceBean.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PlaceBean
     * @function getTypeUrl
     * @memberof PlaceBean
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PlaceBean.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/PlaceBean";
    };

    return PlaceBean;
})();

export const RedPacketBase = $root.RedPacketBase = (() => {

    /**
     * Properties of a RedPacketBase.
     * @exports IRedPacketBase
     * @interface IRedPacketBase
     * @property {number|Long|null} [redPacketId] RedPacketBase redPacketId
     * @property {ICoinTypeBase|null} [coinTypeBase] RedPacketBase coinTypeBase
     * @property {string|null} [coin] RedPacketBase coin
     * @property {string|null} [desc] RedPacketBase desc
     * @property {number|null} [type] RedPacketBase type
     * @property {number|null} [count] RedPacketBase count
     * @property {number|Long|null} [expiredTime] RedPacketBase expiredTime
     * @property {IUserBase|null} [userBase] RedPacketBase userBase
     * @property {number|null} [status] RedPacketBase status
     * @property {number|Long|null} [groupId] RedPacketBase groupId
     */

    /**
     * Constructs a new RedPacketBase.
     * @exports RedPacketBase
     * @classdesc Represents a RedPacketBase.
     * @implements IRedPacketBase
     * @constructor
     * @param {IRedPacketBase=} [properties] Properties to set
     */
    function RedPacketBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * RedPacketBase redPacketId.
     * @member {number|Long} redPacketId
     * @memberof RedPacketBase
     * @instance
     */
    RedPacketBase.prototype.redPacketId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * RedPacketBase coinTypeBase.
     * @member {ICoinTypeBase|null|undefined} coinTypeBase
     * @memberof RedPacketBase
     * @instance
     */
    RedPacketBase.prototype.coinTypeBase = null;

    /**
     * RedPacketBase coin.
     * @member {string} coin
     * @memberof RedPacketBase
     * @instance
     */
    RedPacketBase.prototype.coin = "";

    /**
     * RedPacketBase desc.
     * @member {string} desc
     * @memberof RedPacketBase
     * @instance
     */
    RedPacketBase.prototype.desc = "";

    /**
     * RedPacketBase type.
     * @member {number} type
     * @memberof RedPacketBase
     * @instance
     */
    RedPacketBase.prototype.type = 0;

    /**
     * RedPacketBase count.
     * @member {number} count
     * @memberof RedPacketBase
     * @instance
     */
    RedPacketBase.prototype.count = 0;

    /**
     * RedPacketBase expiredTime.
     * @member {number|Long} expiredTime
     * @memberof RedPacketBase
     * @instance
     */
    RedPacketBase.prototype.expiredTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * RedPacketBase userBase.
     * @member {IUserBase|null|undefined} userBase
     * @memberof RedPacketBase
     * @instance
     */
    RedPacketBase.prototype.userBase = null;

    /**
     * RedPacketBase status.
     * @member {number} status
     * @memberof RedPacketBase
     * @instance
     */
    RedPacketBase.prototype.status = 0;

    /**
     * RedPacketBase groupId.
     * @member {number|Long} groupId
     * @memberof RedPacketBase
     * @instance
     */
    RedPacketBase.prototype.groupId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * Creates a new RedPacketBase instance using the specified properties.
     * @function create
     * @memberof RedPacketBase
     * @static
     * @param {IRedPacketBase=} [properties] Properties to set
     * @returns {RedPacketBase} RedPacketBase instance
     */
    RedPacketBase.create = function create(properties) {
        return new RedPacketBase(properties);
    };

    /**
     * Encodes the specified RedPacketBase message. Does not implicitly {@link RedPacketBase.verify|verify} messages.
     * @function encode
     * @memberof RedPacketBase
     * @static
     * @param {IRedPacketBase} message RedPacketBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.redPacketId != null && Object.hasOwnProperty.call(message, "redPacketId"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.redPacketId);
        if (message.coinTypeBase != null && Object.hasOwnProperty.call(message, "coinTypeBase"))
            $root.CoinTypeBase.encode(message.coinTypeBase, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
        if (message.coin != null && Object.hasOwnProperty.call(message, "coin"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.coin);
        if (message.desc != null && Object.hasOwnProperty.call(message, "desc"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.desc);
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.type);
        if (message.count != null && Object.hasOwnProperty.call(message, "count"))
            writer.uint32(/* id 6, wireType 0 =*/48).int32(message.count);
        if (message.expiredTime != null && Object.hasOwnProperty.call(message, "expiredTime"))
            writer.uint32(/* id 7, wireType 0 =*/56).int64(message.expiredTime);
        if (message.userBase != null && Object.hasOwnProperty.call(message, "userBase"))
            $root.UserBase.encode(message.userBase, writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
        if (message.status != null && Object.hasOwnProperty.call(message, "status"))
            writer.uint32(/* id 9, wireType 0 =*/72).int32(message.status);
        if (message.groupId != null && Object.hasOwnProperty.call(message, "groupId"))
            writer.uint32(/* id 10, wireType 0 =*/80).int64(message.groupId);
        return writer;
    };

    /**
     * Encodes the specified RedPacketBase message, length delimited. Does not implicitly {@link RedPacketBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof RedPacketBase
     * @static
     * @param {IRedPacketBase} message RedPacketBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a RedPacketBase message from the specified reader or buffer.
     * @function decode
     * @memberof RedPacketBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {RedPacketBase} RedPacketBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketBase.decode = function decode(reader, length, error) {
    RedPacketBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.RedPacketBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.redPacketId = reader.int64();
                    break;
                }
            case 2: {
                    message.coinTypeBase = $root.CoinTypeBase.decode(reader, reader.uint32());
                    break;
                }
            case 3: {
                    message.coin = reader.string();
                    break;
                }
            case 4: {
                    message.desc = reader.string();
                    break;
                }
            case 5: {
                    message.type = reader.int32();
                    break;
                }
            case 6: {
                    message.count = reader.int32();
                    break;
                }
            case 7: {
                    message.expiredTime = reader.int64();
                    break;
                }
            case 8: {
                    message.userBase = $root.UserBase.decode(reader, reader.uint32());
                    break;
                }
            case 9: {
                    message.status = reader.int32();
                    break;
                }
            case 10: {
                    message.groupId = reader.int64();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a RedPacketBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof RedPacketBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {RedPacketBase} RedPacketBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RedPacketBase message.
     * @function verify
     * @memberof RedPacketBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RedPacketBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.redPacketId != null && message.hasOwnProperty("redPacketId"))
            if (!$util.isInteger(message.redPacketId) && !(message.redPacketId && $util.isInteger(message.redPacketId.low) && $util.isInteger(message.redPacketId.high)))
                return "redPacketId: integer|Long expected";
        if (message.coinTypeBase != null && message.hasOwnProperty("coinTypeBase")) {
            let error = $root.CoinTypeBase.verify(message.coinTypeBase);
            if (error)
                return "coinTypeBase." + error;
        }
        if (message.coin != null && message.hasOwnProperty("coin"))
            if (!$util.isString(message.coin))
                return "coin: string expected";
        if (message.desc != null && message.hasOwnProperty("desc"))
            if (!$util.isString(message.desc))
                return "desc: string expected";
        if (message.type != null && message.hasOwnProperty("type"))
            if (!$util.isInteger(message.type))
                return "type: integer expected";
        if (message.count != null && message.hasOwnProperty("count"))
            if (!$util.isInteger(message.count))
                return "count: integer expected";
        if (message.expiredTime != null && message.hasOwnProperty("expiredTime"))
            if (!$util.isInteger(message.expiredTime) && !(message.expiredTime && $util.isInteger(message.expiredTime.low) && $util.isInteger(message.expiredTime.high)))
                return "expiredTime: integer|Long expected";
        if (message.userBase != null && message.hasOwnProperty("userBase")) {
            let error = $root.UserBase.verify(message.userBase);
            if (error)
                return "userBase." + error;
        }
        if (message.status != null && message.hasOwnProperty("status"))
            if (!$util.isInteger(message.status))
                return "status: integer expected";
        if (message.groupId != null && message.hasOwnProperty("groupId"))
            if (!$util.isInteger(message.groupId) && !(message.groupId && $util.isInteger(message.groupId.low) && $util.isInteger(message.groupId.high)))
                return "groupId: integer|Long expected";
        return null;
    };

    /**
     * Creates a RedPacketBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof RedPacketBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {RedPacketBase} RedPacketBase
     */
    RedPacketBase.fromObject = function fromObject(object) {
        if (object instanceof $root.RedPacketBase)
            return object;
        let message = new $root.RedPacketBase();
        if (object.redPacketId != null)
            if ($util.Long)
                (message.redPacketId = $util.Long.fromValue(object.redPacketId)).unsigned = false;
            else if (typeof object.redPacketId === "string")
                message.redPacketId = parseInt(object.redPacketId, 10);
            else if (typeof object.redPacketId === "number")
                message.redPacketId = object.redPacketId;
            else if (typeof object.redPacketId === "object")
                message.redPacketId = new $util.LongBits(object.redPacketId.low >>> 0, object.redPacketId.high >>> 0).toNumber();
        if (object.coinTypeBase != null) {
            if (typeof object.coinTypeBase !== "object")
                throw TypeError(".RedPacketBase.coinTypeBase: object expected");
            message.coinTypeBase = $root.CoinTypeBase.fromObject(object.coinTypeBase);
        }
        if (object.coin != null)
            message.coin = String(object.coin);
        if (object.desc != null)
            message.desc = String(object.desc);
        if (object.type != null)
            message.type = object.type | 0;
        if (object.count != null)
            message.count = object.count | 0;
        if (object.expiredTime != null)
            if ($util.Long)
                (message.expiredTime = $util.Long.fromValue(object.expiredTime)).unsigned = false;
            else if (typeof object.expiredTime === "string")
                message.expiredTime = parseInt(object.expiredTime, 10);
            else if (typeof object.expiredTime === "number")
                message.expiredTime = object.expiredTime;
            else if (typeof object.expiredTime === "object")
                message.expiredTime = new $util.LongBits(object.expiredTime.low >>> 0, object.expiredTime.high >>> 0).toNumber();
        if (object.userBase != null) {
            if (typeof object.userBase !== "object")
                throw TypeError(".RedPacketBase.userBase: object expected");
            message.userBase = $root.UserBase.fromObject(object.userBase);
        }
        if (object.status != null)
            message.status = object.status | 0;
        if (object.groupId != null)
            if ($util.Long)
                (message.groupId = $util.Long.fromValue(object.groupId)).unsigned = false;
            else if (typeof object.groupId === "string")
                message.groupId = parseInt(object.groupId, 10);
            else if (typeof object.groupId === "number")
                message.groupId = object.groupId;
            else if (typeof object.groupId === "object")
                message.groupId = new $util.LongBits(object.groupId.low >>> 0, object.groupId.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a RedPacketBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof RedPacketBase
     * @static
     * @param {RedPacketBase} message RedPacketBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RedPacketBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.redPacketId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.redPacketId = options.longs === String ? "0" : 0;
            object.coinTypeBase = null;
            object.coin = "";
            object.desc = "";
            object.type = 0;
            object.count = 0;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.expiredTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.expiredTime = options.longs === String ? "0" : 0;
            object.userBase = null;
            object.status = 0;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.groupId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.groupId = options.longs === String ? "0" : 0;
        }
        if (message.redPacketId != null && message.hasOwnProperty("redPacketId"))
            if (typeof message.redPacketId === "number")
                object.redPacketId = options.longs === String ? String(message.redPacketId) : message.redPacketId;
            else
                object.redPacketId = options.longs === String ? $util.Long.prototype.toString.call(message.redPacketId) : options.longs === Number ? new $util.LongBits(message.redPacketId.low >>> 0, message.redPacketId.high >>> 0).toNumber() : message.redPacketId;
        if (message.coinTypeBase != null && message.hasOwnProperty("coinTypeBase"))
            object.coinTypeBase = $root.CoinTypeBase.toObject(message.coinTypeBase, options);
        if (message.coin != null && message.hasOwnProperty("coin"))
            object.coin = message.coin;
        if (message.desc != null && message.hasOwnProperty("desc"))
            object.desc = message.desc;
        if (message.type != null && message.hasOwnProperty("type"))
            object.type = message.type;
        if (message.count != null && message.hasOwnProperty("count"))
            object.count = message.count;
        if (message.expiredTime != null && message.hasOwnProperty("expiredTime"))
            if (typeof message.expiredTime === "number")
                object.expiredTime = options.longs === String ? String(message.expiredTime) : message.expiredTime;
            else
                object.expiredTime = options.longs === String ? $util.Long.prototype.toString.call(message.expiredTime) : options.longs === Number ? new $util.LongBits(message.expiredTime.low >>> 0, message.expiredTime.high >>> 0).toNumber() : message.expiredTime;
        if (message.userBase != null && message.hasOwnProperty("userBase"))
            object.userBase = $root.UserBase.toObject(message.userBase, options);
        if (message.status != null && message.hasOwnProperty("status"))
            object.status = message.status;
        if (message.groupId != null && message.hasOwnProperty("groupId"))
            if (typeof message.groupId === "number")
                object.groupId = options.longs === String ? String(message.groupId) : message.groupId;
            else
                object.groupId = options.longs === String ? $util.Long.prototype.toString.call(message.groupId) : options.longs === Number ? new $util.LongBits(message.groupId.low >>> 0, message.groupId.high >>> 0).toNumber() : message.groupId;
        return object;
    };

    /**
     * Converts this RedPacketBase to JSON.
     * @function toJSON
     * @memberof RedPacketBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RedPacketBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RedPacketBase
     * @function getTypeUrl
     * @memberof RedPacketBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RedPacketBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/RedPacketBase";
    };

    return RedPacketBase;
})();

export const RedPacketOpenRecord = $root.RedPacketOpenRecord = (() => {

    /**
     * Properties of a RedPacketOpenRecord.
     * @exports IRedPacketOpenRecord
     * @interface IRedPacketOpenRecord
     * @property {IUserBase|null} [userBase] RedPacketOpenRecord userBase
     * @property {string|null} [coin] RedPacketOpenRecord coin
     * @property {number|Long|null} [createTime] RedPacketOpenRecord createTime
     */

    /**
     * Constructs a new RedPacketOpenRecord.
     * @exports RedPacketOpenRecord
     * @classdesc Represents a RedPacketOpenRecord.
     * @implements IRedPacketOpenRecord
     * @constructor
     * @param {IRedPacketOpenRecord=} [properties] Properties to set
     */
    function RedPacketOpenRecord(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * RedPacketOpenRecord userBase.
     * @member {IUserBase|null|undefined} userBase
     * @memberof RedPacketOpenRecord
     * @instance
     */
    RedPacketOpenRecord.prototype.userBase = null;

    /**
     * RedPacketOpenRecord coin.
     * @member {string} coin
     * @memberof RedPacketOpenRecord
     * @instance
     */
    RedPacketOpenRecord.prototype.coin = "";

    /**
     * RedPacketOpenRecord createTime.
     * @member {number|Long} createTime
     * @memberof RedPacketOpenRecord
     * @instance
     */
    RedPacketOpenRecord.prototype.createTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * Creates a new RedPacketOpenRecord instance using the specified properties.
     * @function create
     * @memberof RedPacketOpenRecord
     * @static
     * @param {IRedPacketOpenRecord=} [properties] Properties to set
     * @returns {RedPacketOpenRecord} RedPacketOpenRecord instance
     */
    RedPacketOpenRecord.create = function create(properties) {
        return new RedPacketOpenRecord(properties);
    };

    /**
     * Encodes the specified RedPacketOpenRecord message. Does not implicitly {@link RedPacketOpenRecord.verify|verify} messages.
     * @function encode
     * @memberof RedPacketOpenRecord
     * @static
     * @param {IRedPacketOpenRecord} message RedPacketOpenRecord message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketOpenRecord.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.userBase != null && Object.hasOwnProperty.call(message, "userBase"))
            $root.UserBase.encode(message.userBase, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.coin != null && Object.hasOwnProperty.call(message, "coin"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.coin);
        if (message.createTime != null && Object.hasOwnProperty.call(message, "createTime"))
            writer.uint32(/* id 3, wireType 0 =*/24).int64(message.createTime);
        return writer;
    };

    /**
     * Encodes the specified RedPacketOpenRecord message, length delimited. Does not implicitly {@link RedPacketOpenRecord.verify|verify} messages.
     * @function encodeDelimited
     * @memberof RedPacketOpenRecord
     * @static
     * @param {IRedPacketOpenRecord} message RedPacketOpenRecord message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketOpenRecord.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a RedPacketOpenRecord message from the specified reader or buffer.
     * @function decode
     * @memberof RedPacketOpenRecord
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {RedPacketOpenRecord} RedPacketOpenRecord
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketOpenRecord.decode = function decode(reader, length, error) {
    RedPacketOpenRecord.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.RedPacketOpenRecord();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.userBase = $root.UserBase.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.coin = reader.string();
                    break;
                }
            case 3: {
                    message.createTime = reader.int64();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a RedPacketOpenRecord message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof RedPacketOpenRecord
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {RedPacketOpenRecord} RedPacketOpenRecord
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketOpenRecord.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RedPacketOpenRecord message.
     * @function verify
     * @memberof RedPacketOpenRecord
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RedPacketOpenRecord.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.userBase != null && message.hasOwnProperty("userBase")) {
            let error = $root.UserBase.verify(message.userBase);
            if (error)
                return "userBase." + error;
        }
        if (message.coin != null && message.hasOwnProperty("coin"))
            if (!$util.isString(message.coin))
                return "coin: string expected";
        if (message.createTime != null && message.hasOwnProperty("createTime"))
            if (!$util.isInteger(message.createTime) && !(message.createTime && $util.isInteger(message.createTime.low) && $util.isInteger(message.createTime.high)))
                return "createTime: integer|Long expected";
        return null;
    };

    /**
     * Creates a RedPacketOpenRecord message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof RedPacketOpenRecord
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {RedPacketOpenRecord} RedPacketOpenRecord
     */
    RedPacketOpenRecord.fromObject = function fromObject(object) {
        if (object instanceof $root.RedPacketOpenRecord)
            return object;
        let message = new $root.RedPacketOpenRecord();
        if (object.userBase != null) {
            if (typeof object.userBase !== "object")
                throw TypeError(".RedPacketOpenRecord.userBase: object expected");
            message.userBase = $root.UserBase.fromObject(object.userBase);
        }
        if (object.coin != null)
            message.coin = String(object.coin);
        if (object.createTime != null)
            if ($util.Long)
                (message.createTime = $util.Long.fromValue(object.createTime)).unsigned = false;
            else if (typeof object.createTime === "string")
                message.createTime = parseInt(object.createTime, 10);
            else if (typeof object.createTime === "number")
                message.createTime = object.createTime;
            else if (typeof object.createTime === "object")
                message.createTime = new $util.LongBits(object.createTime.low >>> 0, object.createTime.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a RedPacketOpenRecord message. Also converts values to other types if specified.
     * @function toObject
     * @memberof RedPacketOpenRecord
     * @static
     * @param {RedPacketOpenRecord} message RedPacketOpenRecord
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RedPacketOpenRecord.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.userBase = null;
            object.coin = "";
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.createTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.createTime = options.longs === String ? "0" : 0;
        }
        if (message.userBase != null && message.hasOwnProperty("userBase"))
            object.userBase = $root.UserBase.toObject(message.userBase, options);
        if (message.coin != null && message.hasOwnProperty("coin"))
            object.coin = message.coin;
        if (message.createTime != null && message.hasOwnProperty("createTime"))
            if (typeof message.createTime === "number")
                object.createTime = options.longs === String ? String(message.createTime) : message.createTime;
            else
                object.createTime = options.longs === String ? $util.Long.prototype.toString.call(message.createTime) : options.longs === Number ? new $util.LongBits(message.createTime.low >>> 0, message.createTime.high >>> 0).toNumber() : message.createTime;
        return object;
    };

    /**
     * Converts this RedPacketOpenRecord to JSON.
     * @function toJSON
     * @memberof RedPacketOpenRecord
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RedPacketOpenRecord.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RedPacketOpenRecord
     * @function getTypeUrl
     * @memberof RedPacketOpenRecord
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RedPacketOpenRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/RedPacketOpenRecord";
    };

    return RedPacketOpenRecord;
})();

export const RedPacketRecordBase = $root.RedPacketRecordBase = (() => {

    /**
     * Properties of a RedPacketRecordBase.
     * @exports IRedPacketRecordBase
     * @interface IRedPacketRecordBase
     * @property {IRedPacketBase|null} [redPacketBase] RedPacketRecordBase redPacketBase
     * @property {string|null} [coin] RedPacketRecordBase coin
     * @property {number|Long|null} [createTime] RedPacketRecordBase createTime
     * @property {number|null} [openCount] RedPacketRecordBase openCount
     */

    /**
     * Constructs a new RedPacketRecordBase.
     * @exports RedPacketRecordBase
     * @classdesc Represents a RedPacketRecordBase.
     * @implements IRedPacketRecordBase
     * @constructor
     * @param {IRedPacketRecordBase=} [properties] Properties to set
     */
    function RedPacketRecordBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * RedPacketRecordBase redPacketBase.
     * @member {IRedPacketBase|null|undefined} redPacketBase
     * @memberof RedPacketRecordBase
     * @instance
     */
    RedPacketRecordBase.prototype.redPacketBase = null;

    /**
     * RedPacketRecordBase coin.
     * @member {string} coin
     * @memberof RedPacketRecordBase
     * @instance
     */
    RedPacketRecordBase.prototype.coin = "";

    /**
     * RedPacketRecordBase createTime.
     * @member {number|Long} createTime
     * @memberof RedPacketRecordBase
     * @instance
     */
    RedPacketRecordBase.prototype.createTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * RedPacketRecordBase openCount.
     * @member {number} openCount
     * @memberof RedPacketRecordBase
     * @instance
     */
    RedPacketRecordBase.prototype.openCount = 0;

    /**
     * Creates a new RedPacketRecordBase instance using the specified properties.
     * @function create
     * @memberof RedPacketRecordBase
     * @static
     * @param {IRedPacketRecordBase=} [properties] Properties to set
     * @returns {RedPacketRecordBase} RedPacketRecordBase instance
     */
    RedPacketRecordBase.create = function create(properties) {
        return new RedPacketRecordBase(properties);
    };

    /**
     * Encodes the specified RedPacketRecordBase message. Does not implicitly {@link RedPacketRecordBase.verify|verify} messages.
     * @function encode
     * @memberof RedPacketRecordBase
     * @static
     * @param {IRedPacketRecordBase} message RedPacketRecordBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketRecordBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.redPacketBase != null && Object.hasOwnProperty.call(message, "redPacketBase"))
            $root.RedPacketBase.encode(message.redPacketBase, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.coin != null && Object.hasOwnProperty.call(message, "coin"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.coin);
        if (message.createTime != null && Object.hasOwnProperty.call(message, "createTime"))
            writer.uint32(/* id 3, wireType 0 =*/24).int64(message.createTime);
        if (message.openCount != null && Object.hasOwnProperty.call(message, "openCount"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.openCount);
        return writer;
    };

    /**
     * Encodes the specified RedPacketRecordBase message, length delimited. Does not implicitly {@link RedPacketRecordBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof RedPacketRecordBase
     * @static
     * @param {IRedPacketRecordBase} message RedPacketRecordBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketRecordBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a RedPacketRecordBase message from the specified reader or buffer.
     * @function decode
     * @memberof RedPacketRecordBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {RedPacketRecordBase} RedPacketRecordBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketRecordBase.decode = function decode(reader, length, error) {
    RedPacketRecordBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.RedPacketRecordBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.redPacketBase = $root.RedPacketBase.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.coin = reader.string();
                    break;
                }
            case 3: {
                    message.createTime = reader.int64();
                    break;
                }
            case 4: {
                    message.openCount = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a RedPacketRecordBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof RedPacketRecordBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {RedPacketRecordBase} RedPacketRecordBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketRecordBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RedPacketRecordBase message.
     * @function verify
     * @memberof RedPacketRecordBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RedPacketRecordBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.redPacketBase != null && message.hasOwnProperty("redPacketBase")) {
            let error = $root.RedPacketBase.verify(message.redPacketBase);
            if (error)
                return "redPacketBase." + error;
        }
        if (message.coin != null && message.hasOwnProperty("coin"))
            if (!$util.isString(message.coin))
                return "coin: string expected";
        if (message.createTime != null && message.hasOwnProperty("createTime"))
            if (!$util.isInteger(message.createTime) && !(message.createTime && $util.isInteger(message.createTime.low) && $util.isInteger(message.createTime.high)))
                return "createTime: integer|Long expected";
        if (message.openCount != null && message.hasOwnProperty("openCount"))
            if (!$util.isInteger(message.openCount))
                return "openCount: integer expected";
        return null;
    };

    /**
     * Creates a RedPacketRecordBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof RedPacketRecordBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {RedPacketRecordBase} RedPacketRecordBase
     */
    RedPacketRecordBase.fromObject = function fromObject(object) {
        if (object instanceof $root.RedPacketRecordBase)
            return object;
        let message = new $root.RedPacketRecordBase();
        if (object.redPacketBase != null) {
            if (typeof object.redPacketBase !== "object")
                throw TypeError(".RedPacketRecordBase.redPacketBase: object expected");
            message.redPacketBase = $root.RedPacketBase.fromObject(object.redPacketBase);
        }
        if (object.coin != null)
            message.coin = String(object.coin);
        if (object.createTime != null)
            if ($util.Long)
                (message.createTime = $util.Long.fromValue(object.createTime)).unsigned = false;
            else if (typeof object.createTime === "string")
                message.createTime = parseInt(object.createTime, 10);
            else if (typeof object.createTime === "number")
                message.createTime = object.createTime;
            else if (typeof object.createTime === "object")
                message.createTime = new $util.LongBits(object.createTime.low >>> 0, object.createTime.high >>> 0).toNumber();
        if (object.openCount != null)
            message.openCount = object.openCount | 0;
        return message;
    };

    /**
     * Creates a plain object from a RedPacketRecordBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof RedPacketRecordBase
     * @static
     * @param {RedPacketRecordBase} message RedPacketRecordBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RedPacketRecordBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.redPacketBase = null;
            object.coin = "";
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.createTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.createTime = options.longs === String ? "0" : 0;
            object.openCount = 0;
        }
        if (message.redPacketBase != null && message.hasOwnProperty("redPacketBase"))
            object.redPacketBase = $root.RedPacketBase.toObject(message.redPacketBase, options);
        if (message.coin != null && message.hasOwnProperty("coin"))
            object.coin = message.coin;
        if (message.createTime != null && message.hasOwnProperty("createTime"))
            if (typeof message.createTime === "number")
                object.createTime = options.longs === String ? String(message.createTime) : message.createTime;
            else
                object.createTime = options.longs === String ? $util.Long.prototype.toString.call(message.createTime) : options.longs === Number ? new $util.LongBits(message.createTime.low >>> 0, message.createTime.high >>> 0).toNumber() : message.createTime;
        if (message.openCount != null && message.hasOwnProperty("openCount"))
            object.openCount = message.openCount;
        return object;
    };

    /**
     * Converts this RedPacketRecordBase to JSON.
     * @function toJSON
     * @memberof RedPacketRecordBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RedPacketRecordBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RedPacketRecordBase
     * @function getTypeUrl
     * @memberof RedPacketRecordBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RedPacketRecordBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/RedPacketRecordBase";
    };

    return RedPacketRecordBase;
})();

export const GetSmsCodeReq = $root.GetSmsCodeReq = (() => {

    /**
     * Properties of a GetSmsCodeReq.
     * @exports IGetSmsCodeReq
     * @interface IGetSmsCodeReq
     * @property {IClientInfo|null} [clientInfo] GetSmsCodeReq clientInfo
     * @property {string|null} [countryCode] GetSmsCodeReq countryCode
     * @property {string|null} [phone] GetSmsCodeReq phone
     * @property {GetValidateCodeType|null} [type] GetSmsCodeReq type
     * @property {number|null} [flag] GetSmsCodeReq flag
     */

    /**
     * Constructs a new GetSmsCodeReq.
     * @exports GetSmsCodeReq
     * @classdesc Represents a GetSmsCodeReq.
     * @implements IGetSmsCodeReq
     * @constructor
     * @param {IGetSmsCodeReq=} [properties] Properties to set
     */
    function GetSmsCodeReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetSmsCodeReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GetSmsCodeReq
     * @instance
     */
    GetSmsCodeReq.prototype.clientInfo = null;

    /**
     * GetSmsCodeReq countryCode.
     * @member {string} countryCode
     * @memberof GetSmsCodeReq
     * @instance
     */
    GetSmsCodeReq.prototype.countryCode = "";

    /**
     * GetSmsCodeReq phone.
     * @member {string} phone
     * @memberof GetSmsCodeReq
     * @instance
     */
    GetSmsCodeReq.prototype.phone = "";

    /**
     * GetSmsCodeReq type.
     * @member {GetValidateCodeType} type
     * @memberof GetSmsCodeReq
     * @instance
     */
    GetSmsCodeReq.prototype.type = 0;

    /**
     * GetSmsCodeReq flag.
     * @member {number} flag
     * @memberof GetSmsCodeReq
     * @instance
     */
    GetSmsCodeReq.prototype.flag = 0;

    /**
     * Creates a new GetSmsCodeReq instance using the specified properties.
     * @function create
     * @memberof GetSmsCodeReq
     * @static
     * @param {IGetSmsCodeReq=} [properties] Properties to set
     * @returns {GetSmsCodeReq} GetSmsCodeReq instance
     */
    GetSmsCodeReq.create = function create(properties) {
        return new GetSmsCodeReq(properties);
    };

    /**
     * Encodes the specified GetSmsCodeReq message. Does not implicitly {@link GetSmsCodeReq.verify|verify} messages.
     * @function encode
     * @memberof GetSmsCodeReq
     * @static
     * @param {IGetSmsCodeReq} message GetSmsCodeReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetSmsCodeReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.countryCode != null && Object.hasOwnProperty.call(message, "countryCode"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.countryCode);
        if (message.phone != null && Object.hasOwnProperty.call(message, "phone"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.phone);
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.type);
        if (message.flag != null && Object.hasOwnProperty.call(message, "flag"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.flag);
        return writer;
    };

    /**
     * Encodes the specified GetSmsCodeReq message, length delimited. Does not implicitly {@link GetSmsCodeReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetSmsCodeReq
     * @static
     * @param {IGetSmsCodeReq} message GetSmsCodeReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetSmsCodeReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetSmsCodeReq message from the specified reader or buffer.
     * @function decode
     * @memberof GetSmsCodeReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetSmsCodeReq} GetSmsCodeReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetSmsCodeReq.decode = function decode(reader, length, error) {
    GetSmsCodeReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetSmsCodeReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.countryCode = reader.string();
                    break;
                }
            case 3: {
                    message.phone = reader.string();
                    break;
                }
            case 4: {
                    message.type = reader.int32();
                    break;
                }
            case 5: {
                    message.flag = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetSmsCodeReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetSmsCodeReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetSmsCodeReq} GetSmsCodeReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetSmsCodeReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetSmsCodeReq message.
     * @function verify
     * @memberof GetSmsCodeReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetSmsCodeReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.countryCode != null && message.hasOwnProperty("countryCode"))
            if (!$util.isString(message.countryCode))
                return "countryCode: string expected";
        if (message.phone != null && message.hasOwnProperty("phone"))
            if (!$util.isString(message.phone))
                return "phone: string expected";
        if (message.type != null && message.hasOwnProperty("type"))
            switch (message.type) {
            default:
                return "type: enum value expected";
            case 0:
            case 1:
            case 2:
            case 3:
            case 4:
            case 6:
            case 7:
            case 8:
            case 9:
            case 10:
            case 11:
            case 12:
            case 13:
            case 14:
            case 15:
            case 16:
            case 17:
            case 18:
            case 19:
            case 20:
            case 21:
            case 22:
            case 23:
            case 23:
                break;
            }
        if (message.flag != null && message.hasOwnProperty("flag"))
            if (!$util.isInteger(message.flag))
                return "flag: integer expected";
        return null;
    };

    /**
     * Creates a GetSmsCodeReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetSmsCodeReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetSmsCodeReq} GetSmsCodeReq
     */
    GetSmsCodeReq.fromObject = function fromObject(object) {
        if (object instanceof $root.GetSmsCodeReq)
            return object;
        let message = new $root.GetSmsCodeReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GetSmsCodeReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.countryCode != null)
            message.countryCode = String(object.countryCode);
        if (object.phone != null)
            message.phone = String(object.phone);
        switch (object.type) {
        default:
            if (typeof object.type === "number") {
                message.type = object.type;
                break;
            }
            break;
        case "REG":
        case 0:
            message.type = 0;
            break;
        case "LOGIN":
        case 1:
            message.type = 1;
            break;
        case "FIND_PASSWORD":
        case 2:
            message.type = 2;
            break;
        case "UPDATE_PASSWORD":
        case 3:
            message.type = 3;
            break;
        case "UPDATE_PHONE":
        case 4:
            message.type = 4;
            break;
        case "VALIDATE_PASSWORD":
        case 6:
            message.type = 6;
            break;
        case "FIND_GESTURE_PASSWORD":
        case 7:
            message.type = 7;
            break;
        case "TRADE_PASSWORD":
        case 8:
            message.type = 8;
            break;
        case "UPDATE_TRADE_PASSWORD":
        case 9:
            message.type = 9;
            break;
        case "BIND_PHONE":
        case 10:
            message.type = 10;
            break;
        case "BIND_EMAIL":
        case 11:
            message.type = 11;
            break;
        case "UPDATE_EMAIL":
        case 12:
            message.type = 12;
            break;
        case "VALIDATE_GOOGLE_AUTH":
        case 13:
            message.type = 13;
            break;
        case "UPDATE_SECURE":
        case 14:
            message.type = 14;
            break;
        case "CASH_OUT":
        case 15:
            message.type = 15;
            break;
        case "VERIFY_EMAIL":
        case 16:
            message.type = 16;
            break;
        case "VERIFY_PHONE":
        case 17:
            message.type = 17;
            break;
        case "VERIFY_TRADE_PASSWORD":
        case 18:
            message.type = 18;
            break;
        case "VERIFY_GOOGLE_CODE":
        case 19:
            message.type = 19;
            break;
        case "BIND_ACCOUNT":
        case 20:
            message.type = 20;
            break;
        case "VERIFY_LOGIN_PHONE":
        case 21:
            message.type = 21;
            break;
        case "VERIFY_LOGIN_EMAIL":
        case 22:
            message.type = 22;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.type = 23;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.type = 23;
            break;
        }
        if (object.flag != null)
            message.flag = object.flag | 0;
        return message;
    };

    /**
     * Creates a plain object from a GetSmsCodeReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetSmsCodeReq
     * @static
     * @param {GetSmsCodeReq} message GetSmsCodeReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetSmsCodeReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.countryCode = "";
            object.phone = "";
            object.type = options.enums === String ? "REG" : 0;
            object.flag = 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.countryCode != null && message.hasOwnProperty("countryCode"))
            object.countryCode = message.countryCode;
        if (message.phone != null && message.hasOwnProperty("phone"))
            object.phone = message.phone;
        if (message.type != null && message.hasOwnProperty("type"))
            object.type = options.enums === String ? $root.GetValidateCodeType[message.type] === undefined ? message.type : $root.GetValidateCodeType[message.type] : message.type;
        if (message.flag != null && message.hasOwnProperty("flag"))
            object.flag = message.flag;
        return object;
    };

    /**
     * Converts this GetSmsCodeReq to JSON.
     * @function toJSON
     * @memberof GetSmsCodeReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetSmsCodeReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetSmsCodeReq
     * @function getTypeUrl
     * @memberof GetSmsCodeReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetSmsCodeReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetSmsCodeReq";
    };

    return GetSmsCodeReq;
})();

export const GetSmsCodeResp = $root.GetSmsCodeResp = (() => {

    /**
     * Properties of a GetSmsCodeResp.
     * @exports IGetSmsCodeResp
     * @interface IGetSmsCodeResp
     * @property {ICommonResult|null} [commonResult] GetSmsCodeResp commonResult
     */

    /**
     * Constructs a new GetSmsCodeResp.
     * @exports GetSmsCodeResp
     * @classdesc Represents a GetSmsCodeResp.
     * @implements IGetSmsCodeResp
     * @constructor
     * @param {IGetSmsCodeResp=} [properties] Properties to set
     */
    function GetSmsCodeResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetSmsCodeResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GetSmsCodeResp
     * @instance
     */
    GetSmsCodeResp.prototype.commonResult = null;

    /**
     * Creates a new GetSmsCodeResp instance using the specified properties.
     * @function create
     * @memberof GetSmsCodeResp
     * @static
     * @param {IGetSmsCodeResp=} [properties] Properties to set
     * @returns {GetSmsCodeResp} GetSmsCodeResp instance
     */
    GetSmsCodeResp.create = function create(properties) {
        return new GetSmsCodeResp(properties);
    };

    /**
     * Encodes the specified GetSmsCodeResp message. Does not implicitly {@link GetSmsCodeResp.verify|verify} messages.
     * @function encode
     * @memberof GetSmsCodeResp
     * @static
     * @param {IGetSmsCodeResp} message GetSmsCodeResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetSmsCodeResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified GetSmsCodeResp message, length delimited. Does not implicitly {@link GetSmsCodeResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetSmsCodeResp
     * @static
     * @param {IGetSmsCodeResp} message GetSmsCodeResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetSmsCodeResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetSmsCodeResp message from the specified reader or buffer.
     * @function decode
     * @memberof GetSmsCodeResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetSmsCodeResp} GetSmsCodeResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetSmsCodeResp.decode = function decode(reader, length, error) {
    GetSmsCodeResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetSmsCodeResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetSmsCodeResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetSmsCodeResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetSmsCodeResp} GetSmsCodeResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetSmsCodeResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetSmsCodeResp message.
     * @function verify
     * @memberof GetSmsCodeResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetSmsCodeResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates a GetSmsCodeResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetSmsCodeResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetSmsCodeResp} GetSmsCodeResp
     */
    GetSmsCodeResp.fromObject = function fromObject(object) {
        if (object instanceof $root.GetSmsCodeResp)
            return object;
        let message = new $root.GetSmsCodeResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GetSmsCodeResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from a GetSmsCodeResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetSmsCodeResp
     * @static
     * @param {GetSmsCodeResp} message GetSmsCodeResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetSmsCodeResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this GetSmsCodeResp to JSON.
     * @function toJSON
     * @memberof GetSmsCodeResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetSmsCodeResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetSmsCodeResp
     * @function getTypeUrl
     * @memberof GetSmsCodeResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetSmsCodeResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetSmsCodeResp";
    };

    return GetSmsCodeResp;
})();

export const GetEmailCodeReq = $root.GetEmailCodeReq = (() => {

    /**
     * Properties of a GetEmailCodeReq.
     * @exports IGetEmailCodeReq
     * @interface IGetEmailCodeReq
     * @property {IClientInfo|null} [clientInfo] GetEmailCodeReq clientInfo
     * @property {string|null} [email] GetEmailCodeReq email
     * @property {GetValidateCodeType|null} [type] GetEmailCodeReq type
     * @property {number|null} [flag] GetEmailCodeReq flag
     */

    /**
     * Constructs a new GetEmailCodeReq.
     * @exports GetEmailCodeReq
     * @classdesc Represents a GetEmailCodeReq.
     * @implements IGetEmailCodeReq
     * @constructor
     * @param {IGetEmailCodeReq=} [properties] Properties to set
     */
    function GetEmailCodeReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetEmailCodeReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GetEmailCodeReq
     * @instance
     */
    GetEmailCodeReq.prototype.clientInfo = null;

    /**
     * GetEmailCodeReq email.
     * @member {string} email
     * @memberof GetEmailCodeReq
     * @instance
     */
    GetEmailCodeReq.prototype.email = "";

    /**
     * GetEmailCodeReq type.
     * @member {GetValidateCodeType} type
     * @memberof GetEmailCodeReq
     * @instance
     */
    GetEmailCodeReq.prototype.type = 0;

    /**
     * GetEmailCodeReq flag.
     * @member {number} flag
     * @memberof GetEmailCodeReq
     * @instance
     */
    GetEmailCodeReq.prototype.flag = 0;

    /**
     * Creates a new GetEmailCodeReq instance using the specified properties.
     * @function create
     * @memberof GetEmailCodeReq
     * @static
     * @param {IGetEmailCodeReq=} [properties] Properties to set
     * @returns {GetEmailCodeReq} GetEmailCodeReq instance
     */
    GetEmailCodeReq.create = function create(properties) {
        return new GetEmailCodeReq(properties);
    };

    /**
     * Encodes the specified GetEmailCodeReq message. Does not implicitly {@link GetEmailCodeReq.verify|verify} messages.
     * @function encode
     * @memberof GetEmailCodeReq
     * @static
     * @param {IGetEmailCodeReq} message GetEmailCodeReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetEmailCodeReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.email != null && Object.hasOwnProperty.call(message, "email"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.email);
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.type);
        if (message.flag != null && Object.hasOwnProperty.call(message, "flag"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.flag);
        return writer;
    };

    /**
     * Encodes the specified GetEmailCodeReq message, length delimited. Does not implicitly {@link GetEmailCodeReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetEmailCodeReq
     * @static
     * @param {IGetEmailCodeReq} message GetEmailCodeReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetEmailCodeReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetEmailCodeReq message from the specified reader or buffer.
     * @function decode
     * @memberof GetEmailCodeReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetEmailCodeReq} GetEmailCodeReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetEmailCodeReq.decode = function decode(reader, length, error) {
    GetEmailCodeReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetEmailCodeReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.email = reader.string();
                    break;
                }
            case 3: {
                    message.type = reader.int32();
                    break;
                }
            case 4: {
                    message.flag = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetEmailCodeReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetEmailCodeReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetEmailCodeReq} GetEmailCodeReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetEmailCodeReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetEmailCodeReq message.
     * @function verify
     * @memberof GetEmailCodeReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetEmailCodeReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.email != null && message.hasOwnProperty("email"))
            if (!$util.isString(message.email))
                return "email: string expected";
        if (message.type != null && message.hasOwnProperty("type"))
            switch (message.type) {
            default:
                return "type: enum value expected";
            case 0:
            case 1:
            case 2:
            case 3:
            case 4:
            case 6:
            case 7:
            case 8:
            case 9:
            case 10:
            case 11:
            case 12:
            case 13:
            case 14:
            case 15:
            case 16:
            case 17:
            case 18:
            case 19:
            case 20:
            case 21:
            case 22:
            case 23:
            case 23:
                break;
            }
        if (message.flag != null && message.hasOwnProperty("flag"))
            if (!$util.isInteger(message.flag))
                return "flag: integer expected";
        return null;
    };

    /**
     * Creates a GetEmailCodeReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetEmailCodeReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetEmailCodeReq} GetEmailCodeReq
     */
    GetEmailCodeReq.fromObject = function fromObject(object) {
        if (object instanceof $root.GetEmailCodeReq)
            return object;
        let message = new $root.GetEmailCodeReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GetEmailCodeReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.email != null)
            message.email = String(object.email);
        switch (object.type) {
        default:
            if (typeof object.type === "number") {
                message.type = object.type;
                break;
            }
            break;
        case "REG":
        case 0:
            message.type = 0;
            break;
        case "LOGIN":
        case 1:
            message.type = 1;
            break;
        case "FIND_PASSWORD":
        case 2:
            message.type = 2;
            break;
        case "UPDATE_PASSWORD":
        case 3:
            message.type = 3;
            break;
        case "UPDATE_PHONE":
        case 4:
            message.type = 4;
            break;
        case "VALIDATE_PASSWORD":
        case 6:
            message.type = 6;
            break;
        case "FIND_GESTURE_PASSWORD":
        case 7:
            message.type = 7;
            break;
        case "TRADE_PASSWORD":
        case 8:
            message.type = 8;
            break;
        case "UPDATE_TRADE_PASSWORD":
        case 9:
            message.type = 9;
            break;
        case "BIND_PHONE":
        case 10:
            message.type = 10;
            break;
        case "BIND_EMAIL":
        case 11:
            message.type = 11;
            break;
        case "UPDATE_EMAIL":
        case 12:
            message.type = 12;
            break;
        case "VALIDATE_GOOGLE_AUTH":
        case 13:
            message.type = 13;
            break;
        case "UPDATE_SECURE":
        case 14:
            message.type = 14;
            break;
        case "CASH_OUT":
        case 15:
            message.type = 15;
            break;
        case "VERIFY_EMAIL":
        case 16:
            message.type = 16;
            break;
        case "VERIFY_PHONE":
        case 17:
            message.type = 17;
            break;
        case "VERIFY_TRADE_PASSWORD":
        case 18:
            message.type = 18;
            break;
        case "VERIFY_GOOGLE_CODE":
        case 19:
            message.type = 19;
            break;
        case "BIND_ACCOUNT":
        case 20:
            message.type = 20;
            break;
        case "VERIFY_LOGIN_PHONE":
        case 21:
            message.type = 21;
            break;
        case "VERIFY_LOGIN_EMAIL":
        case 22:
            message.type = 22;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.type = 23;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.type = 23;
            break;
        }
        if (object.flag != null)
            message.flag = object.flag | 0;
        return message;
    };

    /**
     * Creates a plain object from a GetEmailCodeReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetEmailCodeReq
     * @static
     * @param {GetEmailCodeReq} message GetEmailCodeReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetEmailCodeReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.email = "";
            object.type = options.enums === String ? "REG" : 0;
            object.flag = 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.email != null && message.hasOwnProperty("email"))
            object.email = message.email;
        if (message.type != null && message.hasOwnProperty("type"))
            object.type = options.enums === String ? $root.GetValidateCodeType[message.type] === undefined ? message.type : $root.GetValidateCodeType[message.type] : message.type;
        if (message.flag != null && message.hasOwnProperty("flag"))
            object.flag = message.flag;
        return object;
    };

    /**
     * Converts this GetEmailCodeReq to JSON.
     * @function toJSON
     * @memberof GetEmailCodeReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetEmailCodeReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetEmailCodeReq
     * @function getTypeUrl
     * @memberof GetEmailCodeReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetEmailCodeReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetEmailCodeReq";
    };

    return GetEmailCodeReq;
})();

export const GetEmailCodeResp = $root.GetEmailCodeResp = (() => {

    /**
     * Properties of a GetEmailCodeResp.
     * @exports IGetEmailCodeResp
     * @interface IGetEmailCodeResp
     * @property {ICommonResult|null} [commonResult] GetEmailCodeResp commonResult
     */

    /**
     * Constructs a new GetEmailCodeResp.
     * @exports GetEmailCodeResp
     * @classdesc Represents a GetEmailCodeResp.
     * @implements IGetEmailCodeResp
     * @constructor
     * @param {IGetEmailCodeResp=} [properties] Properties to set
     */
    function GetEmailCodeResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetEmailCodeResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GetEmailCodeResp
     * @instance
     */
    GetEmailCodeResp.prototype.commonResult = null;

    /**
     * Creates a new GetEmailCodeResp instance using the specified properties.
     * @function create
     * @memberof GetEmailCodeResp
     * @static
     * @param {IGetEmailCodeResp=} [properties] Properties to set
     * @returns {GetEmailCodeResp} GetEmailCodeResp instance
     */
    GetEmailCodeResp.create = function create(properties) {
        return new GetEmailCodeResp(properties);
    };

    /**
     * Encodes the specified GetEmailCodeResp message. Does not implicitly {@link GetEmailCodeResp.verify|verify} messages.
     * @function encode
     * @memberof GetEmailCodeResp
     * @static
     * @param {IGetEmailCodeResp} message GetEmailCodeResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetEmailCodeResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified GetEmailCodeResp message, length delimited. Does not implicitly {@link GetEmailCodeResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetEmailCodeResp
     * @static
     * @param {IGetEmailCodeResp} message GetEmailCodeResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetEmailCodeResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetEmailCodeResp message from the specified reader or buffer.
     * @function decode
     * @memberof GetEmailCodeResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetEmailCodeResp} GetEmailCodeResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetEmailCodeResp.decode = function decode(reader, length, error) {
    GetEmailCodeResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetEmailCodeResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetEmailCodeResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetEmailCodeResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetEmailCodeResp} GetEmailCodeResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetEmailCodeResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetEmailCodeResp message.
     * @function verify
     * @memberof GetEmailCodeResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetEmailCodeResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates a GetEmailCodeResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetEmailCodeResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetEmailCodeResp} GetEmailCodeResp
     */
    GetEmailCodeResp.fromObject = function fromObject(object) {
        if (object instanceof $root.GetEmailCodeResp)
            return object;
        let message = new $root.GetEmailCodeResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GetEmailCodeResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from a GetEmailCodeResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetEmailCodeResp
     * @static
     * @param {GetEmailCodeResp} message GetEmailCodeResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetEmailCodeResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this GetEmailCodeResp to JSON.
     * @function toJSON
     * @memberof GetEmailCodeResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetEmailCodeResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetEmailCodeResp
     * @function getTypeUrl
     * @memberof GetEmailCodeResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetEmailCodeResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetEmailCodeResp";
    };

    return GetEmailCodeResp;
})();

export const GetTokenReq = $root.GetTokenReq = (() => {

    /**
     * Properties of a GetTokenReq.
     * @exports IGetTokenReq
     * @interface IGetTokenReq
     * @property {IClientInfo|null} [clientInfo] GetTokenReq clientInfo
     * @property {AttachWorkSpaceType|null} [attachWorkspaceType] GetTokenReq attachWorkspaceType
     * @property {AttachType|null} [attachType] GetTokenReq attachType
     */

    /**
     * Constructs a new GetTokenReq.
     * @exports GetTokenReq
     * @classdesc Represents a GetTokenReq.
     * @implements IGetTokenReq
     * @constructor
     * @param {IGetTokenReq=} [properties] Properties to set
     */
    function GetTokenReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetTokenReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GetTokenReq
     * @instance
     */
    GetTokenReq.prototype.clientInfo = null;

    /**
     * GetTokenReq attachWorkspaceType.
     * @member {AttachWorkSpaceType} attachWorkspaceType
     * @memberof GetTokenReq
     * @instance
     */
    GetTokenReq.prototype.attachWorkspaceType = 0;

    /**
     * GetTokenReq attachType.
     * @member {AttachType} attachType
     * @memberof GetTokenReq
     * @instance
     */
    GetTokenReq.prototype.attachType = 0;

    /**
     * Creates a new GetTokenReq instance using the specified properties.
     * @function create
     * @memberof GetTokenReq
     * @static
     * @param {IGetTokenReq=} [properties] Properties to set
     * @returns {GetTokenReq} GetTokenReq instance
     */
    GetTokenReq.create = function create(properties) {
        return new GetTokenReq(properties);
    };

    /**
     * Encodes the specified GetTokenReq message. Does not implicitly {@link GetTokenReq.verify|verify} messages.
     * @function encode
     * @memberof GetTokenReq
     * @static
     * @param {IGetTokenReq} message GetTokenReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetTokenReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.attachWorkspaceType != null && Object.hasOwnProperty.call(message, "attachWorkspaceType"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.attachWorkspaceType);
        if (message.attachType != null && Object.hasOwnProperty.call(message, "attachType"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.attachType);
        return writer;
    };

    /**
     * Encodes the specified GetTokenReq message, length delimited. Does not implicitly {@link GetTokenReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetTokenReq
     * @static
     * @param {IGetTokenReq} message GetTokenReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetTokenReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetTokenReq message from the specified reader or buffer.
     * @function decode
     * @memberof GetTokenReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetTokenReq} GetTokenReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetTokenReq.decode = function decode(reader, length, error) {
    GetTokenReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetTokenReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.attachWorkspaceType = reader.int32();
                    break;
                }
            case 3: {
                    message.attachType = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetTokenReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetTokenReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetTokenReq} GetTokenReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetTokenReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetTokenReq message.
     * @function verify
     * @memberof GetTokenReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetTokenReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.attachWorkspaceType != null && message.hasOwnProperty("attachWorkspaceType"))
            switch (message.attachWorkspaceType) {
            default:
                return "attachWorkspaceType: enum value expected";
            case 0:
            case 1:
                break;
            }
        if (message.attachType != null && message.hasOwnProperty("attachType"))
            switch (message.attachType) {
            default:
                return "attachType: enum value expected";
            case 0:
            case 1:
            case 2:
            case 3:
            case 4:
            case 5:
                break;
            }
        return null;
    };

    /**
     * Creates a GetTokenReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetTokenReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetTokenReq} GetTokenReq
     */
    GetTokenReq.fromObject = function fromObject(object) {
        if (object instanceof $root.GetTokenReq)
            return object;
        let message = new $root.GetTokenReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GetTokenReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        switch (object.attachWorkspaceType) {
        default:
            if (typeof object.attachWorkspaceType === "number") {
                message.attachWorkspaceType = object.attachWorkspaceType;
                break;
            }
            break;
        case "COMMON":
        case 0:
            message.attachWorkspaceType = 0;
            break;
        case "CHAT":
        case 1:
            message.attachWorkspaceType = 1;
            break;
        }
        switch (object.attachType) {
        default:
            if (typeof object.attachType === "number") {
                message.attachType = object.attachType;
                break;
            }
            break;
        case "PIC":
        case 0:
            message.attachType = 0;
            break;
        case "AUDIO":
        case 1:
            message.attachType = 1;
            break;
        case "VIDEO":
        case 2:
            message.attachType = 2;
            break;
        case "FILE":
        case 3:
            message.attachType = 3;
            break;
        case "LOG":
        case 4:
            message.attachType = 4;
            break;
        case "EMOTICON":
        case 5:
            message.attachType = 5;
            break;
        }
        return message;
    };

    /**
     * Creates a plain object from a GetTokenReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetTokenReq
     * @static
     * @param {GetTokenReq} message GetTokenReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetTokenReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.attachWorkspaceType = options.enums === String ? "COMMON" : 0;
            object.attachType = options.enums === String ? "PIC" : 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.attachWorkspaceType != null && message.hasOwnProperty("attachWorkspaceType"))
            object.attachWorkspaceType = options.enums === String ? $root.AttachWorkSpaceType[message.attachWorkspaceType] === undefined ? message.attachWorkspaceType : $root.AttachWorkSpaceType[message.attachWorkspaceType] : message.attachWorkspaceType;
        if (message.attachType != null && message.hasOwnProperty("attachType"))
            object.attachType = options.enums === String ? $root.AttachType[message.attachType] === undefined ? message.attachType : $root.AttachType[message.attachType] : message.attachType;
        return object;
    };

    /**
     * Converts this GetTokenReq to JSON.
     * @function toJSON
     * @memberof GetTokenReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetTokenReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetTokenReq
     * @function getTypeUrl
     * @memberof GetTokenReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetTokenReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetTokenReq";
    };

    return GetTokenReq;
})();

export const GetTokenResp = $root.GetTokenResp = (() => {

    /**
     * Properties of a GetTokenResp.
     * @exports IGetTokenResp
     * @interface IGetTokenResp
     * @property {ICommonResult|null} [commonResult] GetTokenResp commonResult
     * @property {string|null} [token] GetTokenResp token
     * @property {string|null} [fileId] GetTokenResp fileId
     * @property {string|null} [url] GetTokenResp url
     */

    /**
     * Constructs a new GetTokenResp.
     * @exports GetTokenResp
     * @classdesc Represents a GetTokenResp.
     * @implements IGetTokenResp
     * @constructor
     * @param {IGetTokenResp=} [properties] Properties to set
     */
    function GetTokenResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetTokenResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GetTokenResp
     * @instance
     */
    GetTokenResp.prototype.commonResult = null;

    /**
     * GetTokenResp token.
     * @member {string} token
     * @memberof GetTokenResp
     * @instance
     */
    GetTokenResp.prototype.token = "";

    /**
     * GetTokenResp fileId.
     * @member {string} fileId
     * @memberof GetTokenResp
     * @instance
     */
    GetTokenResp.prototype.fileId = "";

    /**
     * GetTokenResp url.
     * @member {string} url
     * @memberof GetTokenResp
     * @instance
     */
    GetTokenResp.prototype.url = "";

    /**
     * Creates a new GetTokenResp instance using the specified properties.
     * @function create
     * @memberof GetTokenResp
     * @static
     * @param {IGetTokenResp=} [properties] Properties to set
     * @returns {GetTokenResp} GetTokenResp instance
     */
    GetTokenResp.create = function create(properties) {
        return new GetTokenResp(properties);
    };

    /**
     * Encodes the specified GetTokenResp message. Does not implicitly {@link GetTokenResp.verify|verify} messages.
     * @function encode
     * @memberof GetTokenResp
     * @static
     * @param {IGetTokenResp} message GetTokenResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetTokenResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.token != null && Object.hasOwnProperty.call(message, "token"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.token);
        if (message.fileId != null && Object.hasOwnProperty.call(message, "fileId"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.fileId);
        if (message.url != null && Object.hasOwnProperty.call(message, "url"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.url);
        return writer;
    };

    /**
     * Encodes the specified GetTokenResp message, length delimited. Does not implicitly {@link GetTokenResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetTokenResp
     * @static
     * @param {IGetTokenResp} message GetTokenResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetTokenResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetTokenResp message from the specified reader or buffer.
     * @function decode
     * @memberof GetTokenResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetTokenResp} GetTokenResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetTokenResp.decode = function decode(reader, length, error) {
    GetTokenResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetTokenResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.token = reader.string();
                    break;
                }
            case 3: {
                    message.fileId = reader.string();
                    break;
                }
            case 4: {
                    message.url = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetTokenResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetTokenResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetTokenResp} GetTokenResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetTokenResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetTokenResp message.
     * @function verify
     * @memberof GetTokenResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetTokenResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.token != null && message.hasOwnProperty("token"))
            if (!$util.isString(message.token))
                return "token: string expected";
        if (message.fileId != null && message.hasOwnProperty("fileId"))
            if (!$util.isString(message.fileId))
                return "fileId: string expected";
        if (message.url != null && message.hasOwnProperty("url"))
            if (!$util.isString(message.url))
                return "url: string expected";
        return null;
    };

    /**
     * Creates a GetTokenResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetTokenResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetTokenResp} GetTokenResp
     */
    GetTokenResp.fromObject = function fromObject(object) {
        if (object instanceof $root.GetTokenResp)
            return object;
        let message = new $root.GetTokenResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GetTokenResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.token != null)
            message.token = String(object.token);
        if (object.fileId != null)
            message.fileId = String(object.fileId);
        if (object.url != null)
            message.url = String(object.url);
        return message;
    };

    /**
     * Creates a plain object from a GetTokenResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetTokenResp
     * @static
     * @param {GetTokenResp} message GetTokenResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetTokenResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.token = "";
            object.fileId = "";
            object.url = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.token != null && message.hasOwnProperty("token"))
            object.token = message.token;
        if (message.fileId != null && message.hasOwnProperty("fileId"))
            object.fileId = message.fileId;
        if (message.url != null && message.hasOwnProperty("url"))
            object.url = message.url;
        return object;
    };

    /**
     * Converts this GetTokenResp to JSON.
     * @function toJSON
     * @memberof GetTokenResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetTokenResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetTokenResp
     * @function getTypeUrl
     * @memberof GetTokenResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetTokenResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetTokenResp";
    };

    return GetTokenResp;
})();

export const CheckSmsCodeReq = $root.CheckSmsCodeReq = (() => {

    /**
     * Properties of a CheckSmsCodeReq.
     * @exports ICheckSmsCodeReq
     * @interface ICheckSmsCodeReq
     * @property {IClientInfo|null} [clientInfo] CheckSmsCodeReq clientInfo
     * @property {string|null} [countryCode] CheckSmsCodeReq countryCode
     * @property {string|null} [phone] CheckSmsCodeReq phone
     * @property {GetValidateCodeType|null} [type] CheckSmsCodeReq type
     * @property {string|null} [smsCode] CheckSmsCodeReq smsCode
     */

    /**
     * Constructs a new CheckSmsCodeReq.
     * @exports CheckSmsCodeReq
     * @classdesc Represents a CheckSmsCodeReq.
     * @implements ICheckSmsCodeReq
     * @constructor
     * @param {ICheckSmsCodeReq=} [properties] Properties to set
     */
    function CheckSmsCodeReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CheckSmsCodeReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof CheckSmsCodeReq
     * @instance
     */
    CheckSmsCodeReq.prototype.clientInfo = null;

    /**
     * CheckSmsCodeReq countryCode.
     * @member {string} countryCode
     * @memberof CheckSmsCodeReq
     * @instance
     */
    CheckSmsCodeReq.prototype.countryCode = "";

    /**
     * CheckSmsCodeReq phone.
     * @member {string} phone
     * @memberof CheckSmsCodeReq
     * @instance
     */
    CheckSmsCodeReq.prototype.phone = "";

    /**
     * CheckSmsCodeReq type.
     * @member {GetValidateCodeType} type
     * @memberof CheckSmsCodeReq
     * @instance
     */
    CheckSmsCodeReq.prototype.type = 0;

    /**
     * CheckSmsCodeReq smsCode.
     * @member {string} smsCode
     * @memberof CheckSmsCodeReq
     * @instance
     */
    CheckSmsCodeReq.prototype.smsCode = "";

    /**
     * Creates a new CheckSmsCodeReq instance using the specified properties.
     * @function create
     * @memberof CheckSmsCodeReq
     * @static
     * @param {ICheckSmsCodeReq=} [properties] Properties to set
     * @returns {CheckSmsCodeReq} CheckSmsCodeReq instance
     */
    CheckSmsCodeReq.create = function create(properties) {
        return new CheckSmsCodeReq(properties);
    };

    /**
     * Encodes the specified CheckSmsCodeReq message. Does not implicitly {@link CheckSmsCodeReq.verify|verify} messages.
     * @function encode
     * @memberof CheckSmsCodeReq
     * @static
     * @param {ICheckSmsCodeReq} message CheckSmsCodeReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CheckSmsCodeReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.countryCode != null && Object.hasOwnProperty.call(message, "countryCode"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.countryCode);
        if (message.phone != null && Object.hasOwnProperty.call(message, "phone"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.phone);
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.type);
        if (message.smsCode != null && Object.hasOwnProperty.call(message, "smsCode"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.smsCode);
        return writer;
    };

    /**
     * Encodes the specified CheckSmsCodeReq message, length delimited. Does not implicitly {@link CheckSmsCodeReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CheckSmsCodeReq
     * @static
     * @param {ICheckSmsCodeReq} message CheckSmsCodeReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CheckSmsCodeReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CheckSmsCodeReq message from the specified reader or buffer.
     * @function decode
     * @memberof CheckSmsCodeReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CheckSmsCodeReq} CheckSmsCodeReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CheckSmsCodeReq.decode = function decode(reader, length, error) {
    CheckSmsCodeReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CheckSmsCodeReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.countryCode = reader.string();
                    break;
                }
            case 3: {
                    message.phone = reader.string();
                    break;
                }
            case 4: {
                    message.type = reader.int32();
                    break;
                }
            case 5: {
                    message.smsCode = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CheckSmsCodeReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CheckSmsCodeReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CheckSmsCodeReq} CheckSmsCodeReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CheckSmsCodeReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CheckSmsCodeReq message.
     * @function verify
     * @memberof CheckSmsCodeReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CheckSmsCodeReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.countryCode != null && message.hasOwnProperty("countryCode"))
            if (!$util.isString(message.countryCode))
                return "countryCode: string expected";
        if (message.phone != null && message.hasOwnProperty("phone"))
            if (!$util.isString(message.phone))
                return "phone: string expected";
        if (message.type != null && message.hasOwnProperty("type"))
            switch (message.type) {
            default:
                return "type: enum value expected";
            case 0:
            case 1:
            case 2:
            case 3:
            case 4:
            case 6:
            case 7:
            case 8:
            case 9:
            case 10:
            case 11:
            case 12:
            case 13:
            case 14:
            case 15:
            case 16:
            case 17:
            case 18:
            case 19:
            case 20:
            case 21:
            case 22:
            case 23:
            case 23:
                break;
            }
        if (message.smsCode != null && message.hasOwnProperty("smsCode"))
            if (!$util.isString(message.smsCode))
                return "smsCode: string expected";
        return null;
    };

    /**
     * Creates a CheckSmsCodeReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CheckSmsCodeReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CheckSmsCodeReq} CheckSmsCodeReq
     */
    CheckSmsCodeReq.fromObject = function fromObject(object) {
        if (object instanceof $root.CheckSmsCodeReq)
            return object;
        let message = new $root.CheckSmsCodeReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".CheckSmsCodeReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.countryCode != null)
            message.countryCode = String(object.countryCode);
        if (object.phone != null)
            message.phone = String(object.phone);
        switch (object.type) {
        default:
            if (typeof object.type === "number") {
                message.type = object.type;
                break;
            }
            break;
        case "REG":
        case 0:
            message.type = 0;
            break;
        case "LOGIN":
        case 1:
            message.type = 1;
            break;
        case "FIND_PASSWORD":
        case 2:
            message.type = 2;
            break;
        case "UPDATE_PASSWORD":
        case 3:
            message.type = 3;
            break;
        case "UPDATE_PHONE":
        case 4:
            message.type = 4;
            break;
        case "VALIDATE_PASSWORD":
        case 6:
            message.type = 6;
            break;
        case "FIND_GESTURE_PASSWORD":
        case 7:
            message.type = 7;
            break;
        case "TRADE_PASSWORD":
        case 8:
            message.type = 8;
            break;
        case "UPDATE_TRADE_PASSWORD":
        case 9:
            message.type = 9;
            break;
        case "BIND_PHONE":
        case 10:
            message.type = 10;
            break;
        case "BIND_EMAIL":
        case 11:
            message.type = 11;
            break;
        case "UPDATE_EMAIL":
        case 12:
            message.type = 12;
            break;
        case "VALIDATE_GOOGLE_AUTH":
        case 13:
            message.type = 13;
            break;
        case "UPDATE_SECURE":
        case 14:
            message.type = 14;
            break;
        case "CASH_OUT":
        case 15:
            message.type = 15;
            break;
        case "VERIFY_EMAIL":
        case 16:
            message.type = 16;
            break;
        case "VERIFY_PHONE":
        case 17:
            message.type = 17;
            break;
        case "VERIFY_TRADE_PASSWORD":
        case 18:
            message.type = 18;
            break;
        case "VERIFY_GOOGLE_CODE":
        case 19:
            message.type = 19;
            break;
        case "BIND_ACCOUNT":
        case 20:
            message.type = 20;
            break;
        case "VERIFY_LOGIN_PHONE":
        case 21:
            message.type = 21;
            break;
        case "VERIFY_LOGIN_EMAIL":
        case 22:
            message.type = 22;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.type = 23;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.type = 23;
            break;
        }
        if (object.smsCode != null)
            message.smsCode = String(object.smsCode);
        return message;
    };

    /**
     * Creates a plain object from a CheckSmsCodeReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CheckSmsCodeReq
     * @static
     * @param {CheckSmsCodeReq} message CheckSmsCodeReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CheckSmsCodeReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.countryCode = "";
            object.phone = "";
            object.type = options.enums === String ? "REG" : 0;
            object.smsCode = "";
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.countryCode != null && message.hasOwnProperty("countryCode"))
            object.countryCode = message.countryCode;
        if (message.phone != null && message.hasOwnProperty("phone"))
            object.phone = message.phone;
        if (message.type != null && message.hasOwnProperty("type"))
            object.type = options.enums === String ? $root.GetValidateCodeType[message.type] === undefined ? message.type : $root.GetValidateCodeType[message.type] : message.type;
        if (message.smsCode != null && message.hasOwnProperty("smsCode"))
            object.smsCode = message.smsCode;
        return object;
    };

    /**
     * Converts this CheckSmsCodeReq to JSON.
     * @function toJSON
     * @memberof CheckSmsCodeReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CheckSmsCodeReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CheckSmsCodeReq
     * @function getTypeUrl
     * @memberof CheckSmsCodeReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CheckSmsCodeReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CheckSmsCodeReq";
    };

    return CheckSmsCodeReq;
})();

export const CheckSmsCodeResp = $root.CheckSmsCodeResp = (() => {

    /**
     * Properties of a CheckSmsCodeResp.
     * @exports ICheckSmsCodeResp
     * @interface ICheckSmsCodeResp
     * @property {ICommonResult|null} [commonResult] CheckSmsCodeResp commonResult
     */

    /**
     * Constructs a new CheckSmsCodeResp.
     * @exports CheckSmsCodeResp
     * @classdesc Represents a CheckSmsCodeResp.
     * @implements ICheckSmsCodeResp
     * @constructor
     * @param {ICheckSmsCodeResp=} [properties] Properties to set
     */
    function CheckSmsCodeResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CheckSmsCodeResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof CheckSmsCodeResp
     * @instance
     */
    CheckSmsCodeResp.prototype.commonResult = null;

    /**
     * Creates a new CheckSmsCodeResp instance using the specified properties.
     * @function create
     * @memberof CheckSmsCodeResp
     * @static
     * @param {ICheckSmsCodeResp=} [properties] Properties to set
     * @returns {CheckSmsCodeResp} CheckSmsCodeResp instance
     */
    CheckSmsCodeResp.create = function create(properties) {
        return new CheckSmsCodeResp(properties);
    };

    /**
     * Encodes the specified CheckSmsCodeResp message. Does not implicitly {@link CheckSmsCodeResp.verify|verify} messages.
     * @function encode
     * @memberof CheckSmsCodeResp
     * @static
     * @param {ICheckSmsCodeResp} message CheckSmsCodeResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CheckSmsCodeResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified CheckSmsCodeResp message, length delimited. Does not implicitly {@link CheckSmsCodeResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CheckSmsCodeResp
     * @static
     * @param {ICheckSmsCodeResp} message CheckSmsCodeResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CheckSmsCodeResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CheckSmsCodeResp message from the specified reader or buffer.
     * @function decode
     * @memberof CheckSmsCodeResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CheckSmsCodeResp} CheckSmsCodeResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CheckSmsCodeResp.decode = function decode(reader, length, error) {
    CheckSmsCodeResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CheckSmsCodeResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CheckSmsCodeResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CheckSmsCodeResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CheckSmsCodeResp} CheckSmsCodeResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CheckSmsCodeResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CheckSmsCodeResp message.
     * @function verify
     * @memberof CheckSmsCodeResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CheckSmsCodeResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates a CheckSmsCodeResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CheckSmsCodeResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CheckSmsCodeResp} CheckSmsCodeResp
     */
    CheckSmsCodeResp.fromObject = function fromObject(object) {
        if (object instanceof $root.CheckSmsCodeResp)
            return object;
        let message = new $root.CheckSmsCodeResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".CheckSmsCodeResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from a CheckSmsCodeResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CheckSmsCodeResp
     * @static
     * @param {CheckSmsCodeResp} message CheckSmsCodeResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CheckSmsCodeResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this CheckSmsCodeResp to JSON.
     * @function toJSON
     * @memberof CheckSmsCodeResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CheckSmsCodeResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CheckSmsCodeResp
     * @function getTypeUrl
     * @memberof CheckSmsCodeResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CheckSmsCodeResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CheckSmsCodeResp";
    };

    return CheckSmsCodeResp;
})();

export const FeedbackReq = $root.FeedbackReq = (() => {

    /**
     * Properties of a FeedbackReq.
     * @exports IFeedbackReq
     * @interface IFeedbackReq
     * @property {IClientInfo|null} [clientInfo] FeedbackReq clientInfo
     * @property {FeedbackType|null} [type] FeedbackReq type
     * @property {string|null} [msg] FeedbackReq msg
     * @property {string|null} [picUrls] FeedbackReq picUrls
     */

    /**
     * Constructs a new FeedbackReq.
     * @exports FeedbackReq
     * @classdesc Represents a FeedbackReq.
     * @implements IFeedbackReq
     * @constructor
     * @param {IFeedbackReq=} [properties] Properties to set
     */
    function FeedbackReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * FeedbackReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof FeedbackReq
     * @instance
     */
    FeedbackReq.prototype.clientInfo = null;

    /**
     * FeedbackReq type.
     * @member {FeedbackType} type
     * @memberof FeedbackReq
     * @instance
     */
    FeedbackReq.prototype.type = 0;

    /**
     * FeedbackReq msg.
     * @member {string} msg
     * @memberof FeedbackReq
     * @instance
     */
    FeedbackReq.prototype.msg = "";

    /**
     * FeedbackReq picUrls.
     * @member {string} picUrls
     * @memberof FeedbackReq
     * @instance
     */
    FeedbackReq.prototype.picUrls = "";

    /**
     * Creates a new FeedbackReq instance using the specified properties.
     * @function create
     * @memberof FeedbackReq
     * @static
     * @param {IFeedbackReq=} [properties] Properties to set
     * @returns {FeedbackReq} FeedbackReq instance
     */
    FeedbackReq.create = function create(properties) {
        return new FeedbackReq(properties);
    };

    /**
     * Encodes the specified FeedbackReq message. Does not implicitly {@link FeedbackReq.verify|verify} messages.
     * @function encode
     * @memberof FeedbackReq
     * @static
     * @param {IFeedbackReq} message FeedbackReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FeedbackReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
        if (message.msg != null && Object.hasOwnProperty.call(message, "msg"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.msg);
        if (message.picUrls != null && Object.hasOwnProperty.call(message, "picUrls"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.picUrls);
        return writer;
    };

    /**
     * Encodes the specified FeedbackReq message, length delimited. Does not implicitly {@link FeedbackReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof FeedbackReq
     * @static
     * @param {IFeedbackReq} message FeedbackReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FeedbackReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a FeedbackReq message from the specified reader or buffer.
     * @function decode
     * @memberof FeedbackReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {FeedbackReq} FeedbackReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FeedbackReq.decode = function decode(reader, length, error) {
    FeedbackReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.FeedbackReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.type = reader.int32();
                    break;
                }
            case 3: {
                    message.msg = reader.string();
                    break;
                }
            case 4: {
                    message.picUrls = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a FeedbackReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof FeedbackReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {FeedbackReq} FeedbackReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FeedbackReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a FeedbackReq message.
     * @function verify
     * @memberof FeedbackReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    FeedbackReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.type != null && message.hasOwnProperty("type"))
            switch (message.type) {
            default:
                return "type: enum value expected";
            case 0:
            case 1:
            case 2:
                break;
            }
        if (message.msg != null && message.hasOwnProperty("msg"))
            if (!$util.isString(message.msg))
                return "msg: string expected";
        if (message.picUrls != null && message.hasOwnProperty("picUrls"))
            if (!$util.isString(message.picUrls))
                return "picUrls: string expected";
        return null;
    };

    /**
     * Creates a FeedbackReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof FeedbackReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {FeedbackReq} FeedbackReq
     */
    FeedbackReq.fromObject = function fromObject(object) {
        if (object instanceof $root.FeedbackReq)
            return object;
        let message = new $root.FeedbackReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".FeedbackReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        switch (object.type) {
        default:
            if (typeof object.type === "number") {
                message.type = object.type;
                break;
            }
            break;
        case "SUGGEST":
        case 0:
            message.type = 0;
            break;
        case "MISTAKE":
        case 1:
            message.type = 1;
            break;
        case "OTHER":
        case 2:
            message.type = 2;
            break;
        }
        if (object.msg != null)
            message.msg = String(object.msg);
        if (object.picUrls != null)
            message.picUrls = String(object.picUrls);
        return message;
    };

    /**
     * Creates a plain object from a FeedbackReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof FeedbackReq
     * @static
     * @param {FeedbackReq} message FeedbackReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    FeedbackReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.type = options.enums === String ? "SUGGEST" : 0;
            object.msg = "";
            object.picUrls = "";
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.type != null && message.hasOwnProperty("type"))
            object.type = options.enums === String ? $root.FeedbackType[message.type] === undefined ? message.type : $root.FeedbackType[message.type] : message.type;
        if (message.msg != null && message.hasOwnProperty("msg"))
            object.msg = message.msg;
        if (message.picUrls != null && message.hasOwnProperty("picUrls"))
            object.picUrls = message.picUrls;
        return object;
    };

    /**
     * Converts this FeedbackReq to JSON.
     * @function toJSON
     * @memberof FeedbackReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    FeedbackReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for FeedbackReq
     * @function getTypeUrl
     * @memberof FeedbackReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    FeedbackReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/FeedbackReq";
    };

    return FeedbackReq;
})();

export const FeedbackResp = $root.FeedbackResp = (() => {

    /**
     * Properties of a FeedbackResp.
     * @exports IFeedbackResp
     * @interface IFeedbackResp
     * @property {ICommonResult|null} [commonResult] FeedbackResp commonResult
     */

    /**
     * Constructs a new FeedbackResp.
     * @exports FeedbackResp
     * @classdesc Represents a FeedbackResp.
     * @implements IFeedbackResp
     * @constructor
     * @param {IFeedbackResp=} [properties] Properties to set
     */
    function FeedbackResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * FeedbackResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof FeedbackResp
     * @instance
     */
    FeedbackResp.prototype.commonResult = null;

    /**
     * Creates a new FeedbackResp instance using the specified properties.
     * @function create
     * @memberof FeedbackResp
     * @static
     * @param {IFeedbackResp=} [properties] Properties to set
     * @returns {FeedbackResp} FeedbackResp instance
     */
    FeedbackResp.create = function create(properties) {
        return new FeedbackResp(properties);
    };

    /**
     * Encodes the specified FeedbackResp message. Does not implicitly {@link FeedbackResp.verify|verify} messages.
     * @function encode
     * @memberof FeedbackResp
     * @static
     * @param {IFeedbackResp} message FeedbackResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FeedbackResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified FeedbackResp message, length delimited. Does not implicitly {@link FeedbackResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof FeedbackResp
     * @static
     * @param {IFeedbackResp} message FeedbackResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FeedbackResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a FeedbackResp message from the specified reader or buffer.
     * @function decode
     * @memberof FeedbackResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {FeedbackResp} FeedbackResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FeedbackResp.decode = function decode(reader, length, error) {
    FeedbackResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.FeedbackResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a FeedbackResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof FeedbackResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {FeedbackResp} FeedbackResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FeedbackResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a FeedbackResp message.
     * @function verify
     * @memberof FeedbackResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    FeedbackResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates a FeedbackResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof FeedbackResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {FeedbackResp} FeedbackResp
     */
    FeedbackResp.fromObject = function fromObject(object) {
        if (object instanceof $root.FeedbackResp)
            return object;
        let message = new $root.FeedbackResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".FeedbackResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from a FeedbackResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof FeedbackResp
     * @static
     * @param {FeedbackResp} message FeedbackResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    FeedbackResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this FeedbackResp to JSON.
     * @function toJSON
     * @memberof FeedbackResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    FeedbackResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for FeedbackResp
     * @function getTypeUrl
     * @memberof FeedbackResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    FeedbackResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/FeedbackResp";
    };

    return FeedbackResp;
})();

export const CheckVersionReq = $root.CheckVersionReq = (() => {

    /**
     * Properties of a CheckVersionReq.
     * @exports ICheckVersionReq
     * @interface ICheckVersionReq
     * @property {IClientInfo|null} [clientInfo] CheckVersionReq clientInfo
     */

    /**
     * Constructs a new CheckVersionReq.
     * @exports CheckVersionReq
     * @classdesc Represents a CheckVersionReq.
     * @implements ICheckVersionReq
     * @constructor
     * @param {ICheckVersionReq=} [properties] Properties to set
     */
    function CheckVersionReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CheckVersionReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof CheckVersionReq
     * @instance
     */
    CheckVersionReq.prototype.clientInfo = null;

    /**
     * Creates a new CheckVersionReq instance using the specified properties.
     * @function create
     * @memberof CheckVersionReq
     * @static
     * @param {ICheckVersionReq=} [properties] Properties to set
     * @returns {CheckVersionReq} CheckVersionReq instance
     */
    CheckVersionReq.create = function create(properties) {
        return new CheckVersionReq(properties);
    };

    /**
     * Encodes the specified CheckVersionReq message. Does not implicitly {@link CheckVersionReq.verify|verify} messages.
     * @function encode
     * @memberof CheckVersionReq
     * @static
     * @param {ICheckVersionReq} message CheckVersionReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CheckVersionReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified CheckVersionReq message, length delimited. Does not implicitly {@link CheckVersionReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CheckVersionReq
     * @static
     * @param {ICheckVersionReq} message CheckVersionReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CheckVersionReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CheckVersionReq message from the specified reader or buffer.
     * @function decode
     * @memberof CheckVersionReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CheckVersionReq} CheckVersionReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CheckVersionReq.decode = function decode(reader, length, error) {
    CheckVersionReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CheckVersionReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CheckVersionReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CheckVersionReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CheckVersionReq} CheckVersionReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CheckVersionReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CheckVersionReq message.
     * @function verify
     * @memberof CheckVersionReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CheckVersionReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        return null;
    };

    /**
     * Creates a CheckVersionReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CheckVersionReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CheckVersionReq} CheckVersionReq
     */
    CheckVersionReq.fromObject = function fromObject(object) {
        if (object instanceof $root.CheckVersionReq)
            return object;
        let message = new $root.CheckVersionReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".CheckVersionReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        return message;
    };

    /**
     * Creates a plain object from a CheckVersionReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CheckVersionReq
     * @static
     * @param {CheckVersionReq} message CheckVersionReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CheckVersionReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.clientInfo = null;
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        return object;
    };

    /**
     * Converts this CheckVersionReq to JSON.
     * @function toJSON
     * @memberof CheckVersionReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CheckVersionReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CheckVersionReq
     * @function getTypeUrl
     * @memberof CheckVersionReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CheckVersionReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CheckVersionReq";
    };

    return CheckVersionReq;
})();

export const CheckVersionResp = $root.CheckVersionResp = (() => {

    /**
     * Properties of a CheckVersionResp.
     * @exports ICheckVersionResp
     * @interface ICheckVersionResp
     * @property {ICommonResult|null} [commonResult] CheckVersionResp commonResult
     * @property {CheckVersionFlag|null} [flag] CheckVersionResp flag
     * @property {string|null} [title] CheckVersionResp title
     * @property {string|null} [content] CheckVersionResp content
     * @property {string|null} [url] CheckVersionResp url
     * @property {number|null} [version] CheckVersionResp version
     */

    /**
     * Constructs a new CheckVersionResp.
     * @exports CheckVersionResp
     * @classdesc Represents a CheckVersionResp.
     * @implements ICheckVersionResp
     * @constructor
     * @param {ICheckVersionResp=} [properties] Properties to set
     */
    function CheckVersionResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CheckVersionResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof CheckVersionResp
     * @instance
     */
    CheckVersionResp.prototype.commonResult = null;

    /**
     * CheckVersionResp flag.
     * @member {CheckVersionFlag} flag
     * @memberof CheckVersionResp
     * @instance
     */
    CheckVersionResp.prototype.flag = 0;

    /**
     * CheckVersionResp title.
     * @member {string} title
     * @memberof CheckVersionResp
     * @instance
     */
    CheckVersionResp.prototype.title = "";

    /**
     * CheckVersionResp content.
     * @member {string} content
     * @memberof CheckVersionResp
     * @instance
     */
    CheckVersionResp.prototype.content = "";

    /**
     * CheckVersionResp url.
     * @member {string} url
     * @memberof CheckVersionResp
     * @instance
     */
    CheckVersionResp.prototype.url = "";

    /**
     * CheckVersionResp version.
     * @member {number} version
     * @memberof CheckVersionResp
     * @instance
     */
    CheckVersionResp.prototype.version = 0;

    /**
     * Creates a new CheckVersionResp instance using the specified properties.
     * @function create
     * @memberof CheckVersionResp
     * @static
     * @param {ICheckVersionResp=} [properties] Properties to set
     * @returns {CheckVersionResp} CheckVersionResp instance
     */
    CheckVersionResp.create = function create(properties) {
        return new CheckVersionResp(properties);
    };

    /**
     * Encodes the specified CheckVersionResp message. Does not implicitly {@link CheckVersionResp.verify|verify} messages.
     * @function encode
     * @memberof CheckVersionResp
     * @static
     * @param {ICheckVersionResp} message CheckVersionResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CheckVersionResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.flag != null && Object.hasOwnProperty.call(message, "flag"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.flag);
        if (message.title != null && Object.hasOwnProperty.call(message, "title"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.title);
        if (message.content != null && Object.hasOwnProperty.call(message, "content"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.content);
        if (message.url != null && Object.hasOwnProperty.call(message, "url"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.url);
        if (message.version != null && Object.hasOwnProperty.call(message, "version"))
            writer.uint32(/* id 6, wireType 0 =*/48).int32(message.version);
        return writer;
    };

    /**
     * Encodes the specified CheckVersionResp message, length delimited. Does not implicitly {@link CheckVersionResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CheckVersionResp
     * @static
     * @param {ICheckVersionResp} message CheckVersionResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CheckVersionResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CheckVersionResp message from the specified reader or buffer.
     * @function decode
     * @memberof CheckVersionResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CheckVersionResp} CheckVersionResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CheckVersionResp.decode = function decode(reader, length, error) {
    CheckVersionResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CheckVersionResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.flag = reader.int32();
                    break;
                }
            case 3: {
                    message.title = reader.string();
                    break;
                }
            case 4: {
                    message.content = reader.string();
                    break;
                }
            case 5: {
                    message.url = reader.string();
                    break;
                }
            case 6: {
                    message.version = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CheckVersionResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CheckVersionResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CheckVersionResp} CheckVersionResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CheckVersionResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CheckVersionResp message.
     * @function verify
     * @memberof CheckVersionResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CheckVersionResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.flag != null && message.hasOwnProperty("flag"))
            switch (message.flag) {
            default:
                return "flag: enum value expected";
            case 0:
            case 1:
            case 2:
                break;
            }
        if (message.title != null && message.hasOwnProperty("title"))
            if (!$util.isString(message.title))
                return "title: string expected";
        if (message.content != null && message.hasOwnProperty("content"))
            if (!$util.isString(message.content))
                return "content: string expected";
        if (message.url != null && message.hasOwnProperty("url"))
            if (!$util.isString(message.url))
                return "url: string expected";
        if (message.version != null && message.hasOwnProperty("version"))
            if (!$util.isInteger(message.version))
                return "version: integer expected";
        return null;
    };

    /**
     * Creates a CheckVersionResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CheckVersionResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CheckVersionResp} CheckVersionResp
     */
    CheckVersionResp.fromObject = function fromObject(object) {
        if (object instanceof $root.CheckVersionResp)
            return object;
        let message = new $root.CheckVersionResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".CheckVersionResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        switch (object.flag) {
        default:
            if (typeof object.flag === "number") {
                message.flag = object.flag;
                break;
            }
            break;
        case "NOT_UP":
        case 0:
            message.flag = 0;
            break;
        case "CAN_UP":
        case 1:
            message.flag = 1;
            break;
        case "MUST_UP":
        case 2:
            message.flag = 2;
            break;
        }
        if (object.title != null)
            message.title = String(object.title);
        if (object.content != null)
            message.content = String(object.content);
        if (object.url != null)
            message.url = String(object.url);
        if (object.version != null)
            message.version = object.version | 0;
        return message;
    };

    /**
     * Creates a plain object from a CheckVersionResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CheckVersionResp
     * @static
     * @param {CheckVersionResp} message CheckVersionResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CheckVersionResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.flag = options.enums === String ? "NOT_UP" : 0;
            object.title = "";
            object.content = "";
            object.url = "";
            object.version = 0;
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.flag != null && message.hasOwnProperty("flag"))
            object.flag = options.enums === String ? $root.CheckVersionFlag[message.flag] === undefined ? message.flag : $root.CheckVersionFlag[message.flag] : message.flag;
        if (message.title != null && message.hasOwnProperty("title"))
            object.title = message.title;
        if (message.content != null && message.hasOwnProperty("content"))
            object.content = message.content;
        if (message.url != null && message.hasOwnProperty("url"))
            object.url = message.url;
        if (message.version != null && message.hasOwnProperty("version"))
            object.version = message.version;
        return object;
    };

    /**
     * Converts this CheckVersionResp to JSON.
     * @function toJSON
     * @memberof CheckVersionResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CheckVersionResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CheckVersionResp
     * @function getTypeUrl
     * @memberof CheckVersionResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CheckVersionResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CheckVersionResp";
    };

    return CheckVersionResp;
})();

export const CoinConfig = $root.CoinConfig = (() => {

    /**
     * Properties of a CoinConfig.
     * @exports ICoinConfig
     * @interface ICoinConfig
     * @property {string|null} [coinName] CoinConfig coinName
     * @property {number|null} [readScale] CoinConfig readScale
     * @property {number|null} [writeScale] CoinConfig writeScale
     * @property {string|null} [primaryTradeDay] CoinConfig primaryTradeDay
     * @property {string|null} [advancedTradeDay] CoinConfig advancedTradeDay
     */

    /**
     * Constructs a new CoinConfig.
     * @exports CoinConfig
     * @classdesc Represents a CoinConfig.
     * @implements ICoinConfig
     * @constructor
     * @param {ICoinConfig=} [properties] Properties to set
     */
    function CoinConfig(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CoinConfig coinName.
     * @member {string} coinName
     * @memberof CoinConfig
     * @instance
     */
    CoinConfig.prototype.coinName = "";

    /**
     * CoinConfig readScale.
     * @member {number} readScale
     * @memberof CoinConfig
     * @instance
     */
    CoinConfig.prototype.readScale = 0;

    /**
     * CoinConfig writeScale.
     * @member {number} writeScale
     * @memberof CoinConfig
     * @instance
     */
    CoinConfig.prototype.writeScale = 0;

    /**
     * CoinConfig primaryTradeDay.
     * @member {string} primaryTradeDay
     * @memberof CoinConfig
     * @instance
     */
    CoinConfig.prototype.primaryTradeDay = "";

    /**
     * CoinConfig advancedTradeDay.
     * @member {string} advancedTradeDay
     * @memberof CoinConfig
     * @instance
     */
    CoinConfig.prototype.advancedTradeDay = "";

    /**
     * Creates a new CoinConfig instance using the specified properties.
     * @function create
     * @memberof CoinConfig
     * @static
     * @param {ICoinConfig=} [properties] Properties to set
     * @returns {CoinConfig} CoinConfig instance
     */
    CoinConfig.create = function create(properties) {
        return new CoinConfig(properties);
    };

    /**
     * Encodes the specified CoinConfig message. Does not implicitly {@link CoinConfig.verify|verify} messages.
     * @function encode
     * @memberof CoinConfig
     * @static
     * @param {ICoinConfig} message CoinConfig message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CoinConfig.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.coinName != null && Object.hasOwnProperty.call(message, "coinName"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.coinName);
        if (message.readScale != null && Object.hasOwnProperty.call(message, "readScale"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.readScale);
        if (message.writeScale != null && Object.hasOwnProperty.call(message, "writeScale"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.writeScale);
        if (message.primaryTradeDay != null && Object.hasOwnProperty.call(message, "primaryTradeDay"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.primaryTradeDay);
        if (message.advancedTradeDay != null && Object.hasOwnProperty.call(message, "advancedTradeDay"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.advancedTradeDay);
        return writer;
    };

    /**
     * Encodes the specified CoinConfig message, length delimited. Does not implicitly {@link CoinConfig.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CoinConfig
     * @static
     * @param {ICoinConfig} message CoinConfig message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CoinConfig.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CoinConfig message from the specified reader or buffer.
     * @function decode
     * @memberof CoinConfig
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CoinConfig} CoinConfig
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CoinConfig.decode = function decode(reader, length, error) {
    CoinConfig.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CoinConfig();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.coinName = reader.string();
                    break;
                }
            case 2: {
                    message.readScale = reader.int32();
                    break;
                }
            case 3: {
                    message.writeScale = reader.int32();
                    break;
                }
            case 4: {
                    message.primaryTradeDay = reader.string();
                    break;
                }
            case 5: {
                    message.advancedTradeDay = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CoinConfig message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CoinConfig
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CoinConfig} CoinConfig
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CoinConfig.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CoinConfig message.
     * @function verify
     * @memberof CoinConfig
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CoinConfig.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.coinName != null && message.hasOwnProperty("coinName"))
            if (!$util.isString(message.coinName))
                return "coinName: string expected";
        if (message.readScale != null && message.hasOwnProperty("readScale"))
            if (!$util.isInteger(message.readScale))
                return "readScale: integer expected";
        if (message.writeScale != null && message.hasOwnProperty("writeScale"))
            if (!$util.isInteger(message.writeScale))
                return "writeScale: integer expected";
        if (message.primaryTradeDay != null && message.hasOwnProperty("primaryTradeDay"))
            if (!$util.isString(message.primaryTradeDay))
                return "primaryTradeDay: string expected";
        if (message.advancedTradeDay != null && message.hasOwnProperty("advancedTradeDay"))
            if (!$util.isString(message.advancedTradeDay))
                return "advancedTradeDay: string expected";
        return null;
    };

    /**
     * Creates a CoinConfig message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CoinConfig
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CoinConfig} CoinConfig
     */
    CoinConfig.fromObject = function fromObject(object) {
        if (object instanceof $root.CoinConfig)
            return object;
        let message = new $root.CoinConfig();
        if (object.coinName != null)
            message.coinName = String(object.coinName);
        if (object.readScale != null)
            message.readScale = object.readScale | 0;
        if (object.writeScale != null)
            message.writeScale = object.writeScale | 0;
        if (object.primaryTradeDay != null)
            message.primaryTradeDay = String(object.primaryTradeDay);
        if (object.advancedTradeDay != null)
            message.advancedTradeDay = String(object.advancedTradeDay);
        return message;
    };

    /**
     * Creates a plain object from a CoinConfig message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CoinConfig
     * @static
     * @param {CoinConfig} message CoinConfig
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CoinConfig.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.coinName = "";
            object.readScale = 0;
            object.writeScale = 0;
            object.primaryTradeDay = "";
            object.advancedTradeDay = "";
        }
        if (message.coinName != null && message.hasOwnProperty("coinName"))
            object.coinName = message.coinName;
        if (message.readScale != null && message.hasOwnProperty("readScale"))
            object.readScale = message.readScale;
        if (message.writeScale != null && message.hasOwnProperty("writeScale"))
            object.writeScale = message.writeScale;
        if (message.primaryTradeDay != null && message.hasOwnProperty("primaryTradeDay"))
            object.primaryTradeDay = message.primaryTradeDay;
        if (message.advancedTradeDay != null && message.hasOwnProperty("advancedTradeDay"))
            object.advancedTradeDay = message.advancedTradeDay;
        return object;
    };

    /**
     * Converts this CoinConfig to JSON.
     * @function toJSON
     * @memberof CoinConfig
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CoinConfig.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CoinConfig
     * @function getTypeUrl
     * @memberof CoinConfig
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CoinConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CoinConfig";
    };

    return CoinConfig;
})();

export const ConfigReq = $root.ConfigReq = (() => {

    /**
     * Properties of a ConfigReq.
     * @exports IConfigReq
     * @interface IConfigReq
     * @property {IClientInfo|null} [clientInfo] ConfigReq clientInfo
     * @property {Array.<string>|null} [coinNames] ConfigReq coinNames
     */

    /**
     * Constructs a new ConfigReq.
     * @exports ConfigReq
     * @classdesc Represents a ConfigReq.
     * @implements IConfigReq
     * @constructor
     * @param {IConfigReq=} [properties] Properties to set
     */
    function ConfigReq(properties) {
        this.coinNames = [];
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * ConfigReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof ConfigReq
     * @instance
     */
    ConfigReq.prototype.clientInfo = null;

    /**
     * ConfigReq coinNames.
     * @member {Array.<string>} coinNames
     * @memberof ConfigReq
     * @instance
     */
    ConfigReq.prototype.coinNames = $util.emptyArray;

    /**
     * Creates a new ConfigReq instance using the specified properties.
     * @function create
     * @memberof ConfigReq
     * @static
     * @param {IConfigReq=} [properties] Properties to set
     * @returns {ConfigReq} ConfigReq instance
     */
    ConfigReq.create = function create(properties) {
        return new ConfigReq(properties);
    };

    /**
     * Encodes the specified ConfigReq message. Does not implicitly {@link ConfigReq.verify|verify} messages.
     * @function encode
     * @memberof ConfigReq
     * @static
     * @param {IConfigReq} message ConfigReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ConfigReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.coinNames != null && message.coinNames.length)
            for (let i = 0; i < message.coinNames.length; ++i)
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.coinNames[i]);
        return writer;
    };

    /**
     * Encodes the specified ConfigReq message, length delimited. Does not implicitly {@link ConfigReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof ConfigReq
     * @static
     * @param {IConfigReq} message ConfigReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ConfigReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ConfigReq message from the specified reader or buffer.
     * @function decode
     * @memberof ConfigReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {ConfigReq} ConfigReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ConfigReq.decode = function decode(reader, length, error) {
    ConfigReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.ConfigReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    if (!(message.coinNames && message.coinNames.length))
                        message.coinNames = [];
                    message.coinNames.push(reader.string());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a ConfigReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof ConfigReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {ConfigReq} ConfigReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ConfigReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ConfigReq message.
     * @function verify
     * @memberof ConfigReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ConfigReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.coinNames != null && message.hasOwnProperty("coinNames")) {
            if (!Array.isArray(message.coinNames))
                return "coinNames: array expected";
            for (let i = 0; i < message.coinNames.length; ++i)
                if (!$util.isString(message.coinNames[i]))
                    return "coinNames: string[] expected";
        }
        return null;
    };

    /**
     * Creates a ConfigReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof ConfigReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {ConfigReq} ConfigReq
     */
    ConfigReq.fromObject = function fromObject(object) {
        if (object instanceof $root.ConfigReq)
            return object;
        let message = new $root.ConfigReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".ConfigReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.coinNames) {
            if (!Array.isArray(object.coinNames))
                throw TypeError(".ConfigReq.coinNames: array expected");
            message.coinNames = [];
            for (let i = 0; i < object.coinNames.length; ++i)
                message.coinNames[i] = String(object.coinNames[i]);
        }
        return message;
    };

    /**
     * Creates a plain object from a ConfigReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof ConfigReq
     * @static
     * @param {ConfigReq} message ConfigReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ConfigReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.arrays || options.defaults)
            object.coinNames = [];
        if (options.defaults)
            object.clientInfo = null;
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.coinNames && message.coinNames.length) {
            object.coinNames = [];
            for (let j = 0; j < message.coinNames.length; ++j)
                object.coinNames[j] = message.coinNames[j];
        }
        return object;
    };

    /**
     * Converts this ConfigReq to JSON.
     * @function toJSON
     * @memberof ConfigReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ConfigReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ConfigReq
     * @function getTypeUrl
     * @memberof ConfigReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ConfigReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/ConfigReq";
    };

    return ConfigReq;
})();

export const ConfigResp = $root.ConfigResp = (() => {

    /**
     * Properties of a ConfigResp.
     * @exports IConfigResp
     * @interface IConfigResp
     * @property {ICommonResult|null} [commonResult] ConfigResp commonResult
     * @property {Array.<ICoinConfig>|null} [CoinConfigs] ConfigResp CoinConfigs
     * @property {number|null} [bankLimit] ConfigResp bankLimit
     */

    /**
     * Constructs a new ConfigResp.
     * @exports ConfigResp
     * @classdesc Represents a ConfigResp.
     * @implements IConfigResp
     * @constructor
     * @param {IConfigResp=} [properties] Properties to set
     */
    function ConfigResp(properties) {
        this.CoinConfigs = [];
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * ConfigResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof ConfigResp
     * @instance
     */
    ConfigResp.prototype.commonResult = null;

    /**
     * ConfigResp CoinConfigs.
     * @member {Array.<ICoinConfig>} CoinConfigs
     * @memberof ConfigResp
     * @instance
     */
    ConfigResp.prototype.CoinConfigs = $util.emptyArray;

    /**
     * ConfigResp bankLimit.
     * @member {number} bankLimit
     * @memberof ConfigResp
     * @instance
     */
    ConfigResp.prototype.bankLimit = 0;

    /**
     * Creates a new ConfigResp instance using the specified properties.
     * @function create
     * @memberof ConfigResp
     * @static
     * @param {IConfigResp=} [properties] Properties to set
     * @returns {ConfigResp} ConfigResp instance
     */
    ConfigResp.create = function create(properties) {
        return new ConfigResp(properties);
    };

    /**
     * Encodes the specified ConfigResp message. Does not implicitly {@link ConfigResp.verify|verify} messages.
     * @function encode
     * @memberof ConfigResp
     * @static
     * @param {IConfigResp} message ConfigResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ConfigResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.CoinConfigs != null && message.CoinConfigs.length)
            for (let i = 0; i < message.CoinConfigs.length; ++i)
                $root.CoinConfig.encode(message.CoinConfigs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
        if (message.bankLimit != null && Object.hasOwnProperty.call(message, "bankLimit"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.bankLimit);
        return writer;
    };

    /**
     * Encodes the specified ConfigResp message, length delimited. Does not implicitly {@link ConfigResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof ConfigResp
     * @static
     * @param {IConfigResp} message ConfigResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ConfigResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ConfigResp message from the specified reader or buffer.
     * @function decode
     * @memberof ConfigResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {ConfigResp} ConfigResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ConfigResp.decode = function decode(reader, length, error) {
    ConfigResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.ConfigResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    if (!(message.CoinConfigs && message.CoinConfigs.length))
                        message.CoinConfigs = [];
                    message.CoinConfigs.push($root.CoinConfig.decode(reader, reader.uint32()));
                    break;
                }
            case 5: {
                    message.bankLimit = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a ConfigResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof ConfigResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {ConfigResp} ConfigResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ConfigResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ConfigResp message.
     * @function verify
     * @memberof ConfigResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ConfigResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.CoinConfigs != null && message.hasOwnProperty("CoinConfigs")) {
            if (!Array.isArray(message.CoinConfigs))
                return "CoinConfigs: array expected";
            for (let i = 0; i < message.CoinConfigs.length; ++i) {
                let error = $root.CoinConfig.verify(message.CoinConfigs[i]);
                if (error)
                    return "CoinConfigs." + error;
            }
        }
        if (message.bankLimit != null && message.hasOwnProperty("bankLimit"))
            if (!$util.isInteger(message.bankLimit))
                return "bankLimit: integer expected";
        return null;
    };

    /**
     * Creates a ConfigResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof ConfigResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {ConfigResp} ConfigResp
     */
    ConfigResp.fromObject = function fromObject(object) {
        if (object instanceof $root.ConfigResp)
            return object;
        let message = new $root.ConfigResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".ConfigResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.CoinConfigs) {
            if (!Array.isArray(object.CoinConfigs))
                throw TypeError(".ConfigResp.CoinConfigs: array expected");
            message.CoinConfigs = [];
            for (let i = 0; i < object.CoinConfigs.length; ++i) {
                if (typeof object.CoinConfigs[i] !== "object")
                    throw TypeError(".ConfigResp.CoinConfigs: object expected");
                message.CoinConfigs[i] = $root.CoinConfig.fromObject(object.CoinConfigs[i]);
            }
        }
        if (object.bankLimit != null)
            message.bankLimit = object.bankLimit | 0;
        return message;
    };

    /**
     * Creates a plain object from a ConfigResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof ConfigResp
     * @static
     * @param {ConfigResp} message ConfigResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ConfigResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.arrays || options.defaults)
            object.CoinConfigs = [];
        if (options.defaults) {
            object.commonResult = null;
            object.bankLimit = 0;
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.CoinConfigs && message.CoinConfigs.length) {
            object.CoinConfigs = [];
            for (let j = 0; j < message.CoinConfigs.length; ++j)
                object.CoinConfigs[j] = $root.CoinConfig.toObject(message.CoinConfigs[j], options);
        }
        if (message.bankLimit != null && message.hasOwnProperty("bankLimit"))
            object.bankLimit = message.bankLimit;
        return object;
    };

    /**
     * Converts this ConfigResp to JSON.
     * @function toJSON
     * @memberof ConfigResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ConfigResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ConfigResp
     * @function getTypeUrl
     * @memberof ConfigResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ConfigResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/ConfigResp";
    };

    return ConfigResp;
})();

export const GetKeyPairReq = $root.GetKeyPairReq = (() => {

    /**
     * Properties of a GetKeyPairReq.
     * @exports IGetKeyPairReq
     * @interface IGetKeyPairReq
     * @property {IClientInfo|null} [clientInfo] GetKeyPairReq clientInfo
     * @property {KeyPairType|null} [flag] GetKeyPairReq flag
     * @property {number|Long|null} [targetId] GetKeyPairReq targetId
     */

    /**
     * Constructs a new GetKeyPairReq.
     * @exports GetKeyPairReq
     * @classdesc Represents a GetKeyPairReq.
     * @implements IGetKeyPairReq
     * @constructor
     * @param {IGetKeyPairReq=} [properties] Properties to set
     */
    function GetKeyPairReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetKeyPairReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GetKeyPairReq
     * @instance
     */
    GetKeyPairReq.prototype.clientInfo = null;

    /**
     * GetKeyPairReq flag.
     * @member {KeyPairType} flag
     * @memberof GetKeyPairReq
     * @instance
     */
    GetKeyPairReq.prototype.flag = 0;

    /**
     * GetKeyPairReq targetId.
     * @member {number|Long} targetId
     * @memberof GetKeyPairReq
     * @instance
     */
    GetKeyPairReq.prototype.targetId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * Creates a new GetKeyPairReq instance using the specified properties.
     * @function create
     * @memberof GetKeyPairReq
     * @static
     * @param {IGetKeyPairReq=} [properties] Properties to set
     * @returns {GetKeyPairReq} GetKeyPairReq instance
     */
    GetKeyPairReq.create = function create(properties) {
        return new GetKeyPairReq(properties);
    };

    /**
     * Encodes the specified GetKeyPairReq message. Does not implicitly {@link GetKeyPairReq.verify|verify} messages.
     * @function encode
     * @memberof GetKeyPairReq
     * @static
     * @param {IGetKeyPairReq} message GetKeyPairReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetKeyPairReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.flag != null && Object.hasOwnProperty.call(message, "flag"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.flag);
        if (message.targetId != null && Object.hasOwnProperty.call(message, "targetId"))
            writer.uint32(/* id 3, wireType 0 =*/24).int64(message.targetId);
        return writer;
    };

    /**
     * Encodes the specified GetKeyPairReq message, length delimited. Does not implicitly {@link GetKeyPairReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetKeyPairReq
     * @static
     * @param {IGetKeyPairReq} message GetKeyPairReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetKeyPairReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetKeyPairReq message from the specified reader or buffer.
     * @function decode
     * @memberof GetKeyPairReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetKeyPairReq} GetKeyPairReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetKeyPairReq.decode = function decode(reader, length, error) {
    GetKeyPairReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetKeyPairReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.flag = reader.int32();
                    break;
                }
            case 3: {
                    message.targetId = reader.int64();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetKeyPairReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetKeyPairReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetKeyPairReq} GetKeyPairReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetKeyPairReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetKeyPairReq message.
     * @function verify
     * @memberof GetKeyPairReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetKeyPairReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.flag != null && message.hasOwnProperty("flag"))
            switch (message.flag) {
            default:
                return "flag: enum value expected";
            case 0:
            case 1:
            case 2:
            case 3:
            case 3:
                break;
            }
        if (message.targetId != null && message.hasOwnProperty("targetId"))
            if (!$util.isInteger(message.targetId) && !(message.targetId && $util.isInteger(message.targetId.low) && $util.isInteger(message.targetId.high)))
                return "targetId: integer|Long expected";
        return null;
    };

    /**
     * Creates a GetKeyPairReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetKeyPairReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetKeyPairReq} GetKeyPairReq
     */
    GetKeyPairReq.fromObject = function fromObject(object) {
        if (object instanceof $root.GetKeyPairReq)
            return object;
        let message = new $root.GetKeyPairReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GetKeyPairReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        switch (object.flag) {
        default:
            if (typeof object.flag === "number") {
                message.flag = object.flag;
                break;
            }
            break;
        case "KEY_USER":
        case 0:
            message.flag = 0;
            break;
        case "KEY_GROUP":
        case 1:
            message.flag = 1;
            break;
        case "KEY_USER_WEB":
        case 2:
            message.flag = 2;
            break;
        case "KEY_CHANNEL":
        case 3:
            message.flag = 3;
            break;
        case "KEY_CHANNEL":
        case 3:
            message.flag = 3;
            break;
        }
        if (object.targetId != null)
            if ($util.Long)
                (message.targetId = $util.Long.fromValue(object.targetId)).unsigned = false;
            else if (typeof object.targetId === "string")
                message.targetId = parseInt(object.targetId, 10);
            else if (typeof object.targetId === "number")
                message.targetId = object.targetId;
            else if (typeof object.targetId === "object")
                message.targetId = new $util.LongBits(object.targetId.low >>> 0, object.targetId.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a GetKeyPairReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetKeyPairReq
     * @static
     * @param {GetKeyPairReq} message GetKeyPairReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetKeyPairReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.flag = options.enums === String ? "KEY_USER" : 0;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.targetId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.targetId = options.longs === String ? "0" : 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.flag != null && message.hasOwnProperty("flag"))
            object.flag = options.enums === String ? $root.KeyPairType[message.flag] === undefined ? message.flag : $root.KeyPairType[message.flag] : message.flag;
        if (message.targetId != null && message.hasOwnProperty("targetId"))
            if (typeof message.targetId === "number")
                object.targetId = options.longs === String ? String(message.targetId) : message.targetId;
            else
                object.targetId = options.longs === String ? $util.Long.prototype.toString.call(message.targetId) : options.longs === Number ? new $util.LongBits(message.targetId.low >>> 0, message.targetId.high >>> 0).toNumber() : message.targetId;
        return object;
    };

    /**
     * Converts this GetKeyPairReq to JSON.
     * @function toJSON
     * @memberof GetKeyPairReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetKeyPairReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetKeyPairReq
     * @function getTypeUrl
     * @memberof GetKeyPairReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetKeyPairReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetKeyPairReq";
    };

    return GetKeyPairReq;
})();

export const GetKeyPairResp = $root.GetKeyPairResp = (() => {

    /**
     * Properties of a GetKeyPairResp.
     * @exports IGetKeyPairResp
     * @interface IGetKeyPairResp
     * @property {ICommonResult|null} [commonResult] GetKeyPairResp commonResult
     * @property {IKeyPairBase|null} [keyPair] GetKeyPairResp keyPair
     */

    /**
     * Constructs a new GetKeyPairResp.
     * @exports GetKeyPairResp
     * @classdesc Represents a GetKeyPairResp.
     * @implements IGetKeyPairResp
     * @constructor
     * @param {IGetKeyPairResp=} [properties] Properties to set
     */
    function GetKeyPairResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetKeyPairResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GetKeyPairResp
     * @instance
     */
    GetKeyPairResp.prototype.commonResult = null;

    /**
     * GetKeyPairResp keyPair.
     * @member {IKeyPairBase|null|undefined} keyPair
     * @memberof GetKeyPairResp
     * @instance
     */
    GetKeyPairResp.prototype.keyPair = null;

    /**
     * Creates a new GetKeyPairResp instance using the specified properties.
     * @function create
     * @memberof GetKeyPairResp
     * @static
     * @param {IGetKeyPairResp=} [properties] Properties to set
     * @returns {GetKeyPairResp} GetKeyPairResp instance
     */
    GetKeyPairResp.create = function create(properties) {
        return new GetKeyPairResp(properties);
    };

    /**
     * Encodes the specified GetKeyPairResp message. Does not implicitly {@link GetKeyPairResp.verify|verify} messages.
     * @function encode
     * @memberof GetKeyPairResp
     * @static
     * @param {IGetKeyPairResp} message GetKeyPairResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetKeyPairResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.keyPair != null && Object.hasOwnProperty.call(message, "keyPair"))
            $root.KeyPairBase.encode(message.keyPair, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified GetKeyPairResp message, length delimited. Does not implicitly {@link GetKeyPairResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetKeyPairResp
     * @static
     * @param {IGetKeyPairResp} message GetKeyPairResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetKeyPairResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetKeyPairResp message from the specified reader or buffer.
     * @function decode
     * @memberof GetKeyPairResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetKeyPairResp} GetKeyPairResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetKeyPairResp.decode = function decode(reader, length, error) {
    GetKeyPairResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetKeyPairResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.keyPair = $root.KeyPairBase.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetKeyPairResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetKeyPairResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetKeyPairResp} GetKeyPairResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetKeyPairResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetKeyPairResp message.
     * @function verify
     * @memberof GetKeyPairResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetKeyPairResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.keyPair != null && message.hasOwnProperty("keyPair")) {
            let error = $root.KeyPairBase.verify(message.keyPair);
            if (error)
                return "keyPair." + error;
        }
        return null;
    };

    /**
     * Creates a GetKeyPairResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetKeyPairResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetKeyPairResp} GetKeyPairResp
     */
    GetKeyPairResp.fromObject = function fromObject(object) {
        if (object instanceof $root.GetKeyPairResp)
            return object;
        let message = new $root.GetKeyPairResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GetKeyPairResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.keyPair != null) {
            if (typeof object.keyPair !== "object")
                throw TypeError(".GetKeyPairResp.keyPair: object expected");
            message.keyPair = $root.KeyPairBase.fromObject(object.keyPair);
        }
        return message;
    };

    /**
     * Creates a plain object from a GetKeyPairResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetKeyPairResp
     * @static
     * @param {GetKeyPairResp} message GetKeyPairResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetKeyPairResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.keyPair = null;
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.keyPair != null && message.hasOwnProperty("keyPair"))
            object.keyPair = $root.KeyPairBase.toObject(message.keyPair, options);
        return object;
    };

    /**
     * Converts this GetKeyPairResp to JSON.
     * @function toJSON
     * @memberof GetKeyPairResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetKeyPairResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetKeyPairResp
     * @function getTypeUrl
     * @memberof GetKeyPairResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetKeyPairResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetKeyPairResp";
    };

    return GetKeyPairResp;
})();

export const GetKeyPairOfVerReq = $root.GetKeyPairOfVerReq = (() => {

    /**
     * Properties of a GetKeyPairOfVerReq.
     * @exports IGetKeyPairOfVerReq
     * @interface IGetKeyPairOfVerReq
     * @property {IClientInfo|null} [clientInfo] GetKeyPairOfVerReq clientInfo
     * @property {number|Long|null} [uid] GetKeyPairOfVerReq uid
     * @property {number|null} [appVer] GetKeyPairOfVerReq appVer
     * @property {number|null} [webVer] GetKeyPairOfVerReq webVer
     */

    /**
     * Constructs a new GetKeyPairOfVerReq.
     * @exports GetKeyPairOfVerReq
     * @classdesc Represents a GetKeyPairOfVerReq.
     * @implements IGetKeyPairOfVerReq
     * @constructor
     * @param {IGetKeyPairOfVerReq=} [properties] Properties to set
     */
    function GetKeyPairOfVerReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetKeyPairOfVerReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GetKeyPairOfVerReq
     * @instance
     */
    GetKeyPairOfVerReq.prototype.clientInfo = null;

    /**
     * GetKeyPairOfVerReq uid.
     * @member {number|Long} uid
     * @memberof GetKeyPairOfVerReq
     * @instance
     */
    GetKeyPairOfVerReq.prototype.uid = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * GetKeyPairOfVerReq appVer.
     * @member {number} appVer
     * @memberof GetKeyPairOfVerReq
     * @instance
     */
    GetKeyPairOfVerReq.prototype.appVer = 0;

    /**
     * GetKeyPairOfVerReq webVer.
     * @member {number} webVer
     * @memberof GetKeyPairOfVerReq
     * @instance
     */
    GetKeyPairOfVerReq.prototype.webVer = 0;

    /**
     * Creates a new GetKeyPairOfVerReq instance using the specified properties.
     * @function create
     * @memberof GetKeyPairOfVerReq
     * @static
     * @param {IGetKeyPairOfVerReq=} [properties] Properties to set
     * @returns {GetKeyPairOfVerReq} GetKeyPairOfVerReq instance
     */
    GetKeyPairOfVerReq.create = function create(properties) {
        return new GetKeyPairOfVerReq(properties);
    };

    /**
     * Encodes the specified GetKeyPairOfVerReq message. Does not implicitly {@link GetKeyPairOfVerReq.verify|verify} messages.
     * @function encode
     * @memberof GetKeyPairOfVerReq
     * @static
     * @param {IGetKeyPairOfVerReq} message GetKeyPairOfVerReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetKeyPairOfVerReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
            writer.uint32(/* id 2, wireType 0 =*/16).int64(message.uid);
        if (message.appVer != null && Object.hasOwnProperty.call(message, "appVer"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.appVer);
        if (message.webVer != null && Object.hasOwnProperty.call(message, "webVer"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.webVer);
        return writer;
    };

    /**
     * Encodes the specified GetKeyPairOfVerReq message, length delimited. Does not implicitly {@link GetKeyPairOfVerReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetKeyPairOfVerReq
     * @static
     * @param {IGetKeyPairOfVerReq} message GetKeyPairOfVerReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetKeyPairOfVerReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetKeyPairOfVerReq message from the specified reader or buffer.
     * @function decode
     * @memberof GetKeyPairOfVerReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetKeyPairOfVerReq} GetKeyPairOfVerReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetKeyPairOfVerReq.decode = function decode(reader, length, error) {
    GetKeyPairOfVerReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetKeyPairOfVerReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.uid = reader.int64();
                    break;
                }
            case 3: {
                    message.appVer = reader.int32();
                    break;
                }
            case 4: {
                    message.webVer = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetKeyPairOfVerReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetKeyPairOfVerReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetKeyPairOfVerReq} GetKeyPairOfVerReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetKeyPairOfVerReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetKeyPairOfVerReq message.
     * @function verify
     * @memberof GetKeyPairOfVerReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetKeyPairOfVerReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.uid != null && message.hasOwnProperty("uid"))
            if (!$util.isInteger(message.uid) && !(message.uid && $util.isInteger(message.uid.low) && $util.isInteger(message.uid.high)))
                return "uid: integer|Long expected";
        if (message.appVer != null && message.hasOwnProperty("appVer"))
            if (!$util.isInteger(message.appVer))
                return "appVer: integer expected";
        if (message.webVer != null && message.hasOwnProperty("webVer"))
            if (!$util.isInteger(message.webVer))
                return "webVer: integer expected";
        return null;
    };

    /**
     * Creates a GetKeyPairOfVerReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetKeyPairOfVerReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetKeyPairOfVerReq} GetKeyPairOfVerReq
     */
    GetKeyPairOfVerReq.fromObject = function fromObject(object) {
        if (object instanceof $root.GetKeyPairOfVerReq)
            return object;
        let message = new $root.GetKeyPairOfVerReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GetKeyPairOfVerReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.uid != null)
            if ($util.Long)
                (message.uid = $util.Long.fromValue(object.uid)).unsigned = false;
            else if (typeof object.uid === "string")
                message.uid = parseInt(object.uid, 10);
            else if (typeof object.uid === "number")
                message.uid = object.uid;
            else if (typeof object.uid === "object")
                message.uid = new $util.LongBits(object.uid.low >>> 0, object.uid.high >>> 0).toNumber();
        if (object.appVer != null)
            message.appVer = object.appVer | 0;
        if (object.webVer != null)
            message.webVer = object.webVer | 0;
        return message;
    };

    /**
     * Creates a plain object from a GetKeyPairOfVerReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetKeyPairOfVerReq
     * @static
     * @param {GetKeyPairOfVerReq} message GetKeyPairOfVerReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetKeyPairOfVerReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.uid = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.uid = options.longs === String ? "0" : 0;
            object.appVer = 0;
            object.webVer = 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.uid != null && message.hasOwnProperty("uid"))
            if (typeof message.uid === "number")
                object.uid = options.longs === String ? String(message.uid) : message.uid;
            else
                object.uid = options.longs === String ? $util.Long.prototype.toString.call(message.uid) : options.longs === Number ? new $util.LongBits(message.uid.low >>> 0, message.uid.high >>> 0).toNumber() : message.uid;
        if (message.appVer != null && message.hasOwnProperty("appVer"))
            object.appVer = message.appVer;
        if (message.webVer != null && message.hasOwnProperty("webVer"))
            object.webVer = message.webVer;
        return object;
    };

    /**
     * Converts this GetKeyPairOfVerReq to JSON.
     * @function toJSON
     * @memberof GetKeyPairOfVerReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetKeyPairOfVerReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetKeyPairOfVerReq
     * @function getTypeUrl
     * @memberof GetKeyPairOfVerReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetKeyPairOfVerReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetKeyPairOfVerReq";
    };

    return GetKeyPairOfVerReq;
})();

export const GetKeyPairOfVerResp = $root.GetKeyPairOfVerResp = (() => {

    /**
     * Properties of a GetKeyPairOfVerResp.
     * @exports IGetKeyPairOfVerResp
     * @interface IGetKeyPairOfVerResp
     * @property {ICommonResult|null} [commonResult] GetKeyPairOfVerResp commonResult
     * @property {IKeyPairBase|null} [appKeyPair] GetKeyPairOfVerResp appKeyPair
     * @property {IKeyPairBase|null} [webKeyPair] GetKeyPairOfVerResp webKeyPair
     */

    /**
     * Constructs a new GetKeyPairOfVerResp.
     * @exports GetKeyPairOfVerResp
     * @classdesc Represents a GetKeyPairOfVerResp.
     * @implements IGetKeyPairOfVerResp
     * @constructor
     * @param {IGetKeyPairOfVerResp=} [properties] Properties to set
     */
    function GetKeyPairOfVerResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetKeyPairOfVerResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GetKeyPairOfVerResp
     * @instance
     */
    GetKeyPairOfVerResp.prototype.commonResult = null;

    /**
     * GetKeyPairOfVerResp appKeyPair.
     * @member {IKeyPairBase|null|undefined} appKeyPair
     * @memberof GetKeyPairOfVerResp
     * @instance
     */
    GetKeyPairOfVerResp.prototype.appKeyPair = null;

    /**
     * GetKeyPairOfVerResp webKeyPair.
     * @member {IKeyPairBase|null|undefined} webKeyPair
     * @memberof GetKeyPairOfVerResp
     * @instance
     */
    GetKeyPairOfVerResp.prototype.webKeyPair = null;

    /**
     * Creates a new GetKeyPairOfVerResp instance using the specified properties.
     * @function create
     * @memberof GetKeyPairOfVerResp
     * @static
     * @param {IGetKeyPairOfVerResp=} [properties] Properties to set
     * @returns {GetKeyPairOfVerResp} GetKeyPairOfVerResp instance
     */
    GetKeyPairOfVerResp.create = function create(properties) {
        return new GetKeyPairOfVerResp(properties);
    };

    /**
     * Encodes the specified GetKeyPairOfVerResp message. Does not implicitly {@link GetKeyPairOfVerResp.verify|verify} messages.
     * @function encode
     * @memberof GetKeyPairOfVerResp
     * @static
     * @param {IGetKeyPairOfVerResp} message GetKeyPairOfVerResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetKeyPairOfVerResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.appKeyPair != null && Object.hasOwnProperty.call(message, "appKeyPair"))
            $root.KeyPairBase.encode(message.appKeyPair, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
        if (message.webKeyPair != null && Object.hasOwnProperty.call(message, "webKeyPair"))
            $root.KeyPairBase.encode(message.webKeyPair, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified GetKeyPairOfVerResp message, length delimited. Does not implicitly {@link GetKeyPairOfVerResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetKeyPairOfVerResp
     * @static
     * @param {IGetKeyPairOfVerResp} message GetKeyPairOfVerResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetKeyPairOfVerResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetKeyPairOfVerResp message from the specified reader or buffer.
     * @function decode
     * @memberof GetKeyPairOfVerResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetKeyPairOfVerResp} GetKeyPairOfVerResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetKeyPairOfVerResp.decode = function decode(reader, length, error) {
    GetKeyPairOfVerResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetKeyPairOfVerResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.appKeyPair = $root.KeyPairBase.decode(reader, reader.uint32());
                    break;
                }
            case 3: {
                    message.webKeyPair = $root.KeyPairBase.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetKeyPairOfVerResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetKeyPairOfVerResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetKeyPairOfVerResp} GetKeyPairOfVerResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetKeyPairOfVerResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetKeyPairOfVerResp message.
     * @function verify
     * @memberof GetKeyPairOfVerResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetKeyPairOfVerResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.appKeyPair != null && message.hasOwnProperty("appKeyPair")) {
            let error = $root.KeyPairBase.verify(message.appKeyPair);
            if (error)
                return "appKeyPair." + error;
        }
        if (message.webKeyPair != null && message.hasOwnProperty("webKeyPair")) {
            let error = $root.KeyPairBase.verify(message.webKeyPair);
            if (error)
                return "webKeyPair." + error;
        }
        return null;
    };

    /**
     * Creates a GetKeyPairOfVerResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetKeyPairOfVerResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetKeyPairOfVerResp} GetKeyPairOfVerResp
     */
    GetKeyPairOfVerResp.fromObject = function fromObject(object) {
        if (object instanceof $root.GetKeyPairOfVerResp)
            return object;
        let message = new $root.GetKeyPairOfVerResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GetKeyPairOfVerResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.appKeyPair != null) {
            if (typeof object.appKeyPair !== "object")
                throw TypeError(".GetKeyPairOfVerResp.appKeyPair: object expected");
            message.appKeyPair = $root.KeyPairBase.fromObject(object.appKeyPair);
        }
        if (object.webKeyPair != null) {
            if (typeof object.webKeyPair !== "object")
                throw TypeError(".GetKeyPairOfVerResp.webKeyPair: object expected");
            message.webKeyPair = $root.KeyPairBase.fromObject(object.webKeyPair);
        }
        return message;
    };

    /**
     * Creates a plain object from a GetKeyPairOfVerResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetKeyPairOfVerResp
     * @static
     * @param {GetKeyPairOfVerResp} message GetKeyPairOfVerResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetKeyPairOfVerResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.appKeyPair = null;
            object.webKeyPair = null;
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.appKeyPair != null && message.hasOwnProperty("appKeyPair"))
            object.appKeyPair = $root.KeyPairBase.toObject(message.appKeyPair, options);
        if (message.webKeyPair != null && message.hasOwnProperty("webKeyPair"))
            object.webKeyPair = $root.KeyPairBase.toObject(message.webKeyPair, options);
        return object;
    };

    /**
     * Converts this GetKeyPairOfVerResp to JSON.
     * @function toJSON
     * @memberof GetKeyPairOfVerResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetKeyPairOfVerResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetKeyPairOfVerResp
     * @function getTypeUrl
     * @memberof GetKeyPairOfVerResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetKeyPairOfVerResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetKeyPairOfVerResp";
    };

    return GetKeyPairOfVerResp;
})();

export const UpdateKeyPairReq = $root.UpdateKeyPairReq = (() => {

    /**
     * Properties of an UpdateKeyPairReq.
     * @exports IUpdateKeyPairReq
     * @interface IUpdateKeyPairReq
     * @property {IClientInfo|null} [clientInfo] UpdateKeyPairReq clientInfo
     * @property {string|null} [publicKey] UpdateKeyPairReq publicKey
     */

    /**
     * Constructs a new UpdateKeyPairReq.
     * @exports UpdateKeyPairReq
     * @classdesc Represents an UpdateKeyPairReq.
     * @implements IUpdateKeyPairReq
     * @constructor
     * @param {IUpdateKeyPairReq=} [properties] Properties to set
     */
    function UpdateKeyPairReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * UpdateKeyPairReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof UpdateKeyPairReq
     * @instance
     */
    UpdateKeyPairReq.prototype.clientInfo = null;

    /**
     * UpdateKeyPairReq publicKey.
     * @member {string} publicKey
     * @memberof UpdateKeyPairReq
     * @instance
     */
    UpdateKeyPairReq.prototype.publicKey = "";

    /**
     * Creates a new UpdateKeyPairReq instance using the specified properties.
     * @function create
     * @memberof UpdateKeyPairReq
     * @static
     * @param {IUpdateKeyPairReq=} [properties] Properties to set
     * @returns {UpdateKeyPairReq} UpdateKeyPairReq instance
     */
    UpdateKeyPairReq.create = function create(properties) {
        return new UpdateKeyPairReq(properties);
    };

    /**
     * Encodes the specified UpdateKeyPairReq message. Does not implicitly {@link UpdateKeyPairReq.verify|verify} messages.
     * @function encode
     * @memberof UpdateKeyPairReq
     * @static
     * @param {IUpdateKeyPairReq} message UpdateKeyPairReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateKeyPairReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.publicKey);
        return writer;
    };

    /**
     * Encodes the specified UpdateKeyPairReq message, length delimited. Does not implicitly {@link UpdateKeyPairReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof UpdateKeyPairReq
     * @static
     * @param {IUpdateKeyPairReq} message UpdateKeyPairReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateKeyPairReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an UpdateKeyPairReq message from the specified reader or buffer.
     * @function decode
     * @memberof UpdateKeyPairReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {UpdateKeyPairReq} UpdateKeyPairReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateKeyPairReq.decode = function decode(reader, length, error) {
    UpdateKeyPairReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.UpdateKeyPairReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.publicKey = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an UpdateKeyPairReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof UpdateKeyPairReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {UpdateKeyPairReq} UpdateKeyPairReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateKeyPairReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an UpdateKeyPairReq message.
     * @function verify
     * @memberof UpdateKeyPairReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    UpdateKeyPairReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.publicKey != null && message.hasOwnProperty("publicKey"))
            if (!$util.isString(message.publicKey))
                return "publicKey: string expected";
        return null;
    };

    /**
     * Creates an UpdateKeyPairReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof UpdateKeyPairReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {UpdateKeyPairReq} UpdateKeyPairReq
     */
    UpdateKeyPairReq.fromObject = function fromObject(object) {
        if (object instanceof $root.UpdateKeyPairReq)
            return object;
        let message = new $root.UpdateKeyPairReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".UpdateKeyPairReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.publicKey != null)
            message.publicKey = String(object.publicKey);
        return message;
    };

    /**
     * Creates a plain object from an UpdateKeyPairReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof UpdateKeyPairReq
     * @static
     * @param {UpdateKeyPairReq} message UpdateKeyPairReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    UpdateKeyPairReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.publicKey = "";
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.publicKey != null && message.hasOwnProperty("publicKey"))
            object.publicKey = message.publicKey;
        return object;
    };

    /**
     * Converts this UpdateKeyPairReq to JSON.
     * @function toJSON
     * @memberof UpdateKeyPairReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    UpdateKeyPairReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for UpdateKeyPairReq
     * @function getTypeUrl
     * @memberof UpdateKeyPairReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    UpdateKeyPairReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/UpdateKeyPairReq";
    };

    return UpdateKeyPairReq;
})();

export const UpdateKeyPairResp = $root.UpdateKeyPairResp = (() => {

    /**
     * Properties of an UpdateKeyPairResp.
     * @exports IUpdateKeyPairResp
     * @interface IUpdateKeyPairResp
     * @property {ICommonResult|null} [commonResult] UpdateKeyPairResp commonResult
     * @property {number|null} [keyVersion] UpdateKeyPairResp keyVersion
     */

    /**
     * Constructs a new UpdateKeyPairResp.
     * @exports UpdateKeyPairResp
     * @classdesc Represents an UpdateKeyPairResp.
     * @implements IUpdateKeyPairResp
     * @constructor
     * @param {IUpdateKeyPairResp=} [properties] Properties to set
     */
    function UpdateKeyPairResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * UpdateKeyPairResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof UpdateKeyPairResp
     * @instance
     */
    UpdateKeyPairResp.prototype.commonResult = null;

    /**
     * UpdateKeyPairResp keyVersion.
     * @member {number} keyVersion
     * @memberof UpdateKeyPairResp
     * @instance
     */
    UpdateKeyPairResp.prototype.keyVersion = 0;

    /**
     * Creates a new UpdateKeyPairResp instance using the specified properties.
     * @function create
     * @memberof UpdateKeyPairResp
     * @static
     * @param {IUpdateKeyPairResp=} [properties] Properties to set
     * @returns {UpdateKeyPairResp} UpdateKeyPairResp instance
     */
    UpdateKeyPairResp.create = function create(properties) {
        return new UpdateKeyPairResp(properties);
    };

    /**
     * Encodes the specified UpdateKeyPairResp message. Does not implicitly {@link UpdateKeyPairResp.verify|verify} messages.
     * @function encode
     * @memberof UpdateKeyPairResp
     * @static
     * @param {IUpdateKeyPairResp} message UpdateKeyPairResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateKeyPairResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.keyVersion != null && Object.hasOwnProperty.call(message, "keyVersion"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.keyVersion);
        return writer;
    };

    /**
     * Encodes the specified UpdateKeyPairResp message, length delimited. Does not implicitly {@link UpdateKeyPairResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof UpdateKeyPairResp
     * @static
     * @param {IUpdateKeyPairResp} message UpdateKeyPairResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateKeyPairResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an UpdateKeyPairResp message from the specified reader or buffer.
     * @function decode
     * @memberof UpdateKeyPairResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {UpdateKeyPairResp} UpdateKeyPairResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateKeyPairResp.decode = function decode(reader, length, error) {
    UpdateKeyPairResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.UpdateKeyPairResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.keyVersion = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an UpdateKeyPairResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof UpdateKeyPairResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {UpdateKeyPairResp} UpdateKeyPairResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateKeyPairResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an UpdateKeyPairResp message.
     * @function verify
     * @memberof UpdateKeyPairResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    UpdateKeyPairResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.keyVersion != null && message.hasOwnProperty("keyVersion"))
            if (!$util.isInteger(message.keyVersion))
                return "keyVersion: integer expected";
        return null;
    };

    /**
     * Creates an UpdateKeyPairResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof UpdateKeyPairResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {UpdateKeyPairResp} UpdateKeyPairResp
     */
    UpdateKeyPairResp.fromObject = function fromObject(object) {
        if (object instanceof $root.UpdateKeyPairResp)
            return object;
        let message = new $root.UpdateKeyPairResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".UpdateKeyPairResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.keyVersion != null)
            message.keyVersion = object.keyVersion | 0;
        return message;
    };

    /**
     * Creates a plain object from an UpdateKeyPairResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof UpdateKeyPairResp
     * @static
     * @param {UpdateKeyPairResp} message UpdateKeyPairResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    UpdateKeyPairResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.keyVersion = 0;
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.keyVersion != null && message.hasOwnProperty("keyVersion"))
            object.keyVersion = message.keyVersion;
        return object;
    };

    /**
     * Converts this UpdateKeyPairResp to JSON.
     * @function toJSON
     * @memberof UpdateKeyPairResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    UpdateKeyPairResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for UpdateKeyPairResp
     * @function getTypeUrl
     * @memberof UpdateKeyPairResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    UpdateKeyPairResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/UpdateKeyPairResp";
    };

    return UpdateKeyPairResp;
})();

export const PlaceListReq = $root.PlaceListReq = (() => {

    /**
     * Properties of a PlaceListReq.
     * @exports IPlaceListReq
     * @interface IPlaceListReq
     * @property {IClientInfo|null} [clientInfo] PlaceListReq clientInfo
     * @property {number|null} [lat] PlaceListReq lat
     * @property {number|null} [lng] PlaceListReq lng
     * @property {string|null} [keyword] PlaceListReq keyword
     * @property {number|null} [pageNum] PlaceListReq pageNum
     * @property {number|null} [pageSize] PlaceListReq pageSize
     * @property {SearchMapType|null} [type] PlaceListReq type
     */

    /**
     * Constructs a new PlaceListReq.
     * @exports PlaceListReq
     * @classdesc Represents a PlaceListReq.
     * @implements IPlaceListReq
     * @constructor
     * @param {IPlaceListReq=} [properties] Properties to set
     */
    function PlaceListReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * PlaceListReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof PlaceListReq
     * @instance
     */
    PlaceListReq.prototype.clientInfo = null;

    /**
     * PlaceListReq lat.
     * @member {number} lat
     * @memberof PlaceListReq
     * @instance
     */
    PlaceListReq.prototype.lat = 0;

    /**
     * PlaceListReq lng.
     * @member {number} lng
     * @memberof PlaceListReq
     * @instance
     */
    PlaceListReq.prototype.lng = 0;

    /**
     * PlaceListReq keyword.
     * @member {string} keyword
     * @memberof PlaceListReq
     * @instance
     */
    PlaceListReq.prototype.keyword = "";

    /**
     * PlaceListReq pageNum.
     * @member {number} pageNum
     * @memberof PlaceListReq
     * @instance
     */
    PlaceListReq.prototype.pageNum = 0;

    /**
     * PlaceListReq pageSize.
     * @member {number} pageSize
     * @memberof PlaceListReq
     * @instance
     */
    PlaceListReq.prototype.pageSize = 0;

    /**
     * PlaceListReq type.
     * @member {SearchMapType} type
     * @memberof PlaceListReq
     * @instance
     */
    PlaceListReq.prototype.type = 0;

    /**
     * Creates a new PlaceListReq instance using the specified properties.
     * @function create
     * @memberof PlaceListReq
     * @static
     * @param {IPlaceListReq=} [properties] Properties to set
     * @returns {PlaceListReq} PlaceListReq instance
     */
    PlaceListReq.create = function create(properties) {
        return new PlaceListReq(properties);
    };

    /**
     * Encodes the specified PlaceListReq message. Does not implicitly {@link PlaceListReq.verify|verify} messages.
     * @function encode
     * @memberof PlaceListReq
     * @static
     * @param {IPlaceListReq} message PlaceListReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PlaceListReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.lat);
        if (message.lng != null && Object.hasOwnProperty.call(message, "lng"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.lng);
        if (message.keyword != null && Object.hasOwnProperty.call(message, "keyword"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.keyword);
        if (message.pageNum != null && Object.hasOwnProperty.call(message, "pageNum"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.pageNum);
        if (message.pageSize != null && Object.hasOwnProperty.call(message, "pageSize"))
            writer.uint32(/* id 6, wireType 0 =*/48).int32(message.pageSize);
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 7, wireType 0 =*/56).int32(message.type);
        return writer;
    };

    /**
     * Encodes the specified PlaceListReq message, length delimited. Does not implicitly {@link PlaceListReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PlaceListReq
     * @static
     * @param {IPlaceListReq} message PlaceListReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PlaceListReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a PlaceListReq message from the specified reader or buffer.
     * @function decode
     * @memberof PlaceListReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PlaceListReq} PlaceListReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PlaceListReq.decode = function decode(reader, length, error) {
    PlaceListReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.PlaceListReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.lat = reader.int32();
                    break;
                }
            case 3: {
                    message.lng = reader.int32();
                    break;
                }
            case 4: {
                    message.keyword = reader.string();
                    break;
                }
            case 5: {
                    message.pageNum = reader.int32();
                    break;
                }
            case 6: {
                    message.pageSize = reader.int32();
                    break;
                }
            case 7: {
                    message.type = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a PlaceListReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PlaceListReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PlaceListReq} PlaceListReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PlaceListReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PlaceListReq message.
     * @function verify
     * @memberof PlaceListReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PlaceListReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.lat != null && message.hasOwnProperty("lat"))
            if (!$util.isInteger(message.lat))
                return "lat: integer expected";
        if (message.lng != null && message.hasOwnProperty("lng"))
            if (!$util.isInteger(message.lng))
                return "lng: integer expected";
        if (message.keyword != null && message.hasOwnProperty("keyword"))
            if (!$util.isString(message.keyword))
                return "keyword: string expected";
        if (message.pageNum != null && message.hasOwnProperty("pageNum"))
            if (!$util.isInteger(message.pageNum))
                return "pageNum: integer expected";
        if (message.pageSize != null && message.hasOwnProperty("pageSize"))
            if (!$util.isInteger(message.pageSize))
                return "pageSize: integer expected";
        if (message.type != null && message.hasOwnProperty("type"))
            switch (message.type) {
            default:
                return "type: enum value expected";
            case 0:
            case 1:
            case 2:
                break;
            }
        return null;
    };

    /**
     * Creates a PlaceListReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PlaceListReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PlaceListReq} PlaceListReq
     */
    PlaceListReq.fromObject = function fromObject(object) {
        if (object instanceof $root.PlaceListReq)
            return object;
        let message = new $root.PlaceListReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".PlaceListReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.lat != null)
            message.lat = object.lat | 0;
        if (object.lng != null)
            message.lng = object.lng | 0;
        if (object.keyword != null)
            message.keyword = String(object.keyword);
        if (object.pageNum != null)
            message.pageNum = object.pageNum | 0;
        if (object.pageSize != null)
            message.pageSize = object.pageSize | 0;
        switch (object.type) {
        default:
            if (typeof object.type === "number") {
                message.type = object.type;
                break;
            }
            break;
        case "GAODE":
        case 0:
            message.type = 0;
            break;
        case "GOOGLE":
        case 1:
            message.type = 1;
            break;
        case "BAIDU":
        case 2:
            message.type = 2;
            break;
        }
        return message;
    };

    /**
     * Creates a plain object from a PlaceListReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PlaceListReq
     * @static
     * @param {PlaceListReq} message PlaceListReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PlaceListReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.lat = 0;
            object.lng = 0;
            object.keyword = "";
            object.pageNum = 0;
            object.pageSize = 0;
            object.type = options.enums === String ? "GAODE" : 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.lat != null && message.hasOwnProperty("lat"))
            object.lat = message.lat;
        if (message.lng != null && message.hasOwnProperty("lng"))
            object.lng = message.lng;
        if (message.keyword != null && message.hasOwnProperty("keyword"))
            object.keyword = message.keyword;
        if (message.pageNum != null && message.hasOwnProperty("pageNum"))
            object.pageNum = message.pageNum;
        if (message.pageSize != null && message.hasOwnProperty("pageSize"))
            object.pageSize = message.pageSize;
        if (message.type != null && message.hasOwnProperty("type"))
            object.type = options.enums === String ? $root.SearchMapType[message.type] === undefined ? message.type : $root.SearchMapType[message.type] : message.type;
        return object;
    };

    /**
     * Converts this PlaceListReq to JSON.
     * @function toJSON
     * @memberof PlaceListReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PlaceListReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PlaceListReq
     * @function getTypeUrl
     * @memberof PlaceListReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PlaceListReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/PlaceListReq";
    };

    return PlaceListReq;
})();

export const PlaceListResp = $root.PlaceListResp = (() => {

    /**
     * Properties of a PlaceListResp.
     * @exports IPlaceListResp
     * @interface IPlaceListResp
     * @property {ICommonResult|null} [commonResult] PlaceListResp commonResult
     * @property {number|null} [count] PlaceListResp count
     * @property {Array.<IPlaceBean>|null} [places] PlaceListResp places
     */

    /**
     * Constructs a new PlaceListResp.
     * @exports PlaceListResp
     * @classdesc Represents a PlaceListResp.
     * @implements IPlaceListResp
     * @constructor
     * @param {IPlaceListResp=} [properties] Properties to set
     */
    function PlaceListResp(properties) {
        this.places = [];
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * PlaceListResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof PlaceListResp
     * @instance
     */
    PlaceListResp.prototype.commonResult = null;

    /**
     * PlaceListResp count.
     * @member {number} count
     * @memberof PlaceListResp
     * @instance
     */
    PlaceListResp.prototype.count = 0;

    /**
     * PlaceListResp places.
     * @member {Array.<IPlaceBean>} places
     * @memberof PlaceListResp
     * @instance
     */
    PlaceListResp.prototype.places = $util.emptyArray;

    /**
     * Creates a new PlaceListResp instance using the specified properties.
     * @function create
     * @memberof PlaceListResp
     * @static
     * @param {IPlaceListResp=} [properties] Properties to set
     * @returns {PlaceListResp} PlaceListResp instance
     */
    PlaceListResp.create = function create(properties) {
        return new PlaceListResp(properties);
    };

    /**
     * Encodes the specified PlaceListResp message. Does not implicitly {@link PlaceListResp.verify|verify} messages.
     * @function encode
     * @memberof PlaceListResp
     * @static
     * @param {IPlaceListResp} message PlaceListResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PlaceListResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.count != null && Object.hasOwnProperty.call(message, "count"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.count);
        if (message.places != null && message.places.length)
            for (let i = 0; i < message.places.length; ++i)
                $root.PlaceBean.encode(message.places[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified PlaceListResp message, length delimited. Does not implicitly {@link PlaceListResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PlaceListResp
     * @static
     * @param {IPlaceListResp} message PlaceListResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PlaceListResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a PlaceListResp message from the specified reader or buffer.
     * @function decode
     * @memberof PlaceListResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PlaceListResp} PlaceListResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PlaceListResp.decode = function decode(reader, length, error) {
    PlaceListResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.PlaceListResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.count = reader.int32();
                    break;
                }
            case 3: {
                    if (!(message.places && message.places.length))
                        message.places = [];
                    message.places.push($root.PlaceBean.decode(reader, reader.uint32()));
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a PlaceListResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PlaceListResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PlaceListResp} PlaceListResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PlaceListResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PlaceListResp message.
     * @function verify
     * @memberof PlaceListResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PlaceListResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.count != null && message.hasOwnProperty("count"))
            if (!$util.isInteger(message.count))
                return "count: integer expected";
        if (message.places != null && message.hasOwnProperty("places")) {
            if (!Array.isArray(message.places))
                return "places: array expected";
            for (let i = 0; i < message.places.length; ++i) {
                let error = $root.PlaceBean.verify(message.places[i]);
                if (error)
                    return "places." + error;
            }
        }
        return null;
    };

    /**
     * Creates a PlaceListResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PlaceListResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PlaceListResp} PlaceListResp
     */
    PlaceListResp.fromObject = function fromObject(object) {
        if (object instanceof $root.PlaceListResp)
            return object;
        let message = new $root.PlaceListResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".PlaceListResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.count != null)
            message.count = object.count | 0;
        if (object.places) {
            if (!Array.isArray(object.places))
                throw TypeError(".PlaceListResp.places: array expected");
            message.places = [];
            for (let i = 0; i < object.places.length; ++i) {
                if (typeof object.places[i] !== "object")
                    throw TypeError(".PlaceListResp.places: object expected");
                message.places[i] = $root.PlaceBean.fromObject(object.places[i]);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a PlaceListResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PlaceListResp
     * @static
     * @param {PlaceListResp} message PlaceListResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PlaceListResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.arrays || options.defaults)
            object.places = [];
        if (options.defaults) {
            object.commonResult = null;
            object.count = 0;
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.count != null && message.hasOwnProperty("count"))
            object.count = message.count;
        if (message.places && message.places.length) {
            object.places = [];
            for (let j = 0; j < message.places.length; ++j)
                object.places[j] = $root.PlaceBean.toObject(message.places[j], options);
        }
        return object;
    };

    /**
     * Converts this PlaceListResp to JSON.
     * @function toJSON
     * @memberof PlaceListResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PlaceListResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PlaceListResp
     * @function getTypeUrl
     * @memberof PlaceListResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PlaceListResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/PlaceListResp";
    };

    return PlaceListResp;
})();

export const GoogleUrlReq = $root.GoogleUrlReq = (() => {

    /**
     * Properties of a GoogleUrlReq.
     * @exports IGoogleUrlReq
     * @interface IGoogleUrlReq
     * @property {IClientInfo|null} [clientInfo] GoogleUrlReq clientInfo
     * @property {number|null} [lat] GoogleUrlReq lat
     * @property {number|null} [lng] GoogleUrlReq lng
     */

    /**
     * Constructs a new GoogleUrlReq.
     * @exports GoogleUrlReq
     * @classdesc Represents a GoogleUrlReq.
     * @implements IGoogleUrlReq
     * @constructor
     * @param {IGoogleUrlReq=} [properties] Properties to set
     */
    function GoogleUrlReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GoogleUrlReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GoogleUrlReq
     * @instance
     */
    GoogleUrlReq.prototype.clientInfo = null;

    /**
     * GoogleUrlReq lat.
     * @member {number} lat
     * @memberof GoogleUrlReq
     * @instance
     */
    GoogleUrlReq.prototype.lat = 0;

    /**
     * GoogleUrlReq lng.
     * @member {number} lng
     * @memberof GoogleUrlReq
     * @instance
     */
    GoogleUrlReq.prototype.lng = 0;

    /**
     * Creates a new GoogleUrlReq instance using the specified properties.
     * @function create
     * @memberof GoogleUrlReq
     * @static
     * @param {IGoogleUrlReq=} [properties] Properties to set
     * @returns {GoogleUrlReq} GoogleUrlReq instance
     */
    GoogleUrlReq.create = function create(properties) {
        return new GoogleUrlReq(properties);
    };

    /**
     * Encodes the specified GoogleUrlReq message. Does not implicitly {@link GoogleUrlReq.verify|verify} messages.
     * @function encode
     * @memberof GoogleUrlReq
     * @static
     * @param {IGoogleUrlReq} message GoogleUrlReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GoogleUrlReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.lat);
        if (message.lng != null && Object.hasOwnProperty.call(message, "lng"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.lng);
        return writer;
    };

    /**
     * Encodes the specified GoogleUrlReq message, length delimited. Does not implicitly {@link GoogleUrlReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GoogleUrlReq
     * @static
     * @param {IGoogleUrlReq} message GoogleUrlReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GoogleUrlReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GoogleUrlReq message from the specified reader or buffer.
     * @function decode
     * @memberof GoogleUrlReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GoogleUrlReq} GoogleUrlReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GoogleUrlReq.decode = function decode(reader, length, error) {
    GoogleUrlReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GoogleUrlReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.lat = reader.int32();
                    break;
                }
            case 3: {
                    message.lng = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GoogleUrlReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GoogleUrlReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GoogleUrlReq} GoogleUrlReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GoogleUrlReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GoogleUrlReq message.
     * @function verify
     * @memberof GoogleUrlReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GoogleUrlReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.lat != null && message.hasOwnProperty("lat"))
            if (!$util.isInteger(message.lat))
                return "lat: integer expected";
        if (message.lng != null && message.hasOwnProperty("lng"))
            if (!$util.isInteger(message.lng))
                return "lng: integer expected";
        return null;
    };

    /**
     * Creates a GoogleUrlReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GoogleUrlReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GoogleUrlReq} GoogleUrlReq
     */
    GoogleUrlReq.fromObject = function fromObject(object) {
        if (object instanceof $root.GoogleUrlReq)
            return object;
        let message = new $root.GoogleUrlReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GoogleUrlReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.lat != null)
            message.lat = object.lat | 0;
        if (object.lng != null)
            message.lng = object.lng | 0;
        return message;
    };

    /**
     * Creates a plain object from a GoogleUrlReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GoogleUrlReq
     * @static
     * @param {GoogleUrlReq} message GoogleUrlReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GoogleUrlReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.lat = 0;
            object.lng = 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.lat != null && message.hasOwnProperty("lat"))
            object.lat = message.lat;
        if (message.lng != null && message.hasOwnProperty("lng"))
            object.lng = message.lng;
        return object;
    };

    /**
     * Converts this GoogleUrlReq to JSON.
     * @function toJSON
     * @memberof GoogleUrlReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GoogleUrlReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GoogleUrlReq
     * @function getTypeUrl
     * @memberof GoogleUrlReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GoogleUrlReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GoogleUrlReq";
    };

    return GoogleUrlReq;
})();

export const SysRelatedLinkReq = $root.SysRelatedLinkReq = (() => {

    /**
     * Properties of a SysRelatedLinkReq.
     * @exports ISysRelatedLinkReq
     * @interface ISysRelatedLinkReq
     * @property {IClientInfo|null} [clientInfo] SysRelatedLinkReq clientInfo
     */

    /**
     * Constructs a new SysRelatedLinkReq.
     * @exports SysRelatedLinkReq
     * @classdesc Represents a SysRelatedLinkReq.
     * @implements ISysRelatedLinkReq
     * @constructor
     * @param {ISysRelatedLinkReq=} [properties] Properties to set
     */
    function SysRelatedLinkReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * SysRelatedLinkReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof SysRelatedLinkReq
     * @instance
     */
    SysRelatedLinkReq.prototype.clientInfo = null;

    /**
     * Creates a new SysRelatedLinkReq instance using the specified properties.
     * @function create
     * @memberof SysRelatedLinkReq
     * @static
     * @param {ISysRelatedLinkReq=} [properties] Properties to set
     * @returns {SysRelatedLinkReq} SysRelatedLinkReq instance
     */
    SysRelatedLinkReq.create = function create(properties) {
        return new SysRelatedLinkReq(properties);
    };

    /**
     * Encodes the specified SysRelatedLinkReq message. Does not implicitly {@link SysRelatedLinkReq.verify|verify} messages.
     * @function encode
     * @memberof SysRelatedLinkReq
     * @static
     * @param {ISysRelatedLinkReq} message SysRelatedLinkReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SysRelatedLinkReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified SysRelatedLinkReq message, length delimited. Does not implicitly {@link SysRelatedLinkReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof SysRelatedLinkReq
     * @static
     * @param {ISysRelatedLinkReq} message SysRelatedLinkReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SysRelatedLinkReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a SysRelatedLinkReq message from the specified reader or buffer.
     * @function decode
     * @memberof SysRelatedLinkReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {SysRelatedLinkReq} SysRelatedLinkReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SysRelatedLinkReq.decode = function decode(reader, length, error) {
    SysRelatedLinkReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.SysRelatedLinkReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a SysRelatedLinkReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof SysRelatedLinkReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {SysRelatedLinkReq} SysRelatedLinkReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SysRelatedLinkReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SysRelatedLinkReq message.
     * @function verify
     * @memberof SysRelatedLinkReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SysRelatedLinkReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        return null;
    };

    /**
     * Creates a SysRelatedLinkReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof SysRelatedLinkReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {SysRelatedLinkReq} SysRelatedLinkReq
     */
    SysRelatedLinkReq.fromObject = function fromObject(object) {
        if (object instanceof $root.SysRelatedLinkReq)
            return object;
        let message = new $root.SysRelatedLinkReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".SysRelatedLinkReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        return message;
    };

    /**
     * Creates a plain object from a SysRelatedLinkReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof SysRelatedLinkReq
     * @static
     * @param {SysRelatedLinkReq} message SysRelatedLinkReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SysRelatedLinkReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.clientInfo = null;
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        return object;
    };

    /**
     * Converts this SysRelatedLinkReq to JSON.
     * @function toJSON
     * @memberof SysRelatedLinkReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SysRelatedLinkReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SysRelatedLinkReq
     * @function getTypeUrl
     * @memberof SysRelatedLinkReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SysRelatedLinkReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/SysRelatedLinkReq";
    };

    return SysRelatedLinkReq;
})();

export const SysRelatedLinkResp = $root.SysRelatedLinkResp = (() => {

    /**
     * Properties of a SysRelatedLinkResp.
     * @exports ISysRelatedLinkResp
     * @interface ISysRelatedLinkResp
     * @property {ICommonResult|null} [commonResult] SysRelatedLinkResp commonResult
     * @property {string|null} [androidDownloadValidatorUrl] SysRelatedLinkResp androidDownloadValidatorUrl
     * @property {string|null} [iosDownloadValidatorUrl] SysRelatedLinkResp iosDownloadValidatorUrl
     * @property {string|null} [vectoringPageUrl] SysRelatedLinkResp vectoringPageUrl
     * @property {string|null} [agreementUrl] SysRelatedLinkResp agreementUrl
     * @property {string|null} [paymentProofExampleUrl] SysRelatedLinkResp paymentProofExampleUrl
     * @property {string|null} [appealVideoExampleUrl] SysRelatedLinkResp appealVideoExampleUrl
     * @property {string|null} [appealPictureExampleUrl] SysRelatedLinkResp appealPictureExampleUrl
     */

    /**
     * Constructs a new SysRelatedLinkResp.
     * @exports SysRelatedLinkResp
     * @classdesc Represents a SysRelatedLinkResp.
     * @implements ISysRelatedLinkResp
     * @constructor
     * @param {ISysRelatedLinkResp=} [properties] Properties to set
     */
    function SysRelatedLinkResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * SysRelatedLinkResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof SysRelatedLinkResp
     * @instance
     */
    SysRelatedLinkResp.prototype.commonResult = null;

    /**
     * SysRelatedLinkResp androidDownloadValidatorUrl.
     * @member {string} androidDownloadValidatorUrl
     * @memberof SysRelatedLinkResp
     * @instance
     */
    SysRelatedLinkResp.prototype.androidDownloadValidatorUrl = "";

    /**
     * SysRelatedLinkResp iosDownloadValidatorUrl.
     * @member {string} iosDownloadValidatorUrl
     * @memberof SysRelatedLinkResp
     * @instance
     */
    SysRelatedLinkResp.prototype.iosDownloadValidatorUrl = "";

    /**
     * SysRelatedLinkResp vectoringPageUrl.
     * @member {string} vectoringPageUrl
     * @memberof SysRelatedLinkResp
     * @instance
     */
    SysRelatedLinkResp.prototype.vectoringPageUrl = "";

    /**
     * SysRelatedLinkResp agreementUrl.
     * @member {string} agreementUrl
     * @memberof SysRelatedLinkResp
     * @instance
     */
    SysRelatedLinkResp.prototype.agreementUrl = "";

    /**
     * SysRelatedLinkResp paymentProofExampleUrl.
     * @member {string} paymentProofExampleUrl
     * @memberof SysRelatedLinkResp
     * @instance
     */
    SysRelatedLinkResp.prototype.paymentProofExampleUrl = "";

    /**
     * SysRelatedLinkResp appealVideoExampleUrl.
     * @member {string} appealVideoExampleUrl
     * @memberof SysRelatedLinkResp
     * @instance
     */
    SysRelatedLinkResp.prototype.appealVideoExampleUrl = "";

    /**
     * SysRelatedLinkResp appealPictureExampleUrl.
     * @member {string} appealPictureExampleUrl
     * @memberof SysRelatedLinkResp
     * @instance
     */
    SysRelatedLinkResp.prototype.appealPictureExampleUrl = "";

    /**
     * Creates a new SysRelatedLinkResp instance using the specified properties.
     * @function create
     * @memberof SysRelatedLinkResp
     * @static
     * @param {ISysRelatedLinkResp=} [properties] Properties to set
     * @returns {SysRelatedLinkResp} SysRelatedLinkResp instance
     */
    SysRelatedLinkResp.create = function create(properties) {
        return new SysRelatedLinkResp(properties);
    };

    /**
     * Encodes the specified SysRelatedLinkResp message. Does not implicitly {@link SysRelatedLinkResp.verify|verify} messages.
     * @function encode
     * @memberof SysRelatedLinkResp
     * @static
     * @param {ISysRelatedLinkResp} message SysRelatedLinkResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SysRelatedLinkResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.androidDownloadValidatorUrl != null && Object.hasOwnProperty.call(message, "androidDownloadValidatorUrl"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.androidDownloadValidatorUrl);
        if (message.iosDownloadValidatorUrl != null && Object.hasOwnProperty.call(message, "iosDownloadValidatorUrl"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.iosDownloadValidatorUrl);
        if (message.vectoringPageUrl != null && Object.hasOwnProperty.call(message, "vectoringPageUrl"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.vectoringPageUrl);
        if (message.agreementUrl != null && Object.hasOwnProperty.call(message, "agreementUrl"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.agreementUrl);
        if (message.paymentProofExampleUrl != null && Object.hasOwnProperty.call(message, "paymentProofExampleUrl"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.paymentProofExampleUrl);
        if (message.appealVideoExampleUrl != null && Object.hasOwnProperty.call(message, "appealVideoExampleUrl"))
            writer.uint32(/* id 7, wireType 2 =*/58).string(message.appealVideoExampleUrl);
        if (message.appealPictureExampleUrl != null && Object.hasOwnProperty.call(message, "appealPictureExampleUrl"))
            writer.uint32(/* id 8, wireType 2 =*/66).string(message.appealPictureExampleUrl);
        return writer;
    };

    /**
     * Encodes the specified SysRelatedLinkResp message, length delimited. Does not implicitly {@link SysRelatedLinkResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof SysRelatedLinkResp
     * @static
     * @param {ISysRelatedLinkResp} message SysRelatedLinkResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SysRelatedLinkResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a SysRelatedLinkResp message from the specified reader or buffer.
     * @function decode
     * @memberof SysRelatedLinkResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {SysRelatedLinkResp} SysRelatedLinkResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SysRelatedLinkResp.decode = function decode(reader, length, error) {
    SysRelatedLinkResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.SysRelatedLinkResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.androidDownloadValidatorUrl = reader.string();
                    break;
                }
            case 3: {
                    message.iosDownloadValidatorUrl = reader.string();
                    break;
                }
            case 4: {
                    message.vectoringPageUrl = reader.string();
                    break;
                }
            case 5: {
                    message.agreementUrl = reader.string();
                    break;
                }
            case 6: {
                    message.paymentProofExampleUrl = reader.string();
                    break;
                }
            case 7: {
                    message.appealVideoExampleUrl = reader.string();
                    break;
                }
            case 8: {
                    message.appealPictureExampleUrl = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a SysRelatedLinkResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof SysRelatedLinkResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {SysRelatedLinkResp} SysRelatedLinkResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SysRelatedLinkResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SysRelatedLinkResp message.
     * @function verify
     * @memberof SysRelatedLinkResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SysRelatedLinkResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.androidDownloadValidatorUrl != null && message.hasOwnProperty("androidDownloadValidatorUrl"))
            if (!$util.isString(message.androidDownloadValidatorUrl))
                return "androidDownloadValidatorUrl: string expected";
        if (message.iosDownloadValidatorUrl != null && message.hasOwnProperty("iosDownloadValidatorUrl"))
            if (!$util.isString(message.iosDownloadValidatorUrl))
                return "iosDownloadValidatorUrl: string expected";
        if (message.vectoringPageUrl != null && message.hasOwnProperty("vectoringPageUrl"))
            if (!$util.isString(message.vectoringPageUrl))
                return "vectoringPageUrl: string expected";
        if (message.agreementUrl != null && message.hasOwnProperty("agreementUrl"))
            if (!$util.isString(message.agreementUrl))
                return "agreementUrl: string expected";
        if (message.paymentProofExampleUrl != null && message.hasOwnProperty("paymentProofExampleUrl"))
            if (!$util.isString(message.paymentProofExampleUrl))
                return "paymentProofExampleUrl: string expected";
        if (message.appealVideoExampleUrl != null && message.hasOwnProperty("appealVideoExampleUrl"))
            if (!$util.isString(message.appealVideoExampleUrl))
                return "appealVideoExampleUrl: string expected";
        if (message.appealPictureExampleUrl != null && message.hasOwnProperty("appealPictureExampleUrl"))
            if (!$util.isString(message.appealPictureExampleUrl))
                return "appealPictureExampleUrl: string expected";
        return null;
    };

    /**
     * Creates a SysRelatedLinkResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof SysRelatedLinkResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {SysRelatedLinkResp} SysRelatedLinkResp
     */
    SysRelatedLinkResp.fromObject = function fromObject(object) {
        if (object instanceof $root.SysRelatedLinkResp)
            return object;
        let message = new $root.SysRelatedLinkResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".SysRelatedLinkResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.androidDownloadValidatorUrl != null)
            message.androidDownloadValidatorUrl = String(object.androidDownloadValidatorUrl);
        if (object.iosDownloadValidatorUrl != null)
            message.iosDownloadValidatorUrl = String(object.iosDownloadValidatorUrl);
        if (object.vectoringPageUrl != null)
            message.vectoringPageUrl = String(object.vectoringPageUrl);
        if (object.agreementUrl != null)
            message.agreementUrl = String(object.agreementUrl);
        if (object.paymentProofExampleUrl != null)
            message.paymentProofExampleUrl = String(object.paymentProofExampleUrl);
        if (object.appealVideoExampleUrl != null)
            message.appealVideoExampleUrl = String(object.appealVideoExampleUrl);
        if (object.appealPictureExampleUrl != null)
            message.appealPictureExampleUrl = String(object.appealPictureExampleUrl);
        return message;
    };

    /**
     * Creates a plain object from a SysRelatedLinkResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof SysRelatedLinkResp
     * @static
     * @param {SysRelatedLinkResp} message SysRelatedLinkResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SysRelatedLinkResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.androidDownloadValidatorUrl = "";
            object.iosDownloadValidatorUrl = "";
            object.vectoringPageUrl = "";
            object.agreementUrl = "";
            object.paymentProofExampleUrl = "";
            object.appealVideoExampleUrl = "";
            object.appealPictureExampleUrl = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.androidDownloadValidatorUrl != null && message.hasOwnProperty("androidDownloadValidatorUrl"))
            object.androidDownloadValidatorUrl = message.androidDownloadValidatorUrl;
        if (message.iosDownloadValidatorUrl != null && message.hasOwnProperty("iosDownloadValidatorUrl"))
            object.iosDownloadValidatorUrl = message.iosDownloadValidatorUrl;
        if (message.vectoringPageUrl != null && message.hasOwnProperty("vectoringPageUrl"))
            object.vectoringPageUrl = message.vectoringPageUrl;
        if (message.agreementUrl != null && message.hasOwnProperty("agreementUrl"))
            object.agreementUrl = message.agreementUrl;
        if (message.paymentProofExampleUrl != null && message.hasOwnProperty("paymentProofExampleUrl"))
            object.paymentProofExampleUrl = message.paymentProofExampleUrl;
        if (message.appealVideoExampleUrl != null && message.hasOwnProperty("appealVideoExampleUrl"))
            object.appealVideoExampleUrl = message.appealVideoExampleUrl;
        if (message.appealPictureExampleUrl != null && message.hasOwnProperty("appealPictureExampleUrl"))
            object.appealPictureExampleUrl = message.appealPictureExampleUrl;
        return object;
    };

    /**
     * Converts this SysRelatedLinkResp to JSON.
     * @function toJSON
     * @memberof SysRelatedLinkResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SysRelatedLinkResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SysRelatedLinkResp
     * @function getTypeUrl
     * @memberof SysRelatedLinkResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SysRelatedLinkResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/SysRelatedLinkResp";
    };

    return SysRelatedLinkResp;
})();

export const SysDependentAddrLinkReq = $root.SysDependentAddrLinkReq = (() => {

    /**
     * Properties of a SysDependentAddrLinkReq.
     * @exports ISysDependentAddrLinkReq
     * @interface ISysDependentAddrLinkReq
     * @property {IClientInfo|null} [clientInfo] SysDependentAddrLinkReq clientInfo
     */

    /**
     * Constructs a new SysDependentAddrLinkReq.
     * @exports SysDependentAddrLinkReq
     * @classdesc Represents a SysDependentAddrLinkReq.
     * @implements ISysDependentAddrLinkReq
     * @constructor
     * @param {ISysDependentAddrLinkReq=} [properties] Properties to set
     */
    function SysDependentAddrLinkReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * SysDependentAddrLinkReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof SysDependentAddrLinkReq
     * @instance
     */
    SysDependentAddrLinkReq.prototype.clientInfo = null;

    /**
     * Creates a new SysDependentAddrLinkReq instance using the specified properties.
     * @function create
     * @memberof SysDependentAddrLinkReq
     * @static
     * @param {ISysDependentAddrLinkReq=} [properties] Properties to set
     * @returns {SysDependentAddrLinkReq} SysDependentAddrLinkReq instance
     */
    SysDependentAddrLinkReq.create = function create(properties) {
        return new SysDependentAddrLinkReq(properties);
    };

    /**
     * Encodes the specified SysDependentAddrLinkReq message. Does not implicitly {@link SysDependentAddrLinkReq.verify|verify} messages.
     * @function encode
     * @memberof SysDependentAddrLinkReq
     * @static
     * @param {ISysDependentAddrLinkReq} message SysDependentAddrLinkReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SysDependentAddrLinkReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified SysDependentAddrLinkReq message, length delimited. Does not implicitly {@link SysDependentAddrLinkReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof SysDependentAddrLinkReq
     * @static
     * @param {ISysDependentAddrLinkReq} message SysDependentAddrLinkReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SysDependentAddrLinkReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a SysDependentAddrLinkReq message from the specified reader or buffer.
     * @function decode
     * @memberof SysDependentAddrLinkReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {SysDependentAddrLinkReq} SysDependentAddrLinkReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SysDependentAddrLinkReq.decode = function decode(reader, length, error) {
    SysDependentAddrLinkReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.SysDependentAddrLinkReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a SysDependentAddrLinkReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof SysDependentAddrLinkReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {SysDependentAddrLinkReq} SysDependentAddrLinkReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SysDependentAddrLinkReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SysDependentAddrLinkReq message.
     * @function verify
     * @memberof SysDependentAddrLinkReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SysDependentAddrLinkReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        return null;
    };

    /**
     * Creates a SysDependentAddrLinkReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof SysDependentAddrLinkReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {SysDependentAddrLinkReq} SysDependentAddrLinkReq
     */
    SysDependentAddrLinkReq.fromObject = function fromObject(object) {
        if (object instanceof $root.SysDependentAddrLinkReq)
            return object;
        let message = new $root.SysDependentAddrLinkReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".SysDependentAddrLinkReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        return message;
    };

    /**
     * Creates a plain object from a SysDependentAddrLinkReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof SysDependentAddrLinkReq
     * @static
     * @param {SysDependentAddrLinkReq} message SysDependentAddrLinkReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SysDependentAddrLinkReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.clientInfo = null;
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        return object;
    };

    /**
     * Converts this SysDependentAddrLinkReq to JSON.
     * @function toJSON
     * @memberof SysDependentAddrLinkReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SysDependentAddrLinkReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SysDependentAddrLinkReq
     * @function getTypeUrl
     * @memberof SysDependentAddrLinkReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SysDependentAddrLinkReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/SysDependentAddrLinkReq";
    };

    return SysDependentAddrLinkReq;
})();

export const SysDependentAddrLinkResp = $root.SysDependentAddrLinkResp = (() => {

    /**
     * Properties of a SysDependentAddrLinkResp.
     * @exports ISysDependentAddrLinkResp
     * @interface ISysDependentAddrLinkResp
     * @property {ICommonResult|null} [commonResult] SysDependentAddrLinkResp commonResult
     * @property {string|null} [androidDownloadValidatorUrl] SysDependentAddrLinkResp androidDownloadValidatorUrl
     * @property {string|null} [iosDownloadValidatorUrl] SysDependentAddrLinkResp iosDownloadValidatorUrl
     * @property {string|null} [vectoringPageUrl] SysDependentAddrLinkResp vectoringPageUrl
     * @property {string|null} [agreementUrl] SysDependentAddrLinkResp agreementUrl
     * @property {IPayExampleUrl|null} [bankCardExampleUrl] SysDependentAddrLinkResp bankCardExampleUrl
     * @property {IPayExampleUrl|null} [weChatExampleUrl] SysDependentAddrLinkResp weChatExampleUrl
     * @property {IPayExampleUrl|null} [aliExampleUrl] SysDependentAddrLinkResp aliExampleUrl
     */

    /**
     * Constructs a new SysDependentAddrLinkResp.
     * @exports SysDependentAddrLinkResp
     * @classdesc Represents a SysDependentAddrLinkResp.
     * @implements ISysDependentAddrLinkResp
     * @constructor
     * @param {ISysDependentAddrLinkResp=} [properties] Properties to set
     */
    function SysDependentAddrLinkResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * SysDependentAddrLinkResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof SysDependentAddrLinkResp
     * @instance
     */
    SysDependentAddrLinkResp.prototype.commonResult = null;

    /**
     * SysDependentAddrLinkResp androidDownloadValidatorUrl.
     * @member {string} androidDownloadValidatorUrl
     * @memberof SysDependentAddrLinkResp
     * @instance
     */
    SysDependentAddrLinkResp.prototype.androidDownloadValidatorUrl = "";

    /**
     * SysDependentAddrLinkResp iosDownloadValidatorUrl.
     * @member {string} iosDownloadValidatorUrl
     * @memberof SysDependentAddrLinkResp
     * @instance
     */
    SysDependentAddrLinkResp.prototype.iosDownloadValidatorUrl = "";

    /**
     * SysDependentAddrLinkResp vectoringPageUrl.
     * @member {string} vectoringPageUrl
     * @memberof SysDependentAddrLinkResp
     * @instance
     */
    SysDependentAddrLinkResp.prototype.vectoringPageUrl = "";

    /**
     * SysDependentAddrLinkResp agreementUrl.
     * @member {string} agreementUrl
     * @memberof SysDependentAddrLinkResp
     * @instance
     */
    SysDependentAddrLinkResp.prototype.agreementUrl = "";

    /**
     * SysDependentAddrLinkResp bankCardExampleUrl.
     * @member {IPayExampleUrl|null|undefined} bankCardExampleUrl
     * @memberof SysDependentAddrLinkResp
     * @instance
     */
    SysDependentAddrLinkResp.prototype.bankCardExampleUrl = null;

    /**
     * SysDependentAddrLinkResp weChatExampleUrl.
     * @member {IPayExampleUrl|null|undefined} weChatExampleUrl
     * @memberof SysDependentAddrLinkResp
     * @instance
     */
    SysDependentAddrLinkResp.prototype.weChatExampleUrl = null;

    /**
     * SysDependentAddrLinkResp aliExampleUrl.
     * @member {IPayExampleUrl|null|undefined} aliExampleUrl
     * @memberof SysDependentAddrLinkResp
     * @instance
     */
    SysDependentAddrLinkResp.prototype.aliExampleUrl = null;

    /**
     * Creates a new SysDependentAddrLinkResp instance using the specified properties.
     * @function create
     * @memberof SysDependentAddrLinkResp
     * @static
     * @param {ISysDependentAddrLinkResp=} [properties] Properties to set
     * @returns {SysDependentAddrLinkResp} SysDependentAddrLinkResp instance
     */
    SysDependentAddrLinkResp.create = function create(properties) {
        return new SysDependentAddrLinkResp(properties);
    };

    /**
     * Encodes the specified SysDependentAddrLinkResp message. Does not implicitly {@link SysDependentAddrLinkResp.verify|verify} messages.
     * @function encode
     * @memberof SysDependentAddrLinkResp
     * @static
     * @param {ISysDependentAddrLinkResp} message SysDependentAddrLinkResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SysDependentAddrLinkResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.androidDownloadValidatorUrl != null && Object.hasOwnProperty.call(message, "androidDownloadValidatorUrl"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.androidDownloadValidatorUrl);
        if (message.iosDownloadValidatorUrl != null && Object.hasOwnProperty.call(message, "iosDownloadValidatorUrl"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.iosDownloadValidatorUrl);
        if (message.vectoringPageUrl != null && Object.hasOwnProperty.call(message, "vectoringPageUrl"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.vectoringPageUrl);
        if (message.agreementUrl != null && Object.hasOwnProperty.call(message, "agreementUrl"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.agreementUrl);
        if (message.bankCardExampleUrl != null && Object.hasOwnProperty.call(message, "bankCardExampleUrl"))
            $root.PayExampleUrl.encode(message.bankCardExampleUrl, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
        if (message.weChatExampleUrl != null && Object.hasOwnProperty.call(message, "weChatExampleUrl"))
            $root.PayExampleUrl.encode(message.weChatExampleUrl, writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
        if (message.aliExampleUrl != null && Object.hasOwnProperty.call(message, "aliExampleUrl"))
            $root.PayExampleUrl.encode(message.aliExampleUrl, writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified SysDependentAddrLinkResp message, length delimited. Does not implicitly {@link SysDependentAddrLinkResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof SysDependentAddrLinkResp
     * @static
     * @param {ISysDependentAddrLinkResp} message SysDependentAddrLinkResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SysDependentAddrLinkResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a SysDependentAddrLinkResp message from the specified reader or buffer.
     * @function decode
     * @memberof SysDependentAddrLinkResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {SysDependentAddrLinkResp} SysDependentAddrLinkResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SysDependentAddrLinkResp.decode = function decode(reader, length, error) {
    SysDependentAddrLinkResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.SysDependentAddrLinkResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.androidDownloadValidatorUrl = reader.string();
                    break;
                }
            case 3: {
                    message.iosDownloadValidatorUrl = reader.string();
                    break;
                }
            case 4: {
                    message.vectoringPageUrl = reader.string();
                    break;
                }
            case 5: {
                    message.agreementUrl = reader.string();
                    break;
                }
            case 6: {
                    message.bankCardExampleUrl = $root.PayExampleUrl.decode(reader, reader.uint32());
                    break;
                }
            case 7: {
                    message.weChatExampleUrl = $root.PayExampleUrl.decode(reader, reader.uint32());
                    break;
                }
            case 8: {
                    message.aliExampleUrl = $root.PayExampleUrl.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a SysDependentAddrLinkResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof SysDependentAddrLinkResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {SysDependentAddrLinkResp} SysDependentAddrLinkResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SysDependentAddrLinkResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SysDependentAddrLinkResp message.
     * @function verify
     * @memberof SysDependentAddrLinkResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SysDependentAddrLinkResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.androidDownloadValidatorUrl != null && message.hasOwnProperty("androidDownloadValidatorUrl"))
            if (!$util.isString(message.androidDownloadValidatorUrl))
                return "androidDownloadValidatorUrl: string expected";
        if (message.iosDownloadValidatorUrl != null && message.hasOwnProperty("iosDownloadValidatorUrl"))
            if (!$util.isString(message.iosDownloadValidatorUrl))
                return "iosDownloadValidatorUrl: string expected";
        if (message.vectoringPageUrl != null && message.hasOwnProperty("vectoringPageUrl"))
            if (!$util.isString(message.vectoringPageUrl))
                return "vectoringPageUrl: string expected";
        if (message.agreementUrl != null && message.hasOwnProperty("agreementUrl"))
            if (!$util.isString(message.agreementUrl))
                return "agreementUrl: string expected";
        if (message.bankCardExampleUrl != null && message.hasOwnProperty("bankCardExampleUrl")) {
            let error = $root.PayExampleUrl.verify(message.bankCardExampleUrl);
            if (error)
                return "bankCardExampleUrl." + error;
        }
        if (message.weChatExampleUrl != null && message.hasOwnProperty("weChatExampleUrl")) {
            let error = $root.PayExampleUrl.verify(message.weChatExampleUrl);
            if (error)
                return "weChatExampleUrl." + error;
        }
        if (message.aliExampleUrl != null && message.hasOwnProperty("aliExampleUrl")) {
            let error = $root.PayExampleUrl.verify(message.aliExampleUrl);
            if (error)
                return "aliExampleUrl." + error;
        }
        return null;
    };

    /**
     * Creates a SysDependentAddrLinkResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof SysDependentAddrLinkResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {SysDependentAddrLinkResp} SysDependentAddrLinkResp
     */
    SysDependentAddrLinkResp.fromObject = function fromObject(object) {
        if (object instanceof $root.SysDependentAddrLinkResp)
            return object;
        let message = new $root.SysDependentAddrLinkResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".SysDependentAddrLinkResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.androidDownloadValidatorUrl != null)
            message.androidDownloadValidatorUrl = String(object.androidDownloadValidatorUrl);
        if (object.iosDownloadValidatorUrl != null)
            message.iosDownloadValidatorUrl = String(object.iosDownloadValidatorUrl);
        if (object.vectoringPageUrl != null)
            message.vectoringPageUrl = String(object.vectoringPageUrl);
        if (object.agreementUrl != null)
            message.agreementUrl = String(object.agreementUrl);
        if (object.bankCardExampleUrl != null) {
            if (typeof object.bankCardExampleUrl !== "object")
                throw TypeError(".SysDependentAddrLinkResp.bankCardExampleUrl: object expected");
            message.bankCardExampleUrl = $root.PayExampleUrl.fromObject(object.bankCardExampleUrl);
        }
        if (object.weChatExampleUrl != null) {
            if (typeof object.weChatExampleUrl !== "object")
                throw TypeError(".SysDependentAddrLinkResp.weChatExampleUrl: object expected");
            message.weChatExampleUrl = $root.PayExampleUrl.fromObject(object.weChatExampleUrl);
        }
        if (object.aliExampleUrl != null) {
            if (typeof object.aliExampleUrl !== "object")
                throw TypeError(".SysDependentAddrLinkResp.aliExampleUrl: object expected");
            message.aliExampleUrl = $root.PayExampleUrl.fromObject(object.aliExampleUrl);
        }
        return message;
    };

    /**
     * Creates a plain object from a SysDependentAddrLinkResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof SysDependentAddrLinkResp
     * @static
     * @param {SysDependentAddrLinkResp} message SysDependentAddrLinkResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SysDependentAddrLinkResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.androidDownloadValidatorUrl = "";
            object.iosDownloadValidatorUrl = "";
            object.vectoringPageUrl = "";
            object.agreementUrl = "";
            object.bankCardExampleUrl = null;
            object.weChatExampleUrl = null;
            object.aliExampleUrl = null;
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.androidDownloadValidatorUrl != null && message.hasOwnProperty("androidDownloadValidatorUrl"))
            object.androidDownloadValidatorUrl = message.androidDownloadValidatorUrl;
        if (message.iosDownloadValidatorUrl != null && message.hasOwnProperty("iosDownloadValidatorUrl"))
            object.iosDownloadValidatorUrl = message.iosDownloadValidatorUrl;
        if (message.vectoringPageUrl != null && message.hasOwnProperty("vectoringPageUrl"))
            object.vectoringPageUrl = message.vectoringPageUrl;
        if (message.agreementUrl != null && message.hasOwnProperty("agreementUrl"))
            object.agreementUrl = message.agreementUrl;
        if (message.bankCardExampleUrl != null && message.hasOwnProperty("bankCardExampleUrl"))
            object.bankCardExampleUrl = $root.PayExampleUrl.toObject(message.bankCardExampleUrl, options);
        if (message.weChatExampleUrl != null && message.hasOwnProperty("weChatExampleUrl"))
            object.weChatExampleUrl = $root.PayExampleUrl.toObject(message.weChatExampleUrl, options);
        if (message.aliExampleUrl != null && message.hasOwnProperty("aliExampleUrl"))
            object.aliExampleUrl = $root.PayExampleUrl.toObject(message.aliExampleUrl, options);
        return object;
    };

    /**
     * Converts this SysDependentAddrLinkResp to JSON.
     * @function toJSON
     * @memberof SysDependentAddrLinkResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SysDependentAddrLinkResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SysDependentAddrLinkResp
     * @function getTypeUrl
     * @memberof SysDependentAddrLinkResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SysDependentAddrLinkResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/SysDependentAddrLinkResp";
    };

    return SysDependentAddrLinkResp;
})();

export const PayExampleUrl = $root.PayExampleUrl = (() => {

    /**
     * Properties of a PayExampleUrl.
     * @exports IPayExampleUrl
     * @interface IPayExampleUrl
     * @property {string|null} [uploadPayVoucher] PayExampleUrl uploadPayVoucher
     * @property {string|null} [appealImage] PayExampleUrl appealImage
     * @property {string|null} [appealVideo] PayExampleUrl appealVideo
     */

    /**
     * Constructs a new PayExampleUrl.
     * @exports PayExampleUrl
     * @classdesc Represents a PayExampleUrl.
     * @implements IPayExampleUrl
     * @constructor
     * @param {IPayExampleUrl=} [properties] Properties to set
     */
    function PayExampleUrl(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * PayExampleUrl uploadPayVoucher.
     * @member {string} uploadPayVoucher
     * @memberof PayExampleUrl
     * @instance
     */
    PayExampleUrl.prototype.uploadPayVoucher = "";

    /**
     * PayExampleUrl appealImage.
     * @member {string} appealImage
     * @memberof PayExampleUrl
     * @instance
     */
    PayExampleUrl.prototype.appealImage = "";

    /**
     * PayExampleUrl appealVideo.
     * @member {string} appealVideo
     * @memberof PayExampleUrl
     * @instance
     */
    PayExampleUrl.prototype.appealVideo = "";

    /**
     * Creates a new PayExampleUrl instance using the specified properties.
     * @function create
     * @memberof PayExampleUrl
     * @static
     * @param {IPayExampleUrl=} [properties] Properties to set
     * @returns {PayExampleUrl} PayExampleUrl instance
     */
    PayExampleUrl.create = function create(properties) {
        return new PayExampleUrl(properties);
    };

    /**
     * Encodes the specified PayExampleUrl message. Does not implicitly {@link PayExampleUrl.verify|verify} messages.
     * @function encode
     * @memberof PayExampleUrl
     * @static
     * @param {IPayExampleUrl} message PayExampleUrl message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PayExampleUrl.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.uploadPayVoucher != null && Object.hasOwnProperty.call(message, "uploadPayVoucher"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.uploadPayVoucher);
        if (message.appealImage != null && Object.hasOwnProperty.call(message, "appealImage"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.appealImage);
        if (message.appealVideo != null && Object.hasOwnProperty.call(message, "appealVideo"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.appealVideo);
        return writer;
    };

    /**
     * Encodes the specified PayExampleUrl message, length delimited. Does not implicitly {@link PayExampleUrl.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PayExampleUrl
     * @static
     * @param {IPayExampleUrl} message PayExampleUrl message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PayExampleUrl.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a PayExampleUrl message from the specified reader or buffer.
     * @function decode
     * @memberof PayExampleUrl
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PayExampleUrl} PayExampleUrl
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PayExampleUrl.decode = function decode(reader, length, error) {
    PayExampleUrl.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.PayExampleUrl();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.uploadPayVoucher = reader.string();
                    break;
                }
            case 2: {
                    message.appealImage = reader.string();
                    break;
                }
            case 3: {
                    message.appealVideo = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a PayExampleUrl message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PayExampleUrl
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PayExampleUrl} PayExampleUrl
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PayExampleUrl.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PayExampleUrl message.
     * @function verify
     * @memberof PayExampleUrl
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PayExampleUrl.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.uploadPayVoucher != null && message.hasOwnProperty("uploadPayVoucher"))
            if (!$util.isString(message.uploadPayVoucher))
                return "uploadPayVoucher: string expected";
        if (message.appealImage != null && message.hasOwnProperty("appealImage"))
            if (!$util.isString(message.appealImage))
                return "appealImage: string expected";
        if (message.appealVideo != null && message.hasOwnProperty("appealVideo"))
            if (!$util.isString(message.appealVideo))
                return "appealVideo: string expected";
        return null;
    };

    /**
     * Creates a PayExampleUrl message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PayExampleUrl
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PayExampleUrl} PayExampleUrl
     */
    PayExampleUrl.fromObject = function fromObject(object) {
        if (object instanceof $root.PayExampleUrl)
            return object;
        let message = new $root.PayExampleUrl();
        if (object.uploadPayVoucher != null)
            message.uploadPayVoucher = String(object.uploadPayVoucher);
        if (object.appealImage != null)
            message.appealImage = String(object.appealImage);
        if (object.appealVideo != null)
            message.appealVideo = String(object.appealVideo);
        return message;
    };

    /**
     * Creates a plain object from a PayExampleUrl message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PayExampleUrl
     * @static
     * @param {PayExampleUrl} message PayExampleUrl
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PayExampleUrl.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.uploadPayVoucher = "";
            object.appealImage = "";
            object.appealVideo = "";
        }
        if (message.uploadPayVoucher != null && message.hasOwnProperty("uploadPayVoucher"))
            object.uploadPayVoucher = message.uploadPayVoucher;
        if (message.appealImage != null && message.hasOwnProperty("appealImage"))
            object.appealImage = message.appealImage;
        if (message.appealVideo != null && message.hasOwnProperty("appealVideo"))
            object.appealVideo = message.appealVideo;
        return object;
    };

    /**
     * Converts this PayExampleUrl to JSON.
     * @function toJSON
     * @memberof PayExampleUrl
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PayExampleUrl.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PayExampleUrl
     * @function getTypeUrl
     * @memberof PayExampleUrl
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PayExampleUrl.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/PayExampleUrl";
    };

    return PayExampleUrl;
})();

export const GoogleUrlResp = $root.GoogleUrlResp = (() => {

    /**
     * Properties of a GoogleUrlResp.
     * @exports IGoogleUrlResp
     * @interface IGoogleUrlResp
     * @property {ICommonResult|null} [commonResult] GoogleUrlResp commonResult
     * @property {string|null} [url] GoogleUrlResp url
     */

    /**
     * Constructs a new GoogleUrlResp.
     * @exports GoogleUrlResp
     * @classdesc Represents a GoogleUrlResp.
     * @implements IGoogleUrlResp
     * @constructor
     * @param {IGoogleUrlResp=} [properties] Properties to set
     */
    function GoogleUrlResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GoogleUrlResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GoogleUrlResp
     * @instance
     */
    GoogleUrlResp.prototype.commonResult = null;

    /**
     * GoogleUrlResp url.
     * @member {string} url
     * @memberof GoogleUrlResp
     * @instance
     */
    GoogleUrlResp.prototype.url = "";

    /**
     * Creates a new GoogleUrlResp instance using the specified properties.
     * @function create
     * @memberof GoogleUrlResp
     * @static
     * @param {IGoogleUrlResp=} [properties] Properties to set
     * @returns {GoogleUrlResp} GoogleUrlResp instance
     */
    GoogleUrlResp.create = function create(properties) {
        return new GoogleUrlResp(properties);
    };

    /**
     * Encodes the specified GoogleUrlResp message. Does not implicitly {@link GoogleUrlResp.verify|verify} messages.
     * @function encode
     * @memberof GoogleUrlResp
     * @static
     * @param {IGoogleUrlResp} message GoogleUrlResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GoogleUrlResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.url != null && Object.hasOwnProperty.call(message, "url"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.url);
        return writer;
    };

    /**
     * Encodes the specified GoogleUrlResp message, length delimited. Does not implicitly {@link GoogleUrlResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GoogleUrlResp
     * @static
     * @param {IGoogleUrlResp} message GoogleUrlResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GoogleUrlResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GoogleUrlResp message from the specified reader or buffer.
     * @function decode
     * @memberof GoogleUrlResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GoogleUrlResp} GoogleUrlResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GoogleUrlResp.decode = function decode(reader, length, error) {
    GoogleUrlResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GoogleUrlResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.url = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GoogleUrlResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GoogleUrlResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GoogleUrlResp} GoogleUrlResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GoogleUrlResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GoogleUrlResp message.
     * @function verify
     * @memberof GoogleUrlResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GoogleUrlResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.url != null && message.hasOwnProperty("url"))
            if (!$util.isString(message.url))
                return "url: string expected";
        return null;
    };

    /**
     * Creates a GoogleUrlResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GoogleUrlResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GoogleUrlResp} GoogleUrlResp
     */
    GoogleUrlResp.fromObject = function fromObject(object) {
        if (object instanceof $root.GoogleUrlResp)
            return object;
        let message = new $root.GoogleUrlResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GoogleUrlResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.url != null)
            message.url = String(object.url);
        return message;
    };

    /**
     * Creates a plain object from a GoogleUrlResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GoogleUrlResp
     * @static
     * @param {GoogleUrlResp} message GoogleUrlResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GoogleUrlResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.url = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.url != null && message.hasOwnProperty("url"))
            object.url = message.url;
        return object;
    };

    /**
     * Converts this GoogleUrlResp to JSON.
     * @function toJSON
     * @memberof GoogleUrlResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GoogleUrlResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GoogleUrlResp
     * @function getTypeUrl
     * @memberof GoogleUrlResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GoogleUrlResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GoogleUrlResp";
    };

    return GoogleUrlResp;
})();

export const OpenInstallReq = $root.OpenInstallReq = (() => {

    /**
     * Properties of an OpenInstallReq.
     * @exports IOpenInstallReq
     * @interface IOpenInstallReq
     * @property {IClientInfo|null} [clientInfo] OpenInstallReq clientInfo
     * @property {string|null} [params] OpenInstallReq params
     * @property {number|null} [type] OpenInstallReq type
     * @property {string|null} [channelCode] OpenInstallReq channelCode
     */

    /**
     * Constructs a new OpenInstallReq.
     * @exports OpenInstallReq
     * @classdesc Represents an OpenInstallReq.
     * @implements IOpenInstallReq
     * @constructor
     * @param {IOpenInstallReq=} [properties] Properties to set
     */
    function OpenInstallReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * OpenInstallReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof OpenInstallReq
     * @instance
     */
    OpenInstallReq.prototype.clientInfo = null;

    /**
     * OpenInstallReq params.
     * @member {string} params
     * @memberof OpenInstallReq
     * @instance
     */
    OpenInstallReq.prototype.params = "";

    /**
     * OpenInstallReq type.
     * @member {number} type
     * @memberof OpenInstallReq
     * @instance
     */
    OpenInstallReq.prototype.type = 0;

    /**
     * OpenInstallReq channelCode.
     * @member {string} channelCode
     * @memberof OpenInstallReq
     * @instance
     */
    OpenInstallReq.prototype.channelCode = "";

    /**
     * Creates a new OpenInstallReq instance using the specified properties.
     * @function create
     * @memberof OpenInstallReq
     * @static
     * @param {IOpenInstallReq=} [properties] Properties to set
     * @returns {OpenInstallReq} OpenInstallReq instance
     */
    OpenInstallReq.create = function create(properties) {
        return new OpenInstallReq(properties);
    };

    /**
     * Encodes the specified OpenInstallReq message. Does not implicitly {@link OpenInstallReq.verify|verify} messages.
     * @function encode
     * @memberof OpenInstallReq
     * @static
     * @param {IOpenInstallReq} message OpenInstallReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OpenInstallReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.params != null && Object.hasOwnProperty.call(message, "params"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.params);
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.type);
        if (message.channelCode != null && Object.hasOwnProperty.call(message, "channelCode"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.channelCode);
        return writer;
    };

    /**
     * Encodes the specified OpenInstallReq message, length delimited. Does not implicitly {@link OpenInstallReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof OpenInstallReq
     * @static
     * @param {IOpenInstallReq} message OpenInstallReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OpenInstallReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an OpenInstallReq message from the specified reader or buffer.
     * @function decode
     * @memberof OpenInstallReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {OpenInstallReq} OpenInstallReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OpenInstallReq.decode = function decode(reader, length, error) {
    OpenInstallReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.OpenInstallReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.params = reader.string();
                    break;
                }
            case 3: {
                    message.type = reader.int32();
                    break;
                }
            case 4: {
                    message.channelCode = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an OpenInstallReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof OpenInstallReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {OpenInstallReq} OpenInstallReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OpenInstallReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an OpenInstallReq message.
     * @function verify
     * @memberof OpenInstallReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    OpenInstallReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.params != null && message.hasOwnProperty("params"))
            if (!$util.isString(message.params))
                return "params: string expected";
        if (message.type != null && message.hasOwnProperty("type"))
            if (!$util.isInteger(message.type))
                return "type: integer expected";
        if (message.channelCode != null && message.hasOwnProperty("channelCode"))
            if (!$util.isString(message.channelCode))
                return "channelCode: string expected";
        return null;
    };

    /**
     * Creates an OpenInstallReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof OpenInstallReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {OpenInstallReq} OpenInstallReq
     */
    OpenInstallReq.fromObject = function fromObject(object) {
        if (object instanceof $root.OpenInstallReq)
            return object;
        let message = new $root.OpenInstallReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".OpenInstallReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.params != null)
            message.params = String(object.params);
        if (object.type != null)
            message.type = object.type | 0;
        if (object.channelCode != null)
            message.channelCode = String(object.channelCode);
        return message;
    };

    /**
     * Creates a plain object from an OpenInstallReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof OpenInstallReq
     * @static
     * @param {OpenInstallReq} message OpenInstallReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    OpenInstallReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.params = "";
            object.type = 0;
            object.channelCode = "";
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.params != null && message.hasOwnProperty("params"))
            object.params = message.params;
        if (message.type != null && message.hasOwnProperty("type"))
            object.type = message.type;
        if (message.channelCode != null && message.hasOwnProperty("channelCode"))
            object.channelCode = message.channelCode;
        return object;
    };

    /**
     * Converts this OpenInstallReq to JSON.
     * @function toJSON
     * @memberof OpenInstallReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    OpenInstallReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for OpenInstallReq
     * @function getTypeUrl
     * @memberof OpenInstallReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    OpenInstallReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/OpenInstallReq";
    };

    return OpenInstallReq;
})();

export const OpenInstallResp = $root.OpenInstallResp = (() => {

    /**
     * Properties of an OpenInstallResp.
     * @exports IOpenInstallResp
     * @interface IOpenInstallResp
     * @property {ICommonResult|null} [commonResult] OpenInstallResp commonResult
     */

    /**
     * Constructs a new OpenInstallResp.
     * @exports OpenInstallResp
     * @classdesc Represents an OpenInstallResp.
     * @implements IOpenInstallResp
     * @constructor
     * @param {IOpenInstallResp=} [properties] Properties to set
     */
    function OpenInstallResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * OpenInstallResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof OpenInstallResp
     * @instance
     */
    OpenInstallResp.prototype.commonResult = null;

    /**
     * Creates a new OpenInstallResp instance using the specified properties.
     * @function create
     * @memberof OpenInstallResp
     * @static
     * @param {IOpenInstallResp=} [properties] Properties to set
     * @returns {OpenInstallResp} OpenInstallResp instance
     */
    OpenInstallResp.create = function create(properties) {
        return new OpenInstallResp(properties);
    };

    /**
     * Encodes the specified OpenInstallResp message. Does not implicitly {@link OpenInstallResp.verify|verify} messages.
     * @function encode
     * @memberof OpenInstallResp
     * @static
     * @param {IOpenInstallResp} message OpenInstallResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OpenInstallResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified OpenInstallResp message, length delimited. Does not implicitly {@link OpenInstallResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof OpenInstallResp
     * @static
     * @param {IOpenInstallResp} message OpenInstallResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OpenInstallResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an OpenInstallResp message from the specified reader or buffer.
     * @function decode
     * @memberof OpenInstallResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {OpenInstallResp} OpenInstallResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OpenInstallResp.decode = function decode(reader, length, error) {
    OpenInstallResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.OpenInstallResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an OpenInstallResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof OpenInstallResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {OpenInstallResp} OpenInstallResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OpenInstallResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an OpenInstallResp message.
     * @function verify
     * @memberof OpenInstallResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    OpenInstallResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates an OpenInstallResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof OpenInstallResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {OpenInstallResp} OpenInstallResp
     */
    OpenInstallResp.fromObject = function fromObject(object) {
        if (object instanceof $root.OpenInstallResp)
            return object;
        let message = new $root.OpenInstallResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".OpenInstallResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from an OpenInstallResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof OpenInstallResp
     * @static
     * @param {OpenInstallResp} message OpenInstallResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    OpenInstallResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this OpenInstallResp to JSON.
     * @function toJSON
     * @memberof OpenInstallResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    OpenInstallResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for OpenInstallResp
     * @function getTypeUrl
     * @memberof OpenInstallResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    OpenInstallResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/OpenInstallResp";
    };

    return OpenInstallResp;
})();

export const GetUploadTokenReq = $root.GetUploadTokenReq = (() => {

    /**
     * Properties of a GetUploadTokenReq.
     * @exports IGetUploadTokenReq
     * @interface IGetUploadTokenReq
     * @property {IClientInfo|null} [clientInfo] GetUploadTokenReq clientInfo
     */

    /**
     * Constructs a new GetUploadTokenReq.
     * @exports GetUploadTokenReq
     * @classdesc Represents a GetUploadTokenReq.
     * @implements IGetUploadTokenReq
     * @constructor
     * @param {IGetUploadTokenReq=} [properties] Properties to set
     */
    function GetUploadTokenReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetUploadTokenReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GetUploadTokenReq
     * @instance
     */
    GetUploadTokenReq.prototype.clientInfo = null;

    /**
     * Creates a new GetUploadTokenReq instance using the specified properties.
     * @function create
     * @memberof GetUploadTokenReq
     * @static
     * @param {IGetUploadTokenReq=} [properties] Properties to set
     * @returns {GetUploadTokenReq} GetUploadTokenReq instance
     */
    GetUploadTokenReq.create = function create(properties) {
        return new GetUploadTokenReq(properties);
    };

    /**
     * Encodes the specified GetUploadTokenReq message. Does not implicitly {@link GetUploadTokenReq.verify|verify} messages.
     * @function encode
     * @memberof GetUploadTokenReq
     * @static
     * @param {IGetUploadTokenReq} message GetUploadTokenReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetUploadTokenReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified GetUploadTokenReq message, length delimited. Does not implicitly {@link GetUploadTokenReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetUploadTokenReq
     * @static
     * @param {IGetUploadTokenReq} message GetUploadTokenReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetUploadTokenReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetUploadTokenReq message from the specified reader or buffer.
     * @function decode
     * @memberof GetUploadTokenReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetUploadTokenReq} GetUploadTokenReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetUploadTokenReq.decode = function decode(reader, length, error) {
    GetUploadTokenReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetUploadTokenReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetUploadTokenReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetUploadTokenReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetUploadTokenReq} GetUploadTokenReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetUploadTokenReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetUploadTokenReq message.
     * @function verify
     * @memberof GetUploadTokenReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetUploadTokenReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        return null;
    };

    /**
     * Creates a GetUploadTokenReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetUploadTokenReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetUploadTokenReq} GetUploadTokenReq
     */
    GetUploadTokenReq.fromObject = function fromObject(object) {
        if (object instanceof $root.GetUploadTokenReq)
            return object;
        let message = new $root.GetUploadTokenReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GetUploadTokenReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        return message;
    };

    /**
     * Creates a plain object from a GetUploadTokenReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetUploadTokenReq
     * @static
     * @param {GetUploadTokenReq} message GetUploadTokenReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetUploadTokenReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.clientInfo = null;
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        return object;
    };

    /**
     * Converts this GetUploadTokenReq to JSON.
     * @function toJSON
     * @memberof GetUploadTokenReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetUploadTokenReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetUploadTokenReq
     * @function getTypeUrl
     * @memberof GetUploadTokenReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetUploadTokenReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetUploadTokenReq";
    };

    return GetUploadTokenReq;
})();

export const GetUploadTokenResp = $root.GetUploadTokenResp = (() => {

    /**
     * Properties of a GetUploadTokenResp.
     * @exports IGetUploadTokenResp
     * @interface IGetUploadTokenResp
     * @property {ICommonResult|null} [commonResult] GetUploadTokenResp commonResult
     * @property {string|null} [securityToken] GetUploadTokenResp securityToken
     * @property {string|null} [accessKeyId] GetUploadTokenResp accessKeyId
     * @property {string|null} [accessKeySecret] GetUploadTokenResp accessKeySecret
     * @property {number|Long|null} [expiration] GetUploadTokenResp expiration
     * @property {string|null} [ossEndpoint] GetUploadTokenResp ossEndpoint
     * @property {string|null} [ossBucket] GetUploadTokenResp ossBucket
     */

    /**
     * Constructs a new GetUploadTokenResp.
     * @exports GetUploadTokenResp
     * @classdesc Represents a GetUploadTokenResp.
     * @implements IGetUploadTokenResp
     * @constructor
     * @param {IGetUploadTokenResp=} [properties] Properties to set
     */
    function GetUploadTokenResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetUploadTokenResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GetUploadTokenResp
     * @instance
     */
    GetUploadTokenResp.prototype.commonResult = null;

    /**
     * GetUploadTokenResp securityToken.
     * @member {string} securityToken
     * @memberof GetUploadTokenResp
     * @instance
     */
    GetUploadTokenResp.prototype.securityToken = "";

    /**
     * GetUploadTokenResp accessKeyId.
     * @member {string} accessKeyId
     * @memberof GetUploadTokenResp
     * @instance
     */
    GetUploadTokenResp.prototype.accessKeyId = "";

    /**
     * GetUploadTokenResp accessKeySecret.
     * @member {string} accessKeySecret
     * @memberof GetUploadTokenResp
     * @instance
     */
    GetUploadTokenResp.prototype.accessKeySecret = "";

    /**
     * GetUploadTokenResp expiration.
     * @member {number|Long} expiration
     * @memberof GetUploadTokenResp
     * @instance
     */
    GetUploadTokenResp.prototype.expiration = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * GetUploadTokenResp ossEndpoint.
     * @member {string} ossEndpoint
     * @memberof GetUploadTokenResp
     * @instance
     */
    GetUploadTokenResp.prototype.ossEndpoint = "";

    /**
     * GetUploadTokenResp ossBucket.
     * @member {string} ossBucket
     * @memberof GetUploadTokenResp
     * @instance
     */
    GetUploadTokenResp.prototype.ossBucket = "";

    /**
     * Creates a new GetUploadTokenResp instance using the specified properties.
     * @function create
     * @memberof GetUploadTokenResp
     * @static
     * @param {IGetUploadTokenResp=} [properties] Properties to set
     * @returns {GetUploadTokenResp} GetUploadTokenResp instance
     */
    GetUploadTokenResp.create = function create(properties) {
        return new GetUploadTokenResp(properties);
    };

    /**
     * Encodes the specified GetUploadTokenResp message. Does not implicitly {@link GetUploadTokenResp.verify|verify} messages.
     * @function encode
     * @memberof GetUploadTokenResp
     * @static
     * @param {IGetUploadTokenResp} message GetUploadTokenResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetUploadTokenResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.securityToken != null && Object.hasOwnProperty.call(message, "securityToken"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.securityToken);
        if (message.accessKeyId != null && Object.hasOwnProperty.call(message, "accessKeyId"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.accessKeyId);
        if (message.accessKeySecret != null && Object.hasOwnProperty.call(message, "accessKeySecret"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.accessKeySecret);
        if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
            writer.uint32(/* id 5, wireType 0 =*/40).int64(message.expiration);
        if (message.ossEndpoint != null && Object.hasOwnProperty.call(message, "ossEndpoint"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.ossEndpoint);
        if (message.ossBucket != null && Object.hasOwnProperty.call(message, "ossBucket"))
            writer.uint32(/* id 7, wireType 2 =*/58).string(message.ossBucket);
        return writer;
    };

    /**
     * Encodes the specified GetUploadTokenResp message, length delimited. Does not implicitly {@link GetUploadTokenResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetUploadTokenResp
     * @static
     * @param {IGetUploadTokenResp} message GetUploadTokenResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetUploadTokenResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetUploadTokenResp message from the specified reader or buffer.
     * @function decode
     * @memberof GetUploadTokenResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetUploadTokenResp} GetUploadTokenResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetUploadTokenResp.decode = function decode(reader, length, error) {
    GetUploadTokenResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetUploadTokenResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.securityToken = reader.string();
                    break;
                }
            case 3: {
                    message.accessKeyId = reader.string();
                    break;
                }
            case 4: {
                    message.accessKeySecret = reader.string();
                    break;
                }
            case 5: {
                    message.expiration = reader.int64();
                    break;
                }
            case 6: {
                    message.ossEndpoint = reader.string();
                    break;
                }
            case 7: {
                    message.ossBucket = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetUploadTokenResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetUploadTokenResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetUploadTokenResp} GetUploadTokenResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetUploadTokenResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetUploadTokenResp message.
     * @function verify
     * @memberof GetUploadTokenResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetUploadTokenResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.securityToken != null && message.hasOwnProperty("securityToken"))
            if (!$util.isString(message.securityToken))
                return "securityToken: string expected";
        if (message.accessKeyId != null && message.hasOwnProperty("accessKeyId"))
            if (!$util.isString(message.accessKeyId))
                return "accessKeyId: string expected";
        if (message.accessKeySecret != null && message.hasOwnProperty("accessKeySecret"))
            if (!$util.isString(message.accessKeySecret))
                return "accessKeySecret: string expected";
        if (message.expiration != null && message.hasOwnProperty("expiration"))
            if (!$util.isInteger(message.expiration) && !(message.expiration && $util.isInteger(message.expiration.low) && $util.isInteger(message.expiration.high)))
                return "expiration: integer|Long expected";
        if (message.ossEndpoint != null && message.hasOwnProperty("ossEndpoint"))
            if (!$util.isString(message.ossEndpoint))
                return "ossEndpoint: string expected";
        if (message.ossBucket != null && message.hasOwnProperty("ossBucket"))
            if (!$util.isString(message.ossBucket))
                return "ossBucket: string expected";
        return null;
    };

    /**
     * Creates a GetUploadTokenResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetUploadTokenResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetUploadTokenResp} GetUploadTokenResp
     */
    GetUploadTokenResp.fromObject = function fromObject(object) {
        if (object instanceof $root.GetUploadTokenResp)
            return object;
        let message = new $root.GetUploadTokenResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GetUploadTokenResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.securityToken != null)
            message.securityToken = String(object.securityToken);
        if (object.accessKeyId != null)
            message.accessKeyId = String(object.accessKeyId);
        if (object.accessKeySecret != null)
            message.accessKeySecret = String(object.accessKeySecret);
        if (object.expiration != null)
            if ($util.Long)
                (message.expiration = $util.Long.fromValue(object.expiration)).unsigned = false;
            else if (typeof object.expiration === "string")
                message.expiration = parseInt(object.expiration, 10);
            else if (typeof object.expiration === "number")
                message.expiration = object.expiration;
            else if (typeof object.expiration === "object")
                message.expiration = new $util.LongBits(object.expiration.low >>> 0, object.expiration.high >>> 0).toNumber();
        if (object.ossEndpoint != null)
            message.ossEndpoint = String(object.ossEndpoint);
        if (object.ossBucket != null)
            message.ossBucket = String(object.ossBucket);
        return message;
    };

    /**
     * Creates a plain object from a GetUploadTokenResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetUploadTokenResp
     * @static
     * @param {GetUploadTokenResp} message GetUploadTokenResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetUploadTokenResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.securityToken = "";
            object.accessKeyId = "";
            object.accessKeySecret = "";
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.expiration = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.expiration = options.longs === String ? "0" : 0;
            object.ossEndpoint = "";
            object.ossBucket = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.securityToken != null && message.hasOwnProperty("securityToken"))
            object.securityToken = message.securityToken;
        if (message.accessKeyId != null && message.hasOwnProperty("accessKeyId"))
            object.accessKeyId = message.accessKeyId;
        if (message.accessKeySecret != null && message.hasOwnProperty("accessKeySecret"))
            object.accessKeySecret = message.accessKeySecret;
        if (message.expiration != null && message.hasOwnProperty("expiration"))
            if (typeof message.expiration === "number")
                object.expiration = options.longs === String ? String(message.expiration) : message.expiration;
            else
                object.expiration = options.longs === String ? $util.Long.prototype.toString.call(message.expiration) : options.longs === Number ? new $util.LongBits(message.expiration.low >>> 0, message.expiration.high >>> 0).toNumber() : message.expiration;
        if (message.ossEndpoint != null && message.hasOwnProperty("ossEndpoint"))
            object.ossEndpoint = message.ossEndpoint;
        if (message.ossBucket != null && message.hasOwnProperty("ossBucket"))
            object.ossBucket = message.ossBucket;
        return object;
    };

    /**
     * Converts this GetUploadTokenResp to JSON.
     * @function toJSON
     * @memberof GetUploadTokenResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetUploadTokenResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetUploadTokenResp
     * @function getTypeUrl
     * @memberof GetUploadTokenResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetUploadTokenResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetUploadTokenResp";
    };

    return GetUploadTokenResp;
})();

export const GetUploadUrlReq = $root.GetUploadUrlReq = (() => {

    /**
     * Properties of a GetUploadUrlReq.
     * @exports IGetUploadUrlReq
     * @interface IGetUploadUrlReq
     * @property {IClientInfo|null} [clientInfo] GetUploadUrlReq clientInfo
     * @property {AttachWorkSpaceType|null} [attachWorkspaceType] GetUploadUrlReq attachWorkspaceType
     * @property {AttachType|null} [attachType] GetUploadUrlReq attachType
     * @property {string|null} [suffix] GetUploadUrlReq suffix
     * @property {number|Long|null} [fileSize] GetUploadUrlReq fileSize
     */

    /**
     * Constructs a new GetUploadUrlReq.
     * @exports GetUploadUrlReq
     * @classdesc Represents a GetUploadUrlReq.
     * @implements IGetUploadUrlReq
     * @constructor
     * @param {IGetUploadUrlReq=} [properties] Properties to set
     */
    function GetUploadUrlReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetUploadUrlReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GetUploadUrlReq
     * @instance
     */
    GetUploadUrlReq.prototype.clientInfo = null;

    /**
     * GetUploadUrlReq attachWorkspaceType.
     * @member {AttachWorkSpaceType} attachWorkspaceType
     * @memberof GetUploadUrlReq
     * @instance
     */
    GetUploadUrlReq.prototype.attachWorkspaceType = 0;

    /**
     * GetUploadUrlReq attachType.
     * @member {AttachType} attachType
     * @memberof GetUploadUrlReq
     * @instance
     */
    GetUploadUrlReq.prototype.attachType = 0;

    /**
     * GetUploadUrlReq suffix.
     * @member {string} suffix
     * @memberof GetUploadUrlReq
     * @instance
     */
    GetUploadUrlReq.prototype.suffix = "";

    /**
     * GetUploadUrlReq fileSize.
     * @member {number|Long} fileSize
     * @memberof GetUploadUrlReq
     * @instance
     */
    GetUploadUrlReq.prototype.fileSize = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * Creates a new GetUploadUrlReq instance using the specified properties.
     * @function create
     * @memberof GetUploadUrlReq
     * @static
     * @param {IGetUploadUrlReq=} [properties] Properties to set
     * @returns {GetUploadUrlReq} GetUploadUrlReq instance
     */
    GetUploadUrlReq.create = function create(properties) {
        return new GetUploadUrlReq(properties);
    };

    /**
     * Encodes the specified GetUploadUrlReq message. Does not implicitly {@link GetUploadUrlReq.verify|verify} messages.
     * @function encode
     * @memberof GetUploadUrlReq
     * @static
     * @param {IGetUploadUrlReq} message GetUploadUrlReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetUploadUrlReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.attachWorkspaceType != null && Object.hasOwnProperty.call(message, "attachWorkspaceType"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.attachWorkspaceType);
        if (message.attachType != null && Object.hasOwnProperty.call(message, "attachType"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.attachType);
        if (message.suffix != null && Object.hasOwnProperty.call(message, "suffix"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.suffix);
        if (message.fileSize != null && Object.hasOwnProperty.call(message, "fileSize"))
            writer.uint32(/* id 5, wireType 0 =*/40).int64(message.fileSize);
        return writer;
    };

    /**
     * Encodes the specified GetUploadUrlReq message, length delimited. Does not implicitly {@link GetUploadUrlReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetUploadUrlReq
     * @static
     * @param {IGetUploadUrlReq} message GetUploadUrlReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetUploadUrlReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetUploadUrlReq message from the specified reader or buffer.
     * @function decode
     * @memberof GetUploadUrlReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetUploadUrlReq} GetUploadUrlReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetUploadUrlReq.decode = function decode(reader, length, error) {
    GetUploadUrlReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetUploadUrlReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.attachWorkspaceType = reader.int32();
                    break;
                }
            case 3: {
                    message.attachType = reader.int32();
                    break;
                }
            case 4: {
                    message.suffix = reader.string();
                    break;
                }
            case 5: {
                    message.fileSize = reader.int64();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetUploadUrlReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetUploadUrlReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetUploadUrlReq} GetUploadUrlReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetUploadUrlReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetUploadUrlReq message.
     * @function verify
     * @memberof GetUploadUrlReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetUploadUrlReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.attachWorkspaceType != null && message.hasOwnProperty("attachWorkspaceType"))
            switch (message.attachWorkspaceType) {
            default:
                return "attachWorkspaceType: enum value expected";
            case 0:
            case 1:
                break;
            }
        if (message.attachType != null && message.hasOwnProperty("attachType"))
            switch (message.attachType) {
            default:
                return "attachType: enum value expected";
            case 0:
            case 1:
            case 2:
            case 3:
            case 4:
            case 5:
                break;
            }
        if (message.suffix != null && message.hasOwnProperty("suffix"))
            if (!$util.isString(message.suffix))
                return "suffix: string expected";
        if (message.fileSize != null && message.hasOwnProperty("fileSize"))
            if (!$util.isInteger(message.fileSize) && !(message.fileSize && $util.isInteger(message.fileSize.low) && $util.isInteger(message.fileSize.high)))
                return "fileSize: integer|Long expected";
        return null;
    };

    /**
     * Creates a GetUploadUrlReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetUploadUrlReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetUploadUrlReq} GetUploadUrlReq
     */
    GetUploadUrlReq.fromObject = function fromObject(object) {
        if (object instanceof $root.GetUploadUrlReq)
            return object;
        let message = new $root.GetUploadUrlReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GetUploadUrlReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        switch (object.attachWorkspaceType) {
        default:
            if (typeof object.attachWorkspaceType === "number") {
                message.attachWorkspaceType = object.attachWorkspaceType;
                break;
            }
            break;
        case "COMMON":
        case 0:
            message.attachWorkspaceType = 0;
            break;
        case "CHAT":
        case 1:
            message.attachWorkspaceType = 1;
            break;
        }
        switch (object.attachType) {
        default:
            if (typeof object.attachType === "number") {
                message.attachType = object.attachType;
                break;
            }
            break;
        case "PIC":
        case 0:
            message.attachType = 0;
            break;
        case "AUDIO":
        case 1:
            message.attachType = 1;
            break;
        case "VIDEO":
        case 2:
            message.attachType = 2;
            break;
        case "FILE":
        case 3:
            message.attachType = 3;
            break;
        case "LOG":
        case 4:
            message.attachType = 4;
            break;
        case "EMOTICON":
        case 5:
            message.attachType = 5;
            break;
        }
        if (object.suffix != null)
            message.suffix = String(object.suffix);
        if (object.fileSize != null)
            if ($util.Long)
                (message.fileSize = $util.Long.fromValue(object.fileSize)).unsigned = false;
            else if (typeof object.fileSize === "string")
                message.fileSize = parseInt(object.fileSize, 10);
            else if (typeof object.fileSize === "number")
                message.fileSize = object.fileSize;
            else if (typeof object.fileSize === "object")
                message.fileSize = new $util.LongBits(object.fileSize.low >>> 0, object.fileSize.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a GetUploadUrlReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetUploadUrlReq
     * @static
     * @param {GetUploadUrlReq} message GetUploadUrlReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetUploadUrlReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.attachWorkspaceType = options.enums === String ? "COMMON" : 0;
            object.attachType = options.enums === String ? "PIC" : 0;
            object.suffix = "";
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.fileSize = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.fileSize = options.longs === String ? "0" : 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.attachWorkspaceType != null && message.hasOwnProperty("attachWorkspaceType"))
            object.attachWorkspaceType = options.enums === String ? $root.AttachWorkSpaceType[message.attachWorkspaceType] === undefined ? message.attachWorkspaceType : $root.AttachWorkSpaceType[message.attachWorkspaceType] : message.attachWorkspaceType;
        if (message.attachType != null && message.hasOwnProperty("attachType"))
            object.attachType = options.enums === String ? $root.AttachType[message.attachType] === undefined ? message.attachType : $root.AttachType[message.attachType] : message.attachType;
        if (message.suffix != null && message.hasOwnProperty("suffix"))
            object.suffix = message.suffix;
        if (message.fileSize != null && message.hasOwnProperty("fileSize"))
            if (typeof message.fileSize === "number")
                object.fileSize = options.longs === String ? String(message.fileSize) : message.fileSize;
            else
                object.fileSize = options.longs === String ? $util.Long.prototype.toString.call(message.fileSize) : options.longs === Number ? new $util.LongBits(message.fileSize.low >>> 0, message.fileSize.high >>> 0).toNumber() : message.fileSize;
        return object;
    };

    /**
     * Converts this GetUploadUrlReq to JSON.
     * @function toJSON
     * @memberof GetUploadUrlReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetUploadUrlReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetUploadUrlReq
     * @function getTypeUrl
     * @memberof GetUploadUrlReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetUploadUrlReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetUploadUrlReq";
    };

    return GetUploadUrlReq;
})();

export const GetUploadUrlResp = $root.GetUploadUrlResp = (() => {

    /**
     * Properties of a GetUploadUrlResp.
     * @exports IGetUploadUrlResp
     * @interface IGetUploadUrlResp
     * @property {ICommonResult|null} [commonResult] GetUploadUrlResp commonResult
     * @property {string|null} [fileId] GetUploadUrlResp fileId
     * @property {string|null} [url] GetUploadUrlResp url
     */

    /**
     * Constructs a new GetUploadUrlResp.
     * @exports GetUploadUrlResp
     * @classdesc Represents a GetUploadUrlResp.
     * @implements IGetUploadUrlResp
     * @constructor
     * @param {IGetUploadUrlResp=} [properties] Properties to set
     */
    function GetUploadUrlResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetUploadUrlResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GetUploadUrlResp
     * @instance
     */
    GetUploadUrlResp.prototype.commonResult = null;

    /**
     * GetUploadUrlResp fileId.
     * @member {string} fileId
     * @memberof GetUploadUrlResp
     * @instance
     */
    GetUploadUrlResp.prototype.fileId = "";

    /**
     * GetUploadUrlResp url.
     * @member {string} url
     * @memberof GetUploadUrlResp
     * @instance
     */
    GetUploadUrlResp.prototype.url = "";

    /**
     * Creates a new GetUploadUrlResp instance using the specified properties.
     * @function create
     * @memberof GetUploadUrlResp
     * @static
     * @param {IGetUploadUrlResp=} [properties] Properties to set
     * @returns {GetUploadUrlResp} GetUploadUrlResp instance
     */
    GetUploadUrlResp.create = function create(properties) {
        return new GetUploadUrlResp(properties);
    };

    /**
     * Encodes the specified GetUploadUrlResp message. Does not implicitly {@link GetUploadUrlResp.verify|verify} messages.
     * @function encode
     * @memberof GetUploadUrlResp
     * @static
     * @param {IGetUploadUrlResp} message GetUploadUrlResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetUploadUrlResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.fileId != null && Object.hasOwnProperty.call(message, "fileId"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.fileId);
        if (message.url != null && Object.hasOwnProperty.call(message, "url"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.url);
        return writer;
    };

    /**
     * Encodes the specified GetUploadUrlResp message, length delimited. Does not implicitly {@link GetUploadUrlResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetUploadUrlResp
     * @static
     * @param {IGetUploadUrlResp} message GetUploadUrlResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetUploadUrlResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetUploadUrlResp message from the specified reader or buffer.
     * @function decode
     * @memberof GetUploadUrlResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetUploadUrlResp} GetUploadUrlResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetUploadUrlResp.decode = function decode(reader, length, error) {
    GetUploadUrlResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetUploadUrlResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.fileId = reader.string();
                    break;
                }
            case 3: {
                    message.url = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetUploadUrlResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetUploadUrlResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetUploadUrlResp} GetUploadUrlResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetUploadUrlResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetUploadUrlResp message.
     * @function verify
     * @memberof GetUploadUrlResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetUploadUrlResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.fileId != null && message.hasOwnProperty("fileId"))
            if (!$util.isString(message.fileId))
                return "fileId: string expected";
        if (message.url != null && message.hasOwnProperty("url"))
            if (!$util.isString(message.url))
                return "url: string expected";
        return null;
    };

    /**
     * Creates a GetUploadUrlResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetUploadUrlResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetUploadUrlResp} GetUploadUrlResp
     */
    GetUploadUrlResp.fromObject = function fromObject(object) {
        if (object instanceof $root.GetUploadUrlResp)
            return object;
        let message = new $root.GetUploadUrlResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GetUploadUrlResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.fileId != null)
            message.fileId = String(object.fileId);
        if (object.url != null)
            message.url = String(object.url);
        return message;
    };

    /**
     * Creates a plain object from a GetUploadUrlResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetUploadUrlResp
     * @static
     * @param {GetUploadUrlResp} message GetUploadUrlResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetUploadUrlResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.fileId = "";
            object.url = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.fileId != null && message.hasOwnProperty("fileId"))
            object.fileId = message.fileId;
        if (message.url != null && message.hasOwnProperty("url"))
            object.url = message.url;
        return object;
    };

    /**
     * Converts this GetUploadUrlResp to JSON.
     * @function toJSON
     * @memberof GetUploadUrlResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetUploadUrlResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetUploadUrlResp
     * @function getTypeUrl
     * @memberof GetUploadUrlResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetUploadUrlResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetUploadUrlResp";
    };

    return GetUploadUrlResp;
})();

export const GetInviteLinkReq = $root.GetInviteLinkReq = (() => {

    /**
     * Properties of a GetInviteLinkReq.
     * @exports IGetInviteLinkReq
     * @interface IGetInviteLinkReq
     * @property {IClientInfo|null} [clientInfo] GetInviteLinkReq clientInfo
     * @property {InviteLinkType|null} [type] GetInviteLinkReq type
     * @property {number|Long|null} [targetId] GetInviteLinkReq targetId
     */

    /**
     * Constructs a new GetInviteLinkReq.
     * @exports GetInviteLinkReq
     * @classdesc Represents a GetInviteLinkReq.
     * @implements IGetInviteLinkReq
     * @constructor
     * @param {IGetInviteLinkReq=} [properties] Properties to set
     */
    function GetInviteLinkReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetInviteLinkReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GetInviteLinkReq
     * @instance
     */
    GetInviteLinkReq.prototype.clientInfo = null;

    /**
     * GetInviteLinkReq type.
     * @member {InviteLinkType} type
     * @memberof GetInviteLinkReq
     * @instance
     */
    GetInviteLinkReq.prototype.type = 0;

    /**
     * GetInviteLinkReq targetId.
     * @member {number|Long} targetId
     * @memberof GetInviteLinkReq
     * @instance
     */
    GetInviteLinkReq.prototype.targetId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * Creates a new GetInviteLinkReq instance using the specified properties.
     * @function create
     * @memberof GetInviteLinkReq
     * @static
     * @param {IGetInviteLinkReq=} [properties] Properties to set
     * @returns {GetInviteLinkReq} GetInviteLinkReq instance
     */
    GetInviteLinkReq.create = function create(properties) {
        return new GetInviteLinkReq(properties);
    };

    /**
     * Encodes the specified GetInviteLinkReq message. Does not implicitly {@link GetInviteLinkReq.verify|verify} messages.
     * @function encode
     * @memberof GetInviteLinkReq
     * @static
     * @param {IGetInviteLinkReq} message GetInviteLinkReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetInviteLinkReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
        if (message.targetId != null && Object.hasOwnProperty.call(message, "targetId"))
            writer.uint32(/* id 3, wireType 0 =*/24).int64(message.targetId);
        return writer;
    };

    /**
     * Encodes the specified GetInviteLinkReq message, length delimited. Does not implicitly {@link GetInviteLinkReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetInviteLinkReq
     * @static
     * @param {IGetInviteLinkReq} message GetInviteLinkReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetInviteLinkReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetInviteLinkReq message from the specified reader or buffer.
     * @function decode
     * @memberof GetInviteLinkReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetInviteLinkReq} GetInviteLinkReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetInviteLinkReq.decode = function decode(reader, length, error) {
    GetInviteLinkReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetInviteLinkReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.type = reader.int32();
                    break;
                }
            case 3: {
                    message.targetId = reader.int64();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetInviteLinkReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetInviteLinkReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetInviteLinkReq} GetInviteLinkReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetInviteLinkReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetInviteLinkReq message.
     * @function verify
     * @memberof GetInviteLinkReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetInviteLinkReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.type != null && message.hasOwnProperty("type"))
            switch (message.type) {
            default:
                return "type: enum value expected";
            case 0:
            case 1:
                break;
            }
        if (message.targetId != null && message.hasOwnProperty("targetId"))
            if (!$util.isInteger(message.targetId) && !(message.targetId && $util.isInteger(message.targetId.low) && $util.isInteger(message.targetId.high)))
                return "targetId: integer|Long expected";
        return null;
    };

    /**
     * Creates a GetInviteLinkReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetInviteLinkReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetInviteLinkReq} GetInviteLinkReq
     */
    GetInviteLinkReq.fromObject = function fromObject(object) {
        if (object instanceof $root.GetInviteLinkReq)
            return object;
        let message = new $root.GetInviteLinkReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GetInviteLinkReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        switch (object.type) {
        default:
            if (typeof object.type === "number") {
                message.type = object.type;
                break;
            }
            break;
        case "LINK_USER":
        case 0:
            message.type = 0;
            break;
        case "LINK_GROUP":
        case 1:
            message.type = 1;
            break;
        }
        if (object.targetId != null)
            if ($util.Long)
                (message.targetId = $util.Long.fromValue(object.targetId)).unsigned = false;
            else if (typeof object.targetId === "string")
                message.targetId = parseInt(object.targetId, 10);
            else if (typeof object.targetId === "number")
                message.targetId = object.targetId;
            else if (typeof object.targetId === "object")
                message.targetId = new $util.LongBits(object.targetId.low >>> 0, object.targetId.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a GetInviteLinkReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetInviteLinkReq
     * @static
     * @param {GetInviteLinkReq} message GetInviteLinkReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetInviteLinkReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.type = options.enums === String ? "LINK_USER" : 0;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.targetId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.targetId = options.longs === String ? "0" : 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.type != null && message.hasOwnProperty("type"))
            object.type = options.enums === String ? $root.InviteLinkType[message.type] === undefined ? message.type : $root.InviteLinkType[message.type] : message.type;
        if (message.targetId != null && message.hasOwnProperty("targetId"))
            if (typeof message.targetId === "number")
                object.targetId = options.longs === String ? String(message.targetId) : message.targetId;
            else
                object.targetId = options.longs === String ? $util.Long.prototype.toString.call(message.targetId) : options.longs === Number ? new $util.LongBits(message.targetId.low >>> 0, message.targetId.high >>> 0).toNumber() : message.targetId;
        return object;
    };

    /**
     * Converts this GetInviteLinkReq to JSON.
     * @function toJSON
     * @memberof GetInviteLinkReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetInviteLinkReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetInviteLinkReq
     * @function getTypeUrl
     * @memberof GetInviteLinkReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetInviteLinkReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetInviteLinkReq";
    };

    return GetInviteLinkReq;
})();

export const GetInviteLinkResp = $root.GetInviteLinkResp = (() => {

    /**
     * Properties of a GetInviteLinkResp.
     * @exports IGetInviteLinkResp
     * @interface IGetInviteLinkResp
     * @property {ICommonResult|null} [commonResult] GetInviteLinkResp commonResult
     * @property {string|null} [inviteLink] GetInviteLinkResp inviteLink
     */

    /**
     * Constructs a new GetInviteLinkResp.
     * @exports GetInviteLinkResp
     * @classdesc Represents a GetInviteLinkResp.
     * @implements IGetInviteLinkResp
     * @constructor
     * @param {IGetInviteLinkResp=} [properties] Properties to set
     */
    function GetInviteLinkResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetInviteLinkResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GetInviteLinkResp
     * @instance
     */
    GetInviteLinkResp.prototype.commonResult = null;

    /**
     * GetInviteLinkResp inviteLink.
     * @member {string} inviteLink
     * @memberof GetInviteLinkResp
     * @instance
     */
    GetInviteLinkResp.prototype.inviteLink = "";

    /**
     * Creates a new GetInviteLinkResp instance using the specified properties.
     * @function create
     * @memberof GetInviteLinkResp
     * @static
     * @param {IGetInviteLinkResp=} [properties] Properties to set
     * @returns {GetInviteLinkResp} GetInviteLinkResp instance
     */
    GetInviteLinkResp.create = function create(properties) {
        return new GetInviteLinkResp(properties);
    };

    /**
     * Encodes the specified GetInviteLinkResp message. Does not implicitly {@link GetInviteLinkResp.verify|verify} messages.
     * @function encode
     * @memberof GetInviteLinkResp
     * @static
     * @param {IGetInviteLinkResp} message GetInviteLinkResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetInviteLinkResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.inviteLink != null && Object.hasOwnProperty.call(message, "inviteLink"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.inviteLink);
        return writer;
    };

    /**
     * Encodes the specified GetInviteLinkResp message, length delimited. Does not implicitly {@link GetInviteLinkResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetInviteLinkResp
     * @static
     * @param {IGetInviteLinkResp} message GetInviteLinkResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetInviteLinkResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetInviteLinkResp message from the specified reader or buffer.
     * @function decode
     * @memberof GetInviteLinkResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetInviteLinkResp} GetInviteLinkResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetInviteLinkResp.decode = function decode(reader, length, error) {
    GetInviteLinkResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetInviteLinkResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.inviteLink = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetInviteLinkResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetInviteLinkResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetInviteLinkResp} GetInviteLinkResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetInviteLinkResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetInviteLinkResp message.
     * @function verify
     * @memberof GetInviteLinkResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetInviteLinkResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.inviteLink != null && message.hasOwnProperty("inviteLink"))
            if (!$util.isString(message.inviteLink))
                return "inviteLink: string expected";
        return null;
    };

    /**
     * Creates a GetInviteLinkResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetInviteLinkResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetInviteLinkResp} GetInviteLinkResp
     */
    GetInviteLinkResp.fromObject = function fromObject(object) {
        if (object instanceof $root.GetInviteLinkResp)
            return object;
        let message = new $root.GetInviteLinkResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GetInviteLinkResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.inviteLink != null)
            message.inviteLink = String(object.inviteLink);
        return message;
    };

    /**
     * Creates a plain object from a GetInviteLinkResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetInviteLinkResp
     * @static
     * @param {GetInviteLinkResp} message GetInviteLinkResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetInviteLinkResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.inviteLink = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.inviteLink != null && message.hasOwnProperty("inviteLink"))
            object.inviteLink = message.inviteLink;
        return object;
    };

    /**
     * Converts this GetInviteLinkResp to JSON.
     * @function toJSON
     * @memberof GetInviteLinkResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetInviteLinkResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetInviteLinkResp
     * @function getTypeUrl
     * @memberof GetInviteLinkResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetInviteLinkResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetInviteLinkResp";
    };

    return GetInviteLinkResp;
})();

export const UpdateInviteLinkReq = $root.UpdateInviteLinkReq = (() => {

    /**
     * Properties of an UpdateInviteLinkReq.
     * @exports IUpdateInviteLinkReq
     * @interface IUpdateInviteLinkReq
     * @property {IClientInfo|null} [clientInfo] UpdateInviteLinkReq clientInfo
     * @property {InviteLinkType|null} [type] UpdateInviteLinkReq type
     * @property {number|Long|null} [targetId] UpdateInviteLinkReq targetId
     */

    /**
     * Constructs a new UpdateInviteLinkReq.
     * @exports UpdateInviteLinkReq
     * @classdesc Represents an UpdateInviteLinkReq.
     * @implements IUpdateInviteLinkReq
     * @constructor
     * @param {IUpdateInviteLinkReq=} [properties] Properties to set
     */
    function UpdateInviteLinkReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * UpdateInviteLinkReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof UpdateInviteLinkReq
     * @instance
     */
    UpdateInviteLinkReq.prototype.clientInfo = null;

    /**
     * UpdateInviteLinkReq type.
     * @member {InviteLinkType} type
     * @memberof UpdateInviteLinkReq
     * @instance
     */
    UpdateInviteLinkReq.prototype.type = 0;

    /**
     * UpdateInviteLinkReq targetId.
     * @member {number|Long} targetId
     * @memberof UpdateInviteLinkReq
     * @instance
     */
    UpdateInviteLinkReq.prototype.targetId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * Creates a new UpdateInviteLinkReq instance using the specified properties.
     * @function create
     * @memberof UpdateInviteLinkReq
     * @static
     * @param {IUpdateInviteLinkReq=} [properties] Properties to set
     * @returns {UpdateInviteLinkReq} UpdateInviteLinkReq instance
     */
    UpdateInviteLinkReq.create = function create(properties) {
        return new UpdateInviteLinkReq(properties);
    };

    /**
     * Encodes the specified UpdateInviteLinkReq message. Does not implicitly {@link UpdateInviteLinkReq.verify|verify} messages.
     * @function encode
     * @memberof UpdateInviteLinkReq
     * @static
     * @param {IUpdateInviteLinkReq} message UpdateInviteLinkReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateInviteLinkReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
        if (message.targetId != null && Object.hasOwnProperty.call(message, "targetId"))
            writer.uint32(/* id 3, wireType 0 =*/24).int64(message.targetId);
        return writer;
    };

    /**
     * Encodes the specified UpdateInviteLinkReq message, length delimited. Does not implicitly {@link UpdateInviteLinkReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof UpdateInviteLinkReq
     * @static
     * @param {IUpdateInviteLinkReq} message UpdateInviteLinkReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateInviteLinkReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an UpdateInviteLinkReq message from the specified reader or buffer.
     * @function decode
     * @memberof UpdateInviteLinkReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {UpdateInviteLinkReq} UpdateInviteLinkReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateInviteLinkReq.decode = function decode(reader, length, error) {
    UpdateInviteLinkReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.UpdateInviteLinkReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.type = reader.int32();
                    break;
                }
            case 3: {
                    message.targetId = reader.int64();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an UpdateInviteLinkReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof UpdateInviteLinkReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {UpdateInviteLinkReq} UpdateInviteLinkReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateInviteLinkReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an UpdateInviteLinkReq message.
     * @function verify
     * @memberof UpdateInviteLinkReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    UpdateInviteLinkReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.type != null && message.hasOwnProperty("type"))
            switch (message.type) {
            default:
                return "type: enum value expected";
            case 0:
            case 1:
                break;
            }
        if (message.targetId != null && message.hasOwnProperty("targetId"))
            if (!$util.isInteger(message.targetId) && !(message.targetId && $util.isInteger(message.targetId.low) && $util.isInteger(message.targetId.high)))
                return "targetId: integer|Long expected";
        return null;
    };

    /**
     * Creates an UpdateInviteLinkReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof UpdateInviteLinkReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {UpdateInviteLinkReq} UpdateInviteLinkReq
     */
    UpdateInviteLinkReq.fromObject = function fromObject(object) {
        if (object instanceof $root.UpdateInviteLinkReq)
            return object;
        let message = new $root.UpdateInviteLinkReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".UpdateInviteLinkReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        switch (object.type) {
        default:
            if (typeof object.type === "number") {
                message.type = object.type;
                break;
            }
            break;
        case "LINK_USER":
        case 0:
            message.type = 0;
            break;
        case "LINK_GROUP":
        case 1:
            message.type = 1;
            break;
        }
        if (object.targetId != null)
            if ($util.Long)
                (message.targetId = $util.Long.fromValue(object.targetId)).unsigned = false;
            else if (typeof object.targetId === "string")
                message.targetId = parseInt(object.targetId, 10);
            else if (typeof object.targetId === "number")
                message.targetId = object.targetId;
            else if (typeof object.targetId === "object")
                message.targetId = new $util.LongBits(object.targetId.low >>> 0, object.targetId.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from an UpdateInviteLinkReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof UpdateInviteLinkReq
     * @static
     * @param {UpdateInviteLinkReq} message UpdateInviteLinkReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    UpdateInviteLinkReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.type = options.enums === String ? "LINK_USER" : 0;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.targetId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.targetId = options.longs === String ? "0" : 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.type != null && message.hasOwnProperty("type"))
            object.type = options.enums === String ? $root.InviteLinkType[message.type] === undefined ? message.type : $root.InviteLinkType[message.type] : message.type;
        if (message.targetId != null && message.hasOwnProperty("targetId"))
            if (typeof message.targetId === "number")
                object.targetId = options.longs === String ? String(message.targetId) : message.targetId;
            else
                object.targetId = options.longs === String ? $util.Long.prototype.toString.call(message.targetId) : options.longs === Number ? new $util.LongBits(message.targetId.low >>> 0, message.targetId.high >>> 0).toNumber() : message.targetId;
        return object;
    };

    /**
     * Converts this UpdateInviteLinkReq to JSON.
     * @function toJSON
     * @memberof UpdateInviteLinkReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    UpdateInviteLinkReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for UpdateInviteLinkReq
     * @function getTypeUrl
     * @memberof UpdateInviteLinkReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    UpdateInviteLinkReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/UpdateInviteLinkReq";
    };

    return UpdateInviteLinkReq;
})();

export const UpdateInviteLinkResp = $root.UpdateInviteLinkResp = (() => {

    /**
     * Properties of an UpdateInviteLinkResp.
     * @exports IUpdateInviteLinkResp
     * @interface IUpdateInviteLinkResp
     * @property {ICommonResult|null} [commonResult] UpdateInviteLinkResp commonResult
     * @property {string|null} [inviteLink] UpdateInviteLinkResp inviteLink
     */

    /**
     * Constructs a new UpdateInviteLinkResp.
     * @exports UpdateInviteLinkResp
     * @classdesc Represents an UpdateInviteLinkResp.
     * @implements IUpdateInviteLinkResp
     * @constructor
     * @param {IUpdateInviteLinkResp=} [properties] Properties to set
     */
    function UpdateInviteLinkResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * UpdateInviteLinkResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof UpdateInviteLinkResp
     * @instance
     */
    UpdateInviteLinkResp.prototype.commonResult = null;

    /**
     * UpdateInviteLinkResp inviteLink.
     * @member {string} inviteLink
     * @memberof UpdateInviteLinkResp
     * @instance
     */
    UpdateInviteLinkResp.prototype.inviteLink = "";

    /**
     * Creates a new UpdateInviteLinkResp instance using the specified properties.
     * @function create
     * @memberof UpdateInviteLinkResp
     * @static
     * @param {IUpdateInviteLinkResp=} [properties] Properties to set
     * @returns {UpdateInviteLinkResp} UpdateInviteLinkResp instance
     */
    UpdateInviteLinkResp.create = function create(properties) {
        return new UpdateInviteLinkResp(properties);
    };

    /**
     * Encodes the specified UpdateInviteLinkResp message. Does not implicitly {@link UpdateInviteLinkResp.verify|verify} messages.
     * @function encode
     * @memberof UpdateInviteLinkResp
     * @static
     * @param {IUpdateInviteLinkResp} message UpdateInviteLinkResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateInviteLinkResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.inviteLink != null && Object.hasOwnProperty.call(message, "inviteLink"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.inviteLink);
        return writer;
    };

    /**
     * Encodes the specified UpdateInviteLinkResp message, length delimited. Does not implicitly {@link UpdateInviteLinkResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof UpdateInviteLinkResp
     * @static
     * @param {IUpdateInviteLinkResp} message UpdateInviteLinkResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateInviteLinkResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an UpdateInviteLinkResp message from the specified reader or buffer.
     * @function decode
     * @memberof UpdateInviteLinkResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {UpdateInviteLinkResp} UpdateInviteLinkResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateInviteLinkResp.decode = function decode(reader, length, error) {
    UpdateInviteLinkResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.UpdateInviteLinkResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.inviteLink = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an UpdateInviteLinkResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof UpdateInviteLinkResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {UpdateInviteLinkResp} UpdateInviteLinkResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateInviteLinkResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an UpdateInviteLinkResp message.
     * @function verify
     * @memberof UpdateInviteLinkResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    UpdateInviteLinkResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.inviteLink != null && message.hasOwnProperty("inviteLink"))
            if (!$util.isString(message.inviteLink))
                return "inviteLink: string expected";
        return null;
    };

    /**
     * Creates an UpdateInviteLinkResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof UpdateInviteLinkResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {UpdateInviteLinkResp} UpdateInviteLinkResp
     */
    UpdateInviteLinkResp.fromObject = function fromObject(object) {
        if (object instanceof $root.UpdateInviteLinkResp)
            return object;
        let message = new $root.UpdateInviteLinkResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".UpdateInviteLinkResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.inviteLink != null)
            message.inviteLink = String(object.inviteLink);
        return message;
    };

    /**
     * Creates a plain object from an UpdateInviteLinkResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof UpdateInviteLinkResp
     * @static
     * @param {UpdateInviteLinkResp} message UpdateInviteLinkResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    UpdateInviteLinkResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.inviteLink = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.inviteLink != null && message.hasOwnProperty("inviteLink"))
            object.inviteLink = message.inviteLink;
        return object;
    };

    /**
     * Converts this UpdateInviteLinkResp to JSON.
     * @function toJSON
     * @memberof UpdateInviteLinkResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    UpdateInviteLinkResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for UpdateInviteLinkResp
     * @function getTypeUrl
     * @memberof UpdateInviteLinkResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    UpdateInviteLinkResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/UpdateInviteLinkResp";
    };

    return UpdateInviteLinkResp;
})();

export const DisableAccountReq = $root.DisableAccountReq = (() => {

    /**
     * Properties of a DisableAccountReq.
     * @exports IDisableAccountReq
     * @interface IDisableAccountReq
     * @property {IClientInfo|null} [clientInfo] DisableAccountReq clientInfo
     */

    /**
     * Constructs a new DisableAccountReq.
     * @exports DisableAccountReq
     * @classdesc Represents a DisableAccountReq.
     * @implements IDisableAccountReq
     * @constructor
     * @param {IDisableAccountReq=} [properties] Properties to set
     */
    function DisableAccountReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * DisableAccountReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof DisableAccountReq
     * @instance
     */
    DisableAccountReq.prototype.clientInfo = null;

    /**
     * Creates a new DisableAccountReq instance using the specified properties.
     * @function create
     * @memberof DisableAccountReq
     * @static
     * @param {IDisableAccountReq=} [properties] Properties to set
     * @returns {DisableAccountReq} DisableAccountReq instance
     */
    DisableAccountReq.create = function create(properties) {
        return new DisableAccountReq(properties);
    };

    /**
     * Encodes the specified DisableAccountReq message. Does not implicitly {@link DisableAccountReq.verify|verify} messages.
     * @function encode
     * @memberof DisableAccountReq
     * @static
     * @param {IDisableAccountReq} message DisableAccountReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DisableAccountReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified DisableAccountReq message, length delimited. Does not implicitly {@link DisableAccountReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof DisableAccountReq
     * @static
     * @param {IDisableAccountReq} message DisableAccountReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DisableAccountReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a DisableAccountReq message from the specified reader or buffer.
     * @function decode
     * @memberof DisableAccountReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {DisableAccountReq} DisableAccountReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DisableAccountReq.decode = function decode(reader, length, error) {
    DisableAccountReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.DisableAccountReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a DisableAccountReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof DisableAccountReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {DisableAccountReq} DisableAccountReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DisableAccountReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a DisableAccountReq message.
     * @function verify
     * @memberof DisableAccountReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    DisableAccountReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        return null;
    };

    /**
     * Creates a DisableAccountReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof DisableAccountReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {DisableAccountReq} DisableAccountReq
     */
    DisableAccountReq.fromObject = function fromObject(object) {
        if (object instanceof $root.DisableAccountReq)
            return object;
        let message = new $root.DisableAccountReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".DisableAccountReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        return message;
    };

    /**
     * Creates a plain object from a DisableAccountReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof DisableAccountReq
     * @static
     * @param {DisableAccountReq} message DisableAccountReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    DisableAccountReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.clientInfo = null;
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        return object;
    };

    /**
     * Converts this DisableAccountReq to JSON.
     * @function toJSON
     * @memberof DisableAccountReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    DisableAccountReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for DisableAccountReq
     * @function getTypeUrl
     * @memberof DisableAccountReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    DisableAccountReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/DisableAccountReq";
    };

    return DisableAccountReq;
})();

export const DisableAccountResp = $root.DisableAccountResp = (() => {

    /**
     * Properties of a DisableAccountResp.
     * @exports IDisableAccountResp
     * @interface IDisableAccountResp
     * @property {ICommonResult|null} [commonResult] DisableAccountResp commonResult
     */

    /**
     * Constructs a new DisableAccountResp.
     * @exports DisableAccountResp
     * @classdesc Represents a DisableAccountResp.
     * @implements IDisableAccountResp
     * @constructor
     * @param {IDisableAccountResp=} [properties] Properties to set
     */
    function DisableAccountResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * DisableAccountResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof DisableAccountResp
     * @instance
     */
    DisableAccountResp.prototype.commonResult = null;

    /**
     * Creates a new DisableAccountResp instance using the specified properties.
     * @function create
     * @memberof DisableAccountResp
     * @static
     * @param {IDisableAccountResp=} [properties] Properties to set
     * @returns {DisableAccountResp} DisableAccountResp instance
     */
    DisableAccountResp.create = function create(properties) {
        return new DisableAccountResp(properties);
    };

    /**
     * Encodes the specified DisableAccountResp message. Does not implicitly {@link DisableAccountResp.verify|verify} messages.
     * @function encode
     * @memberof DisableAccountResp
     * @static
     * @param {IDisableAccountResp} message DisableAccountResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DisableAccountResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified DisableAccountResp message, length delimited. Does not implicitly {@link DisableAccountResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof DisableAccountResp
     * @static
     * @param {IDisableAccountResp} message DisableAccountResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DisableAccountResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a DisableAccountResp message from the specified reader or buffer.
     * @function decode
     * @memberof DisableAccountResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {DisableAccountResp} DisableAccountResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DisableAccountResp.decode = function decode(reader, length, error) {
    DisableAccountResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.DisableAccountResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a DisableAccountResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof DisableAccountResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {DisableAccountResp} DisableAccountResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DisableAccountResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a DisableAccountResp message.
     * @function verify
     * @memberof DisableAccountResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    DisableAccountResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates a DisableAccountResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof DisableAccountResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {DisableAccountResp} DisableAccountResp
     */
    DisableAccountResp.fromObject = function fromObject(object) {
        if (object instanceof $root.DisableAccountResp)
            return object;
        let message = new $root.DisableAccountResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".DisableAccountResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from a DisableAccountResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof DisableAccountResp
     * @static
     * @param {DisableAccountResp} message DisableAccountResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    DisableAccountResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this DisableAccountResp to JSON.
     * @function toJSON
     * @memberof DisableAccountResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    DisableAccountResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for DisableAccountResp
     * @function getTypeUrl
     * @memberof DisableAccountResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    DisableAccountResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/DisableAccountResp";
    };

    return DisableAccountResp;
})();

export const GoogleAuthQrCodeReq = $root.GoogleAuthQrCodeReq = (() => {

    /**
     * Properties of a GoogleAuthQrCodeReq.
     * @exports IGoogleAuthQrCodeReq
     * @interface IGoogleAuthQrCodeReq
     * @property {IClientInfo|null} [clientInfo] GoogleAuthQrCodeReq clientInfo
     */

    /**
     * Constructs a new GoogleAuthQrCodeReq.
     * @exports GoogleAuthQrCodeReq
     * @classdesc Represents a GoogleAuthQrCodeReq.
     * @implements IGoogleAuthQrCodeReq
     * @constructor
     * @param {IGoogleAuthQrCodeReq=} [properties] Properties to set
     */
    function GoogleAuthQrCodeReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GoogleAuthQrCodeReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GoogleAuthQrCodeReq
     * @instance
     */
    GoogleAuthQrCodeReq.prototype.clientInfo = null;

    /**
     * Creates a new GoogleAuthQrCodeReq instance using the specified properties.
     * @function create
     * @memberof GoogleAuthQrCodeReq
     * @static
     * @param {IGoogleAuthQrCodeReq=} [properties] Properties to set
     * @returns {GoogleAuthQrCodeReq} GoogleAuthQrCodeReq instance
     */
    GoogleAuthQrCodeReq.create = function create(properties) {
        return new GoogleAuthQrCodeReq(properties);
    };

    /**
     * Encodes the specified GoogleAuthQrCodeReq message. Does not implicitly {@link GoogleAuthQrCodeReq.verify|verify} messages.
     * @function encode
     * @memberof GoogleAuthQrCodeReq
     * @static
     * @param {IGoogleAuthQrCodeReq} message GoogleAuthQrCodeReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GoogleAuthQrCodeReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified GoogleAuthQrCodeReq message, length delimited. Does not implicitly {@link GoogleAuthQrCodeReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GoogleAuthQrCodeReq
     * @static
     * @param {IGoogleAuthQrCodeReq} message GoogleAuthQrCodeReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GoogleAuthQrCodeReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GoogleAuthQrCodeReq message from the specified reader or buffer.
     * @function decode
     * @memberof GoogleAuthQrCodeReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GoogleAuthQrCodeReq} GoogleAuthQrCodeReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GoogleAuthQrCodeReq.decode = function decode(reader, length, error) {
    GoogleAuthQrCodeReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GoogleAuthQrCodeReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GoogleAuthQrCodeReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GoogleAuthQrCodeReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GoogleAuthQrCodeReq} GoogleAuthQrCodeReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GoogleAuthQrCodeReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GoogleAuthQrCodeReq message.
     * @function verify
     * @memberof GoogleAuthQrCodeReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GoogleAuthQrCodeReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        return null;
    };

    /**
     * Creates a GoogleAuthQrCodeReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GoogleAuthQrCodeReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GoogleAuthQrCodeReq} GoogleAuthQrCodeReq
     */
    GoogleAuthQrCodeReq.fromObject = function fromObject(object) {
        if (object instanceof $root.GoogleAuthQrCodeReq)
            return object;
        let message = new $root.GoogleAuthQrCodeReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GoogleAuthQrCodeReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        return message;
    };

    /**
     * Creates a plain object from a GoogleAuthQrCodeReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GoogleAuthQrCodeReq
     * @static
     * @param {GoogleAuthQrCodeReq} message GoogleAuthQrCodeReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GoogleAuthQrCodeReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.clientInfo = null;
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        return object;
    };

    /**
     * Converts this GoogleAuthQrCodeReq to JSON.
     * @function toJSON
     * @memberof GoogleAuthQrCodeReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GoogleAuthQrCodeReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GoogleAuthQrCodeReq
     * @function getTypeUrl
     * @memberof GoogleAuthQrCodeReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GoogleAuthQrCodeReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GoogleAuthQrCodeReq";
    };

    return GoogleAuthQrCodeReq;
})();

export const GoogleAuthQrCodeResp = $root.GoogleAuthQrCodeResp = (() => {

    /**
     * Properties of a GoogleAuthQrCodeResp.
     * @exports IGoogleAuthQrCodeResp
     * @interface IGoogleAuthQrCodeResp
     * @property {ICommonResult|null} [commonResult] GoogleAuthQrCodeResp commonResult
     * @property {string|null} [qrCodeLink] GoogleAuthQrCodeResp qrCodeLink
     * @property {string|null} [secret] GoogleAuthQrCodeResp secret
     */

    /**
     * Constructs a new GoogleAuthQrCodeResp.
     * @exports GoogleAuthQrCodeResp
     * @classdesc Represents a GoogleAuthQrCodeResp.
     * @implements IGoogleAuthQrCodeResp
     * @constructor
     * @param {IGoogleAuthQrCodeResp=} [properties] Properties to set
     */
    function GoogleAuthQrCodeResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GoogleAuthQrCodeResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GoogleAuthQrCodeResp
     * @instance
     */
    GoogleAuthQrCodeResp.prototype.commonResult = null;

    /**
     * GoogleAuthQrCodeResp qrCodeLink.
     * @member {string} qrCodeLink
     * @memberof GoogleAuthQrCodeResp
     * @instance
     */
    GoogleAuthQrCodeResp.prototype.qrCodeLink = "";

    /**
     * GoogleAuthQrCodeResp secret.
     * @member {string} secret
     * @memberof GoogleAuthQrCodeResp
     * @instance
     */
    GoogleAuthQrCodeResp.prototype.secret = "";

    /**
     * Creates a new GoogleAuthQrCodeResp instance using the specified properties.
     * @function create
     * @memberof GoogleAuthQrCodeResp
     * @static
     * @param {IGoogleAuthQrCodeResp=} [properties] Properties to set
     * @returns {GoogleAuthQrCodeResp} GoogleAuthQrCodeResp instance
     */
    GoogleAuthQrCodeResp.create = function create(properties) {
        return new GoogleAuthQrCodeResp(properties);
    };

    /**
     * Encodes the specified GoogleAuthQrCodeResp message. Does not implicitly {@link GoogleAuthQrCodeResp.verify|verify} messages.
     * @function encode
     * @memberof GoogleAuthQrCodeResp
     * @static
     * @param {IGoogleAuthQrCodeResp} message GoogleAuthQrCodeResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GoogleAuthQrCodeResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.qrCodeLink != null && Object.hasOwnProperty.call(message, "qrCodeLink"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.qrCodeLink);
        if (message.secret != null && Object.hasOwnProperty.call(message, "secret"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.secret);
        return writer;
    };

    /**
     * Encodes the specified GoogleAuthQrCodeResp message, length delimited. Does not implicitly {@link GoogleAuthQrCodeResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GoogleAuthQrCodeResp
     * @static
     * @param {IGoogleAuthQrCodeResp} message GoogleAuthQrCodeResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GoogleAuthQrCodeResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GoogleAuthQrCodeResp message from the specified reader or buffer.
     * @function decode
     * @memberof GoogleAuthQrCodeResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GoogleAuthQrCodeResp} GoogleAuthQrCodeResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GoogleAuthQrCodeResp.decode = function decode(reader, length, error) {
    GoogleAuthQrCodeResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GoogleAuthQrCodeResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.qrCodeLink = reader.string();
                    break;
                }
            case 3: {
                    message.secret = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GoogleAuthQrCodeResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GoogleAuthQrCodeResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GoogleAuthQrCodeResp} GoogleAuthQrCodeResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GoogleAuthQrCodeResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GoogleAuthQrCodeResp message.
     * @function verify
     * @memberof GoogleAuthQrCodeResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GoogleAuthQrCodeResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.qrCodeLink != null && message.hasOwnProperty("qrCodeLink"))
            if (!$util.isString(message.qrCodeLink))
                return "qrCodeLink: string expected";
        if (message.secret != null && message.hasOwnProperty("secret"))
            if (!$util.isString(message.secret))
                return "secret: string expected";
        return null;
    };

    /**
     * Creates a GoogleAuthQrCodeResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GoogleAuthQrCodeResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GoogleAuthQrCodeResp} GoogleAuthQrCodeResp
     */
    GoogleAuthQrCodeResp.fromObject = function fromObject(object) {
        if (object instanceof $root.GoogleAuthQrCodeResp)
            return object;
        let message = new $root.GoogleAuthQrCodeResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GoogleAuthQrCodeResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.qrCodeLink != null)
            message.qrCodeLink = String(object.qrCodeLink);
        if (object.secret != null)
            message.secret = String(object.secret);
        return message;
    };

    /**
     * Creates a plain object from a GoogleAuthQrCodeResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GoogleAuthQrCodeResp
     * @static
     * @param {GoogleAuthQrCodeResp} message GoogleAuthQrCodeResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GoogleAuthQrCodeResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.qrCodeLink = "";
            object.secret = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.qrCodeLink != null && message.hasOwnProperty("qrCodeLink"))
            object.qrCodeLink = message.qrCodeLink;
        if (message.secret != null && message.hasOwnProperty("secret"))
            object.secret = message.secret;
        return object;
    };

    /**
     * Converts this GoogleAuthQrCodeResp to JSON.
     * @function toJSON
     * @memberof GoogleAuthQrCodeResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GoogleAuthQrCodeResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GoogleAuthQrCodeResp
     * @function getTypeUrl
     * @memberof GoogleAuthQrCodeResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GoogleAuthQrCodeResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GoogleAuthQrCodeResp";
    };

    return GoogleAuthQrCodeResp;
})();

export const BindGoogleAuthReq = $root.BindGoogleAuthReq = (() => {

    /**
     * Properties of a BindGoogleAuthReq.
     * @exports IBindGoogleAuthReq
     * @interface IBindGoogleAuthReq
     * @property {IClientInfo|null} [clientInfo] BindGoogleAuthReq clientInfo
     * @property {string|null} [code] BindGoogleAuthReq code
     * @property {string|null} [secret] BindGoogleAuthReq secret
     * @property {Array.<IValidateCode>|null} [validateCodes] BindGoogleAuthReq validateCodes
     */

    /**
     * Constructs a new BindGoogleAuthReq.
     * @exports BindGoogleAuthReq
     * @classdesc Represents a BindGoogleAuthReq.
     * @implements IBindGoogleAuthReq
     * @constructor
     * @param {IBindGoogleAuthReq=} [properties] Properties to set
     */
    function BindGoogleAuthReq(properties) {
        this.validateCodes = [];
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * BindGoogleAuthReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof BindGoogleAuthReq
     * @instance
     */
    BindGoogleAuthReq.prototype.clientInfo = null;

    /**
     * BindGoogleAuthReq code.
     * @member {string} code
     * @memberof BindGoogleAuthReq
     * @instance
     */
    BindGoogleAuthReq.prototype.code = "";

    /**
     * BindGoogleAuthReq secret.
     * @member {string} secret
     * @memberof BindGoogleAuthReq
     * @instance
     */
    BindGoogleAuthReq.prototype.secret = "";

    /**
     * BindGoogleAuthReq validateCodes.
     * @member {Array.<IValidateCode>} validateCodes
     * @memberof BindGoogleAuthReq
     * @instance
     */
    BindGoogleAuthReq.prototype.validateCodes = $util.emptyArray;

    /**
     * Creates a new BindGoogleAuthReq instance using the specified properties.
     * @function create
     * @memberof BindGoogleAuthReq
     * @static
     * @param {IBindGoogleAuthReq=} [properties] Properties to set
     * @returns {BindGoogleAuthReq} BindGoogleAuthReq instance
     */
    BindGoogleAuthReq.create = function create(properties) {
        return new BindGoogleAuthReq(properties);
    };

    /**
     * Encodes the specified BindGoogleAuthReq message. Does not implicitly {@link BindGoogleAuthReq.verify|verify} messages.
     * @function encode
     * @memberof BindGoogleAuthReq
     * @static
     * @param {IBindGoogleAuthReq} message BindGoogleAuthReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BindGoogleAuthReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.code != null && Object.hasOwnProperty.call(message, "code"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.code);
        if (message.secret != null && Object.hasOwnProperty.call(message, "secret"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.secret);
        if (message.validateCodes != null && message.validateCodes.length)
            for (let i = 0; i < message.validateCodes.length; ++i)
                $root.ValidateCode.encode(message.validateCodes[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified BindGoogleAuthReq message, length delimited. Does not implicitly {@link BindGoogleAuthReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof BindGoogleAuthReq
     * @static
     * @param {IBindGoogleAuthReq} message BindGoogleAuthReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BindGoogleAuthReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a BindGoogleAuthReq message from the specified reader or buffer.
     * @function decode
     * @memberof BindGoogleAuthReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {BindGoogleAuthReq} BindGoogleAuthReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BindGoogleAuthReq.decode = function decode(reader, length, error) {
    BindGoogleAuthReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.BindGoogleAuthReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.code = reader.string();
                    break;
                }
            case 3: {
                    message.secret = reader.string();
                    break;
                }
            case 4: {
                    if (!(message.validateCodes && message.validateCodes.length))
                        message.validateCodes = [];
                    message.validateCodes.push($root.ValidateCode.decode(reader, reader.uint32()));
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a BindGoogleAuthReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof BindGoogleAuthReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {BindGoogleAuthReq} BindGoogleAuthReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BindGoogleAuthReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a BindGoogleAuthReq message.
     * @function verify
     * @memberof BindGoogleAuthReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    BindGoogleAuthReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.code != null && message.hasOwnProperty("code"))
            if (!$util.isString(message.code))
                return "code: string expected";
        if (message.secret != null && message.hasOwnProperty("secret"))
            if (!$util.isString(message.secret))
                return "secret: string expected";
        if (message.validateCodes != null && message.hasOwnProperty("validateCodes")) {
            if (!Array.isArray(message.validateCodes))
                return "validateCodes: array expected";
            for (let i = 0; i < message.validateCodes.length; ++i) {
                let error = $root.ValidateCode.verify(message.validateCodes[i]);
                if (error)
                    return "validateCodes." + error;
            }
        }
        return null;
    };

    /**
     * Creates a BindGoogleAuthReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof BindGoogleAuthReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {BindGoogleAuthReq} BindGoogleAuthReq
     */
    BindGoogleAuthReq.fromObject = function fromObject(object) {
        if (object instanceof $root.BindGoogleAuthReq)
            return object;
        let message = new $root.BindGoogleAuthReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".BindGoogleAuthReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.code != null)
            message.code = String(object.code);
        if (object.secret != null)
            message.secret = String(object.secret);
        if (object.validateCodes) {
            if (!Array.isArray(object.validateCodes))
                throw TypeError(".BindGoogleAuthReq.validateCodes: array expected");
            message.validateCodes = [];
            for (let i = 0; i < object.validateCodes.length; ++i) {
                if (typeof object.validateCodes[i] !== "object")
                    throw TypeError(".BindGoogleAuthReq.validateCodes: object expected");
                message.validateCodes[i] = $root.ValidateCode.fromObject(object.validateCodes[i]);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a BindGoogleAuthReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof BindGoogleAuthReq
     * @static
     * @param {BindGoogleAuthReq} message BindGoogleAuthReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    BindGoogleAuthReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.arrays || options.defaults)
            object.validateCodes = [];
        if (options.defaults) {
            object.clientInfo = null;
            object.code = "";
            object.secret = "";
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.code != null && message.hasOwnProperty("code"))
            object.code = message.code;
        if (message.secret != null && message.hasOwnProperty("secret"))
            object.secret = message.secret;
        if (message.validateCodes && message.validateCodes.length) {
            object.validateCodes = [];
            for (let j = 0; j < message.validateCodes.length; ++j)
                object.validateCodes[j] = $root.ValidateCode.toObject(message.validateCodes[j], options);
        }
        return object;
    };

    /**
     * Converts this BindGoogleAuthReq to JSON.
     * @function toJSON
     * @memberof BindGoogleAuthReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    BindGoogleAuthReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for BindGoogleAuthReq
     * @function getTypeUrl
     * @memberof BindGoogleAuthReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    BindGoogleAuthReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/BindGoogleAuthReq";
    };

    return BindGoogleAuthReq;
})();

export const BindGoogleAuthResp = $root.BindGoogleAuthResp = (() => {

    /**
     * Properties of a BindGoogleAuthResp.
     * @exports IBindGoogleAuthResp
     * @interface IBindGoogleAuthResp
     * @property {ICommonResult|null} [commonResult] BindGoogleAuthResp commonResult
     */

    /**
     * Constructs a new BindGoogleAuthResp.
     * @exports BindGoogleAuthResp
     * @classdesc Represents a BindGoogleAuthResp.
     * @implements IBindGoogleAuthResp
     * @constructor
     * @param {IBindGoogleAuthResp=} [properties] Properties to set
     */
    function BindGoogleAuthResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * BindGoogleAuthResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof BindGoogleAuthResp
     * @instance
     */
    BindGoogleAuthResp.prototype.commonResult = null;

    /**
     * Creates a new BindGoogleAuthResp instance using the specified properties.
     * @function create
     * @memberof BindGoogleAuthResp
     * @static
     * @param {IBindGoogleAuthResp=} [properties] Properties to set
     * @returns {BindGoogleAuthResp} BindGoogleAuthResp instance
     */
    BindGoogleAuthResp.create = function create(properties) {
        return new BindGoogleAuthResp(properties);
    };

    /**
     * Encodes the specified BindGoogleAuthResp message. Does not implicitly {@link BindGoogleAuthResp.verify|verify} messages.
     * @function encode
     * @memberof BindGoogleAuthResp
     * @static
     * @param {IBindGoogleAuthResp} message BindGoogleAuthResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BindGoogleAuthResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified BindGoogleAuthResp message, length delimited. Does not implicitly {@link BindGoogleAuthResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof BindGoogleAuthResp
     * @static
     * @param {IBindGoogleAuthResp} message BindGoogleAuthResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BindGoogleAuthResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a BindGoogleAuthResp message from the specified reader or buffer.
     * @function decode
     * @memberof BindGoogleAuthResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {BindGoogleAuthResp} BindGoogleAuthResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BindGoogleAuthResp.decode = function decode(reader, length, error) {
    BindGoogleAuthResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.BindGoogleAuthResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a BindGoogleAuthResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof BindGoogleAuthResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {BindGoogleAuthResp} BindGoogleAuthResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BindGoogleAuthResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a BindGoogleAuthResp message.
     * @function verify
     * @memberof BindGoogleAuthResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    BindGoogleAuthResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates a BindGoogleAuthResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof BindGoogleAuthResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {BindGoogleAuthResp} BindGoogleAuthResp
     */
    BindGoogleAuthResp.fromObject = function fromObject(object) {
        if (object instanceof $root.BindGoogleAuthResp)
            return object;
        let message = new $root.BindGoogleAuthResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".BindGoogleAuthResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from a BindGoogleAuthResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof BindGoogleAuthResp
     * @static
     * @param {BindGoogleAuthResp} message BindGoogleAuthResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    BindGoogleAuthResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this BindGoogleAuthResp to JSON.
     * @function toJSON
     * @memberof BindGoogleAuthResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    BindGoogleAuthResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for BindGoogleAuthResp
     * @function getTypeUrl
     * @memberof BindGoogleAuthResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    BindGoogleAuthResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/BindGoogleAuthResp";
    };

    return BindGoogleAuthResp;
})();

export const DisBindGoogleAuthReq = $root.DisBindGoogleAuthReq = (() => {

    /**
     * Properties of a DisBindGoogleAuthReq.
     * @exports IDisBindGoogleAuthReq
     * @interface IDisBindGoogleAuthReq
     * @property {IClientInfo|null} [clientInfo] DisBindGoogleAuthReq clientInfo
     * @property {string|null} [code] DisBindGoogleAuthReq code
     * @property {Array.<IValidateCode>|null} [validateCodes] DisBindGoogleAuthReq validateCodes
     */

    /**
     * Constructs a new DisBindGoogleAuthReq.
     * @exports DisBindGoogleAuthReq
     * @classdesc Represents a DisBindGoogleAuthReq.
     * @implements IDisBindGoogleAuthReq
     * @constructor
     * @param {IDisBindGoogleAuthReq=} [properties] Properties to set
     */
    function DisBindGoogleAuthReq(properties) {
        this.validateCodes = [];
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * DisBindGoogleAuthReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof DisBindGoogleAuthReq
     * @instance
     */
    DisBindGoogleAuthReq.prototype.clientInfo = null;

    /**
     * DisBindGoogleAuthReq code.
     * @member {string} code
     * @memberof DisBindGoogleAuthReq
     * @instance
     */
    DisBindGoogleAuthReq.prototype.code = "";

    /**
     * DisBindGoogleAuthReq validateCodes.
     * @member {Array.<IValidateCode>} validateCodes
     * @memberof DisBindGoogleAuthReq
     * @instance
     */
    DisBindGoogleAuthReq.prototype.validateCodes = $util.emptyArray;

    /**
     * Creates a new DisBindGoogleAuthReq instance using the specified properties.
     * @function create
     * @memberof DisBindGoogleAuthReq
     * @static
     * @param {IDisBindGoogleAuthReq=} [properties] Properties to set
     * @returns {DisBindGoogleAuthReq} DisBindGoogleAuthReq instance
     */
    DisBindGoogleAuthReq.create = function create(properties) {
        return new DisBindGoogleAuthReq(properties);
    };

    /**
     * Encodes the specified DisBindGoogleAuthReq message. Does not implicitly {@link DisBindGoogleAuthReq.verify|verify} messages.
     * @function encode
     * @memberof DisBindGoogleAuthReq
     * @static
     * @param {IDisBindGoogleAuthReq} message DisBindGoogleAuthReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DisBindGoogleAuthReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.code != null && Object.hasOwnProperty.call(message, "code"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.code);
        if (message.validateCodes != null && message.validateCodes.length)
            for (let i = 0; i < message.validateCodes.length; ++i)
                $root.ValidateCode.encode(message.validateCodes[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified DisBindGoogleAuthReq message, length delimited. Does not implicitly {@link DisBindGoogleAuthReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof DisBindGoogleAuthReq
     * @static
     * @param {IDisBindGoogleAuthReq} message DisBindGoogleAuthReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DisBindGoogleAuthReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a DisBindGoogleAuthReq message from the specified reader or buffer.
     * @function decode
     * @memberof DisBindGoogleAuthReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {DisBindGoogleAuthReq} DisBindGoogleAuthReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DisBindGoogleAuthReq.decode = function decode(reader, length, error) {
    DisBindGoogleAuthReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.DisBindGoogleAuthReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.code = reader.string();
                    break;
                }
            case 3: {
                    if (!(message.validateCodes && message.validateCodes.length))
                        message.validateCodes = [];
                    message.validateCodes.push($root.ValidateCode.decode(reader, reader.uint32()));
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a DisBindGoogleAuthReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof DisBindGoogleAuthReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {DisBindGoogleAuthReq} DisBindGoogleAuthReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DisBindGoogleAuthReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a DisBindGoogleAuthReq message.
     * @function verify
     * @memberof DisBindGoogleAuthReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    DisBindGoogleAuthReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.code != null && message.hasOwnProperty("code"))
            if (!$util.isString(message.code))
                return "code: string expected";
        if (message.validateCodes != null && message.hasOwnProperty("validateCodes")) {
            if (!Array.isArray(message.validateCodes))
                return "validateCodes: array expected";
            for (let i = 0; i < message.validateCodes.length; ++i) {
                let error = $root.ValidateCode.verify(message.validateCodes[i]);
                if (error)
                    return "validateCodes." + error;
            }
        }
        return null;
    };

    /**
     * Creates a DisBindGoogleAuthReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof DisBindGoogleAuthReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {DisBindGoogleAuthReq} DisBindGoogleAuthReq
     */
    DisBindGoogleAuthReq.fromObject = function fromObject(object) {
        if (object instanceof $root.DisBindGoogleAuthReq)
            return object;
        let message = new $root.DisBindGoogleAuthReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".DisBindGoogleAuthReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.code != null)
            message.code = String(object.code);
        if (object.validateCodes) {
            if (!Array.isArray(object.validateCodes))
                throw TypeError(".DisBindGoogleAuthReq.validateCodes: array expected");
            message.validateCodes = [];
            for (let i = 0; i < object.validateCodes.length; ++i) {
                if (typeof object.validateCodes[i] !== "object")
                    throw TypeError(".DisBindGoogleAuthReq.validateCodes: object expected");
                message.validateCodes[i] = $root.ValidateCode.fromObject(object.validateCodes[i]);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a DisBindGoogleAuthReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof DisBindGoogleAuthReq
     * @static
     * @param {DisBindGoogleAuthReq} message DisBindGoogleAuthReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    DisBindGoogleAuthReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.arrays || options.defaults)
            object.validateCodes = [];
        if (options.defaults) {
            object.clientInfo = null;
            object.code = "";
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.code != null && message.hasOwnProperty("code"))
            object.code = message.code;
        if (message.validateCodes && message.validateCodes.length) {
            object.validateCodes = [];
            for (let j = 0; j < message.validateCodes.length; ++j)
                object.validateCodes[j] = $root.ValidateCode.toObject(message.validateCodes[j], options);
        }
        return object;
    };

    /**
     * Converts this DisBindGoogleAuthReq to JSON.
     * @function toJSON
     * @memberof DisBindGoogleAuthReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    DisBindGoogleAuthReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for DisBindGoogleAuthReq
     * @function getTypeUrl
     * @memberof DisBindGoogleAuthReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    DisBindGoogleAuthReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/DisBindGoogleAuthReq";
    };

    return DisBindGoogleAuthReq;
})();

export const DisBindGoogleAuthResp = $root.DisBindGoogleAuthResp = (() => {

    /**
     * Properties of a DisBindGoogleAuthResp.
     * @exports IDisBindGoogleAuthResp
     * @interface IDisBindGoogleAuthResp
     * @property {ICommonResult|null} [commonResult] DisBindGoogleAuthResp commonResult
     */

    /**
     * Constructs a new DisBindGoogleAuthResp.
     * @exports DisBindGoogleAuthResp
     * @classdesc Represents a DisBindGoogleAuthResp.
     * @implements IDisBindGoogleAuthResp
     * @constructor
     * @param {IDisBindGoogleAuthResp=} [properties] Properties to set
     */
    function DisBindGoogleAuthResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * DisBindGoogleAuthResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof DisBindGoogleAuthResp
     * @instance
     */
    DisBindGoogleAuthResp.prototype.commonResult = null;

    /**
     * Creates a new DisBindGoogleAuthResp instance using the specified properties.
     * @function create
     * @memberof DisBindGoogleAuthResp
     * @static
     * @param {IDisBindGoogleAuthResp=} [properties] Properties to set
     * @returns {DisBindGoogleAuthResp} DisBindGoogleAuthResp instance
     */
    DisBindGoogleAuthResp.create = function create(properties) {
        return new DisBindGoogleAuthResp(properties);
    };

    /**
     * Encodes the specified DisBindGoogleAuthResp message. Does not implicitly {@link DisBindGoogleAuthResp.verify|verify} messages.
     * @function encode
     * @memberof DisBindGoogleAuthResp
     * @static
     * @param {IDisBindGoogleAuthResp} message DisBindGoogleAuthResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DisBindGoogleAuthResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified DisBindGoogleAuthResp message, length delimited. Does not implicitly {@link DisBindGoogleAuthResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof DisBindGoogleAuthResp
     * @static
     * @param {IDisBindGoogleAuthResp} message DisBindGoogleAuthResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DisBindGoogleAuthResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a DisBindGoogleAuthResp message from the specified reader or buffer.
     * @function decode
     * @memberof DisBindGoogleAuthResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {DisBindGoogleAuthResp} DisBindGoogleAuthResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DisBindGoogleAuthResp.decode = function decode(reader, length, error) {
    DisBindGoogleAuthResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.DisBindGoogleAuthResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a DisBindGoogleAuthResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof DisBindGoogleAuthResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {DisBindGoogleAuthResp} DisBindGoogleAuthResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DisBindGoogleAuthResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a DisBindGoogleAuthResp message.
     * @function verify
     * @memberof DisBindGoogleAuthResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    DisBindGoogleAuthResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates a DisBindGoogleAuthResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof DisBindGoogleAuthResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {DisBindGoogleAuthResp} DisBindGoogleAuthResp
     */
    DisBindGoogleAuthResp.fromObject = function fromObject(object) {
        if (object instanceof $root.DisBindGoogleAuthResp)
            return object;
        let message = new $root.DisBindGoogleAuthResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".DisBindGoogleAuthResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from a DisBindGoogleAuthResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof DisBindGoogleAuthResp
     * @static
     * @param {DisBindGoogleAuthResp} message DisBindGoogleAuthResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    DisBindGoogleAuthResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this DisBindGoogleAuthResp to JSON.
     * @function toJSON
     * @memberof DisBindGoogleAuthResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    DisBindGoogleAuthResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for DisBindGoogleAuthResp
     * @function getTypeUrl
     * @memberof DisBindGoogleAuthResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    DisBindGoogleAuthResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/DisBindGoogleAuthResp";
    };

    return DisBindGoogleAuthResp;
})();

export const GoogleValidateReq = $root.GoogleValidateReq = (() => {

    /**
     * Properties of a GoogleValidateReq.
     * @exports IGoogleValidateReq
     * @interface IGoogleValidateReq
     * @property {IClientInfo|null} [clientInfo] GoogleValidateReq clientInfo
     * @property {boolean|null} [isOpenGoogleAuth] GoogleValidateReq isOpenGoogleAuth
     * @property {Array.<IValidateCode>|null} [validateCodes] GoogleValidateReq validateCodes
     */

    /**
     * Constructs a new GoogleValidateReq.
     * @exports GoogleValidateReq
     * @classdesc Represents a GoogleValidateReq.
     * @implements IGoogleValidateReq
     * @constructor
     * @param {IGoogleValidateReq=} [properties] Properties to set
     */
    function GoogleValidateReq(properties) {
        this.validateCodes = [];
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GoogleValidateReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GoogleValidateReq
     * @instance
     */
    GoogleValidateReq.prototype.clientInfo = null;

    /**
     * GoogleValidateReq isOpenGoogleAuth.
     * @member {boolean} isOpenGoogleAuth
     * @memberof GoogleValidateReq
     * @instance
     */
    GoogleValidateReq.prototype.isOpenGoogleAuth = false;

    /**
     * GoogleValidateReq validateCodes.
     * @member {Array.<IValidateCode>} validateCodes
     * @memberof GoogleValidateReq
     * @instance
     */
    GoogleValidateReq.prototype.validateCodes = $util.emptyArray;

    /**
     * Creates a new GoogleValidateReq instance using the specified properties.
     * @function create
     * @memberof GoogleValidateReq
     * @static
     * @param {IGoogleValidateReq=} [properties] Properties to set
     * @returns {GoogleValidateReq} GoogleValidateReq instance
     */
    GoogleValidateReq.create = function create(properties) {
        return new GoogleValidateReq(properties);
    };

    /**
     * Encodes the specified GoogleValidateReq message. Does not implicitly {@link GoogleValidateReq.verify|verify} messages.
     * @function encode
     * @memberof GoogleValidateReq
     * @static
     * @param {IGoogleValidateReq} message GoogleValidateReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GoogleValidateReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.isOpenGoogleAuth != null && Object.hasOwnProperty.call(message, "isOpenGoogleAuth"))
            writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isOpenGoogleAuth);
        if (message.validateCodes != null && message.validateCodes.length)
            for (let i = 0; i < message.validateCodes.length; ++i)
                $root.ValidateCode.encode(message.validateCodes[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified GoogleValidateReq message, length delimited. Does not implicitly {@link GoogleValidateReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GoogleValidateReq
     * @static
     * @param {IGoogleValidateReq} message GoogleValidateReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GoogleValidateReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GoogleValidateReq message from the specified reader or buffer.
     * @function decode
     * @memberof GoogleValidateReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GoogleValidateReq} GoogleValidateReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GoogleValidateReq.decode = function decode(reader, length, error) {
    GoogleValidateReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GoogleValidateReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.isOpenGoogleAuth = reader.bool();
                    break;
                }
            case 3: {
                    if (!(message.validateCodes && message.validateCodes.length))
                        message.validateCodes = [];
                    message.validateCodes.push($root.ValidateCode.decode(reader, reader.uint32()));
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GoogleValidateReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GoogleValidateReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GoogleValidateReq} GoogleValidateReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GoogleValidateReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GoogleValidateReq message.
     * @function verify
     * @memberof GoogleValidateReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GoogleValidateReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.isOpenGoogleAuth != null && message.hasOwnProperty("isOpenGoogleAuth"))
            if (typeof message.isOpenGoogleAuth !== "boolean")
                return "isOpenGoogleAuth: boolean expected";
        if (message.validateCodes != null && message.hasOwnProperty("validateCodes")) {
            if (!Array.isArray(message.validateCodes))
                return "validateCodes: array expected";
            for (let i = 0; i < message.validateCodes.length; ++i) {
                let error = $root.ValidateCode.verify(message.validateCodes[i]);
                if (error)
                    return "validateCodes." + error;
            }
        }
        return null;
    };

    /**
     * Creates a GoogleValidateReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GoogleValidateReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GoogleValidateReq} GoogleValidateReq
     */
    GoogleValidateReq.fromObject = function fromObject(object) {
        if (object instanceof $root.GoogleValidateReq)
            return object;
        let message = new $root.GoogleValidateReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GoogleValidateReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.isOpenGoogleAuth != null)
            message.isOpenGoogleAuth = Boolean(object.isOpenGoogleAuth);
        if (object.validateCodes) {
            if (!Array.isArray(object.validateCodes))
                throw TypeError(".GoogleValidateReq.validateCodes: array expected");
            message.validateCodes = [];
            for (let i = 0; i < object.validateCodes.length; ++i) {
                if (typeof object.validateCodes[i] !== "object")
                    throw TypeError(".GoogleValidateReq.validateCodes: object expected");
                message.validateCodes[i] = $root.ValidateCode.fromObject(object.validateCodes[i]);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a GoogleValidateReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GoogleValidateReq
     * @static
     * @param {GoogleValidateReq} message GoogleValidateReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GoogleValidateReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.arrays || options.defaults)
            object.validateCodes = [];
        if (options.defaults) {
            object.clientInfo = null;
            object.isOpenGoogleAuth = false;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.isOpenGoogleAuth != null && message.hasOwnProperty("isOpenGoogleAuth"))
            object.isOpenGoogleAuth = message.isOpenGoogleAuth;
        if (message.validateCodes && message.validateCodes.length) {
            object.validateCodes = [];
            for (let j = 0; j < message.validateCodes.length; ++j)
                object.validateCodes[j] = $root.ValidateCode.toObject(message.validateCodes[j], options);
        }
        return object;
    };

    /**
     * Converts this GoogleValidateReq to JSON.
     * @function toJSON
     * @memberof GoogleValidateReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GoogleValidateReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GoogleValidateReq
     * @function getTypeUrl
     * @memberof GoogleValidateReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GoogleValidateReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GoogleValidateReq";
    };

    return GoogleValidateReq;
})();

export const GoogleValidateResp = $root.GoogleValidateResp = (() => {

    /**
     * Properties of a GoogleValidateResp.
     * @exports IGoogleValidateResp
     * @interface IGoogleValidateResp
     * @property {ICommonResult|null} [commonResult] GoogleValidateResp commonResult
     */

    /**
     * Constructs a new GoogleValidateResp.
     * @exports GoogleValidateResp
     * @classdesc Represents a GoogleValidateResp.
     * @implements IGoogleValidateResp
     * @constructor
     * @param {IGoogleValidateResp=} [properties] Properties to set
     */
    function GoogleValidateResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GoogleValidateResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GoogleValidateResp
     * @instance
     */
    GoogleValidateResp.prototype.commonResult = null;

    /**
     * Creates a new GoogleValidateResp instance using the specified properties.
     * @function create
     * @memberof GoogleValidateResp
     * @static
     * @param {IGoogleValidateResp=} [properties] Properties to set
     * @returns {GoogleValidateResp} GoogleValidateResp instance
     */
    GoogleValidateResp.create = function create(properties) {
        return new GoogleValidateResp(properties);
    };

    /**
     * Encodes the specified GoogleValidateResp message. Does not implicitly {@link GoogleValidateResp.verify|verify} messages.
     * @function encode
     * @memberof GoogleValidateResp
     * @static
     * @param {IGoogleValidateResp} message GoogleValidateResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GoogleValidateResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified GoogleValidateResp message, length delimited. Does not implicitly {@link GoogleValidateResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GoogleValidateResp
     * @static
     * @param {IGoogleValidateResp} message GoogleValidateResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GoogleValidateResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GoogleValidateResp message from the specified reader or buffer.
     * @function decode
     * @memberof GoogleValidateResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GoogleValidateResp} GoogleValidateResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GoogleValidateResp.decode = function decode(reader, length, error) {
    GoogleValidateResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GoogleValidateResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GoogleValidateResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GoogleValidateResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GoogleValidateResp} GoogleValidateResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GoogleValidateResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GoogleValidateResp message.
     * @function verify
     * @memberof GoogleValidateResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GoogleValidateResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates a GoogleValidateResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GoogleValidateResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GoogleValidateResp} GoogleValidateResp
     */
    GoogleValidateResp.fromObject = function fromObject(object) {
        if (object instanceof $root.GoogleValidateResp)
            return object;
        let message = new $root.GoogleValidateResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GoogleValidateResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from a GoogleValidateResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GoogleValidateResp
     * @static
     * @param {GoogleValidateResp} message GoogleValidateResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GoogleValidateResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this GoogleValidateResp to JSON.
     * @function toJSON
     * @memberof GoogleValidateResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GoogleValidateResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GoogleValidateResp
     * @function getTypeUrl
     * @memberof GoogleValidateResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GoogleValidateResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GoogleValidateResp";
    };

    return GoogleValidateResp;
})();

export const SendRedPacketReq = $root.SendRedPacketReq = (() => {

    /**
     * Properties of a SendRedPacketReq.
     * @exports ISendRedPacketReq
     * @interface ISendRedPacketReq
     * @property {IClientInfo|null} [clientInfo] SendRedPacketReq clientInfo
     * @property {string|null} [coin] SendRedPacketReq coin
     * @property {string|null} [desc] SendRedPacketReq desc
     * @property {number|null} [type] SendRedPacketReq type
     * @property {number|null} [coinId] SendRedPacketReq coinId
     * @property {number|null} [count] SendRedPacketReq count
     * @property {number|Long|null} [targetUid] SendRedPacketReq targetUid
     * @property {number|Long|null} [groupId] SendRedPacketReq groupId
     * @property {string|null} [code] SendRedPacketReq code
     */

    /**
     * Constructs a new SendRedPacketReq.
     * @exports SendRedPacketReq
     * @classdesc Represents a SendRedPacketReq.
     * @implements ISendRedPacketReq
     * @constructor
     * @param {ISendRedPacketReq=} [properties] Properties to set
     */
    function SendRedPacketReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * SendRedPacketReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof SendRedPacketReq
     * @instance
     */
    SendRedPacketReq.prototype.clientInfo = null;

    /**
     * SendRedPacketReq coin.
     * @member {string} coin
     * @memberof SendRedPacketReq
     * @instance
     */
    SendRedPacketReq.prototype.coin = "";

    /**
     * SendRedPacketReq desc.
     * @member {string} desc
     * @memberof SendRedPacketReq
     * @instance
     */
    SendRedPacketReq.prototype.desc = "";

    /**
     * SendRedPacketReq type.
     * @member {number} type
     * @memberof SendRedPacketReq
     * @instance
     */
    SendRedPacketReq.prototype.type = 0;

    /**
     * SendRedPacketReq coinId.
     * @member {number} coinId
     * @memberof SendRedPacketReq
     * @instance
     */
    SendRedPacketReq.prototype.coinId = 0;

    /**
     * SendRedPacketReq count.
     * @member {number} count
     * @memberof SendRedPacketReq
     * @instance
     */
    SendRedPacketReq.prototype.count = 0;

    /**
     * SendRedPacketReq targetUid.
     * @member {number|Long} targetUid
     * @memberof SendRedPacketReq
     * @instance
     */
    SendRedPacketReq.prototype.targetUid = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * SendRedPacketReq groupId.
     * @member {number|Long} groupId
     * @memberof SendRedPacketReq
     * @instance
     */
    SendRedPacketReq.prototype.groupId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * SendRedPacketReq code.
     * @member {string} code
     * @memberof SendRedPacketReq
     * @instance
     */
    SendRedPacketReq.prototype.code = "";

    /**
     * Creates a new SendRedPacketReq instance using the specified properties.
     * @function create
     * @memberof SendRedPacketReq
     * @static
     * @param {ISendRedPacketReq=} [properties] Properties to set
     * @returns {SendRedPacketReq} SendRedPacketReq instance
     */
    SendRedPacketReq.create = function create(properties) {
        return new SendRedPacketReq(properties);
    };

    /**
     * Encodes the specified SendRedPacketReq message. Does not implicitly {@link SendRedPacketReq.verify|verify} messages.
     * @function encode
     * @memberof SendRedPacketReq
     * @static
     * @param {ISendRedPacketReq} message SendRedPacketReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SendRedPacketReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.coin != null && Object.hasOwnProperty.call(message, "coin"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.coin);
        if (message.desc != null && Object.hasOwnProperty.call(message, "desc"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.desc);
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.type);
        if (message.coinId != null && Object.hasOwnProperty.call(message, "coinId"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.coinId);
        if (message.count != null && Object.hasOwnProperty.call(message, "count"))
            writer.uint32(/* id 6, wireType 0 =*/48).int32(message.count);
        if (message.targetUid != null && Object.hasOwnProperty.call(message, "targetUid"))
            writer.uint32(/* id 7, wireType 0 =*/56).int64(message.targetUid);
        if (message.groupId != null && Object.hasOwnProperty.call(message, "groupId"))
            writer.uint32(/* id 8, wireType 0 =*/64).int64(message.groupId);
        if (message.code != null && Object.hasOwnProperty.call(message, "code"))
            writer.uint32(/* id 9, wireType 2 =*/74).string(message.code);
        return writer;
    };

    /**
     * Encodes the specified SendRedPacketReq message, length delimited. Does not implicitly {@link SendRedPacketReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof SendRedPacketReq
     * @static
     * @param {ISendRedPacketReq} message SendRedPacketReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SendRedPacketReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a SendRedPacketReq message from the specified reader or buffer.
     * @function decode
     * @memberof SendRedPacketReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {SendRedPacketReq} SendRedPacketReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SendRedPacketReq.decode = function decode(reader, length, error) {
    SendRedPacketReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.SendRedPacketReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.coin = reader.string();
                    break;
                }
            case 3: {
                    message.desc = reader.string();
                    break;
                }
            case 4: {
                    message.type = reader.int32();
                    break;
                }
            case 5: {
                    message.coinId = reader.int32();
                    break;
                }
            case 6: {
                    message.count = reader.int32();
                    break;
                }
            case 7: {
                    message.targetUid = reader.int64();
                    break;
                }
            case 8: {
                    message.groupId = reader.int64();
                    break;
                }
            case 9: {
                    message.code = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a SendRedPacketReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof SendRedPacketReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {SendRedPacketReq} SendRedPacketReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SendRedPacketReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SendRedPacketReq message.
     * @function verify
     * @memberof SendRedPacketReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SendRedPacketReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.coin != null && message.hasOwnProperty("coin"))
            if (!$util.isString(message.coin))
                return "coin: string expected";
        if (message.desc != null && message.hasOwnProperty("desc"))
            if (!$util.isString(message.desc))
                return "desc: string expected";
        if (message.type != null && message.hasOwnProperty("type"))
            if (!$util.isInteger(message.type))
                return "type: integer expected";
        if (message.coinId != null && message.hasOwnProperty("coinId"))
            if (!$util.isInteger(message.coinId))
                return "coinId: integer expected";
        if (message.count != null && message.hasOwnProperty("count"))
            if (!$util.isInteger(message.count))
                return "count: integer expected";
        if (message.targetUid != null && message.hasOwnProperty("targetUid"))
            if (!$util.isInteger(message.targetUid) && !(message.targetUid && $util.isInteger(message.targetUid.low) && $util.isInteger(message.targetUid.high)))
                return "targetUid: integer|Long expected";
        if (message.groupId != null && message.hasOwnProperty("groupId"))
            if (!$util.isInteger(message.groupId) && !(message.groupId && $util.isInteger(message.groupId.low) && $util.isInteger(message.groupId.high)))
                return "groupId: integer|Long expected";
        if (message.code != null && message.hasOwnProperty("code"))
            if (!$util.isString(message.code))
                return "code: string expected";
        return null;
    };

    /**
     * Creates a SendRedPacketReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof SendRedPacketReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {SendRedPacketReq} SendRedPacketReq
     */
    SendRedPacketReq.fromObject = function fromObject(object) {
        if (object instanceof $root.SendRedPacketReq)
            return object;
        let message = new $root.SendRedPacketReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".SendRedPacketReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.coin != null)
            message.coin = String(object.coin);
        if (object.desc != null)
            message.desc = String(object.desc);
        if (object.type != null)
            message.type = object.type | 0;
        if (object.coinId != null)
            message.coinId = object.coinId | 0;
        if (object.count != null)
            message.count = object.count | 0;
        if (object.targetUid != null)
            if ($util.Long)
                (message.targetUid = $util.Long.fromValue(object.targetUid)).unsigned = false;
            else if (typeof object.targetUid === "string")
                message.targetUid = parseInt(object.targetUid, 10);
            else if (typeof object.targetUid === "number")
                message.targetUid = object.targetUid;
            else if (typeof object.targetUid === "object")
                message.targetUid = new $util.LongBits(object.targetUid.low >>> 0, object.targetUid.high >>> 0).toNumber();
        if (object.groupId != null)
            if ($util.Long)
                (message.groupId = $util.Long.fromValue(object.groupId)).unsigned = false;
            else if (typeof object.groupId === "string")
                message.groupId = parseInt(object.groupId, 10);
            else if (typeof object.groupId === "number")
                message.groupId = object.groupId;
            else if (typeof object.groupId === "object")
                message.groupId = new $util.LongBits(object.groupId.low >>> 0, object.groupId.high >>> 0).toNumber();
        if (object.code != null)
            message.code = String(object.code);
        return message;
    };

    /**
     * Creates a plain object from a SendRedPacketReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof SendRedPacketReq
     * @static
     * @param {SendRedPacketReq} message SendRedPacketReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SendRedPacketReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.coin = "";
            object.desc = "";
            object.type = 0;
            object.coinId = 0;
            object.count = 0;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.targetUid = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.targetUid = options.longs === String ? "0" : 0;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.groupId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.groupId = options.longs === String ? "0" : 0;
            object.code = "";
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.coin != null && message.hasOwnProperty("coin"))
            object.coin = message.coin;
        if (message.desc != null && message.hasOwnProperty("desc"))
            object.desc = message.desc;
        if (message.type != null && message.hasOwnProperty("type"))
            object.type = message.type;
        if (message.coinId != null && message.hasOwnProperty("coinId"))
            object.coinId = message.coinId;
        if (message.count != null && message.hasOwnProperty("count"))
            object.count = message.count;
        if (message.targetUid != null && message.hasOwnProperty("targetUid"))
            if (typeof message.targetUid === "number")
                object.targetUid = options.longs === String ? String(message.targetUid) : message.targetUid;
            else
                object.targetUid = options.longs === String ? $util.Long.prototype.toString.call(message.targetUid) : options.longs === Number ? new $util.LongBits(message.targetUid.low >>> 0, message.targetUid.high >>> 0).toNumber() : message.targetUid;
        if (message.groupId != null && message.hasOwnProperty("groupId"))
            if (typeof message.groupId === "number")
                object.groupId = options.longs === String ? String(message.groupId) : message.groupId;
            else
                object.groupId = options.longs === String ? $util.Long.prototype.toString.call(message.groupId) : options.longs === Number ? new $util.LongBits(message.groupId.low >>> 0, message.groupId.high >>> 0).toNumber() : message.groupId;
        if (message.code != null && message.hasOwnProperty("code"))
            object.code = message.code;
        return object;
    };

    /**
     * Converts this SendRedPacketReq to JSON.
     * @function toJSON
     * @memberof SendRedPacketReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SendRedPacketReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SendRedPacketReq
     * @function getTypeUrl
     * @memberof SendRedPacketReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SendRedPacketReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/SendRedPacketReq";
    };

    return SendRedPacketReq;
})();

export const SendRedPacketResp = $root.SendRedPacketResp = (() => {

    /**
     * Properties of a SendRedPacketResp.
     * @exports ISendRedPacketResp
     * @interface ISendRedPacketResp
     * @property {ICommonResult|null} [commonResult] SendRedPacketResp commonResult
     * @property {number|Long|null} [redPacketId] SendRedPacketResp redPacketId
     */

    /**
     * Constructs a new SendRedPacketResp.
     * @exports SendRedPacketResp
     * @classdesc Represents a SendRedPacketResp.
     * @implements ISendRedPacketResp
     * @constructor
     * @param {ISendRedPacketResp=} [properties] Properties to set
     */
    function SendRedPacketResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * SendRedPacketResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof SendRedPacketResp
     * @instance
     */
    SendRedPacketResp.prototype.commonResult = null;

    /**
     * SendRedPacketResp redPacketId.
     * @member {number|Long} redPacketId
     * @memberof SendRedPacketResp
     * @instance
     */
    SendRedPacketResp.prototype.redPacketId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * Creates a new SendRedPacketResp instance using the specified properties.
     * @function create
     * @memberof SendRedPacketResp
     * @static
     * @param {ISendRedPacketResp=} [properties] Properties to set
     * @returns {SendRedPacketResp} SendRedPacketResp instance
     */
    SendRedPacketResp.create = function create(properties) {
        return new SendRedPacketResp(properties);
    };

    /**
     * Encodes the specified SendRedPacketResp message. Does not implicitly {@link SendRedPacketResp.verify|verify} messages.
     * @function encode
     * @memberof SendRedPacketResp
     * @static
     * @param {ISendRedPacketResp} message SendRedPacketResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SendRedPacketResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.redPacketId != null && Object.hasOwnProperty.call(message, "redPacketId"))
            writer.uint32(/* id 2, wireType 0 =*/16).int64(message.redPacketId);
        return writer;
    };

    /**
     * Encodes the specified SendRedPacketResp message, length delimited. Does not implicitly {@link SendRedPacketResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof SendRedPacketResp
     * @static
     * @param {ISendRedPacketResp} message SendRedPacketResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SendRedPacketResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a SendRedPacketResp message from the specified reader or buffer.
     * @function decode
     * @memberof SendRedPacketResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {SendRedPacketResp} SendRedPacketResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SendRedPacketResp.decode = function decode(reader, length, error) {
    SendRedPacketResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.SendRedPacketResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.redPacketId = reader.int64();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a SendRedPacketResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof SendRedPacketResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {SendRedPacketResp} SendRedPacketResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SendRedPacketResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SendRedPacketResp message.
     * @function verify
     * @memberof SendRedPacketResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SendRedPacketResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.redPacketId != null && message.hasOwnProperty("redPacketId"))
            if (!$util.isInteger(message.redPacketId) && !(message.redPacketId && $util.isInteger(message.redPacketId.low) && $util.isInteger(message.redPacketId.high)))
                return "redPacketId: integer|Long expected";
        return null;
    };

    /**
     * Creates a SendRedPacketResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof SendRedPacketResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {SendRedPacketResp} SendRedPacketResp
     */
    SendRedPacketResp.fromObject = function fromObject(object) {
        if (object instanceof $root.SendRedPacketResp)
            return object;
        let message = new $root.SendRedPacketResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".SendRedPacketResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.redPacketId != null)
            if ($util.Long)
                (message.redPacketId = $util.Long.fromValue(object.redPacketId)).unsigned = false;
            else if (typeof object.redPacketId === "string")
                message.redPacketId = parseInt(object.redPacketId, 10);
            else if (typeof object.redPacketId === "number")
                message.redPacketId = object.redPacketId;
            else if (typeof object.redPacketId === "object")
                message.redPacketId = new $util.LongBits(object.redPacketId.low >>> 0, object.redPacketId.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a SendRedPacketResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof SendRedPacketResp
     * @static
     * @param {SendRedPacketResp} message SendRedPacketResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SendRedPacketResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.redPacketId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.redPacketId = options.longs === String ? "0" : 0;
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.redPacketId != null && message.hasOwnProperty("redPacketId"))
            if (typeof message.redPacketId === "number")
                object.redPacketId = options.longs === String ? String(message.redPacketId) : message.redPacketId;
            else
                object.redPacketId = options.longs === String ? $util.Long.prototype.toString.call(message.redPacketId) : options.longs === Number ? new $util.LongBits(message.redPacketId.low >>> 0, message.redPacketId.high >>> 0).toNumber() : message.redPacketId;
        return object;
    };

    /**
     * Converts this SendRedPacketResp to JSON.
     * @function toJSON
     * @memberof SendRedPacketResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SendRedPacketResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SendRedPacketResp
     * @function getTypeUrl
     * @memberof SendRedPacketResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SendRedPacketResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/SendRedPacketResp";
    };

    return SendRedPacketResp;
})();

export const QueryRedPacketReq = $root.QueryRedPacketReq = (() => {

    /**
     * Properties of a QueryRedPacketReq.
     * @exports IQueryRedPacketReq
     * @interface IQueryRedPacketReq
     * @property {IClientInfo|null} [clientInfo] QueryRedPacketReq clientInfo
     * @property {number|Long|null} [redPacketId] QueryRedPacketReq redPacketId
     */

    /**
     * Constructs a new QueryRedPacketReq.
     * @exports QueryRedPacketReq
     * @classdesc Represents a QueryRedPacketReq.
     * @implements IQueryRedPacketReq
     * @constructor
     * @param {IQueryRedPacketReq=} [properties] Properties to set
     */
    function QueryRedPacketReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryRedPacketReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof QueryRedPacketReq
     * @instance
     */
    QueryRedPacketReq.prototype.clientInfo = null;

    /**
     * QueryRedPacketReq redPacketId.
     * @member {number|Long} redPacketId
     * @memberof QueryRedPacketReq
     * @instance
     */
    QueryRedPacketReq.prototype.redPacketId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * Creates a new QueryRedPacketReq instance using the specified properties.
     * @function create
     * @memberof QueryRedPacketReq
     * @static
     * @param {IQueryRedPacketReq=} [properties] Properties to set
     * @returns {QueryRedPacketReq} QueryRedPacketReq instance
     */
    QueryRedPacketReq.create = function create(properties) {
        return new QueryRedPacketReq(properties);
    };

    /**
     * Encodes the specified QueryRedPacketReq message. Does not implicitly {@link QueryRedPacketReq.verify|verify} messages.
     * @function encode
     * @memberof QueryRedPacketReq
     * @static
     * @param {IQueryRedPacketReq} message QueryRedPacketReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryRedPacketReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.redPacketId != null && Object.hasOwnProperty.call(message, "redPacketId"))
            writer.uint32(/* id 2, wireType 0 =*/16).int64(message.redPacketId);
        return writer;
    };

    /**
     * Encodes the specified QueryRedPacketReq message, length delimited. Does not implicitly {@link QueryRedPacketReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof QueryRedPacketReq
     * @static
     * @param {IQueryRedPacketReq} message QueryRedPacketReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryRedPacketReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a QueryRedPacketReq message from the specified reader or buffer.
     * @function decode
     * @memberof QueryRedPacketReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {QueryRedPacketReq} QueryRedPacketReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryRedPacketReq.decode = function decode(reader, length, error) {
    QueryRedPacketReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.QueryRedPacketReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.redPacketId = reader.int64();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a QueryRedPacketReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof QueryRedPacketReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {QueryRedPacketReq} QueryRedPacketReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryRedPacketReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a QueryRedPacketReq message.
     * @function verify
     * @memberof QueryRedPacketReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryRedPacketReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.redPacketId != null && message.hasOwnProperty("redPacketId"))
            if (!$util.isInteger(message.redPacketId) && !(message.redPacketId && $util.isInteger(message.redPacketId.low) && $util.isInteger(message.redPacketId.high)))
                return "redPacketId: integer|Long expected";
        return null;
    };

    /**
     * Creates a QueryRedPacketReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof QueryRedPacketReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {QueryRedPacketReq} QueryRedPacketReq
     */
    QueryRedPacketReq.fromObject = function fromObject(object) {
        if (object instanceof $root.QueryRedPacketReq)
            return object;
        let message = new $root.QueryRedPacketReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".QueryRedPacketReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.redPacketId != null)
            if ($util.Long)
                (message.redPacketId = $util.Long.fromValue(object.redPacketId)).unsigned = false;
            else if (typeof object.redPacketId === "string")
                message.redPacketId = parseInt(object.redPacketId, 10);
            else if (typeof object.redPacketId === "number")
                message.redPacketId = object.redPacketId;
            else if (typeof object.redPacketId === "object")
                message.redPacketId = new $util.LongBits(object.redPacketId.low >>> 0, object.redPacketId.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a QueryRedPacketReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof QueryRedPacketReq
     * @static
     * @param {QueryRedPacketReq} message QueryRedPacketReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryRedPacketReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.redPacketId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.redPacketId = options.longs === String ? "0" : 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.redPacketId != null && message.hasOwnProperty("redPacketId"))
            if (typeof message.redPacketId === "number")
                object.redPacketId = options.longs === String ? String(message.redPacketId) : message.redPacketId;
            else
                object.redPacketId = options.longs === String ? $util.Long.prototype.toString.call(message.redPacketId) : options.longs === Number ? new $util.LongBits(message.redPacketId.low >>> 0, message.redPacketId.high >>> 0).toNumber() : message.redPacketId;
        return object;
    };

    /**
     * Converts this QueryRedPacketReq to JSON.
     * @function toJSON
     * @memberof QueryRedPacketReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryRedPacketReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for QueryRedPacketReq
     * @function getTypeUrl
     * @memberof QueryRedPacketReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryRedPacketReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/QueryRedPacketReq";
    };

    return QueryRedPacketReq;
})();

export const QueryRedPacketResp = $root.QueryRedPacketResp = (() => {

    /**
     * Properties of a QueryRedPacketResp.
     * @exports IQueryRedPacketResp
     * @interface IQueryRedPacketResp
     * @property {ICommonResult|null} [commonResult] QueryRedPacketResp commonResult
     * @property {number|null} [status] QueryRedPacketResp status
     */

    /**
     * Constructs a new QueryRedPacketResp.
     * @exports QueryRedPacketResp
     * @classdesc Represents a QueryRedPacketResp.
     * @implements IQueryRedPacketResp
     * @constructor
     * @param {IQueryRedPacketResp=} [properties] Properties to set
     */
    function QueryRedPacketResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryRedPacketResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof QueryRedPacketResp
     * @instance
     */
    QueryRedPacketResp.prototype.commonResult = null;

    /**
     * QueryRedPacketResp status.
     * @member {number} status
     * @memberof QueryRedPacketResp
     * @instance
     */
    QueryRedPacketResp.prototype.status = 0;

    /**
     * Creates a new QueryRedPacketResp instance using the specified properties.
     * @function create
     * @memberof QueryRedPacketResp
     * @static
     * @param {IQueryRedPacketResp=} [properties] Properties to set
     * @returns {QueryRedPacketResp} QueryRedPacketResp instance
     */
    QueryRedPacketResp.create = function create(properties) {
        return new QueryRedPacketResp(properties);
    };

    /**
     * Encodes the specified QueryRedPacketResp message. Does not implicitly {@link QueryRedPacketResp.verify|verify} messages.
     * @function encode
     * @memberof QueryRedPacketResp
     * @static
     * @param {IQueryRedPacketResp} message QueryRedPacketResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryRedPacketResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.status != null && Object.hasOwnProperty.call(message, "status"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.status);
        return writer;
    };

    /**
     * Encodes the specified QueryRedPacketResp message, length delimited. Does not implicitly {@link QueryRedPacketResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof QueryRedPacketResp
     * @static
     * @param {IQueryRedPacketResp} message QueryRedPacketResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryRedPacketResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a QueryRedPacketResp message from the specified reader or buffer.
     * @function decode
     * @memberof QueryRedPacketResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {QueryRedPacketResp} QueryRedPacketResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryRedPacketResp.decode = function decode(reader, length, error) {
    QueryRedPacketResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.QueryRedPacketResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.status = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a QueryRedPacketResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof QueryRedPacketResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {QueryRedPacketResp} QueryRedPacketResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryRedPacketResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a QueryRedPacketResp message.
     * @function verify
     * @memberof QueryRedPacketResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryRedPacketResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.status != null && message.hasOwnProperty("status"))
            if (!$util.isInteger(message.status))
                return "status: integer expected";
        return null;
    };

    /**
     * Creates a QueryRedPacketResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof QueryRedPacketResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {QueryRedPacketResp} QueryRedPacketResp
     */
    QueryRedPacketResp.fromObject = function fromObject(object) {
        if (object instanceof $root.QueryRedPacketResp)
            return object;
        let message = new $root.QueryRedPacketResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".QueryRedPacketResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.status != null)
            message.status = object.status | 0;
        return message;
    };

    /**
     * Creates a plain object from a QueryRedPacketResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof QueryRedPacketResp
     * @static
     * @param {QueryRedPacketResp} message QueryRedPacketResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryRedPacketResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.status = 0;
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.status != null && message.hasOwnProperty("status"))
            object.status = message.status;
        return object;
    };

    /**
     * Converts this QueryRedPacketResp to JSON.
     * @function toJSON
     * @memberof QueryRedPacketResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryRedPacketResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for QueryRedPacketResp
     * @function getTypeUrl
     * @memberof QueryRedPacketResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryRedPacketResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/QueryRedPacketResp";
    };

    return QueryRedPacketResp;
})();

export const OpenRedPacketReq = $root.OpenRedPacketReq = (() => {

    /**
     * Properties of an OpenRedPacketReq.
     * @exports IOpenRedPacketReq
     * @interface IOpenRedPacketReq
     * @property {IClientInfo|null} [clientInfo] OpenRedPacketReq clientInfo
     * @property {number|Long|null} [redPacketId] OpenRedPacketReq redPacketId
     */

    /**
     * Constructs a new OpenRedPacketReq.
     * @exports OpenRedPacketReq
     * @classdesc Represents an OpenRedPacketReq.
     * @implements IOpenRedPacketReq
     * @constructor
     * @param {IOpenRedPacketReq=} [properties] Properties to set
     */
    function OpenRedPacketReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * OpenRedPacketReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof OpenRedPacketReq
     * @instance
     */
    OpenRedPacketReq.prototype.clientInfo = null;

    /**
     * OpenRedPacketReq redPacketId.
     * @member {number|Long} redPacketId
     * @memberof OpenRedPacketReq
     * @instance
     */
    OpenRedPacketReq.prototype.redPacketId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * Creates a new OpenRedPacketReq instance using the specified properties.
     * @function create
     * @memberof OpenRedPacketReq
     * @static
     * @param {IOpenRedPacketReq=} [properties] Properties to set
     * @returns {OpenRedPacketReq} OpenRedPacketReq instance
     */
    OpenRedPacketReq.create = function create(properties) {
        return new OpenRedPacketReq(properties);
    };

    /**
     * Encodes the specified OpenRedPacketReq message. Does not implicitly {@link OpenRedPacketReq.verify|verify} messages.
     * @function encode
     * @memberof OpenRedPacketReq
     * @static
     * @param {IOpenRedPacketReq} message OpenRedPacketReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OpenRedPacketReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.redPacketId != null && Object.hasOwnProperty.call(message, "redPacketId"))
            writer.uint32(/* id 2, wireType 0 =*/16).int64(message.redPacketId);
        return writer;
    };

    /**
     * Encodes the specified OpenRedPacketReq message, length delimited. Does not implicitly {@link OpenRedPacketReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof OpenRedPacketReq
     * @static
     * @param {IOpenRedPacketReq} message OpenRedPacketReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OpenRedPacketReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an OpenRedPacketReq message from the specified reader or buffer.
     * @function decode
     * @memberof OpenRedPacketReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {OpenRedPacketReq} OpenRedPacketReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OpenRedPacketReq.decode = function decode(reader, length, error) {
    OpenRedPacketReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.OpenRedPacketReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.redPacketId = reader.int64();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an OpenRedPacketReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof OpenRedPacketReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {OpenRedPacketReq} OpenRedPacketReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OpenRedPacketReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an OpenRedPacketReq message.
     * @function verify
     * @memberof OpenRedPacketReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    OpenRedPacketReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.redPacketId != null && message.hasOwnProperty("redPacketId"))
            if (!$util.isInteger(message.redPacketId) && !(message.redPacketId && $util.isInteger(message.redPacketId.low) && $util.isInteger(message.redPacketId.high)))
                return "redPacketId: integer|Long expected";
        return null;
    };

    /**
     * Creates an OpenRedPacketReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof OpenRedPacketReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {OpenRedPacketReq} OpenRedPacketReq
     */
    OpenRedPacketReq.fromObject = function fromObject(object) {
        if (object instanceof $root.OpenRedPacketReq)
            return object;
        let message = new $root.OpenRedPacketReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".OpenRedPacketReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.redPacketId != null)
            if ($util.Long)
                (message.redPacketId = $util.Long.fromValue(object.redPacketId)).unsigned = false;
            else if (typeof object.redPacketId === "string")
                message.redPacketId = parseInt(object.redPacketId, 10);
            else if (typeof object.redPacketId === "number")
                message.redPacketId = object.redPacketId;
            else if (typeof object.redPacketId === "object")
                message.redPacketId = new $util.LongBits(object.redPacketId.low >>> 0, object.redPacketId.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from an OpenRedPacketReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof OpenRedPacketReq
     * @static
     * @param {OpenRedPacketReq} message OpenRedPacketReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    OpenRedPacketReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.redPacketId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.redPacketId = options.longs === String ? "0" : 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.redPacketId != null && message.hasOwnProperty("redPacketId"))
            if (typeof message.redPacketId === "number")
                object.redPacketId = options.longs === String ? String(message.redPacketId) : message.redPacketId;
            else
                object.redPacketId = options.longs === String ? $util.Long.prototype.toString.call(message.redPacketId) : options.longs === Number ? new $util.LongBits(message.redPacketId.low >>> 0, message.redPacketId.high >>> 0).toNumber() : message.redPacketId;
        return object;
    };

    /**
     * Converts this OpenRedPacketReq to JSON.
     * @function toJSON
     * @memberof OpenRedPacketReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    OpenRedPacketReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for OpenRedPacketReq
     * @function getTypeUrl
     * @memberof OpenRedPacketReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    OpenRedPacketReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/OpenRedPacketReq";
    };

    return OpenRedPacketReq;
})();

export const OpenRedPacketResp = $root.OpenRedPacketResp = (() => {

    /**
     * Properties of an OpenRedPacketResp.
     * @exports IOpenRedPacketResp
     * @interface IOpenRedPacketResp
     * @property {ICommonResult|null} [commonResult] OpenRedPacketResp commonResult
     * @property {number|null} [status] OpenRedPacketResp status
     * @property {string|null} [coin] OpenRedPacketResp coin
     */

    /**
     * Constructs a new OpenRedPacketResp.
     * @exports OpenRedPacketResp
     * @classdesc Represents an OpenRedPacketResp.
     * @implements IOpenRedPacketResp
     * @constructor
     * @param {IOpenRedPacketResp=} [properties] Properties to set
     */
    function OpenRedPacketResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * OpenRedPacketResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof OpenRedPacketResp
     * @instance
     */
    OpenRedPacketResp.prototype.commonResult = null;

    /**
     * OpenRedPacketResp status.
     * @member {number} status
     * @memberof OpenRedPacketResp
     * @instance
     */
    OpenRedPacketResp.prototype.status = 0;

    /**
     * OpenRedPacketResp coin.
     * @member {string} coin
     * @memberof OpenRedPacketResp
     * @instance
     */
    OpenRedPacketResp.prototype.coin = "";

    /**
     * Creates a new OpenRedPacketResp instance using the specified properties.
     * @function create
     * @memberof OpenRedPacketResp
     * @static
     * @param {IOpenRedPacketResp=} [properties] Properties to set
     * @returns {OpenRedPacketResp} OpenRedPacketResp instance
     */
    OpenRedPacketResp.create = function create(properties) {
        return new OpenRedPacketResp(properties);
    };

    /**
     * Encodes the specified OpenRedPacketResp message. Does not implicitly {@link OpenRedPacketResp.verify|verify} messages.
     * @function encode
     * @memberof OpenRedPacketResp
     * @static
     * @param {IOpenRedPacketResp} message OpenRedPacketResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OpenRedPacketResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.status != null && Object.hasOwnProperty.call(message, "status"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.status);
        if (message.coin != null && Object.hasOwnProperty.call(message, "coin"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.coin);
        return writer;
    };

    /**
     * Encodes the specified OpenRedPacketResp message, length delimited. Does not implicitly {@link OpenRedPacketResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof OpenRedPacketResp
     * @static
     * @param {IOpenRedPacketResp} message OpenRedPacketResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OpenRedPacketResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an OpenRedPacketResp message from the specified reader or buffer.
     * @function decode
     * @memberof OpenRedPacketResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {OpenRedPacketResp} OpenRedPacketResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OpenRedPacketResp.decode = function decode(reader, length, error) {
    OpenRedPacketResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.OpenRedPacketResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.status = reader.int32();
                    break;
                }
            case 3: {
                    message.coin = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an OpenRedPacketResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof OpenRedPacketResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {OpenRedPacketResp} OpenRedPacketResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OpenRedPacketResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an OpenRedPacketResp message.
     * @function verify
     * @memberof OpenRedPacketResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    OpenRedPacketResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.status != null && message.hasOwnProperty("status"))
            if (!$util.isInteger(message.status))
                return "status: integer expected";
        if (message.coin != null && message.hasOwnProperty("coin"))
            if (!$util.isString(message.coin))
                return "coin: string expected";
        return null;
    };

    /**
     * Creates an OpenRedPacketResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof OpenRedPacketResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {OpenRedPacketResp} OpenRedPacketResp
     */
    OpenRedPacketResp.fromObject = function fromObject(object) {
        if (object instanceof $root.OpenRedPacketResp)
            return object;
        let message = new $root.OpenRedPacketResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".OpenRedPacketResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.status != null)
            message.status = object.status | 0;
        if (object.coin != null)
            message.coin = String(object.coin);
        return message;
    };

    /**
     * Creates a plain object from an OpenRedPacketResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof OpenRedPacketResp
     * @static
     * @param {OpenRedPacketResp} message OpenRedPacketResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    OpenRedPacketResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.status = 0;
            object.coin = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.status != null && message.hasOwnProperty("status"))
            object.status = message.status;
        if (message.coin != null && message.hasOwnProperty("coin"))
            object.coin = message.coin;
        return object;
    };

    /**
     * Converts this OpenRedPacketResp to JSON.
     * @function toJSON
     * @memberof OpenRedPacketResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    OpenRedPacketResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for OpenRedPacketResp
     * @function getTypeUrl
     * @memberof OpenRedPacketResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    OpenRedPacketResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/OpenRedPacketResp";
    };

    return OpenRedPacketResp;
})();

export const RedPacketOpenRecordReq = $root.RedPacketOpenRecordReq = (() => {

    /**
     * Properties of a RedPacketOpenRecordReq.
     * @exports IRedPacketOpenRecordReq
     * @interface IRedPacketOpenRecordReq
     * @property {IClientInfo|null} [clientInfo] RedPacketOpenRecordReq clientInfo
     * @property {number|Long|null} [redPacketId] RedPacketOpenRecordReq redPacketId
     */

    /**
     * Constructs a new RedPacketOpenRecordReq.
     * @exports RedPacketOpenRecordReq
     * @classdesc Represents a RedPacketOpenRecordReq.
     * @implements IRedPacketOpenRecordReq
     * @constructor
     * @param {IRedPacketOpenRecordReq=} [properties] Properties to set
     */
    function RedPacketOpenRecordReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * RedPacketOpenRecordReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof RedPacketOpenRecordReq
     * @instance
     */
    RedPacketOpenRecordReq.prototype.clientInfo = null;

    /**
     * RedPacketOpenRecordReq redPacketId.
     * @member {number|Long} redPacketId
     * @memberof RedPacketOpenRecordReq
     * @instance
     */
    RedPacketOpenRecordReq.prototype.redPacketId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * Creates a new RedPacketOpenRecordReq instance using the specified properties.
     * @function create
     * @memberof RedPacketOpenRecordReq
     * @static
     * @param {IRedPacketOpenRecordReq=} [properties] Properties to set
     * @returns {RedPacketOpenRecordReq} RedPacketOpenRecordReq instance
     */
    RedPacketOpenRecordReq.create = function create(properties) {
        return new RedPacketOpenRecordReq(properties);
    };

    /**
     * Encodes the specified RedPacketOpenRecordReq message. Does not implicitly {@link RedPacketOpenRecordReq.verify|verify} messages.
     * @function encode
     * @memberof RedPacketOpenRecordReq
     * @static
     * @param {IRedPacketOpenRecordReq} message RedPacketOpenRecordReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketOpenRecordReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.redPacketId != null && Object.hasOwnProperty.call(message, "redPacketId"))
            writer.uint32(/* id 2, wireType 0 =*/16).int64(message.redPacketId);
        return writer;
    };

    /**
     * Encodes the specified RedPacketOpenRecordReq message, length delimited. Does not implicitly {@link RedPacketOpenRecordReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof RedPacketOpenRecordReq
     * @static
     * @param {IRedPacketOpenRecordReq} message RedPacketOpenRecordReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketOpenRecordReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a RedPacketOpenRecordReq message from the specified reader or buffer.
     * @function decode
     * @memberof RedPacketOpenRecordReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {RedPacketOpenRecordReq} RedPacketOpenRecordReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketOpenRecordReq.decode = function decode(reader, length, error) {
    RedPacketOpenRecordReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.RedPacketOpenRecordReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.redPacketId = reader.int64();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a RedPacketOpenRecordReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof RedPacketOpenRecordReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {RedPacketOpenRecordReq} RedPacketOpenRecordReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketOpenRecordReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RedPacketOpenRecordReq message.
     * @function verify
     * @memberof RedPacketOpenRecordReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RedPacketOpenRecordReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.redPacketId != null && message.hasOwnProperty("redPacketId"))
            if (!$util.isInteger(message.redPacketId) && !(message.redPacketId && $util.isInteger(message.redPacketId.low) && $util.isInteger(message.redPacketId.high)))
                return "redPacketId: integer|Long expected";
        return null;
    };

    /**
     * Creates a RedPacketOpenRecordReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof RedPacketOpenRecordReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {RedPacketOpenRecordReq} RedPacketOpenRecordReq
     */
    RedPacketOpenRecordReq.fromObject = function fromObject(object) {
        if (object instanceof $root.RedPacketOpenRecordReq)
            return object;
        let message = new $root.RedPacketOpenRecordReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".RedPacketOpenRecordReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.redPacketId != null)
            if ($util.Long)
                (message.redPacketId = $util.Long.fromValue(object.redPacketId)).unsigned = false;
            else if (typeof object.redPacketId === "string")
                message.redPacketId = parseInt(object.redPacketId, 10);
            else if (typeof object.redPacketId === "number")
                message.redPacketId = object.redPacketId;
            else if (typeof object.redPacketId === "object")
                message.redPacketId = new $util.LongBits(object.redPacketId.low >>> 0, object.redPacketId.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a RedPacketOpenRecordReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof RedPacketOpenRecordReq
     * @static
     * @param {RedPacketOpenRecordReq} message RedPacketOpenRecordReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RedPacketOpenRecordReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.redPacketId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.redPacketId = options.longs === String ? "0" : 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.redPacketId != null && message.hasOwnProperty("redPacketId"))
            if (typeof message.redPacketId === "number")
                object.redPacketId = options.longs === String ? String(message.redPacketId) : message.redPacketId;
            else
                object.redPacketId = options.longs === String ? $util.Long.prototype.toString.call(message.redPacketId) : options.longs === Number ? new $util.LongBits(message.redPacketId.low >>> 0, message.redPacketId.high >>> 0).toNumber() : message.redPacketId;
        return object;
    };

    /**
     * Converts this RedPacketOpenRecordReq to JSON.
     * @function toJSON
     * @memberof RedPacketOpenRecordReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RedPacketOpenRecordReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RedPacketOpenRecordReq
     * @function getTypeUrl
     * @memberof RedPacketOpenRecordReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RedPacketOpenRecordReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/RedPacketOpenRecordReq";
    };

    return RedPacketOpenRecordReq;
})();

export const RedPacketOpenRecordResp = $root.RedPacketOpenRecordResp = (() => {

    /**
     * Properties of a RedPacketOpenRecordResp.
     * @exports IRedPacketOpenRecordResp
     * @interface IRedPacketOpenRecordResp
     * @property {ICommonResult|null} [commonResult] RedPacketOpenRecordResp commonResult
     * @property {number|null} [costTime] RedPacketOpenRecordResp costTime
     * @property {IRedPacketBase|null} [redPacketBase] RedPacketOpenRecordResp redPacketBase
     * @property {Array.<IRedPacketOpenRecord>|null} [redPacketRecord] RedPacketOpenRecordResp redPacketRecord
     * @property {string|null} [totalCoin] RedPacketOpenRecordResp totalCoin
     * @property {string|null} [openCoin] RedPacketOpenRecordResp openCoin
     */

    /**
     * Constructs a new RedPacketOpenRecordResp.
     * @exports RedPacketOpenRecordResp
     * @classdesc Represents a RedPacketOpenRecordResp.
     * @implements IRedPacketOpenRecordResp
     * @constructor
     * @param {IRedPacketOpenRecordResp=} [properties] Properties to set
     */
    function RedPacketOpenRecordResp(properties) {
        this.redPacketRecord = [];
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * RedPacketOpenRecordResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof RedPacketOpenRecordResp
     * @instance
     */
    RedPacketOpenRecordResp.prototype.commonResult = null;

    /**
     * RedPacketOpenRecordResp costTime.
     * @member {number} costTime
     * @memberof RedPacketOpenRecordResp
     * @instance
     */
    RedPacketOpenRecordResp.prototype.costTime = 0;

    /**
     * RedPacketOpenRecordResp redPacketBase.
     * @member {IRedPacketBase|null|undefined} redPacketBase
     * @memberof RedPacketOpenRecordResp
     * @instance
     */
    RedPacketOpenRecordResp.prototype.redPacketBase = null;

    /**
     * RedPacketOpenRecordResp redPacketRecord.
     * @member {Array.<IRedPacketOpenRecord>} redPacketRecord
     * @memberof RedPacketOpenRecordResp
     * @instance
     */
    RedPacketOpenRecordResp.prototype.redPacketRecord = $util.emptyArray;

    /**
     * RedPacketOpenRecordResp totalCoin.
     * @member {string} totalCoin
     * @memberof RedPacketOpenRecordResp
     * @instance
     */
    RedPacketOpenRecordResp.prototype.totalCoin = "";

    /**
     * RedPacketOpenRecordResp openCoin.
     * @member {string} openCoin
     * @memberof RedPacketOpenRecordResp
     * @instance
     */
    RedPacketOpenRecordResp.prototype.openCoin = "";

    /**
     * Creates a new RedPacketOpenRecordResp instance using the specified properties.
     * @function create
     * @memberof RedPacketOpenRecordResp
     * @static
     * @param {IRedPacketOpenRecordResp=} [properties] Properties to set
     * @returns {RedPacketOpenRecordResp} RedPacketOpenRecordResp instance
     */
    RedPacketOpenRecordResp.create = function create(properties) {
        return new RedPacketOpenRecordResp(properties);
    };

    /**
     * Encodes the specified RedPacketOpenRecordResp message. Does not implicitly {@link RedPacketOpenRecordResp.verify|verify} messages.
     * @function encode
     * @memberof RedPacketOpenRecordResp
     * @static
     * @param {IRedPacketOpenRecordResp} message RedPacketOpenRecordResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketOpenRecordResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.costTime != null && Object.hasOwnProperty.call(message, "costTime"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.costTime);
        if (message.redPacketBase != null && Object.hasOwnProperty.call(message, "redPacketBase"))
            $root.RedPacketBase.encode(message.redPacketBase, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
        if (message.redPacketRecord != null && message.redPacketRecord.length)
            for (let i = 0; i < message.redPacketRecord.length; ++i)
                $root.RedPacketOpenRecord.encode(message.redPacketRecord[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
        if (message.totalCoin != null && Object.hasOwnProperty.call(message, "totalCoin"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.totalCoin);
        if (message.openCoin != null && Object.hasOwnProperty.call(message, "openCoin"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.openCoin);
        return writer;
    };

    /**
     * Encodes the specified RedPacketOpenRecordResp message, length delimited. Does not implicitly {@link RedPacketOpenRecordResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof RedPacketOpenRecordResp
     * @static
     * @param {IRedPacketOpenRecordResp} message RedPacketOpenRecordResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketOpenRecordResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a RedPacketOpenRecordResp message from the specified reader or buffer.
     * @function decode
     * @memberof RedPacketOpenRecordResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {RedPacketOpenRecordResp} RedPacketOpenRecordResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketOpenRecordResp.decode = function decode(reader, length, error) {
    RedPacketOpenRecordResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.RedPacketOpenRecordResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.costTime = reader.int32();
                    break;
                }
            case 3: {
                    message.redPacketBase = $root.RedPacketBase.decode(reader, reader.uint32());
                    break;
                }
            case 4: {
                    if (!(message.redPacketRecord && message.redPacketRecord.length))
                        message.redPacketRecord = [];
                    message.redPacketRecord.push($root.RedPacketOpenRecord.decode(reader, reader.uint32()));
                    break;
                }
            case 5: {
                    message.totalCoin = reader.string();
                    break;
                }
            case 6: {
                    message.openCoin = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a RedPacketOpenRecordResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof RedPacketOpenRecordResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {RedPacketOpenRecordResp} RedPacketOpenRecordResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketOpenRecordResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RedPacketOpenRecordResp message.
     * @function verify
     * @memberof RedPacketOpenRecordResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RedPacketOpenRecordResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.costTime != null && message.hasOwnProperty("costTime"))
            if (!$util.isInteger(message.costTime))
                return "costTime: integer expected";
        if (message.redPacketBase != null && message.hasOwnProperty("redPacketBase")) {
            let error = $root.RedPacketBase.verify(message.redPacketBase);
            if (error)
                return "redPacketBase." + error;
        }
        if (message.redPacketRecord != null && message.hasOwnProperty("redPacketRecord")) {
            if (!Array.isArray(message.redPacketRecord))
                return "redPacketRecord: array expected";
            for (let i = 0; i < message.redPacketRecord.length; ++i) {
                let error = $root.RedPacketOpenRecord.verify(message.redPacketRecord[i]);
                if (error)
                    return "redPacketRecord." + error;
            }
        }
        if (message.totalCoin != null && message.hasOwnProperty("totalCoin"))
            if (!$util.isString(message.totalCoin))
                return "totalCoin: string expected";
        if (message.openCoin != null && message.hasOwnProperty("openCoin"))
            if (!$util.isString(message.openCoin))
                return "openCoin: string expected";
        return null;
    };

    /**
     * Creates a RedPacketOpenRecordResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof RedPacketOpenRecordResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {RedPacketOpenRecordResp} RedPacketOpenRecordResp
     */
    RedPacketOpenRecordResp.fromObject = function fromObject(object) {
        if (object instanceof $root.RedPacketOpenRecordResp)
            return object;
        let message = new $root.RedPacketOpenRecordResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".RedPacketOpenRecordResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.costTime != null)
            message.costTime = object.costTime | 0;
        if (object.redPacketBase != null) {
            if (typeof object.redPacketBase !== "object")
                throw TypeError(".RedPacketOpenRecordResp.redPacketBase: object expected");
            message.redPacketBase = $root.RedPacketBase.fromObject(object.redPacketBase);
        }
        if (object.redPacketRecord) {
            if (!Array.isArray(object.redPacketRecord))
                throw TypeError(".RedPacketOpenRecordResp.redPacketRecord: array expected");
            message.redPacketRecord = [];
            for (let i = 0; i < object.redPacketRecord.length; ++i) {
                if (typeof object.redPacketRecord[i] !== "object")
                    throw TypeError(".RedPacketOpenRecordResp.redPacketRecord: object expected");
                message.redPacketRecord[i] = $root.RedPacketOpenRecord.fromObject(object.redPacketRecord[i]);
            }
        }
        if (object.totalCoin != null)
            message.totalCoin = String(object.totalCoin);
        if (object.openCoin != null)
            message.openCoin = String(object.openCoin);
        return message;
    };

    /**
     * Creates a plain object from a RedPacketOpenRecordResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof RedPacketOpenRecordResp
     * @static
     * @param {RedPacketOpenRecordResp} message RedPacketOpenRecordResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RedPacketOpenRecordResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.arrays || options.defaults)
            object.redPacketRecord = [];
        if (options.defaults) {
            object.commonResult = null;
            object.costTime = 0;
            object.redPacketBase = null;
            object.totalCoin = "";
            object.openCoin = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.costTime != null && message.hasOwnProperty("costTime"))
            object.costTime = message.costTime;
        if (message.redPacketBase != null && message.hasOwnProperty("redPacketBase"))
            object.redPacketBase = $root.RedPacketBase.toObject(message.redPacketBase, options);
        if (message.redPacketRecord && message.redPacketRecord.length) {
            object.redPacketRecord = [];
            for (let j = 0; j < message.redPacketRecord.length; ++j)
                object.redPacketRecord[j] = $root.RedPacketOpenRecord.toObject(message.redPacketRecord[j], options);
        }
        if (message.totalCoin != null && message.hasOwnProperty("totalCoin"))
            object.totalCoin = message.totalCoin;
        if (message.openCoin != null && message.hasOwnProperty("openCoin"))
            object.openCoin = message.openCoin;
        return object;
    };

    /**
     * Converts this RedPacketOpenRecordResp to JSON.
     * @function toJSON
     * @memberof RedPacketOpenRecordResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RedPacketOpenRecordResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RedPacketOpenRecordResp
     * @function getTypeUrl
     * @memberof RedPacketOpenRecordResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RedPacketOpenRecordResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/RedPacketOpenRecordResp";
    };

    return RedPacketOpenRecordResp;
})();

export const RedPacketRecordReq = $root.RedPacketRecordReq = (() => {

    /**
     * Properties of a RedPacketRecordReq.
     * @exports IRedPacketRecordReq
     * @interface IRedPacketRecordReq
     * @property {IClientInfo|null} [clientInfo] RedPacketRecordReq clientInfo
     * @property {number|null} [type] RedPacketRecordReq type
     * @property {number|null} [pageNum] RedPacketRecordReq pageNum
     * @property {number|null} [pageSize] RedPacketRecordReq pageSize
     */

    /**
     * Constructs a new RedPacketRecordReq.
     * @exports RedPacketRecordReq
     * @classdesc Represents a RedPacketRecordReq.
     * @implements IRedPacketRecordReq
     * @constructor
     * @param {IRedPacketRecordReq=} [properties] Properties to set
     */
    function RedPacketRecordReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * RedPacketRecordReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof RedPacketRecordReq
     * @instance
     */
    RedPacketRecordReq.prototype.clientInfo = null;

    /**
     * RedPacketRecordReq type.
     * @member {number} type
     * @memberof RedPacketRecordReq
     * @instance
     */
    RedPacketRecordReq.prototype.type = 0;

    /**
     * RedPacketRecordReq pageNum.
     * @member {number} pageNum
     * @memberof RedPacketRecordReq
     * @instance
     */
    RedPacketRecordReq.prototype.pageNum = 0;

    /**
     * RedPacketRecordReq pageSize.
     * @member {number} pageSize
     * @memberof RedPacketRecordReq
     * @instance
     */
    RedPacketRecordReq.prototype.pageSize = 0;

    /**
     * Creates a new RedPacketRecordReq instance using the specified properties.
     * @function create
     * @memberof RedPacketRecordReq
     * @static
     * @param {IRedPacketRecordReq=} [properties] Properties to set
     * @returns {RedPacketRecordReq} RedPacketRecordReq instance
     */
    RedPacketRecordReq.create = function create(properties) {
        return new RedPacketRecordReq(properties);
    };

    /**
     * Encodes the specified RedPacketRecordReq message. Does not implicitly {@link RedPacketRecordReq.verify|verify} messages.
     * @function encode
     * @memberof RedPacketRecordReq
     * @static
     * @param {IRedPacketRecordReq} message RedPacketRecordReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketRecordReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
        if (message.pageNum != null && Object.hasOwnProperty.call(message, "pageNum"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.pageNum);
        if (message.pageSize != null && Object.hasOwnProperty.call(message, "pageSize"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.pageSize);
        return writer;
    };

    /**
     * Encodes the specified RedPacketRecordReq message, length delimited. Does not implicitly {@link RedPacketRecordReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof RedPacketRecordReq
     * @static
     * @param {IRedPacketRecordReq} message RedPacketRecordReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketRecordReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a RedPacketRecordReq message from the specified reader or buffer.
     * @function decode
     * @memberof RedPacketRecordReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {RedPacketRecordReq} RedPacketRecordReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketRecordReq.decode = function decode(reader, length, error) {
    RedPacketRecordReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.RedPacketRecordReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.type = reader.int32();
                    break;
                }
            case 3: {
                    message.pageNum = reader.int32();
                    break;
                }
            case 4: {
                    message.pageSize = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a RedPacketRecordReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof RedPacketRecordReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {RedPacketRecordReq} RedPacketRecordReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketRecordReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RedPacketRecordReq message.
     * @function verify
     * @memberof RedPacketRecordReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RedPacketRecordReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.type != null && message.hasOwnProperty("type"))
            if (!$util.isInteger(message.type))
                return "type: integer expected";
        if (message.pageNum != null && message.hasOwnProperty("pageNum"))
            if (!$util.isInteger(message.pageNum))
                return "pageNum: integer expected";
        if (message.pageSize != null && message.hasOwnProperty("pageSize"))
            if (!$util.isInteger(message.pageSize))
                return "pageSize: integer expected";
        return null;
    };

    /**
     * Creates a RedPacketRecordReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof RedPacketRecordReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {RedPacketRecordReq} RedPacketRecordReq
     */
    RedPacketRecordReq.fromObject = function fromObject(object) {
        if (object instanceof $root.RedPacketRecordReq)
            return object;
        let message = new $root.RedPacketRecordReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".RedPacketRecordReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.type != null)
            message.type = object.type | 0;
        if (object.pageNum != null)
            message.pageNum = object.pageNum | 0;
        if (object.pageSize != null)
            message.pageSize = object.pageSize | 0;
        return message;
    };

    /**
     * Creates a plain object from a RedPacketRecordReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof RedPacketRecordReq
     * @static
     * @param {RedPacketRecordReq} message RedPacketRecordReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RedPacketRecordReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.type = 0;
            object.pageNum = 0;
            object.pageSize = 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.type != null && message.hasOwnProperty("type"))
            object.type = message.type;
        if (message.pageNum != null && message.hasOwnProperty("pageNum"))
            object.pageNum = message.pageNum;
        if (message.pageSize != null && message.hasOwnProperty("pageSize"))
            object.pageSize = message.pageSize;
        return object;
    };

    /**
     * Converts this RedPacketRecordReq to JSON.
     * @function toJSON
     * @memberof RedPacketRecordReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RedPacketRecordReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RedPacketRecordReq
     * @function getTypeUrl
     * @memberof RedPacketRecordReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RedPacketRecordReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/RedPacketRecordReq";
    };

    return RedPacketRecordReq;
})();

export const RedPacketRecordResp = $root.RedPacketRecordResp = (() => {

    /**
     * Properties of a RedPacketRecordResp.
     * @exports IRedPacketRecordResp
     * @interface IRedPacketRecordResp
     * @property {ICommonResult|null} [commonResult] RedPacketRecordResp commonResult
     * @property {number|null} [count] RedPacketRecordResp count
     * @property {number|null} [pageNum] RedPacketRecordResp pageNum
     * @property {Array.<IRedPacketRecordBase>|null} [redPacketRecordBase] RedPacketRecordResp redPacketRecordBase
     */

    /**
     * Constructs a new RedPacketRecordResp.
     * @exports RedPacketRecordResp
     * @classdesc Represents a RedPacketRecordResp.
     * @implements IRedPacketRecordResp
     * @constructor
     * @param {IRedPacketRecordResp=} [properties] Properties to set
     */
    function RedPacketRecordResp(properties) {
        this.redPacketRecordBase = [];
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * RedPacketRecordResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof RedPacketRecordResp
     * @instance
     */
    RedPacketRecordResp.prototype.commonResult = null;

    /**
     * RedPacketRecordResp count.
     * @member {number} count
     * @memberof RedPacketRecordResp
     * @instance
     */
    RedPacketRecordResp.prototype.count = 0;

    /**
     * RedPacketRecordResp pageNum.
     * @member {number} pageNum
     * @memberof RedPacketRecordResp
     * @instance
     */
    RedPacketRecordResp.prototype.pageNum = 0;

    /**
     * RedPacketRecordResp redPacketRecordBase.
     * @member {Array.<IRedPacketRecordBase>} redPacketRecordBase
     * @memberof RedPacketRecordResp
     * @instance
     */
    RedPacketRecordResp.prototype.redPacketRecordBase = $util.emptyArray;

    /**
     * Creates a new RedPacketRecordResp instance using the specified properties.
     * @function create
     * @memberof RedPacketRecordResp
     * @static
     * @param {IRedPacketRecordResp=} [properties] Properties to set
     * @returns {RedPacketRecordResp} RedPacketRecordResp instance
     */
    RedPacketRecordResp.create = function create(properties) {
        return new RedPacketRecordResp(properties);
    };

    /**
     * Encodes the specified RedPacketRecordResp message. Does not implicitly {@link RedPacketRecordResp.verify|verify} messages.
     * @function encode
     * @memberof RedPacketRecordResp
     * @static
     * @param {IRedPacketRecordResp} message RedPacketRecordResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketRecordResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.count != null && Object.hasOwnProperty.call(message, "count"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.count);
        if (message.pageNum != null && Object.hasOwnProperty.call(message, "pageNum"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.pageNum);
        if (message.redPacketRecordBase != null && message.redPacketRecordBase.length)
            for (let i = 0; i < message.redPacketRecordBase.length; ++i)
                $root.RedPacketRecordBase.encode(message.redPacketRecordBase[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified RedPacketRecordResp message, length delimited. Does not implicitly {@link RedPacketRecordResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof RedPacketRecordResp
     * @static
     * @param {IRedPacketRecordResp} message RedPacketRecordResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedPacketRecordResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a RedPacketRecordResp message from the specified reader or buffer.
     * @function decode
     * @memberof RedPacketRecordResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {RedPacketRecordResp} RedPacketRecordResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketRecordResp.decode = function decode(reader, length, error) {
    RedPacketRecordResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.RedPacketRecordResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.count = reader.int32();
                    break;
                }
            case 3: {
                    message.pageNum = reader.int32();
                    break;
                }
            case 4: {
                    if (!(message.redPacketRecordBase && message.redPacketRecordBase.length))
                        message.redPacketRecordBase = [];
                    message.redPacketRecordBase.push($root.RedPacketRecordBase.decode(reader, reader.uint32()));
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a RedPacketRecordResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof RedPacketRecordResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {RedPacketRecordResp} RedPacketRecordResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedPacketRecordResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RedPacketRecordResp message.
     * @function verify
     * @memberof RedPacketRecordResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RedPacketRecordResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.count != null && message.hasOwnProperty("count"))
            if (!$util.isInteger(message.count))
                return "count: integer expected";
        if (message.pageNum != null && message.hasOwnProperty("pageNum"))
            if (!$util.isInteger(message.pageNum))
                return "pageNum: integer expected";
        if (message.redPacketRecordBase != null && message.hasOwnProperty("redPacketRecordBase")) {
            if (!Array.isArray(message.redPacketRecordBase))
                return "redPacketRecordBase: array expected";
            for (let i = 0; i < message.redPacketRecordBase.length; ++i) {
                let error = $root.RedPacketRecordBase.verify(message.redPacketRecordBase[i]);
                if (error)
                    return "redPacketRecordBase." + error;
            }
        }
        return null;
    };

    /**
     * Creates a RedPacketRecordResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof RedPacketRecordResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {RedPacketRecordResp} RedPacketRecordResp
     */
    RedPacketRecordResp.fromObject = function fromObject(object) {
        if (object instanceof $root.RedPacketRecordResp)
            return object;
        let message = new $root.RedPacketRecordResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".RedPacketRecordResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.count != null)
            message.count = object.count | 0;
        if (object.pageNum != null)
            message.pageNum = object.pageNum | 0;
        if (object.redPacketRecordBase) {
            if (!Array.isArray(object.redPacketRecordBase))
                throw TypeError(".RedPacketRecordResp.redPacketRecordBase: array expected");
            message.redPacketRecordBase = [];
            for (let i = 0; i < object.redPacketRecordBase.length; ++i) {
                if (typeof object.redPacketRecordBase[i] !== "object")
                    throw TypeError(".RedPacketRecordResp.redPacketRecordBase: object expected");
                message.redPacketRecordBase[i] = $root.RedPacketRecordBase.fromObject(object.redPacketRecordBase[i]);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a RedPacketRecordResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof RedPacketRecordResp
     * @static
     * @param {RedPacketRecordResp} message RedPacketRecordResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RedPacketRecordResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.arrays || options.defaults)
            object.redPacketRecordBase = [];
        if (options.defaults) {
            object.commonResult = null;
            object.count = 0;
            object.pageNum = 0;
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.count != null && message.hasOwnProperty("count"))
            object.count = message.count;
        if (message.pageNum != null && message.hasOwnProperty("pageNum"))
            object.pageNum = message.pageNum;
        if (message.redPacketRecordBase && message.redPacketRecordBase.length) {
            object.redPacketRecordBase = [];
            for (let j = 0; j < message.redPacketRecordBase.length; ++j)
                object.redPacketRecordBase[j] = $root.RedPacketRecordBase.toObject(message.redPacketRecordBase[j], options);
        }
        return object;
    };

    /**
     * Converts this RedPacketRecordResp to JSON.
     * @function toJSON
     * @memberof RedPacketRecordResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RedPacketRecordResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RedPacketRecordResp
     * @function getTypeUrl
     * @memberof RedPacketRecordResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RedPacketRecordResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/RedPacketRecordResp";
    };

    return RedPacketRecordResp;
})();

export const SecureCheckReq = $root.SecureCheckReq = (() => {

    /**
     * Properties of a SecureCheckReq.
     * @exports ISecureCheckReq
     * @interface ISecureCheckReq
     * @property {IClientInfo|null} [clientInfo] SecureCheckReq clientInfo
     * @property {GetValidateCodeType|null} [codeType] SecureCheckReq codeType
     * @property {string|null} [smsCode] SecureCheckReq smsCode
     * @property {string|null} [emailCode] SecureCheckReq emailCode
     * @property {string|null} [code] SecureCheckReq code
     * @property {string|null} [tradePassword] SecureCheckReq tradePassword
     */

    /**
     * Constructs a new SecureCheckReq.
     * @exports SecureCheckReq
     * @classdesc Represents a SecureCheckReq.
     * @implements ISecureCheckReq
     * @constructor
     * @param {ISecureCheckReq=} [properties] Properties to set
     */
    function SecureCheckReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * SecureCheckReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof SecureCheckReq
     * @instance
     */
    SecureCheckReq.prototype.clientInfo = null;

    /**
     * SecureCheckReq codeType.
     * @member {GetValidateCodeType} codeType
     * @memberof SecureCheckReq
     * @instance
     */
    SecureCheckReq.prototype.codeType = 0;

    /**
     * SecureCheckReq smsCode.
     * @member {string} smsCode
     * @memberof SecureCheckReq
     * @instance
     */
    SecureCheckReq.prototype.smsCode = "";

    /**
     * SecureCheckReq emailCode.
     * @member {string} emailCode
     * @memberof SecureCheckReq
     * @instance
     */
    SecureCheckReq.prototype.emailCode = "";

    /**
     * SecureCheckReq code.
     * @member {string} code
     * @memberof SecureCheckReq
     * @instance
     */
    SecureCheckReq.prototype.code = "";

    /**
     * SecureCheckReq tradePassword.
     * @member {string} tradePassword
     * @memberof SecureCheckReq
     * @instance
     */
    SecureCheckReq.prototype.tradePassword = "";

    /**
     * Creates a new SecureCheckReq instance using the specified properties.
     * @function create
     * @memberof SecureCheckReq
     * @static
     * @param {ISecureCheckReq=} [properties] Properties to set
     * @returns {SecureCheckReq} SecureCheckReq instance
     */
    SecureCheckReq.create = function create(properties) {
        return new SecureCheckReq(properties);
    };

    /**
     * Encodes the specified SecureCheckReq message. Does not implicitly {@link SecureCheckReq.verify|verify} messages.
     * @function encode
     * @memberof SecureCheckReq
     * @static
     * @param {ISecureCheckReq} message SecureCheckReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SecureCheckReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.codeType != null && Object.hasOwnProperty.call(message, "codeType"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.codeType);
        if (message.smsCode != null && Object.hasOwnProperty.call(message, "smsCode"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.smsCode);
        if (message.emailCode != null && Object.hasOwnProperty.call(message, "emailCode"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.emailCode);
        if (message.code != null && Object.hasOwnProperty.call(message, "code"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.code);
        if (message.tradePassword != null && Object.hasOwnProperty.call(message, "tradePassword"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.tradePassword);
        return writer;
    };

    /**
     * Encodes the specified SecureCheckReq message, length delimited. Does not implicitly {@link SecureCheckReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof SecureCheckReq
     * @static
     * @param {ISecureCheckReq} message SecureCheckReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SecureCheckReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a SecureCheckReq message from the specified reader or buffer.
     * @function decode
     * @memberof SecureCheckReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {SecureCheckReq} SecureCheckReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SecureCheckReq.decode = function decode(reader, length, error) {
    SecureCheckReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.SecureCheckReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.codeType = reader.int32();
                    break;
                }
            case 3: {
                    message.smsCode = reader.string();
                    break;
                }
            case 4: {
                    message.emailCode = reader.string();
                    break;
                }
            case 5: {
                    message.code = reader.string();
                    break;
                }
            case 6: {
                    message.tradePassword = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a SecureCheckReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof SecureCheckReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {SecureCheckReq} SecureCheckReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SecureCheckReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SecureCheckReq message.
     * @function verify
     * @memberof SecureCheckReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SecureCheckReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.codeType != null && message.hasOwnProperty("codeType"))
            switch (message.codeType) {
            default:
                return "codeType: enum value expected";
            case 0:
            case 1:
            case 2:
            case 3:
            case 4:
            case 6:
            case 7:
            case 8:
            case 9:
            case 10:
            case 11:
            case 12:
            case 13:
            case 14:
            case 15:
            case 16:
            case 17:
            case 18:
            case 19:
            case 20:
            case 21:
            case 22:
            case 23:
            case 23:
                break;
            }
        if (message.smsCode != null && message.hasOwnProperty("smsCode"))
            if (!$util.isString(message.smsCode))
                return "smsCode: string expected";
        if (message.emailCode != null && message.hasOwnProperty("emailCode"))
            if (!$util.isString(message.emailCode))
                return "emailCode: string expected";
        if (message.code != null && message.hasOwnProperty("code"))
            if (!$util.isString(message.code))
                return "code: string expected";
        if (message.tradePassword != null && message.hasOwnProperty("tradePassword"))
            if (!$util.isString(message.tradePassword))
                return "tradePassword: string expected";
        return null;
    };

    /**
     * Creates a SecureCheckReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof SecureCheckReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {SecureCheckReq} SecureCheckReq
     */
    SecureCheckReq.fromObject = function fromObject(object) {
        if (object instanceof $root.SecureCheckReq)
            return object;
        let message = new $root.SecureCheckReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".SecureCheckReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        switch (object.codeType) {
        default:
            if (typeof object.codeType === "number") {
                message.codeType = object.codeType;
                break;
            }
            break;
        case "REG":
        case 0:
            message.codeType = 0;
            break;
        case "LOGIN":
        case 1:
            message.codeType = 1;
            break;
        case "FIND_PASSWORD":
        case 2:
            message.codeType = 2;
            break;
        case "UPDATE_PASSWORD":
        case 3:
            message.codeType = 3;
            break;
        case "UPDATE_PHONE":
        case 4:
            message.codeType = 4;
            break;
        case "VALIDATE_PASSWORD":
        case 6:
            message.codeType = 6;
            break;
        case "FIND_GESTURE_PASSWORD":
        case 7:
            message.codeType = 7;
            break;
        case "TRADE_PASSWORD":
        case 8:
            message.codeType = 8;
            break;
        case "UPDATE_TRADE_PASSWORD":
        case 9:
            message.codeType = 9;
            break;
        case "BIND_PHONE":
        case 10:
            message.codeType = 10;
            break;
        case "BIND_EMAIL":
        case 11:
            message.codeType = 11;
            break;
        case "UPDATE_EMAIL":
        case 12:
            message.codeType = 12;
            break;
        case "VALIDATE_GOOGLE_AUTH":
        case 13:
            message.codeType = 13;
            break;
        case "UPDATE_SECURE":
        case 14:
            message.codeType = 14;
            break;
        case "CASH_OUT":
        case 15:
            message.codeType = 15;
            break;
        case "VERIFY_EMAIL":
        case 16:
            message.codeType = 16;
            break;
        case "VERIFY_PHONE":
        case 17:
            message.codeType = 17;
            break;
        case "VERIFY_TRADE_PASSWORD":
        case 18:
            message.codeType = 18;
            break;
        case "VERIFY_GOOGLE_CODE":
        case 19:
            message.codeType = 19;
            break;
        case "BIND_ACCOUNT":
        case 20:
            message.codeType = 20;
            break;
        case "VERIFY_LOGIN_PHONE":
        case 21:
            message.codeType = 21;
            break;
        case "VERIFY_LOGIN_EMAIL":
        case 22:
            message.codeType = 22;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.codeType = 23;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.codeType = 23;
            break;
        }
        if (object.smsCode != null)
            message.smsCode = String(object.smsCode);
        if (object.emailCode != null)
            message.emailCode = String(object.emailCode);
        if (object.code != null)
            message.code = String(object.code);
        if (object.tradePassword != null)
            message.tradePassword = String(object.tradePassword);
        return message;
    };

    /**
     * Creates a plain object from a SecureCheckReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof SecureCheckReq
     * @static
     * @param {SecureCheckReq} message SecureCheckReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SecureCheckReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.codeType = options.enums === String ? "REG" : 0;
            object.smsCode = "";
            object.emailCode = "";
            object.code = "";
            object.tradePassword = "";
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.codeType != null && message.hasOwnProperty("codeType"))
            object.codeType = options.enums === String ? $root.GetValidateCodeType[message.codeType] === undefined ? message.codeType : $root.GetValidateCodeType[message.codeType] : message.codeType;
        if (message.smsCode != null && message.hasOwnProperty("smsCode"))
            object.smsCode = message.smsCode;
        if (message.emailCode != null && message.hasOwnProperty("emailCode"))
            object.emailCode = message.emailCode;
        if (message.code != null && message.hasOwnProperty("code"))
            object.code = message.code;
        if (message.tradePassword != null && message.hasOwnProperty("tradePassword"))
            object.tradePassword = message.tradePassword;
        return object;
    };

    /**
     * Converts this SecureCheckReq to JSON.
     * @function toJSON
     * @memberof SecureCheckReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SecureCheckReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SecureCheckReq
     * @function getTypeUrl
     * @memberof SecureCheckReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SecureCheckReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/SecureCheckReq";
    };

    return SecureCheckReq;
})();

export const SecureCheckResp = $root.SecureCheckResp = (() => {

    /**
     * Properties of a SecureCheckResp.
     * @exports ISecureCheckResp
     * @interface ISecureCheckResp
     * @property {ICommonResult|null} [commonResult] SecureCheckResp commonResult
     * @property {string|null} [secureCode] SecureCheckResp secureCode
     */

    /**
     * Constructs a new SecureCheckResp.
     * @exports SecureCheckResp
     * @classdesc Represents a SecureCheckResp.
     * @implements ISecureCheckResp
     * @constructor
     * @param {ISecureCheckResp=} [properties] Properties to set
     */
    function SecureCheckResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * SecureCheckResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof SecureCheckResp
     * @instance
     */
    SecureCheckResp.prototype.commonResult = null;

    /**
     * SecureCheckResp secureCode.
     * @member {string} secureCode
     * @memberof SecureCheckResp
     * @instance
     */
    SecureCheckResp.prototype.secureCode = "";

    /**
     * Creates a new SecureCheckResp instance using the specified properties.
     * @function create
     * @memberof SecureCheckResp
     * @static
     * @param {ISecureCheckResp=} [properties] Properties to set
     * @returns {SecureCheckResp} SecureCheckResp instance
     */
    SecureCheckResp.create = function create(properties) {
        return new SecureCheckResp(properties);
    };

    /**
     * Encodes the specified SecureCheckResp message. Does not implicitly {@link SecureCheckResp.verify|verify} messages.
     * @function encode
     * @memberof SecureCheckResp
     * @static
     * @param {ISecureCheckResp} message SecureCheckResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SecureCheckResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.secureCode != null && Object.hasOwnProperty.call(message, "secureCode"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.secureCode);
        return writer;
    };

    /**
     * Encodes the specified SecureCheckResp message, length delimited. Does not implicitly {@link SecureCheckResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof SecureCheckResp
     * @static
     * @param {ISecureCheckResp} message SecureCheckResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SecureCheckResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a SecureCheckResp message from the specified reader or buffer.
     * @function decode
     * @memberof SecureCheckResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {SecureCheckResp} SecureCheckResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SecureCheckResp.decode = function decode(reader, length, error) {
    SecureCheckResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.SecureCheckResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.secureCode = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a SecureCheckResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof SecureCheckResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {SecureCheckResp} SecureCheckResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SecureCheckResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SecureCheckResp message.
     * @function verify
     * @memberof SecureCheckResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SecureCheckResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.secureCode != null && message.hasOwnProperty("secureCode"))
            if (!$util.isString(message.secureCode))
                return "secureCode: string expected";
        return null;
    };

    /**
     * Creates a SecureCheckResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof SecureCheckResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {SecureCheckResp} SecureCheckResp
     */
    SecureCheckResp.fromObject = function fromObject(object) {
        if (object instanceof $root.SecureCheckResp)
            return object;
        let message = new $root.SecureCheckResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".SecureCheckResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.secureCode != null)
            message.secureCode = String(object.secureCode);
        return message;
    };

    /**
     * Creates a plain object from a SecureCheckResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof SecureCheckResp
     * @static
     * @param {SecureCheckResp} message SecureCheckResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SecureCheckResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.secureCode = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.secureCode != null && message.hasOwnProperty("secureCode"))
            object.secureCode = message.secureCode;
        return object;
    };

    /**
     * Converts this SecureCheckResp to JSON.
     * @function toJSON
     * @memberof SecureCheckResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SecureCheckResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SecureCheckResp
     * @function getTypeUrl
     * @memberof SecureCheckResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SecureCheckResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/SecureCheckResp";
    };

    return SecureCheckResp;
})();

export const UpdateSecureCheckReq = $root.UpdateSecureCheckReq = (() => {

    /**
     * Properties of an UpdateSecureCheckReq.
     * @exports IUpdateSecureCheckReq
     * @interface IUpdateSecureCheckReq
     * @property {IClientInfo|null} [clientInfo] UpdateSecureCheckReq clientInfo
     * @property {GetValidateCodeType|null} [codeType] UpdateSecureCheckReq codeType
     * @property {string|null} [smsCode] UpdateSecureCheckReq smsCode
     * @property {string|null} [emailCode] UpdateSecureCheckReq emailCode
     * @property {string|null} [code] UpdateSecureCheckReq code
     * @property {string|null} [tradePassword] UpdateSecureCheckReq tradePassword
     * @property {string|null} [updateType] UpdateSecureCheckReq updateType
     */

    /**
     * Constructs a new UpdateSecureCheckReq.
     * @exports UpdateSecureCheckReq
     * @classdesc Represents an UpdateSecureCheckReq.
     * @implements IUpdateSecureCheckReq
     * @constructor
     * @param {IUpdateSecureCheckReq=} [properties] Properties to set
     */
    function UpdateSecureCheckReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * UpdateSecureCheckReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof UpdateSecureCheckReq
     * @instance
     */
    UpdateSecureCheckReq.prototype.clientInfo = null;

    /**
     * UpdateSecureCheckReq codeType.
     * @member {GetValidateCodeType} codeType
     * @memberof UpdateSecureCheckReq
     * @instance
     */
    UpdateSecureCheckReq.prototype.codeType = 0;

    /**
     * UpdateSecureCheckReq smsCode.
     * @member {string} smsCode
     * @memberof UpdateSecureCheckReq
     * @instance
     */
    UpdateSecureCheckReq.prototype.smsCode = "";

    /**
     * UpdateSecureCheckReq emailCode.
     * @member {string} emailCode
     * @memberof UpdateSecureCheckReq
     * @instance
     */
    UpdateSecureCheckReq.prototype.emailCode = "";

    /**
     * UpdateSecureCheckReq code.
     * @member {string} code
     * @memberof UpdateSecureCheckReq
     * @instance
     */
    UpdateSecureCheckReq.prototype.code = "";

    /**
     * UpdateSecureCheckReq tradePassword.
     * @member {string} tradePassword
     * @memberof UpdateSecureCheckReq
     * @instance
     */
    UpdateSecureCheckReq.prototype.tradePassword = "";

    /**
     * UpdateSecureCheckReq updateType.
     * @member {string} updateType
     * @memberof UpdateSecureCheckReq
     * @instance
     */
    UpdateSecureCheckReq.prototype.updateType = "";

    /**
     * Creates a new UpdateSecureCheckReq instance using the specified properties.
     * @function create
     * @memberof UpdateSecureCheckReq
     * @static
     * @param {IUpdateSecureCheckReq=} [properties] Properties to set
     * @returns {UpdateSecureCheckReq} UpdateSecureCheckReq instance
     */
    UpdateSecureCheckReq.create = function create(properties) {
        return new UpdateSecureCheckReq(properties);
    };

    /**
     * Encodes the specified UpdateSecureCheckReq message. Does not implicitly {@link UpdateSecureCheckReq.verify|verify} messages.
     * @function encode
     * @memberof UpdateSecureCheckReq
     * @static
     * @param {IUpdateSecureCheckReq} message UpdateSecureCheckReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateSecureCheckReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.codeType != null && Object.hasOwnProperty.call(message, "codeType"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.codeType);
        if (message.smsCode != null && Object.hasOwnProperty.call(message, "smsCode"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.smsCode);
        if (message.emailCode != null && Object.hasOwnProperty.call(message, "emailCode"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.emailCode);
        if (message.code != null && Object.hasOwnProperty.call(message, "code"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.code);
        if (message.tradePassword != null && Object.hasOwnProperty.call(message, "tradePassword"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.tradePassword);
        if (message.updateType != null && Object.hasOwnProperty.call(message, "updateType"))
            writer.uint32(/* id 7, wireType 2 =*/58).string(message.updateType);
        return writer;
    };

    /**
     * Encodes the specified UpdateSecureCheckReq message, length delimited. Does not implicitly {@link UpdateSecureCheckReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof UpdateSecureCheckReq
     * @static
     * @param {IUpdateSecureCheckReq} message UpdateSecureCheckReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateSecureCheckReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an UpdateSecureCheckReq message from the specified reader or buffer.
     * @function decode
     * @memberof UpdateSecureCheckReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {UpdateSecureCheckReq} UpdateSecureCheckReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateSecureCheckReq.decode = function decode(reader, length, error) {
    UpdateSecureCheckReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.UpdateSecureCheckReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.codeType = reader.int32();
                    break;
                }
            case 3: {
                    message.smsCode = reader.string();
                    break;
                }
            case 4: {
                    message.emailCode = reader.string();
                    break;
                }
            case 5: {
                    message.code = reader.string();
                    break;
                }
            case 6: {
                    message.tradePassword = reader.string();
                    break;
                }
            case 7: {
                    message.updateType = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an UpdateSecureCheckReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof UpdateSecureCheckReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {UpdateSecureCheckReq} UpdateSecureCheckReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateSecureCheckReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an UpdateSecureCheckReq message.
     * @function verify
     * @memberof UpdateSecureCheckReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    UpdateSecureCheckReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.codeType != null && message.hasOwnProperty("codeType"))
            switch (message.codeType) {
            default:
                return "codeType: enum value expected";
            case 0:
            case 1:
            case 2:
            case 3:
            case 4:
            case 6:
            case 7:
            case 8:
            case 9:
            case 10:
            case 11:
            case 12:
            case 13:
            case 14:
            case 15:
            case 16:
            case 17:
            case 18:
            case 19:
            case 20:
            case 21:
            case 22:
            case 23:
            case 23:
                break;
            }
        if (message.smsCode != null && message.hasOwnProperty("smsCode"))
            if (!$util.isString(message.smsCode))
                return "smsCode: string expected";
        if (message.emailCode != null && message.hasOwnProperty("emailCode"))
            if (!$util.isString(message.emailCode))
                return "emailCode: string expected";
        if (message.code != null && message.hasOwnProperty("code"))
            if (!$util.isString(message.code))
                return "code: string expected";
        if (message.tradePassword != null && message.hasOwnProperty("tradePassword"))
            if (!$util.isString(message.tradePassword))
                return "tradePassword: string expected";
        if (message.updateType != null && message.hasOwnProperty("updateType"))
            if (!$util.isString(message.updateType))
                return "updateType: string expected";
        return null;
    };

    /**
     * Creates an UpdateSecureCheckReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof UpdateSecureCheckReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {UpdateSecureCheckReq} UpdateSecureCheckReq
     */
    UpdateSecureCheckReq.fromObject = function fromObject(object) {
        if (object instanceof $root.UpdateSecureCheckReq)
            return object;
        let message = new $root.UpdateSecureCheckReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".UpdateSecureCheckReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        switch (object.codeType) {
        default:
            if (typeof object.codeType === "number") {
                message.codeType = object.codeType;
                break;
            }
            break;
        case "REG":
        case 0:
            message.codeType = 0;
            break;
        case "LOGIN":
        case 1:
            message.codeType = 1;
            break;
        case "FIND_PASSWORD":
        case 2:
            message.codeType = 2;
            break;
        case "UPDATE_PASSWORD":
        case 3:
            message.codeType = 3;
            break;
        case "UPDATE_PHONE":
        case 4:
            message.codeType = 4;
            break;
        case "VALIDATE_PASSWORD":
        case 6:
            message.codeType = 6;
            break;
        case "FIND_GESTURE_PASSWORD":
        case 7:
            message.codeType = 7;
            break;
        case "TRADE_PASSWORD":
        case 8:
            message.codeType = 8;
            break;
        case "UPDATE_TRADE_PASSWORD":
        case 9:
            message.codeType = 9;
            break;
        case "BIND_PHONE":
        case 10:
            message.codeType = 10;
            break;
        case "BIND_EMAIL":
        case 11:
            message.codeType = 11;
            break;
        case "UPDATE_EMAIL":
        case 12:
            message.codeType = 12;
            break;
        case "VALIDATE_GOOGLE_AUTH":
        case 13:
            message.codeType = 13;
            break;
        case "UPDATE_SECURE":
        case 14:
            message.codeType = 14;
            break;
        case "CASH_OUT":
        case 15:
            message.codeType = 15;
            break;
        case "VERIFY_EMAIL":
        case 16:
            message.codeType = 16;
            break;
        case "VERIFY_PHONE":
        case 17:
            message.codeType = 17;
            break;
        case "VERIFY_TRADE_PASSWORD":
        case 18:
            message.codeType = 18;
            break;
        case "VERIFY_GOOGLE_CODE":
        case 19:
            message.codeType = 19;
            break;
        case "BIND_ACCOUNT":
        case 20:
            message.codeType = 20;
            break;
        case "VERIFY_LOGIN_PHONE":
        case 21:
            message.codeType = 21;
            break;
        case "VERIFY_LOGIN_EMAIL":
        case 22:
            message.codeType = 22;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.codeType = 23;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.codeType = 23;
            break;
        }
        if (object.smsCode != null)
            message.smsCode = String(object.smsCode);
        if (object.emailCode != null)
            message.emailCode = String(object.emailCode);
        if (object.code != null)
            message.code = String(object.code);
        if (object.tradePassword != null)
            message.tradePassword = String(object.tradePassword);
        if (object.updateType != null)
            message.updateType = String(object.updateType);
        return message;
    };

    /**
     * Creates a plain object from an UpdateSecureCheckReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof UpdateSecureCheckReq
     * @static
     * @param {UpdateSecureCheckReq} message UpdateSecureCheckReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    UpdateSecureCheckReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.codeType = options.enums === String ? "REG" : 0;
            object.smsCode = "";
            object.emailCode = "";
            object.code = "";
            object.tradePassword = "";
            object.updateType = "";
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.codeType != null && message.hasOwnProperty("codeType"))
            object.codeType = options.enums === String ? $root.GetValidateCodeType[message.codeType] === undefined ? message.codeType : $root.GetValidateCodeType[message.codeType] : message.codeType;
        if (message.smsCode != null && message.hasOwnProperty("smsCode"))
            object.smsCode = message.smsCode;
        if (message.emailCode != null && message.hasOwnProperty("emailCode"))
            object.emailCode = message.emailCode;
        if (message.code != null && message.hasOwnProperty("code"))
            object.code = message.code;
        if (message.tradePassword != null && message.hasOwnProperty("tradePassword"))
            object.tradePassword = message.tradePassword;
        if (message.updateType != null && message.hasOwnProperty("updateType"))
            object.updateType = message.updateType;
        return object;
    };

    /**
     * Converts this UpdateSecureCheckReq to JSON.
     * @function toJSON
     * @memberof UpdateSecureCheckReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    UpdateSecureCheckReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for UpdateSecureCheckReq
     * @function getTypeUrl
     * @memberof UpdateSecureCheckReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    UpdateSecureCheckReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/UpdateSecureCheckReq";
    };

    return UpdateSecureCheckReq;
})();

export const UpdateSecureCheckResp = $root.UpdateSecureCheckResp = (() => {

    /**
     * Properties of an UpdateSecureCheckResp.
     * @exports IUpdateSecureCheckResp
     * @interface IUpdateSecureCheckResp
     * @property {ICommonResult|null} [commonResult] UpdateSecureCheckResp commonResult
     * @property {string|null} [secureCode] UpdateSecureCheckResp secureCode
     */

    /**
     * Constructs a new UpdateSecureCheckResp.
     * @exports UpdateSecureCheckResp
     * @classdesc Represents an UpdateSecureCheckResp.
     * @implements IUpdateSecureCheckResp
     * @constructor
     * @param {IUpdateSecureCheckResp=} [properties] Properties to set
     */
    function UpdateSecureCheckResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * UpdateSecureCheckResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof UpdateSecureCheckResp
     * @instance
     */
    UpdateSecureCheckResp.prototype.commonResult = null;

    /**
     * UpdateSecureCheckResp secureCode.
     * @member {string} secureCode
     * @memberof UpdateSecureCheckResp
     * @instance
     */
    UpdateSecureCheckResp.prototype.secureCode = "";

    /**
     * Creates a new UpdateSecureCheckResp instance using the specified properties.
     * @function create
     * @memberof UpdateSecureCheckResp
     * @static
     * @param {IUpdateSecureCheckResp=} [properties] Properties to set
     * @returns {UpdateSecureCheckResp} UpdateSecureCheckResp instance
     */
    UpdateSecureCheckResp.create = function create(properties) {
        return new UpdateSecureCheckResp(properties);
    };

    /**
     * Encodes the specified UpdateSecureCheckResp message. Does not implicitly {@link UpdateSecureCheckResp.verify|verify} messages.
     * @function encode
     * @memberof UpdateSecureCheckResp
     * @static
     * @param {IUpdateSecureCheckResp} message UpdateSecureCheckResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateSecureCheckResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.secureCode != null && Object.hasOwnProperty.call(message, "secureCode"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.secureCode);
        return writer;
    };

    /**
     * Encodes the specified UpdateSecureCheckResp message, length delimited. Does not implicitly {@link UpdateSecureCheckResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof UpdateSecureCheckResp
     * @static
     * @param {IUpdateSecureCheckResp} message UpdateSecureCheckResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateSecureCheckResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an UpdateSecureCheckResp message from the specified reader or buffer.
     * @function decode
     * @memberof UpdateSecureCheckResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {UpdateSecureCheckResp} UpdateSecureCheckResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateSecureCheckResp.decode = function decode(reader, length, error) {
    UpdateSecureCheckResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.UpdateSecureCheckResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.secureCode = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an UpdateSecureCheckResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof UpdateSecureCheckResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {UpdateSecureCheckResp} UpdateSecureCheckResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateSecureCheckResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an UpdateSecureCheckResp message.
     * @function verify
     * @memberof UpdateSecureCheckResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    UpdateSecureCheckResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.secureCode != null && message.hasOwnProperty("secureCode"))
            if (!$util.isString(message.secureCode))
                return "secureCode: string expected";
        return null;
    };

    /**
     * Creates an UpdateSecureCheckResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof UpdateSecureCheckResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {UpdateSecureCheckResp} UpdateSecureCheckResp
     */
    UpdateSecureCheckResp.fromObject = function fromObject(object) {
        if (object instanceof $root.UpdateSecureCheckResp)
            return object;
        let message = new $root.UpdateSecureCheckResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".UpdateSecureCheckResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.secureCode != null)
            message.secureCode = String(object.secureCode);
        return message;
    };

    /**
     * Creates a plain object from an UpdateSecureCheckResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof UpdateSecureCheckResp
     * @static
     * @param {UpdateSecureCheckResp} message UpdateSecureCheckResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    UpdateSecureCheckResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.secureCode = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.secureCode != null && message.hasOwnProperty("secureCode"))
            object.secureCode = message.secureCode;
        return object;
    };

    /**
     * Converts this UpdateSecureCheckResp to JSON.
     * @function toJSON
     * @memberof UpdateSecureCheckResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    UpdateSecureCheckResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for UpdateSecureCheckResp
     * @function getTypeUrl
     * @memberof UpdateSecureCheckResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    UpdateSecureCheckResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/UpdateSecureCheckResp";
    };

    return UpdateSecureCheckResp;
})();

export const UpdateSecureReq = $root.UpdateSecureReq = (() => {

    /**
     * Properties of an UpdateSecureReq.
     * @exports IUpdateSecureReq
     * @interface IUpdateSecureReq
     * @property {IClientInfo|null} [clientInfo] UpdateSecureReq clientInfo
     * @property {string|null} [account] UpdateSecureReq account
     * @property {string|null} [code] UpdateSecureReq code
     * @property {string|null} [idPhoto1] UpdateSecureReq idPhoto1
     * @property {string|null} [idPhoto2] UpdateSecureReq idPhoto2
     * @property {string|null} [updateType] UpdateSecureReq updateType
     * @property {string|null} [secureCode] UpdateSecureReq secureCode
     */

    /**
     * Constructs a new UpdateSecureReq.
     * @exports UpdateSecureReq
     * @classdesc Represents an UpdateSecureReq.
     * @implements IUpdateSecureReq
     * @constructor
     * @param {IUpdateSecureReq=} [properties] Properties to set
     */
    function UpdateSecureReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * UpdateSecureReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof UpdateSecureReq
     * @instance
     */
    UpdateSecureReq.prototype.clientInfo = null;

    /**
     * UpdateSecureReq account.
     * @member {string} account
     * @memberof UpdateSecureReq
     * @instance
     */
    UpdateSecureReq.prototype.account = "";

    /**
     * UpdateSecureReq code.
     * @member {string} code
     * @memberof UpdateSecureReq
     * @instance
     */
    UpdateSecureReq.prototype.code = "";

    /**
     * UpdateSecureReq idPhoto1.
     * @member {string} idPhoto1
     * @memberof UpdateSecureReq
     * @instance
     */
    UpdateSecureReq.prototype.idPhoto1 = "";

    /**
     * UpdateSecureReq idPhoto2.
     * @member {string} idPhoto2
     * @memberof UpdateSecureReq
     * @instance
     */
    UpdateSecureReq.prototype.idPhoto2 = "";

    /**
     * UpdateSecureReq updateType.
     * @member {string} updateType
     * @memberof UpdateSecureReq
     * @instance
     */
    UpdateSecureReq.prototype.updateType = "";

    /**
     * UpdateSecureReq secureCode.
     * @member {string} secureCode
     * @memberof UpdateSecureReq
     * @instance
     */
    UpdateSecureReq.prototype.secureCode = "";

    /**
     * Creates a new UpdateSecureReq instance using the specified properties.
     * @function create
     * @memberof UpdateSecureReq
     * @static
     * @param {IUpdateSecureReq=} [properties] Properties to set
     * @returns {UpdateSecureReq} UpdateSecureReq instance
     */
    UpdateSecureReq.create = function create(properties) {
        return new UpdateSecureReq(properties);
    };

    /**
     * Encodes the specified UpdateSecureReq message. Does not implicitly {@link UpdateSecureReq.verify|verify} messages.
     * @function encode
     * @memberof UpdateSecureReq
     * @static
     * @param {IUpdateSecureReq} message UpdateSecureReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateSecureReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.account != null && Object.hasOwnProperty.call(message, "account"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.account);
        if (message.code != null && Object.hasOwnProperty.call(message, "code"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.code);
        if (message.idPhoto1 != null && Object.hasOwnProperty.call(message, "idPhoto1"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.idPhoto1);
        if (message.idPhoto2 != null && Object.hasOwnProperty.call(message, "idPhoto2"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.idPhoto2);
        if (message.updateType != null && Object.hasOwnProperty.call(message, "updateType"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.updateType);
        if (message.secureCode != null && Object.hasOwnProperty.call(message, "secureCode"))
            writer.uint32(/* id 7, wireType 2 =*/58).string(message.secureCode);
        return writer;
    };

    /**
     * Encodes the specified UpdateSecureReq message, length delimited. Does not implicitly {@link UpdateSecureReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof UpdateSecureReq
     * @static
     * @param {IUpdateSecureReq} message UpdateSecureReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateSecureReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an UpdateSecureReq message from the specified reader or buffer.
     * @function decode
     * @memberof UpdateSecureReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {UpdateSecureReq} UpdateSecureReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateSecureReq.decode = function decode(reader, length, error) {
    UpdateSecureReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.UpdateSecureReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.account = reader.string();
                    break;
                }
            case 3: {
                    message.code = reader.string();
                    break;
                }
            case 4: {
                    message.idPhoto1 = reader.string();
                    break;
                }
            case 5: {
                    message.idPhoto2 = reader.string();
                    break;
                }
            case 6: {
                    message.updateType = reader.string();
                    break;
                }
            case 7: {
                    message.secureCode = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an UpdateSecureReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof UpdateSecureReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {UpdateSecureReq} UpdateSecureReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateSecureReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an UpdateSecureReq message.
     * @function verify
     * @memberof UpdateSecureReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    UpdateSecureReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.account != null && message.hasOwnProperty("account"))
            if (!$util.isString(message.account))
                return "account: string expected";
        if (message.code != null && message.hasOwnProperty("code"))
            if (!$util.isString(message.code))
                return "code: string expected";
        if (message.idPhoto1 != null && message.hasOwnProperty("idPhoto1"))
            if (!$util.isString(message.idPhoto1))
                return "idPhoto1: string expected";
        if (message.idPhoto2 != null && message.hasOwnProperty("idPhoto2"))
            if (!$util.isString(message.idPhoto2))
                return "idPhoto2: string expected";
        if (message.updateType != null && message.hasOwnProperty("updateType"))
            if (!$util.isString(message.updateType))
                return "updateType: string expected";
        if (message.secureCode != null && message.hasOwnProperty("secureCode"))
            if (!$util.isString(message.secureCode))
                return "secureCode: string expected";
        return null;
    };

    /**
     * Creates an UpdateSecureReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof UpdateSecureReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {UpdateSecureReq} UpdateSecureReq
     */
    UpdateSecureReq.fromObject = function fromObject(object) {
        if (object instanceof $root.UpdateSecureReq)
            return object;
        let message = new $root.UpdateSecureReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".UpdateSecureReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.account != null)
            message.account = String(object.account);
        if (object.code != null)
            message.code = String(object.code);
        if (object.idPhoto1 != null)
            message.idPhoto1 = String(object.idPhoto1);
        if (object.idPhoto2 != null)
            message.idPhoto2 = String(object.idPhoto2);
        if (object.updateType != null)
            message.updateType = String(object.updateType);
        if (object.secureCode != null)
            message.secureCode = String(object.secureCode);
        return message;
    };

    /**
     * Creates a plain object from an UpdateSecureReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof UpdateSecureReq
     * @static
     * @param {UpdateSecureReq} message UpdateSecureReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    UpdateSecureReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.account = "";
            object.code = "";
            object.idPhoto1 = "";
            object.idPhoto2 = "";
            object.updateType = "";
            object.secureCode = "";
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.account != null && message.hasOwnProperty("account"))
            object.account = message.account;
        if (message.code != null && message.hasOwnProperty("code"))
            object.code = message.code;
        if (message.idPhoto1 != null && message.hasOwnProperty("idPhoto1"))
            object.idPhoto1 = message.idPhoto1;
        if (message.idPhoto2 != null && message.hasOwnProperty("idPhoto2"))
            object.idPhoto2 = message.idPhoto2;
        if (message.updateType != null && message.hasOwnProperty("updateType"))
            object.updateType = message.updateType;
        if (message.secureCode != null && message.hasOwnProperty("secureCode"))
            object.secureCode = message.secureCode;
        return object;
    };

    /**
     * Converts this UpdateSecureReq to JSON.
     * @function toJSON
     * @memberof UpdateSecureReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    UpdateSecureReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for UpdateSecureReq
     * @function getTypeUrl
     * @memberof UpdateSecureReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    UpdateSecureReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/UpdateSecureReq";
    };

    return UpdateSecureReq;
})();

export const UpdateSecureResp = $root.UpdateSecureResp = (() => {

    /**
     * Properties of an UpdateSecureResp.
     * @exports IUpdateSecureResp
     * @interface IUpdateSecureResp
     * @property {ICommonResult|null} [commonResult] UpdateSecureResp commonResult
     */

    /**
     * Constructs a new UpdateSecureResp.
     * @exports UpdateSecureResp
     * @classdesc Represents an UpdateSecureResp.
     * @implements IUpdateSecureResp
     * @constructor
     * @param {IUpdateSecureResp=} [properties] Properties to set
     */
    function UpdateSecureResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * UpdateSecureResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof UpdateSecureResp
     * @instance
     */
    UpdateSecureResp.prototype.commonResult = null;

    /**
     * Creates a new UpdateSecureResp instance using the specified properties.
     * @function create
     * @memberof UpdateSecureResp
     * @static
     * @param {IUpdateSecureResp=} [properties] Properties to set
     * @returns {UpdateSecureResp} UpdateSecureResp instance
     */
    UpdateSecureResp.create = function create(properties) {
        return new UpdateSecureResp(properties);
    };

    /**
     * Encodes the specified UpdateSecureResp message. Does not implicitly {@link UpdateSecureResp.verify|verify} messages.
     * @function encode
     * @memberof UpdateSecureResp
     * @static
     * @param {IUpdateSecureResp} message UpdateSecureResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateSecureResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified UpdateSecureResp message, length delimited. Does not implicitly {@link UpdateSecureResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof UpdateSecureResp
     * @static
     * @param {IUpdateSecureResp} message UpdateSecureResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UpdateSecureResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an UpdateSecureResp message from the specified reader or buffer.
     * @function decode
     * @memberof UpdateSecureResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {UpdateSecureResp} UpdateSecureResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateSecureResp.decode = function decode(reader, length, error) {
    UpdateSecureResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.UpdateSecureResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an UpdateSecureResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof UpdateSecureResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {UpdateSecureResp} UpdateSecureResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UpdateSecureResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an UpdateSecureResp message.
     * @function verify
     * @memberof UpdateSecureResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    UpdateSecureResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates an UpdateSecureResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof UpdateSecureResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {UpdateSecureResp} UpdateSecureResp
     */
    UpdateSecureResp.fromObject = function fromObject(object) {
        if (object instanceof $root.UpdateSecureResp)
            return object;
        let message = new $root.UpdateSecureResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".UpdateSecureResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from an UpdateSecureResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof UpdateSecureResp
     * @static
     * @param {UpdateSecureResp} message UpdateSecureResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    UpdateSecureResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this UpdateSecureResp to JSON.
     * @function toJSON
     * @memberof UpdateSecureResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    UpdateSecureResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for UpdateSecureResp
     * @function getTypeUrl
     * @memberof UpdateSecureResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    UpdateSecureResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/UpdateSecureResp";
    };

    return UpdateSecureResp;
})();

export const GetGoogleDownloadUrlReq = $root.GetGoogleDownloadUrlReq = (() => {

    /**
     * Properties of a GetGoogleDownloadUrlReq.
     * @exports IGetGoogleDownloadUrlReq
     * @interface IGetGoogleDownloadUrlReq
     * @property {IClientInfo|null} [clientInfo] GetGoogleDownloadUrlReq clientInfo
     */

    /**
     * Constructs a new GetGoogleDownloadUrlReq.
     * @exports GetGoogleDownloadUrlReq
     * @classdesc Represents a GetGoogleDownloadUrlReq.
     * @implements IGetGoogleDownloadUrlReq
     * @constructor
     * @param {IGetGoogleDownloadUrlReq=} [properties] Properties to set
     */
    function GetGoogleDownloadUrlReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetGoogleDownloadUrlReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GetGoogleDownloadUrlReq
     * @instance
     */
    GetGoogleDownloadUrlReq.prototype.clientInfo = null;

    /**
     * Creates a new GetGoogleDownloadUrlReq instance using the specified properties.
     * @function create
     * @memberof GetGoogleDownloadUrlReq
     * @static
     * @param {IGetGoogleDownloadUrlReq=} [properties] Properties to set
     * @returns {GetGoogleDownloadUrlReq} GetGoogleDownloadUrlReq instance
     */
    GetGoogleDownloadUrlReq.create = function create(properties) {
        return new GetGoogleDownloadUrlReq(properties);
    };

    /**
     * Encodes the specified GetGoogleDownloadUrlReq message. Does not implicitly {@link GetGoogleDownloadUrlReq.verify|verify} messages.
     * @function encode
     * @memberof GetGoogleDownloadUrlReq
     * @static
     * @param {IGetGoogleDownloadUrlReq} message GetGoogleDownloadUrlReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetGoogleDownloadUrlReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified GetGoogleDownloadUrlReq message, length delimited. Does not implicitly {@link GetGoogleDownloadUrlReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetGoogleDownloadUrlReq
     * @static
     * @param {IGetGoogleDownloadUrlReq} message GetGoogleDownloadUrlReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetGoogleDownloadUrlReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetGoogleDownloadUrlReq message from the specified reader or buffer.
     * @function decode
     * @memberof GetGoogleDownloadUrlReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetGoogleDownloadUrlReq} GetGoogleDownloadUrlReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetGoogleDownloadUrlReq.decode = function decode(reader, length, error) {
    GetGoogleDownloadUrlReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetGoogleDownloadUrlReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetGoogleDownloadUrlReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetGoogleDownloadUrlReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetGoogleDownloadUrlReq} GetGoogleDownloadUrlReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetGoogleDownloadUrlReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetGoogleDownloadUrlReq message.
     * @function verify
     * @memberof GetGoogleDownloadUrlReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetGoogleDownloadUrlReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        return null;
    };

    /**
     * Creates a GetGoogleDownloadUrlReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetGoogleDownloadUrlReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetGoogleDownloadUrlReq} GetGoogleDownloadUrlReq
     */
    GetGoogleDownloadUrlReq.fromObject = function fromObject(object) {
        if (object instanceof $root.GetGoogleDownloadUrlReq)
            return object;
        let message = new $root.GetGoogleDownloadUrlReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GetGoogleDownloadUrlReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        return message;
    };

    /**
     * Creates a plain object from a GetGoogleDownloadUrlReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetGoogleDownloadUrlReq
     * @static
     * @param {GetGoogleDownloadUrlReq} message GetGoogleDownloadUrlReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetGoogleDownloadUrlReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.clientInfo = null;
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        return object;
    };

    /**
     * Converts this GetGoogleDownloadUrlReq to JSON.
     * @function toJSON
     * @memberof GetGoogleDownloadUrlReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetGoogleDownloadUrlReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetGoogleDownloadUrlReq
     * @function getTypeUrl
     * @memberof GetGoogleDownloadUrlReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetGoogleDownloadUrlReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetGoogleDownloadUrlReq";
    };

    return GetGoogleDownloadUrlReq;
})();

export const GetGoogleDownloadUrlResp = $root.GetGoogleDownloadUrlResp = (() => {

    /**
     * Properties of a GetGoogleDownloadUrlResp.
     * @exports IGetGoogleDownloadUrlResp
     * @interface IGetGoogleDownloadUrlResp
     * @property {ICommonResult|null} [commonResult] GetGoogleDownloadUrlResp commonResult
     * @property {string|null} [url] GetGoogleDownloadUrlResp url
     */

    /**
     * Constructs a new GetGoogleDownloadUrlResp.
     * @exports GetGoogleDownloadUrlResp
     * @classdesc Represents a GetGoogleDownloadUrlResp.
     * @implements IGetGoogleDownloadUrlResp
     * @constructor
     * @param {IGetGoogleDownloadUrlResp=} [properties] Properties to set
     */
    function GetGoogleDownloadUrlResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetGoogleDownloadUrlResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GetGoogleDownloadUrlResp
     * @instance
     */
    GetGoogleDownloadUrlResp.prototype.commonResult = null;

    /**
     * GetGoogleDownloadUrlResp url.
     * @member {string} url
     * @memberof GetGoogleDownloadUrlResp
     * @instance
     */
    GetGoogleDownloadUrlResp.prototype.url = "";

    /**
     * Creates a new GetGoogleDownloadUrlResp instance using the specified properties.
     * @function create
     * @memberof GetGoogleDownloadUrlResp
     * @static
     * @param {IGetGoogleDownloadUrlResp=} [properties] Properties to set
     * @returns {GetGoogleDownloadUrlResp} GetGoogleDownloadUrlResp instance
     */
    GetGoogleDownloadUrlResp.create = function create(properties) {
        return new GetGoogleDownloadUrlResp(properties);
    };

    /**
     * Encodes the specified GetGoogleDownloadUrlResp message. Does not implicitly {@link GetGoogleDownloadUrlResp.verify|verify} messages.
     * @function encode
     * @memberof GetGoogleDownloadUrlResp
     * @static
     * @param {IGetGoogleDownloadUrlResp} message GetGoogleDownloadUrlResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetGoogleDownloadUrlResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.url != null && Object.hasOwnProperty.call(message, "url"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.url);
        return writer;
    };

    /**
     * Encodes the specified GetGoogleDownloadUrlResp message, length delimited. Does not implicitly {@link GetGoogleDownloadUrlResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetGoogleDownloadUrlResp
     * @static
     * @param {IGetGoogleDownloadUrlResp} message GetGoogleDownloadUrlResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetGoogleDownloadUrlResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetGoogleDownloadUrlResp message from the specified reader or buffer.
     * @function decode
     * @memberof GetGoogleDownloadUrlResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetGoogleDownloadUrlResp} GetGoogleDownloadUrlResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetGoogleDownloadUrlResp.decode = function decode(reader, length, error) {
    GetGoogleDownloadUrlResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetGoogleDownloadUrlResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.url = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetGoogleDownloadUrlResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetGoogleDownloadUrlResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetGoogleDownloadUrlResp} GetGoogleDownloadUrlResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetGoogleDownloadUrlResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetGoogleDownloadUrlResp message.
     * @function verify
     * @memberof GetGoogleDownloadUrlResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetGoogleDownloadUrlResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.url != null && message.hasOwnProperty("url"))
            if (!$util.isString(message.url))
                return "url: string expected";
        return null;
    };

    /**
     * Creates a GetGoogleDownloadUrlResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetGoogleDownloadUrlResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetGoogleDownloadUrlResp} GetGoogleDownloadUrlResp
     */
    GetGoogleDownloadUrlResp.fromObject = function fromObject(object) {
        if (object instanceof $root.GetGoogleDownloadUrlResp)
            return object;
        let message = new $root.GetGoogleDownloadUrlResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GetGoogleDownloadUrlResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.url != null)
            message.url = String(object.url);
        return message;
    };

    /**
     * Creates a plain object from a GetGoogleDownloadUrlResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetGoogleDownloadUrlResp
     * @static
     * @param {GetGoogleDownloadUrlResp} message GetGoogleDownloadUrlResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetGoogleDownloadUrlResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.url = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.url != null && message.hasOwnProperty("url"))
            object.url = message.url;
        return object;
    };

    /**
     * Converts this GetGoogleDownloadUrlResp to JSON.
     * @function toJSON
     * @memberof GetGoogleDownloadUrlResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetGoogleDownloadUrlResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetGoogleDownloadUrlResp
     * @function getTypeUrl
     * @memberof GetGoogleDownloadUrlResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetGoogleDownloadUrlResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetGoogleDownloadUrlResp";
    };

    return GetGoogleDownloadUrlResp;
})();

export const RedisCacheKeyReq = $root.RedisCacheKeyReq = (() => {

    /**
     * Properties of a RedisCacheKeyReq.
     * @exports IRedisCacheKeyReq
     * @interface IRedisCacheKeyReq
     * @property {IClientInfo|null} [clientInfo] RedisCacheKeyReq clientInfo
     * @property {string|null} [cacheName] RedisCacheKeyReq cacheName
     * @property {string|null} [key] RedisCacheKeyReq key
     */

    /**
     * Constructs a new RedisCacheKeyReq.
     * @exports RedisCacheKeyReq
     * @classdesc Represents a RedisCacheKeyReq.
     * @implements IRedisCacheKeyReq
     * @constructor
     * @param {IRedisCacheKeyReq=} [properties] Properties to set
     */
    function RedisCacheKeyReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * RedisCacheKeyReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof RedisCacheKeyReq
     * @instance
     */
    RedisCacheKeyReq.prototype.clientInfo = null;

    /**
     * RedisCacheKeyReq cacheName.
     * @member {string} cacheName
     * @memberof RedisCacheKeyReq
     * @instance
     */
    RedisCacheKeyReq.prototype.cacheName = "";

    /**
     * RedisCacheKeyReq key.
     * @member {string} key
     * @memberof RedisCacheKeyReq
     * @instance
     */
    RedisCacheKeyReq.prototype.key = "";

    /**
     * Creates a new RedisCacheKeyReq instance using the specified properties.
     * @function create
     * @memberof RedisCacheKeyReq
     * @static
     * @param {IRedisCacheKeyReq=} [properties] Properties to set
     * @returns {RedisCacheKeyReq} RedisCacheKeyReq instance
     */
    RedisCacheKeyReq.create = function create(properties) {
        return new RedisCacheKeyReq(properties);
    };

    /**
     * Encodes the specified RedisCacheKeyReq message. Does not implicitly {@link RedisCacheKeyReq.verify|verify} messages.
     * @function encode
     * @memberof RedisCacheKeyReq
     * @static
     * @param {IRedisCacheKeyReq} message RedisCacheKeyReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedisCacheKeyReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.cacheName != null && Object.hasOwnProperty.call(message, "cacheName"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.cacheName);
        if (message.key != null && Object.hasOwnProperty.call(message, "key"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.key);
        return writer;
    };

    /**
     * Encodes the specified RedisCacheKeyReq message, length delimited. Does not implicitly {@link RedisCacheKeyReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof RedisCacheKeyReq
     * @static
     * @param {IRedisCacheKeyReq} message RedisCacheKeyReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedisCacheKeyReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a RedisCacheKeyReq message from the specified reader or buffer.
     * @function decode
     * @memberof RedisCacheKeyReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {RedisCacheKeyReq} RedisCacheKeyReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedisCacheKeyReq.decode = function decode(reader, length, error) {
    RedisCacheKeyReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.RedisCacheKeyReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.cacheName = reader.string();
                    break;
                }
            case 3: {
                    message.key = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a RedisCacheKeyReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof RedisCacheKeyReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {RedisCacheKeyReq} RedisCacheKeyReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedisCacheKeyReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RedisCacheKeyReq message.
     * @function verify
     * @memberof RedisCacheKeyReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RedisCacheKeyReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.cacheName != null && message.hasOwnProperty("cacheName"))
            if (!$util.isString(message.cacheName))
                return "cacheName: string expected";
        if (message.key != null && message.hasOwnProperty("key"))
            if (!$util.isString(message.key))
                return "key: string expected";
        return null;
    };

    /**
     * Creates a RedisCacheKeyReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof RedisCacheKeyReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {RedisCacheKeyReq} RedisCacheKeyReq
     */
    RedisCacheKeyReq.fromObject = function fromObject(object) {
        if (object instanceof $root.RedisCacheKeyReq)
            return object;
        let message = new $root.RedisCacheKeyReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".RedisCacheKeyReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.cacheName != null)
            message.cacheName = String(object.cacheName);
        if (object.key != null)
            message.key = String(object.key);
        return message;
    };

    /**
     * Creates a plain object from a RedisCacheKeyReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof RedisCacheKeyReq
     * @static
     * @param {RedisCacheKeyReq} message RedisCacheKeyReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RedisCacheKeyReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.cacheName = "";
            object.key = "";
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.cacheName != null && message.hasOwnProperty("cacheName"))
            object.cacheName = message.cacheName;
        if (message.key != null && message.hasOwnProperty("key"))
            object.key = message.key;
        return object;
    };

    /**
     * Converts this RedisCacheKeyReq to JSON.
     * @function toJSON
     * @memberof RedisCacheKeyReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RedisCacheKeyReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RedisCacheKeyReq
     * @function getTypeUrl
     * @memberof RedisCacheKeyReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RedisCacheKeyReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/RedisCacheKeyReq";
    };

    return RedisCacheKeyReq;
})();

export const RedisCacheKeyResp = $root.RedisCacheKeyResp = (() => {

    /**
     * Properties of a RedisCacheKeyResp.
     * @exports IRedisCacheKeyResp
     * @interface IRedisCacheKeyResp
     * @property {ICommonResult|null} [commonResult] RedisCacheKeyResp commonResult
     * @property {string|null} [data] RedisCacheKeyResp data
     */

    /**
     * Constructs a new RedisCacheKeyResp.
     * @exports RedisCacheKeyResp
     * @classdesc Represents a RedisCacheKeyResp.
     * @implements IRedisCacheKeyResp
     * @constructor
     * @param {IRedisCacheKeyResp=} [properties] Properties to set
     */
    function RedisCacheKeyResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * RedisCacheKeyResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof RedisCacheKeyResp
     * @instance
     */
    RedisCacheKeyResp.prototype.commonResult = null;

    /**
     * RedisCacheKeyResp data.
     * @member {string} data
     * @memberof RedisCacheKeyResp
     * @instance
     */
    RedisCacheKeyResp.prototype.data = "";

    /**
     * Creates a new RedisCacheKeyResp instance using the specified properties.
     * @function create
     * @memberof RedisCacheKeyResp
     * @static
     * @param {IRedisCacheKeyResp=} [properties] Properties to set
     * @returns {RedisCacheKeyResp} RedisCacheKeyResp instance
     */
    RedisCacheKeyResp.create = function create(properties) {
        return new RedisCacheKeyResp(properties);
    };

    /**
     * Encodes the specified RedisCacheKeyResp message. Does not implicitly {@link RedisCacheKeyResp.verify|verify} messages.
     * @function encode
     * @memberof RedisCacheKeyResp
     * @static
     * @param {IRedisCacheKeyResp} message RedisCacheKeyResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedisCacheKeyResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.data != null && Object.hasOwnProperty.call(message, "data"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.data);
        return writer;
    };

    /**
     * Encodes the specified RedisCacheKeyResp message, length delimited. Does not implicitly {@link RedisCacheKeyResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof RedisCacheKeyResp
     * @static
     * @param {IRedisCacheKeyResp} message RedisCacheKeyResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RedisCacheKeyResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a RedisCacheKeyResp message from the specified reader or buffer.
     * @function decode
     * @memberof RedisCacheKeyResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {RedisCacheKeyResp} RedisCacheKeyResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedisCacheKeyResp.decode = function decode(reader, length, error) {
    RedisCacheKeyResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.RedisCacheKeyResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.data = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a RedisCacheKeyResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof RedisCacheKeyResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {RedisCacheKeyResp} RedisCacheKeyResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RedisCacheKeyResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RedisCacheKeyResp message.
     * @function verify
     * @memberof RedisCacheKeyResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RedisCacheKeyResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.data != null && message.hasOwnProperty("data"))
            if (!$util.isString(message.data))
                return "data: string expected";
        return null;
    };

    /**
     * Creates a RedisCacheKeyResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof RedisCacheKeyResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {RedisCacheKeyResp} RedisCacheKeyResp
     */
    RedisCacheKeyResp.fromObject = function fromObject(object) {
        if (object instanceof $root.RedisCacheKeyResp)
            return object;
        let message = new $root.RedisCacheKeyResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".RedisCacheKeyResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.data != null)
            message.data = String(object.data);
        return message;
    };

    /**
     * Creates a plain object from a RedisCacheKeyResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof RedisCacheKeyResp
     * @static
     * @param {RedisCacheKeyResp} message RedisCacheKeyResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RedisCacheKeyResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.commonResult = null;
            object.data = "";
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.data != null && message.hasOwnProperty("data"))
            object.data = message.data;
        return object;
    };

    /**
     * Converts this RedisCacheKeyResp to JSON.
     * @function toJSON
     * @memberof RedisCacheKeyResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RedisCacheKeyResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RedisCacheKeyResp
     * @function getTypeUrl
     * @memberof RedisCacheKeyResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RedisCacheKeyResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/RedisCacheKeyResp";
    };

    return RedisCacheKeyResp;
})();

export const UploadLogReq = $root.UploadLogReq = (() => {

    /**
     * Properties of an UploadLogReq.
     * @exports IUploadLogReq
     * @interface IUploadLogReq
     * @property {IClientInfo|null} [clientInfo] UploadLogReq clientInfo
     * @property {string|null} [content] UploadLogReq content
     */

    /**
     * Constructs a new UploadLogReq.
     * @exports UploadLogReq
     * @classdesc Represents an UploadLogReq.
     * @implements IUploadLogReq
     * @constructor
     * @param {IUploadLogReq=} [properties] Properties to set
     */
    function UploadLogReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * UploadLogReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof UploadLogReq
     * @instance
     */
    UploadLogReq.prototype.clientInfo = null;

    /**
     * UploadLogReq content.
     * @member {string} content
     * @memberof UploadLogReq
     * @instance
     */
    UploadLogReq.prototype.content = "";

    /**
     * Creates a new UploadLogReq instance using the specified properties.
     * @function create
     * @memberof UploadLogReq
     * @static
     * @param {IUploadLogReq=} [properties] Properties to set
     * @returns {UploadLogReq} UploadLogReq instance
     */
    UploadLogReq.create = function create(properties) {
        return new UploadLogReq(properties);
    };

    /**
     * Encodes the specified UploadLogReq message. Does not implicitly {@link UploadLogReq.verify|verify} messages.
     * @function encode
     * @memberof UploadLogReq
     * @static
     * @param {IUploadLogReq} message UploadLogReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UploadLogReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.content != null && Object.hasOwnProperty.call(message, "content"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.content);
        return writer;
    };

    /**
     * Encodes the specified UploadLogReq message, length delimited. Does not implicitly {@link UploadLogReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof UploadLogReq
     * @static
     * @param {IUploadLogReq} message UploadLogReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UploadLogReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an UploadLogReq message from the specified reader or buffer.
     * @function decode
     * @memberof UploadLogReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {UploadLogReq} UploadLogReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UploadLogReq.decode = function decode(reader, length, error) {
    UploadLogReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.UploadLogReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.content = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an UploadLogReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof UploadLogReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {UploadLogReq} UploadLogReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UploadLogReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an UploadLogReq message.
     * @function verify
     * @memberof UploadLogReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    UploadLogReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.content != null && message.hasOwnProperty("content"))
            if (!$util.isString(message.content))
                return "content: string expected";
        return null;
    };

    /**
     * Creates an UploadLogReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof UploadLogReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {UploadLogReq} UploadLogReq
     */
    UploadLogReq.fromObject = function fromObject(object) {
        if (object instanceof $root.UploadLogReq)
            return object;
        let message = new $root.UploadLogReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".UploadLogReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.content != null)
            message.content = String(object.content);
        return message;
    };

    /**
     * Creates a plain object from an UploadLogReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof UploadLogReq
     * @static
     * @param {UploadLogReq} message UploadLogReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    UploadLogReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.content = "";
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.content != null && message.hasOwnProperty("content"))
            object.content = message.content;
        return object;
    };

    /**
     * Converts this UploadLogReq to JSON.
     * @function toJSON
     * @memberof UploadLogReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    UploadLogReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for UploadLogReq
     * @function getTypeUrl
     * @memberof UploadLogReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    UploadLogReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/UploadLogReq";
    };

    return UploadLogReq;
})();

export const UploadLogResp = $root.UploadLogResp = (() => {

    /**
     * Properties of an UploadLogResp.
     * @exports IUploadLogResp
     * @interface IUploadLogResp
     * @property {ICommonResult|null} [commonResult] UploadLogResp commonResult
     */

    /**
     * Constructs a new UploadLogResp.
     * @exports UploadLogResp
     * @classdesc Represents an UploadLogResp.
     * @implements IUploadLogResp
     * @constructor
     * @param {IUploadLogResp=} [properties] Properties to set
     */
    function UploadLogResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * UploadLogResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof UploadLogResp
     * @instance
     */
    UploadLogResp.prototype.commonResult = null;

    /**
     * Creates a new UploadLogResp instance using the specified properties.
     * @function create
     * @memberof UploadLogResp
     * @static
     * @param {IUploadLogResp=} [properties] Properties to set
     * @returns {UploadLogResp} UploadLogResp instance
     */
    UploadLogResp.create = function create(properties) {
        return new UploadLogResp(properties);
    };

    /**
     * Encodes the specified UploadLogResp message. Does not implicitly {@link UploadLogResp.verify|verify} messages.
     * @function encode
     * @memberof UploadLogResp
     * @static
     * @param {IUploadLogResp} message UploadLogResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UploadLogResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified UploadLogResp message, length delimited. Does not implicitly {@link UploadLogResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof UploadLogResp
     * @static
     * @param {IUploadLogResp} message UploadLogResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UploadLogResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an UploadLogResp message from the specified reader or buffer.
     * @function decode
     * @memberof UploadLogResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {UploadLogResp} UploadLogResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UploadLogResp.decode = function decode(reader, length, error) {
    UploadLogResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.UploadLogResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an UploadLogResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof UploadLogResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {UploadLogResp} UploadLogResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UploadLogResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an UploadLogResp message.
     * @function verify
     * @memberof UploadLogResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    UploadLogResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates an UploadLogResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof UploadLogResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {UploadLogResp} UploadLogResp
     */
    UploadLogResp.fromObject = function fromObject(object) {
        if (object instanceof $root.UploadLogResp)
            return object;
        let message = new $root.UploadLogResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".UploadLogResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from an UploadLogResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof UploadLogResp
     * @static
     * @param {UploadLogResp} message UploadLogResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    UploadLogResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this UploadLogResp to JSON.
     * @function toJSON
     * @memberof UploadLogResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    UploadLogResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for UploadLogResp
     * @function getTypeUrl
     * @memberof UploadLogResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    UploadLogResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/UploadLogResp";
    };

    return UploadLogResp;
})();

export const CountryCodeReq = $root.CountryCodeReq = (() => {

    /**
     * Properties of a CountryCodeReq.
     * @exports ICountryCodeReq
     * @interface ICountryCodeReq
     * @property {IClientInfo|null} [clientInfo] CountryCodeReq clientInfo
     */

    /**
     * Constructs a new CountryCodeReq.
     * @exports CountryCodeReq
     * @classdesc Represents a CountryCodeReq.
     * @implements ICountryCodeReq
     * @constructor
     * @param {ICountryCodeReq=} [properties] Properties to set
     */
    function CountryCodeReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CountryCodeReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof CountryCodeReq
     * @instance
     */
    CountryCodeReq.prototype.clientInfo = null;

    /**
     * Creates a new CountryCodeReq instance using the specified properties.
     * @function create
     * @memberof CountryCodeReq
     * @static
     * @param {ICountryCodeReq=} [properties] Properties to set
     * @returns {CountryCodeReq} CountryCodeReq instance
     */
    CountryCodeReq.create = function create(properties) {
        return new CountryCodeReq(properties);
    };

    /**
     * Encodes the specified CountryCodeReq message. Does not implicitly {@link CountryCodeReq.verify|verify} messages.
     * @function encode
     * @memberof CountryCodeReq
     * @static
     * @param {ICountryCodeReq} message CountryCodeReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CountryCodeReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified CountryCodeReq message, length delimited. Does not implicitly {@link CountryCodeReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CountryCodeReq
     * @static
     * @param {ICountryCodeReq} message CountryCodeReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CountryCodeReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CountryCodeReq message from the specified reader or buffer.
     * @function decode
     * @memberof CountryCodeReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CountryCodeReq} CountryCodeReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CountryCodeReq.decode = function decode(reader, length, error) {
    CountryCodeReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CountryCodeReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CountryCodeReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CountryCodeReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CountryCodeReq} CountryCodeReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CountryCodeReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CountryCodeReq message.
     * @function verify
     * @memberof CountryCodeReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CountryCodeReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        return null;
    };

    /**
     * Creates a CountryCodeReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CountryCodeReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CountryCodeReq} CountryCodeReq
     */
    CountryCodeReq.fromObject = function fromObject(object) {
        if (object instanceof $root.CountryCodeReq)
            return object;
        let message = new $root.CountryCodeReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".CountryCodeReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        return message;
    };

    /**
     * Creates a plain object from a CountryCodeReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CountryCodeReq
     * @static
     * @param {CountryCodeReq} message CountryCodeReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CountryCodeReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.clientInfo = null;
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        return object;
    };

    /**
     * Converts this CountryCodeReq to JSON.
     * @function toJSON
     * @memberof CountryCodeReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CountryCodeReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CountryCodeReq
     * @function getTypeUrl
     * @memberof CountryCodeReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CountryCodeReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CountryCodeReq";
    };

    return CountryCodeReq;
})();

export const CountryCodeResp = $root.CountryCodeResp = (() => {

    /**
     * Properties of a CountryCodeResp.
     * @exports ICountryCodeResp
     * @interface ICountryCodeResp
     * @property {Array.<ICountryCodeDto>|null} [countryCodeList] CountryCodeResp countryCodeList
     * @property {ICommonResult|null} [commonResult] CountryCodeResp commonResult
     */

    /**
     * Constructs a new CountryCodeResp.
     * @exports CountryCodeResp
     * @classdesc Represents a CountryCodeResp.
     * @implements ICountryCodeResp
     * @constructor
     * @param {ICountryCodeResp=} [properties] Properties to set
     */
    function CountryCodeResp(properties) {
        this.countryCodeList = [];
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CountryCodeResp countryCodeList.
     * @member {Array.<ICountryCodeDto>} countryCodeList
     * @memberof CountryCodeResp
     * @instance
     */
    CountryCodeResp.prototype.countryCodeList = $util.emptyArray;

    /**
     * CountryCodeResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof CountryCodeResp
     * @instance
     */
    CountryCodeResp.prototype.commonResult = null;

    /**
     * Creates a new CountryCodeResp instance using the specified properties.
     * @function create
     * @memberof CountryCodeResp
     * @static
     * @param {ICountryCodeResp=} [properties] Properties to set
     * @returns {CountryCodeResp} CountryCodeResp instance
     */
    CountryCodeResp.create = function create(properties) {
        return new CountryCodeResp(properties);
    };

    /**
     * Encodes the specified CountryCodeResp message. Does not implicitly {@link CountryCodeResp.verify|verify} messages.
     * @function encode
     * @memberof CountryCodeResp
     * @static
     * @param {ICountryCodeResp} message CountryCodeResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CountryCodeResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.countryCodeList != null && message.countryCodeList.length)
            for (let i = 0; i < message.countryCodeList.length; ++i)
                $root.CountryCodeDto.encode(message.countryCodeList[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified CountryCodeResp message, length delimited. Does not implicitly {@link CountryCodeResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CountryCodeResp
     * @static
     * @param {ICountryCodeResp} message CountryCodeResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CountryCodeResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CountryCodeResp message from the specified reader or buffer.
     * @function decode
     * @memberof CountryCodeResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CountryCodeResp} CountryCodeResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CountryCodeResp.decode = function decode(reader, length, error) {
    CountryCodeResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CountryCodeResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    if (!(message.countryCodeList && message.countryCodeList.length))
                        message.countryCodeList = [];
                    message.countryCodeList.push($root.CountryCodeDto.decode(reader, reader.uint32()));
                    break;
                }
            case 2: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CountryCodeResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CountryCodeResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CountryCodeResp} CountryCodeResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CountryCodeResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CountryCodeResp message.
     * @function verify
     * @memberof CountryCodeResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CountryCodeResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.countryCodeList != null && message.hasOwnProperty("countryCodeList")) {
            if (!Array.isArray(message.countryCodeList))
                return "countryCodeList: array expected";
            for (let i = 0; i < message.countryCodeList.length; ++i) {
                let error = $root.CountryCodeDto.verify(message.countryCodeList[i]);
                if (error)
                    return "countryCodeList." + error;
            }
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates a CountryCodeResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CountryCodeResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CountryCodeResp} CountryCodeResp
     */
    CountryCodeResp.fromObject = function fromObject(object) {
        if (object instanceof $root.CountryCodeResp)
            return object;
        let message = new $root.CountryCodeResp();
        if (object.countryCodeList) {
            if (!Array.isArray(object.countryCodeList))
                throw TypeError(".CountryCodeResp.countryCodeList: array expected");
            message.countryCodeList = [];
            for (let i = 0; i < object.countryCodeList.length; ++i) {
                if (typeof object.countryCodeList[i] !== "object")
                    throw TypeError(".CountryCodeResp.countryCodeList: object expected");
                message.countryCodeList[i] = $root.CountryCodeDto.fromObject(object.countryCodeList[i]);
            }
        }
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".CountryCodeResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from a CountryCodeResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CountryCodeResp
     * @static
     * @param {CountryCodeResp} message CountryCodeResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CountryCodeResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.arrays || options.defaults)
            object.countryCodeList = [];
        if (options.defaults)
            object.commonResult = null;
        if (message.countryCodeList && message.countryCodeList.length) {
            object.countryCodeList = [];
            for (let j = 0; j < message.countryCodeList.length; ++j)
                object.countryCodeList[j] = $root.CountryCodeDto.toObject(message.countryCodeList[j], options);
        }
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this CountryCodeResp to JSON.
     * @function toJSON
     * @memberof CountryCodeResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CountryCodeResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CountryCodeResp
     * @function getTypeUrl
     * @memberof CountryCodeResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CountryCodeResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CountryCodeResp";
    };

    return CountryCodeResp;
})();

export const CountryCodeDto = $root.CountryCodeDto = (() => {

    /**
     * Properties of a CountryCodeDto.
     * @exports ICountryCodeDto
     * @interface ICountryCodeDto
     * @property {number|null} [countryCodeId] CountryCodeDto countryCodeId
     * @property {number|null} [countryCode] CountryCodeDto countryCode
     * @property {string|null} [countryName] CountryCodeDto countryName
     * @property {string|null} [countryNameUs] CountryCodeDto countryNameUs
     * @property {string|null} [countryNameTw] CountryCodeDto countryNameTw
     * @property {string|null} [pinyinHead] CountryCodeDto pinyinHead
     * @property {string|null} [englishHead] CountryCodeDto englishHead
     * @property {number|null} [strokeCount] CountryCodeDto strokeCount
     */

    /**
     * Constructs a new CountryCodeDto.
     * @exports CountryCodeDto
     * @classdesc Represents a CountryCodeDto.
     * @implements ICountryCodeDto
     * @constructor
     * @param {ICountryCodeDto=} [properties] Properties to set
     */
    function CountryCodeDto(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CountryCodeDto countryCodeId.
     * @member {number} countryCodeId
     * @memberof CountryCodeDto
     * @instance
     */
    CountryCodeDto.prototype.countryCodeId = 0;

    /**
     * CountryCodeDto countryCode.
     * @member {number} countryCode
     * @memberof CountryCodeDto
     * @instance
     */
    CountryCodeDto.prototype.countryCode = 0;

    /**
     * CountryCodeDto countryName.
     * @member {string} countryName
     * @memberof CountryCodeDto
     * @instance
     */
    CountryCodeDto.prototype.countryName = "";

    /**
     * CountryCodeDto countryNameUs.
     * @member {string} countryNameUs
     * @memberof CountryCodeDto
     * @instance
     */
    CountryCodeDto.prototype.countryNameUs = "";

    /**
     * CountryCodeDto countryNameTw.
     * @member {string} countryNameTw
     * @memberof CountryCodeDto
     * @instance
     */
    CountryCodeDto.prototype.countryNameTw = "";

    /**
     * CountryCodeDto pinyinHead.
     * @member {string} pinyinHead
     * @memberof CountryCodeDto
     * @instance
     */
    CountryCodeDto.prototype.pinyinHead = "";

    /**
     * CountryCodeDto englishHead.
     * @member {string} englishHead
     * @memberof CountryCodeDto
     * @instance
     */
    CountryCodeDto.prototype.englishHead = "";

    /**
     * CountryCodeDto strokeCount.
     * @member {number} strokeCount
     * @memberof CountryCodeDto
     * @instance
     */
    CountryCodeDto.prototype.strokeCount = 0;

    /**
     * Creates a new CountryCodeDto instance using the specified properties.
     * @function create
     * @memberof CountryCodeDto
     * @static
     * @param {ICountryCodeDto=} [properties] Properties to set
     * @returns {CountryCodeDto} CountryCodeDto instance
     */
    CountryCodeDto.create = function create(properties) {
        return new CountryCodeDto(properties);
    };

    /**
     * Encodes the specified CountryCodeDto message. Does not implicitly {@link CountryCodeDto.verify|verify} messages.
     * @function encode
     * @memberof CountryCodeDto
     * @static
     * @param {ICountryCodeDto} message CountryCodeDto message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CountryCodeDto.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.countryCodeId != null && Object.hasOwnProperty.call(message, "countryCodeId"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.countryCodeId);
        if (message.countryCode != null && Object.hasOwnProperty.call(message, "countryCode"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.countryCode);
        if (message.countryName != null && Object.hasOwnProperty.call(message, "countryName"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.countryName);
        if (message.countryNameUs != null && Object.hasOwnProperty.call(message, "countryNameUs"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.countryNameUs);
        if (message.countryNameTw != null && Object.hasOwnProperty.call(message, "countryNameTw"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.countryNameTw);
        if (message.pinyinHead != null && Object.hasOwnProperty.call(message, "pinyinHead"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.pinyinHead);
        if (message.englishHead != null && Object.hasOwnProperty.call(message, "englishHead"))
            writer.uint32(/* id 7, wireType 2 =*/58).string(message.englishHead);
        if (message.strokeCount != null && Object.hasOwnProperty.call(message, "strokeCount"))
            writer.uint32(/* id 8, wireType 0 =*/64).int32(message.strokeCount);
        return writer;
    };

    /**
     * Encodes the specified CountryCodeDto message, length delimited. Does not implicitly {@link CountryCodeDto.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CountryCodeDto
     * @static
     * @param {ICountryCodeDto} message CountryCodeDto message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CountryCodeDto.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CountryCodeDto message from the specified reader or buffer.
     * @function decode
     * @memberof CountryCodeDto
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CountryCodeDto} CountryCodeDto
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CountryCodeDto.decode = function decode(reader, length, error) {
    CountryCodeDto.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CountryCodeDto();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.countryCodeId = reader.int32();
                    break;
                }
            case 2: {
                    message.countryCode = reader.int32();
                    break;
                }
            case 3: {
                    message.countryName = reader.string();
                    break;
                }
            case 4: {
                    message.countryNameUs = reader.string();
                    break;
                }
            case 5: {
                    message.countryNameTw = reader.string();
                    break;
                }
            case 6: {
                    message.pinyinHead = reader.string();
                    break;
                }
            case 7: {
                    message.englishHead = reader.string();
                    break;
                }
            case 8: {
                    message.strokeCount = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CountryCodeDto message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CountryCodeDto
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CountryCodeDto} CountryCodeDto
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CountryCodeDto.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CountryCodeDto message.
     * @function verify
     * @memberof CountryCodeDto
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CountryCodeDto.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.countryCodeId != null && message.hasOwnProperty("countryCodeId"))
            if (!$util.isInteger(message.countryCodeId))
                return "countryCodeId: integer expected";
        if (message.countryCode != null && message.hasOwnProperty("countryCode"))
            if (!$util.isInteger(message.countryCode))
                return "countryCode: integer expected";
        if (message.countryName != null && message.hasOwnProperty("countryName"))
            if (!$util.isString(message.countryName))
                return "countryName: string expected";
        if (message.countryNameUs != null && message.hasOwnProperty("countryNameUs"))
            if (!$util.isString(message.countryNameUs))
                return "countryNameUs: string expected";
        if (message.countryNameTw != null && message.hasOwnProperty("countryNameTw"))
            if (!$util.isString(message.countryNameTw))
                return "countryNameTw: string expected";
        if (message.pinyinHead != null && message.hasOwnProperty("pinyinHead"))
            if (!$util.isString(message.pinyinHead))
                return "pinyinHead: string expected";
        if (message.englishHead != null && message.hasOwnProperty("englishHead"))
            if (!$util.isString(message.englishHead))
                return "englishHead: string expected";
        if (message.strokeCount != null && message.hasOwnProperty("strokeCount"))
            if (!$util.isInteger(message.strokeCount))
                return "strokeCount: integer expected";
        return null;
    };

    /**
     * Creates a CountryCodeDto message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CountryCodeDto
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CountryCodeDto} CountryCodeDto
     */
    CountryCodeDto.fromObject = function fromObject(object) {
        if (object instanceof $root.CountryCodeDto)
            return object;
        let message = new $root.CountryCodeDto();
        if (object.countryCodeId != null)
            message.countryCodeId = object.countryCodeId | 0;
        if (object.countryCode != null)
            message.countryCode = object.countryCode | 0;
        if (object.countryName != null)
            message.countryName = String(object.countryName);
        if (object.countryNameUs != null)
            message.countryNameUs = String(object.countryNameUs);
        if (object.countryNameTw != null)
            message.countryNameTw = String(object.countryNameTw);
        if (object.pinyinHead != null)
            message.pinyinHead = String(object.pinyinHead);
        if (object.englishHead != null)
            message.englishHead = String(object.englishHead);
        if (object.strokeCount != null)
            message.strokeCount = object.strokeCount | 0;
        return message;
    };

    /**
     * Creates a plain object from a CountryCodeDto message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CountryCodeDto
     * @static
     * @param {CountryCodeDto} message CountryCodeDto
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CountryCodeDto.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.countryCodeId = 0;
            object.countryCode = 0;
            object.countryName = "";
            object.countryNameUs = "";
            object.countryNameTw = "";
            object.pinyinHead = "";
            object.englishHead = "";
            object.strokeCount = 0;
        }
        if (message.countryCodeId != null && message.hasOwnProperty("countryCodeId"))
            object.countryCodeId = message.countryCodeId;
        if (message.countryCode != null && message.hasOwnProperty("countryCode"))
            object.countryCode = message.countryCode;
        if (message.countryName != null && message.hasOwnProperty("countryName"))
            object.countryName = message.countryName;
        if (message.countryNameUs != null && message.hasOwnProperty("countryNameUs"))
            object.countryNameUs = message.countryNameUs;
        if (message.countryNameTw != null && message.hasOwnProperty("countryNameTw"))
            object.countryNameTw = message.countryNameTw;
        if (message.pinyinHead != null && message.hasOwnProperty("pinyinHead"))
            object.pinyinHead = message.pinyinHead;
        if (message.englishHead != null && message.hasOwnProperty("englishHead"))
            object.englishHead = message.englishHead;
        if (message.strokeCount != null && message.hasOwnProperty("strokeCount"))
            object.strokeCount = message.strokeCount;
        return object;
    };

    /**
     * Converts this CountryCodeDto to JSON.
     * @function toJSON
     * @memberof CountryCodeDto
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CountryCodeDto.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CountryCodeDto
     * @function getTypeUrl
     * @memberof CountryCodeDto
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CountryCodeDto.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CountryCodeDto";
    };

    return CountryCodeDto;
})();

export const GetSmsCodeByGt4Req = $root.GetSmsCodeByGt4Req = (() => {

    /**
     * Properties of a GetSmsCodeByGt4Req.
     * @exports IGetSmsCodeByGt4Req
     * @interface IGetSmsCodeByGt4Req
     * @property {IClientInfo|null} [clientInfo] GetSmsCodeByGt4Req clientInfo
     * @property {string|null} [countryCode] GetSmsCodeByGt4Req countryCode
     * @property {string|null} [account] GetSmsCodeByGt4Req account
     * @property {GetValidateCodeType|null} [type] GetSmsCodeByGt4Req type
     * @property {string|null} [lotNumber] GetSmsCodeByGt4Req lotNumber
     * @property {string|null} [captchaOutput] GetSmsCodeByGt4Req captchaOutput
     * @property {string|null} [passToken] GetSmsCodeByGt4Req passToken
     * @property {string|null} [genTime] GetSmsCodeByGt4Req genTime
     */

    /**
     * Constructs a new GetSmsCodeByGt4Req.
     * @exports GetSmsCodeByGt4Req
     * @classdesc Represents a GetSmsCodeByGt4Req.
     * @implements IGetSmsCodeByGt4Req
     * @constructor
     * @param {IGetSmsCodeByGt4Req=} [properties] Properties to set
     */
    function GetSmsCodeByGt4Req(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetSmsCodeByGt4Req clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GetSmsCodeByGt4Req
     * @instance
     */
    GetSmsCodeByGt4Req.prototype.clientInfo = null;

    /**
     * GetSmsCodeByGt4Req countryCode.
     * @member {string} countryCode
     * @memberof GetSmsCodeByGt4Req
     * @instance
     */
    GetSmsCodeByGt4Req.prototype.countryCode = "";

    /**
     * GetSmsCodeByGt4Req account.
     * @member {string} account
     * @memberof GetSmsCodeByGt4Req
     * @instance
     */
    GetSmsCodeByGt4Req.prototype.account = "";

    /**
     * GetSmsCodeByGt4Req type.
     * @member {GetValidateCodeType} type
     * @memberof GetSmsCodeByGt4Req
     * @instance
     */
    GetSmsCodeByGt4Req.prototype.type = 0;

    /**
     * GetSmsCodeByGt4Req lotNumber.
     * @member {string} lotNumber
     * @memberof GetSmsCodeByGt4Req
     * @instance
     */
    GetSmsCodeByGt4Req.prototype.lotNumber = "";

    /**
     * GetSmsCodeByGt4Req captchaOutput.
     * @member {string} captchaOutput
     * @memberof GetSmsCodeByGt4Req
     * @instance
     */
    GetSmsCodeByGt4Req.prototype.captchaOutput = "";

    /**
     * GetSmsCodeByGt4Req passToken.
     * @member {string} passToken
     * @memberof GetSmsCodeByGt4Req
     * @instance
     */
    GetSmsCodeByGt4Req.prototype.passToken = "";

    /**
     * GetSmsCodeByGt4Req genTime.
     * @member {string} genTime
     * @memberof GetSmsCodeByGt4Req
     * @instance
     */
    GetSmsCodeByGt4Req.prototype.genTime = "";

    /**
     * Creates a new GetSmsCodeByGt4Req instance using the specified properties.
     * @function create
     * @memberof GetSmsCodeByGt4Req
     * @static
     * @param {IGetSmsCodeByGt4Req=} [properties] Properties to set
     * @returns {GetSmsCodeByGt4Req} GetSmsCodeByGt4Req instance
     */
    GetSmsCodeByGt4Req.create = function create(properties) {
        return new GetSmsCodeByGt4Req(properties);
    };

    /**
     * Encodes the specified GetSmsCodeByGt4Req message. Does not implicitly {@link GetSmsCodeByGt4Req.verify|verify} messages.
     * @function encode
     * @memberof GetSmsCodeByGt4Req
     * @static
     * @param {IGetSmsCodeByGt4Req} message GetSmsCodeByGt4Req message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetSmsCodeByGt4Req.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.countryCode != null && Object.hasOwnProperty.call(message, "countryCode"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.countryCode);
        if (message.account != null && Object.hasOwnProperty.call(message, "account"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.account);
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.type);
        if (message.lotNumber != null && Object.hasOwnProperty.call(message, "lotNumber"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.lotNumber);
        if (message.captchaOutput != null && Object.hasOwnProperty.call(message, "captchaOutput"))
            writer.uint32(/* id 7, wireType 2 =*/58).string(message.captchaOutput);
        if (message.passToken != null && Object.hasOwnProperty.call(message, "passToken"))
            writer.uint32(/* id 8, wireType 2 =*/66).string(message.passToken);
        if (message.genTime != null && Object.hasOwnProperty.call(message, "genTime"))
            writer.uint32(/* id 9, wireType 2 =*/74).string(message.genTime);
        return writer;
    };

    /**
     * Encodes the specified GetSmsCodeByGt4Req message, length delimited. Does not implicitly {@link GetSmsCodeByGt4Req.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetSmsCodeByGt4Req
     * @static
     * @param {IGetSmsCodeByGt4Req} message GetSmsCodeByGt4Req message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetSmsCodeByGt4Req.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetSmsCodeByGt4Req message from the specified reader or buffer.
     * @function decode
     * @memberof GetSmsCodeByGt4Req
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetSmsCodeByGt4Req} GetSmsCodeByGt4Req
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetSmsCodeByGt4Req.decode = function decode(reader, length, error) {
    GetSmsCodeByGt4Req.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetSmsCodeByGt4Req();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.countryCode = reader.string();
                    break;
                }
            case 3: {
                    message.account = reader.string();
                    break;
                }
            case 4: {
                    message.type = reader.int32();
                    break;
                }
            case 6: {
                    message.lotNumber = reader.string();
                    break;
                }
            case 7: {
                    message.captchaOutput = reader.string();
                    break;
                }
            case 8: {
                    message.passToken = reader.string();
                    break;
                }
            case 9: {
                    message.genTime = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetSmsCodeByGt4Req message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetSmsCodeByGt4Req
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetSmsCodeByGt4Req} GetSmsCodeByGt4Req
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetSmsCodeByGt4Req.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetSmsCodeByGt4Req message.
     * @function verify
     * @memberof GetSmsCodeByGt4Req
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetSmsCodeByGt4Req.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.countryCode != null && message.hasOwnProperty("countryCode"))
            if (!$util.isString(message.countryCode))
                return "countryCode: string expected";
        if (message.account != null && message.hasOwnProperty("account"))
            if (!$util.isString(message.account))
                return "account: string expected";
        if (message.type != null && message.hasOwnProperty("type"))
            switch (message.type) {
            default:
                return "type: enum value expected";
            case 0:
            case 1:
            case 2:
            case 3:
            case 4:
            case 6:
            case 7:
            case 8:
            case 9:
            case 10:
            case 11:
            case 12:
            case 13:
            case 14:
            case 15:
            case 16:
            case 17:
            case 18:
            case 19:
            case 20:
            case 21:
            case 22:
            case 23:
            case 23:
                break;
            }
        if (message.lotNumber != null && message.hasOwnProperty("lotNumber"))
            if (!$util.isString(message.lotNumber))
                return "lotNumber: string expected";
        if (message.captchaOutput != null && message.hasOwnProperty("captchaOutput"))
            if (!$util.isString(message.captchaOutput))
                return "captchaOutput: string expected";
        if (message.passToken != null && message.hasOwnProperty("passToken"))
            if (!$util.isString(message.passToken))
                return "passToken: string expected";
        if (message.genTime != null && message.hasOwnProperty("genTime"))
            if (!$util.isString(message.genTime))
                return "genTime: string expected";
        return null;
    };

    /**
     * Creates a GetSmsCodeByGt4Req message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetSmsCodeByGt4Req
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetSmsCodeByGt4Req} GetSmsCodeByGt4Req
     */
    GetSmsCodeByGt4Req.fromObject = function fromObject(object) {
        if (object instanceof $root.GetSmsCodeByGt4Req)
            return object;
        let message = new $root.GetSmsCodeByGt4Req();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GetSmsCodeByGt4Req.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.countryCode != null)
            message.countryCode = String(object.countryCode);
        if (object.account != null)
            message.account = String(object.account);
        switch (object.type) {
        default:
            if (typeof object.type === "number") {
                message.type = object.type;
                break;
            }
            break;
        case "REG":
        case 0:
            message.type = 0;
            break;
        case "LOGIN":
        case 1:
            message.type = 1;
            break;
        case "FIND_PASSWORD":
        case 2:
            message.type = 2;
            break;
        case "UPDATE_PASSWORD":
        case 3:
            message.type = 3;
            break;
        case "UPDATE_PHONE":
        case 4:
            message.type = 4;
            break;
        case "VALIDATE_PASSWORD":
        case 6:
            message.type = 6;
            break;
        case "FIND_GESTURE_PASSWORD":
        case 7:
            message.type = 7;
            break;
        case "TRADE_PASSWORD":
        case 8:
            message.type = 8;
            break;
        case "UPDATE_TRADE_PASSWORD":
        case 9:
            message.type = 9;
            break;
        case "BIND_PHONE":
        case 10:
            message.type = 10;
            break;
        case "BIND_EMAIL":
        case 11:
            message.type = 11;
            break;
        case "UPDATE_EMAIL":
        case 12:
            message.type = 12;
            break;
        case "VALIDATE_GOOGLE_AUTH":
        case 13:
            message.type = 13;
            break;
        case "UPDATE_SECURE":
        case 14:
            message.type = 14;
            break;
        case "CASH_OUT":
        case 15:
            message.type = 15;
            break;
        case "VERIFY_EMAIL":
        case 16:
            message.type = 16;
            break;
        case "VERIFY_PHONE":
        case 17:
            message.type = 17;
            break;
        case "VERIFY_TRADE_PASSWORD":
        case 18:
            message.type = 18;
            break;
        case "VERIFY_GOOGLE_CODE":
        case 19:
            message.type = 19;
            break;
        case "BIND_ACCOUNT":
        case 20:
            message.type = 20;
            break;
        case "VERIFY_LOGIN_PHONE":
        case 21:
            message.type = 21;
            break;
        case "VERIFY_LOGIN_EMAIL":
        case 22:
            message.type = 22;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.type = 23;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.type = 23;
            break;
        }
        if (object.lotNumber != null)
            message.lotNumber = String(object.lotNumber);
        if (object.captchaOutput != null)
            message.captchaOutput = String(object.captchaOutput);
        if (object.passToken != null)
            message.passToken = String(object.passToken);
        if (object.genTime != null)
            message.genTime = String(object.genTime);
        return message;
    };

    /**
     * Creates a plain object from a GetSmsCodeByGt4Req message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetSmsCodeByGt4Req
     * @static
     * @param {GetSmsCodeByGt4Req} message GetSmsCodeByGt4Req
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetSmsCodeByGt4Req.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.countryCode = "";
            object.account = "";
            object.type = options.enums === String ? "REG" : 0;
            object.lotNumber = "";
            object.captchaOutput = "";
            object.passToken = "";
            object.genTime = "";
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.countryCode != null && message.hasOwnProperty("countryCode"))
            object.countryCode = message.countryCode;
        if (message.account != null && message.hasOwnProperty("account"))
            object.account = message.account;
        if (message.type != null && message.hasOwnProperty("type"))
            object.type = options.enums === String ? $root.GetValidateCodeType[message.type] === undefined ? message.type : $root.GetValidateCodeType[message.type] : message.type;
        if (message.lotNumber != null && message.hasOwnProperty("lotNumber"))
            object.lotNumber = message.lotNumber;
        if (message.captchaOutput != null && message.hasOwnProperty("captchaOutput"))
            object.captchaOutput = message.captchaOutput;
        if (message.passToken != null && message.hasOwnProperty("passToken"))
            object.passToken = message.passToken;
        if (message.genTime != null && message.hasOwnProperty("genTime"))
            object.genTime = message.genTime;
        return object;
    };

    /**
     * Converts this GetSmsCodeByGt4Req to JSON.
     * @function toJSON
     * @memberof GetSmsCodeByGt4Req
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetSmsCodeByGt4Req.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetSmsCodeByGt4Req
     * @function getTypeUrl
     * @memberof GetSmsCodeByGt4Req
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetSmsCodeByGt4Req.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetSmsCodeByGt4Req";
    };

    return GetSmsCodeByGt4Req;
})();

export const GetSmsCodeByGt4Resp = $root.GetSmsCodeByGt4Resp = (() => {

    /**
     * Properties of a GetSmsCodeByGt4Resp.
     * @exports IGetSmsCodeByGt4Resp
     * @interface IGetSmsCodeByGt4Resp
     * @property {ICommonResult|null} [commonResult] GetSmsCodeByGt4Resp commonResult
     */

    /**
     * Constructs a new GetSmsCodeByGt4Resp.
     * @exports GetSmsCodeByGt4Resp
     * @classdesc Represents a GetSmsCodeByGt4Resp.
     * @implements IGetSmsCodeByGt4Resp
     * @constructor
     * @param {IGetSmsCodeByGt4Resp=} [properties] Properties to set
     */
    function GetSmsCodeByGt4Resp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetSmsCodeByGt4Resp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GetSmsCodeByGt4Resp
     * @instance
     */
    GetSmsCodeByGt4Resp.prototype.commonResult = null;

    /**
     * Creates a new GetSmsCodeByGt4Resp instance using the specified properties.
     * @function create
     * @memberof GetSmsCodeByGt4Resp
     * @static
     * @param {IGetSmsCodeByGt4Resp=} [properties] Properties to set
     * @returns {GetSmsCodeByGt4Resp} GetSmsCodeByGt4Resp instance
     */
    GetSmsCodeByGt4Resp.create = function create(properties) {
        return new GetSmsCodeByGt4Resp(properties);
    };

    /**
     * Encodes the specified GetSmsCodeByGt4Resp message. Does not implicitly {@link GetSmsCodeByGt4Resp.verify|verify} messages.
     * @function encode
     * @memberof GetSmsCodeByGt4Resp
     * @static
     * @param {IGetSmsCodeByGt4Resp} message GetSmsCodeByGt4Resp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetSmsCodeByGt4Resp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified GetSmsCodeByGt4Resp message, length delimited. Does not implicitly {@link GetSmsCodeByGt4Resp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetSmsCodeByGt4Resp
     * @static
     * @param {IGetSmsCodeByGt4Resp} message GetSmsCodeByGt4Resp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetSmsCodeByGt4Resp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetSmsCodeByGt4Resp message from the specified reader or buffer.
     * @function decode
     * @memberof GetSmsCodeByGt4Resp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetSmsCodeByGt4Resp} GetSmsCodeByGt4Resp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetSmsCodeByGt4Resp.decode = function decode(reader, length, error) {
    GetSmsCodeByGt4Resp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetSmsCodeByGt4Resp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetSmsCodeByGt4Resp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetSmsCodeByGt4Resp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetSmsCodeByGt4Resp} GetSmsCodeByGt4Resp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetSmsCodeByGt4Resp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetSmsCodeByGt4Resp message.
     * @function verify
     * @memberof GetSmsCodeByGt4Resp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetSmsCodeByGt4Resp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates a GetSmsCodeByGt4Resp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetSmsCodeByGt4Resp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetSmsCodeByGt4Resp} GetSmsCodeByGt4Resp
     */
    GetSmsCodeByGt4Resp.fromObject = function fromObject(object) {
        if (object instanceof $root.GetSmsCodeByGt4Resp)
            return object;
        let message = new $root.GetSmsCodeByGt4Resp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GetSmsCodeByGt4Resp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from a GetSmsCodeByGt4Resp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetSmsCodeByGt4Resp
     * @static
     * @param {GetSmsCodeByGt4Resp} message GetSmsCodeByGt4Resp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetSmsCodeByGt4Resp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this GetSmsCodeByGt4Resp to JSON.
     * @function toJSON
     * @memberof GetSmsCodeByGt4Resp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetSmsCodeByGt4Resp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetSmsCodeByGt4Resp
     * @function getTypeUrl
     * @memberof GetSmsCodeByGt4Resp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetSmsCodeByGt4Resp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetSmsCodeByGt4Resp";
    };

    return GetSmsCodeByGt4Resp;
})();

export const GetAdvertisingReq = $root.GetAdvertisingReq = (() => {

    /**
     * Properties of a GetAdvertisingReq.
     * @exports IGetAdvertisingReq
     * @interface IGetAdvertisingReq
     * @property {IClientInfo|null} [clientInfo] GetAdvertisingReq clientInfo
     * @property {number|null} [placeType] GetAdvertisingReq placeType
     */

    /**
     * Constructs a new GetAdvertisingReq.
     * @exports GetAdvertisingReq
     * @classdesc Represents a GetAdvertisingReq.
     * @implements IGetAdvertisingReq
     * @constructor
     * @param {IGetAdvertisingReq=} [properties] Properties to set
     */
    function GetAdvertisingReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetAdvertisingReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof GetAdvertisingReq
     * @instance
     */
    GetAdvertisingReq.prototype.clientInfo = null;

    /**
     * GetAdvertisingReq placeType.
     * @member {number} placeType
     * @memberof GetAdvertisingReq
     * @instance
     */
    GetAdvertisingReq.prototype.placeType = 0;

    /**
     * Creates a new GetAdvertisingReq instance using the specified properties.
     * @function create
     * @memberof GetAdvertisingReq
     * @static
     * @param {IGetAdvertisingReq=} [properties] Properties to set
     * @returns {GetAdvertisingReq} GetAdvertisingReq instance
     */
    GetAdvertisingReq.create = function create(properties) {
        return new GetAdvertisingReq(properties);
    };

    /**
     * Encodes the specified GetAdvertisingReq message. Does not implicitly {@link GetAdvertisingReq.verify|verify} messages.
     * @function encode
     * @memberof GetAdvertisingReq
     * @static
     * @param {IGetAdvertisingReq} message GetAdvertisingReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetAdvertisingReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.placeType != null && Object.hasOwnProperty.call(message, "placeType"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.placeType);
        return writer;
    };

    /**
     * Encodes the specified GetAdvertisingReq message, length delimited. Does not implicitly {@link GetAdvertisingReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetAdvertisingReq
     * @static
     * @param {IGetAdvertisingReq} message GetAdvertisingReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetAdvertisingReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetAdvertisingReq message from the specified reader or buffer.
     * @function decode
     * @memberof GetAdvertisingReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetAdvertisingReq} GetAdvertisingReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetAdvertisingReq.decode = function decode(reader, length, error) {
    GetAdvertisingReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetAdvertisingReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.placeType = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetAdvertisingReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetAdvertisingReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetAdvertisingReq} GetAdvertisingReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetAdvertisingReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetAdvertisingReq message.
     * @function verify
     * @memberof GetAdvertisingReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetAdvertisingReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        if (message.placeType != null && message.hasOwnProperty("placeType"))
            if (!$util.isInteger(message.placeType))
                return "placeType: integer expected";
        return null;
    };

    /**
     * Creates a GetAdvertisingReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetAdvertisingReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetAdvertisingReq} GetAdvertisingReq
     */
    GetAdvertisingReq.fromObject = function fromObject(object) {
        if (object instanceof $root.GetAdvertisingReq)
            return object;
        let message = new $root.GetAdvertisingReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".GetAdvertisingReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        if (object.placeType != null)
            message.placeType = object.placeType | 0;
        return message;
    };

    /**
     * Creates a plain object from a GetAdvertisingReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetAdvertisingReq
     * @static
     * @param {GetAdvertisingReq} message GetAdvertisingReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetAdvertisingReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.clientInfo = null;
            object.placeType = 0;
        }
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        if (message.placeType != null && message.hasOwnProperty("placeType"))
            object.placeType = message.placeType;
        return object;
    };

    /**
     * Converts this GetAdvertisingReq to JSON.
     * @function toJSON
     * @memberof GetAdvertisingReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetAdvertisingReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetAdvertisingReq
     * @function getTypeUrl
     * @memberof GetAdvertisingReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetAdvertisingReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetAdvertisingReq";
    };

    return GetAdvertisingReq;
})();

export const GetAdvertisingResp = $root.GetAdvertisingResp = (() => {

    /**
     * Properties of a GetAdvertisingResp.
     * @exports IGetAdvertisingResp
     * @interface IGetAdvertisingResp
     * @property {ICommonResult|null} [commonResult] GetAdvertisingResp commonResult
     * @property {Array.<IAdvertising>|null} [list] GetAdvertisingResp list
     */

    /**
     * Constructs a new GetAdvertisingResp.
     * @exports GetAdvertisingResp
     * @classdesc Represents a GetAdvertisingResp.
     * @implements IGetAdvertisingResp
     * @constructor
     * @param {IGetAdvertisingResp=} [properties] Properties to set
     */
    function GetAdvertisingResp(properties) {
        this.list = [];
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GetAdvertisingResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof GetAdvertisingResp
     * @instance
     */
    GetAdvertisingResp.prototype.commonResult = null;

    /**
     * GetAdvertisingResp list.
     * @member {Array.<IAdvertising>} list
     * @memberof GetAdvertisingResp
     * @instance
     */
    GetAdvertisingResp.prototype.list = $util.emptyArray;

    /**
     * Creates a new GetAdvertisingResp instance using the specified properties.
     * @function create
     * @memberof GetAdvertisingResp
     * @static
     * @param {IGetAdvertisingResp=} [properties] Properties to set
     * @returns {GetAdvertisingResp} GetAdvertisingResp instance
     */
    GetAdvertisingResp.create = function create(properties) {
        return new GetAdvertisingResp(properties);
    };

    /**
     * Encodes the specified GetAdvertisingResp message. Does not implicitly {@link GetAdvertisingResp.verify|verify} messages.
     * @function encode
     * @memberof GetAdvertisingResp
     * @static
     * @param {IGetAdvertisingResp} message GetAdvertisingResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetAdvertisingResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.list != null && message.list.length)
            for (let i = 0; i < message.list.length; ++i)
                $root.Advertising.encode(message.list[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified GetAdvertisingResp message, length delimited. Does not implicitly {@link GetAdvertisingResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GetAdvertisingResp
     * @static
     * @param {IGetAdvertisingResp} message GetAdvertisingResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GetAdvertisingResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GetAdvertisingResp message from the specified reader or buffer.
     * @function decode
     * @memberof GetAdvertisingResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GetAdvertisingResp} GetAdvertisingResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetAdvertisingResp.decode = function decode(reader, length, error) {
    GetAdvertisingResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GetAdvertisingResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    if (!(message.list && message.list.length))
                        message.list = [];
                    message.list.push($root.Advertising.decode(reader, reader.uint32()));
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GetAdvertisingResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GetAdvertisingResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GetAdvertisingResp} GetAdvertisingResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GetAdvertisingResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GetAdvertisingResp message.
     * @function verify
     * @memberof GetAdvertisingResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GetAdvertisingResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        if (message.list != null && message.hasOwnProperty("list")) {
            if (!Array.isArray(message.list))
                return "list: array expected";
            for (let i = 0; i < message.list.length; ++i) {
                let error = $root.Advertising.verify(message.list[i]);
                if (error)
                    return "list." + error;
            }
        }
        return null;
    };

    /**
     * Creates a GetAdvertisingResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GetAdvertisingResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GetAdvertisingResp} GetAdvertisingResp
     */
    GetAdvertisingResp.fromObject = function fromObject(object) {
        if (object instanceof $root.GetAdvertisingResp)
            return object;
        let message = new $root.GetAdvertisingResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".GetAdvertisingResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        if (object.list) {
            if (!Array.isArray(object.list))
                throw TypeError(".GetAdvertisingResp.list: array expected");
            message.list = [];
            for (let i = 0; i < object.list.length; ++i) {
                if (typeof object.list[i] !== "object")
                    throw TypeError(".GetAdvertisingResp.list: object expected");
                message.list[i] = $root.Advertising.fromObject(object.list[i]);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a GetAdvertisingResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GetAdvertisingResp
     * @static
     * @param {GetAdvertisingResp} message GetAdvertisingResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GetAdvertisingResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.arrays || options.defaults)
            object.list = [];
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        if (message.list && message.list.length) {
            object.list = [];
            for (let j = 0; j < message.list.length; ++j)
                object.list[j] = $root.Advertising.toObject(message.list[j], options);
        }
        return object;
    };

    /**
     * Converts this GetAdvertisingResp to JSON.
     * @function toJSON
     * @memberof GetAdvertisingResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GetAdvertisingResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GetAdvertisingResp
     * @function getTypeUrl
     * @memberof GetAdvertisingResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GetAdvertisingResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GetAdvertisingResp";
    };

    return GetAdvertisingResp;
})();

export const Advertising = $root.Advertising = (() => {

    /**
     * Properties of an Advertising.
     * @exports IAdvertising
     * @interface IAdvertising
     * @property {string|null} [title] Advertising title
     * @property {string|null} [imageUrl] Advertising imageUrl
     * @property {string|null} [redirectUrl] Advertising redirectUrl
     * @property {number|null} [sort] Advertising sort
     * @property {number|null} [placeType] Advertising placeType
     */

    /**
     * Constructs a new Advertising.
     * @exports Advertising
     * @classdesc Represents an Advertising.
     * @implements IAdvertising
     * @constructor
     * @param {IAdvertising=} [properties] Properties to set
     */
    function Advertising(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * Advertising title.
     * @member {string} title
     * @memberof Advertising
     * @instance
     */
    Advertising.prototype.title = "";

    /**
     * Advertising imageUrl.
     * @member {string} imageUrl
     * @memberof Advertising
     * @instance
     */
    Advertising.prototype.imageUrl = "";

    /**
     * Advertising redirectUrl.
     * @member {string} redirectUrl
     * @memberof Advertising
     * @instance
     */
    Advertising.prototype.redirectUrl = "";

    /**
     * Advertising sort.
     * @member {number} sort
     * @memberof Advertising
     * @instance
     */
    Advertising.prototype.sort = 0;

    /**
     * Advertising placeType.
     * @member {number} placeType
     * @memberof Advertising
     * @instance
     */
    Advertising.prototype.placeType = 0;

    /**
     * Creates a new Advertising instance using the specified properties.
     * @function create
     * @memberof Advertising
     * @static
     * @param {IAdvertising=} [properties] Properties to set
     * @returns {Advertising} Advertising instance
     */
    Advertising.create = function create(properties) {
        return new Advertising(properties);
    };

    /**
     * Encodes the specified Advertising message. Does not implicitly {@link Advertising.verify|verify} messages.
     * @function encode
     * @memberof Advertising
     * @static
     * @param {IAdvertising} message Advertising message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Advertising.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.title != null && Object.hasOwnProperty.call(message, "title"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.title);
        if (message.imageUrl != null && Object.hasOwnProperty.call(message, "imageUrl"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.imageUrl);
        if (message.redirectUrl != null && Object.hasOwnProperty.call(message, "redirectUrl"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.redirectUrl);
        if (message.sort != null && Object.hasOwnProperty.call(message, "sort"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.sort);
        if (message.placeType != null && Object.hasOwnProperty.call(message, "placeType"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.placeType);
        return writer;
    };

    /**
     * Encodes the specified Advertising message, length delimited. Does not implicitly {@link Advertising.verify|verify} messages.
     * @function encodeDelimited
     * @memberof Advertising
     * @static
     * @param {IAdvertising} message Advertising message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Advertising.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an Advertising message from the specified reader or buffer.
     * @function decode
     * @memberof Advertising
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {Advertising} Advertising
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Advertising.decode = function decode(reader, length, error) {
    Advertising.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Advertising();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.title = reader.string();
                    break;
                }
            case 2: {
                    message.imageUrl = reader.string();
                    break;
                }
            case 3: {
                    message.redirectUrl = reader.string();
                    break;
                }
            case 4: {
                    message.sort = reader.int32();
                    break;
                }
            case 5: {
                    message.placeType = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an Advertising message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof Advertising
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {Advertising} Advertising
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Advertising.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an Advertising message.
     * @function verify
     * @memberof Advertising
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Advertising.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.title != null && message.hasOwnProperty("title"))
            if (!$util.isString(message.title))
                return "title: string expected";
        if (message.imageUrl != null && message.hasOwnProperty("imageUrl"))
            if (!$util.isString(message.imageUrl))
                return "imageUrl: string expected";
        if (message.redirectUrl != null && message.hasOwnProperty("redirectUrl"))
            if (!$util.isString(message.redirectUrl))
                return "redirectUrl: string expected";
        if (message.sort != null && message.hasOwnProperty("sort"))
            if (!$util.isInteger(message.sort))
                return "sort: integer expected";
        if (message.placeType != null && message.hasOwnProperty("placeType"))
            if (!$util.isInteger(message.placeType))
                return "placeType: integer expected";
        return null;
    };

    /**
     * Creates an Advertising message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof Advertising
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {Advertising} Advertising
     */
    Advertising.fromObject = function fromObject(object) {
        if (object instanceof $root.Advertising)
            return object;
        let message = new $root.Advertising();
        if (object.title != null)
            message.title = String(object.title);
        if (object.imageUrl != null)
            message.imageUrl = String(object.imageUrl);
        if (object.redirectUrl != null)
            message.redirectUrl = String(object.redirectUrl);
        if (object.sort != null)
            message.sort = object.sort | 0;
        if (object.placeType != null)
            message.placeType = object.placeType | 0;
        return message;
    };

    /**
     * Creates a plain object from an Advertising message. Also converts values to other types if specified.
     * @function toObject
     * @memberof Advertising
     * @static
     * @param {Advertising} message Advertising
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Advertising.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.title = "";
            object.imageUrl = "";
            object.redirectUrl = "";
            object.sort = 0;
            object.placeType = 0;
        }
        if (message.title != null && message.hasOwnProperty("title"))
            object.title = message.title;
        if (message.imageUrl != null && message.hasOwnProperty("imageUrl"))
            object.imageUrl = message.imageUrl;
        if (message.redirectUrl != null && message.hasOwnProperty("redirectUrl"))
            object.redirectUrl = message.redirectUrl;
        if (message.sort != null && message.hasOwnProperty("sort"))
            object.sort = message.sort;
        if (message.placeType != null && message.hasOwnProperty("placeType"))
            object.placeType = message.placeType;
        return object;
    };

    /**
     * Converts this Advertising to JSON.
     * @function toJSON
     * @memberof Advertising
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Advertising.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Advertising
     * @function getTypeUrl
     * @memberof Advertising
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Advertising.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/Advertising";
    };

    return Advertising;
})();

export const ClientInfo = $root.ClientInfo = (() => {

    /**
     * Properties of a ClientInfo.
     * @exports IClientInfo
     * @interface IClientInfo
     * @property {string|null} [sessionId] ClientInfo sessionId
     * @property {number|null} [appVer] ClientInfo appVer
     * @property {number|null} [packageCode] ClientInfo packageCode
     * @property {Platform|null} [plat] ClientInfo plat
     * @property {number|null} [language] ClientInfo language
     * @property {string|null} [sysMac] ClientInfo sysMac
     * @property {string|null} [sysModel] ClientInfo sysModel
     * @property {string|null} [token] ClientInfo token
     * @property {string|null} [version] ClientInfo version
     */

    /**
     * Constructs a new ClientInfo.
     * @exports ClientInfo
     * @classdesc Represents a ClientInfo.
     * @implements IClientInfo
     * @constructor
     * @param {IClientInfo=} [properties] Properties to set
     */
    function ClientInfo(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * ClientInfo sessionId.
     * @member {string} sessionId
     * @memberof ClientInfo
     * @instance
     */
    ClientInfo.prototype.sessionId = "";

    /**
     * ClientInfo appVer.
     * @member {number} appVer
     * @memberof ClientInfo
     * @instance
     */
    ClientInfo.prototype.appVer = 0;

    /**
     * ClientInfo packageCode.
     * @member {number} packageCode
     * @memberof ClientInfo
     * @instance
     */
    ClientInfo.prototype.packageCode = 0;

    /**
     * ClientInfo plat.
     * @member {Platform} plat
     * @memberof ClientInfo
     * @instance
     */
    ClientInfo.prototype.plat = 0;

    /**
     * ClientInfo language.
     * @member {number} language
     * @memberof ClientInfo
     * @instance
     */
    ClientInfo.prototype.language = 0;

    /**
     * ClientInfo sysMac.
     * @member {string} sysMac
     * @memberof ClientInfo
     * @instance
     */
    ClientInfo.prototype.sysMac = "";

    /**
     * ClientInfo sysModel.
     * @member {string} sysModel
     * @memberof ClientInfo
     * @instance
     */
    ClientInfo.prototype.sysModel = "";

    /**
     * ClientInfo token.
     * @member {string} token
     * @memberof ClientInfo
     * @instance
     */
    ClientInfo.prototype.token = "";

    /**
     * ClientInfo version.
     * @member {string} version
     * @memberof ClientInfo
     * @instance
     */
    ClientInfo.prototype.version = "";

    /**
     * Creates a new ClientInfo instance using the specified properties.
     * @function create
     * @memberof ClientInfo
     * @static
     * @param {IClientInfo=} [properties] Properties to set
     * @returns {ClientInfo} ClientInfo instance
     */
    ClientInfo.create = function create(properties) {
        return new ClientInfo(properties);
    };

    /**
     * Encodes the specified ClientInfo message. Does not implicitly {@link ClientInfo.verify|verify} messages.
     * @function encode
     * @memberof ClientInfo
     * @static
     * @param {IClientInfo} message ClientInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ClientInfo.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.sessionId);
        if (message.appVer != null && Object.hasOwnProperty.call(message, "appVer"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.appVer);
        if (message.packageCode != null && Object.hasOwnProperty.call(message, "packageCode"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.packageCode);
        if (message.plat != null && Object.hasOwnProperty.call(message, "plat"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.plat);
        if (message.language != null && Object.hasOwnProperty.call(message, "language"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.language);
        if (message.sysMac != null && Object.hasOwnProperty.call(message, "sysMac"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.sysMac);
        if (message.sysModel != null && Object.hasOwnProperty.call(message, "sysModel"))
            writer.uint32(/* id 7, wireType 2 =*/58).string(message.sysModel);
        if (message.token != null && Object.hasOwnProperty.call(message, "token"))
            writer.uint32(/* id 8, wireType 2 =*/66).string(message.token);
        if (message.version != null && Object.hasOwnProperty.call(message, "version"))
            writer.uint32(/* id 9, wireType 2 =*/74).string(message.version);
        return writer;
    };

    /**
     * Encodes the specified ClientInfo message, length delimited. Does not implicitly {@link ClientInfo.verify|verify} messages.
     * @function encodeDelimited
     * @memberof ClientInfo
     * @static
     * @param {IClientInfo} message ClientInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ClientInfo.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ClientInfo message from the specified reader or buffer.
     * @function decode
     * @memberof ClientInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {ClientInfo} ClientInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ClientInfo.decode = function decode(reader, length, error) {
    ClientInfo.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.ClientInfo();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.sessionId = reader.string();
                    break;
                }
            case 2: {
                    message.appVer = reader.int32();
                    break;
                }
            case 3: {
                    message.packageCode = reader.int32();
                    break;
                }
            case 4: {
                    message.plat = reader.int32();
                    break;
                }
            case 5: {
                    message.language = reader.int32();
                    break;
                }
            case 6: {
                    message.sysMac = reader.string();
                    break;
                }
            case 7: {
                    message.sysModel = reader.string();
                    break;
                }
            case 8: {
                    message.token = reader.string();
                    break;
                }
            case 9: {
                    message.version = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a ClientInfo message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof ClientInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {ClientInfo} ClientInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ClientInfo.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ClientInfo message.
     * @function verify
     * @memberof ClientInfo
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ClientInfo.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.sessionId != null && message.hasOwnProperty("sessionId"))
            if (!$util.isString(message.sessionId))
                return "sessionId: string expected";
        if (message.appVer != null && message.hasOwnProperty("appVer"))
            if (!$util.isInteger(message.appVer))
                return "appVer: integer expected";
        if (message.packageCode != null && message.hasOwnProperty("packageCode"))
            if (!$util.isInteger(message.packageCode))
                return "packageCode: integer expected";
        if (message.plat != null && message.hasOwnProperty("plat"))
            switch (message.plat) {
            default:
                return "plat: enum value expected";
            case 0:
            case 1:
            case 2:
            case 3:
            case 4:
            case 5:
            case 5:
                break;
            }
        if (message.language != null && message.hasOwnProperty("language"))
            if (!$util.isInteger(message.language))
                return "language: integer expected";
        if (message.sysMac != null && message.hasOwnProperty("sysMac"))
            if (!$util.isString(message.sysMac))
                return "sysMac: string expected";
        if (message.sysModel != null && message.hasOwnProperty("sysModel"))
            if (!$util.isString(message.sysModel))
                return "sysModel: string expected";
        if (message.token != null && message.hasOwnProperty("token"))
            if (!$util.isString(message.token))
                return "token: string expected";
        if (message.version != null && message.hasOwnProperty("version"))
            if (!$util.isString(message.version))
                return "version: string expected";
        return null;
    };

    /**
     * Creates a ClientInfo message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof ClientInfo
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {ClientInfo} ClientInfo
     */
    ClientInfo.fromObject = function fromObject(object) {
        if (object instanceof $root.ClientInfo)
            return object;
        let message = new $root.ClientInfo();
        if (object.sessionId != null)
            message.sessionId = String(object.sessionId);
        if (object.appVer != null)
            message.appVer = object.appVer | 0;
        if (object.packageCode != null)
            message.packageCode = object.packageCode | 0;
        switch (object.plat) {
        default:
            if (typeof object.plat === "number") {
                message.plat = object.plat;
                break;
            }
            break;
        case "ANDROID":
        case 0:
            message.plat = 0;
            break;
        case "IPHONE":
        case 1:
            message.plat = 1;
            break;
        case "UNKOWN":
        case 2:
            message.plat = 2;
            break;
        case "MAC":
        case 3:
            message.plat = 3;
            break;
        case "WIN":
        case 4:
            message.plat = 4;
            break;
        case "HARMONYOS":
        case 5:
            message.plat = 5;
            break;
        case "HARMONYOS":
        case 5:
            message.plat = 5;
            break;
        }
        if (object.language != null)
            message.language = object.language | 0;
        if (object.sysMac != null)
            message.sysMac = String(object.sysMac);
        if (object.sysModel != null)
            message.sysModel = String(object.sysModel);
        if (object.token != null)
            message.token = String(object.token);
        if (object.version != null)
            message.version = String(object.version);
        return message;
    };

    /**
     * Creates a plain object from a ClientInfo message. Also converts values to other types if specified.
     * @function toObject
     * @memberof ClientInfo
     * @static
     * @param {ClientInfo} message ClientInfo
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ClientInfo.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.sessionId = "";
            object.appVer = 0;
            object.packageCode = 0;
            object.plat = options.enums === String ? "ANDROID" : 0;
            object.language = 0;
            object.sysMac = "";
            object.sysModel = "";
            object.token = "";
            object.version = "";
        }
        if (message.sessionId != null && message.hasOwnProperty("sessionId"))
            object.sessionId = message.sessionId;
        if (message.appVer != null && message.hasOwnProperty("appVer"))
            object.appVer = message.appVer;
        if (message.packageCode != null && message.hasOwnProperty("packageCode"))
            object.packageCode = message.packageCode;
        if (message.plat != null && message.hasOwnProperty("plat"))
            object.plat = options.enums === String ? $root.Platform[message.plat] === undefined ? message.plat : $root.Platform[message.plat] : message.plat;
        if (message.language != null && message.hasOwnProperty("language"))
            object.language = message.language;
        if (message.sysMac != null && message.hasOwnProperty("sysMac"))
            object.sysMac = message.sysMac;
        if (message.sysModel != null && message.hasOwnProperty("sysModel"))
            object.sysModel = message.sysModel;
        if (message.token != null && message.hasOwnProperty("token"))
            object.token = message.token;
        if (message.version != null && message.hasOwnProperty("version"))
            object.version = message.version;
        return object;
    };

    /**
     * Converts this ClientInfo to JSON.
     * @function toJSON
     * @memberof ClientInfo
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ClientInfo.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ClientInfo
     * @function getTypeUrl
     * @memberof ClientInfo
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ClientInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/ClientInfo";
    };

    return ClientInfo;
})();

export const CommonResult = $root.CommonResult = (() => {

    /**
     * Properties of a CommonResult.
     * @exports ICommonResult
     * @interface ICommonResult
     * @property {number|null} [errCode] CommonResult errCode
     * @property {string|null} [errMsg] CommonResult errMsg
     * @property {string|null} [flag] CommonResult flag
     */

    /**
     * Constructs a new CommonResult.
     * @exports CommonResult
     * @classdesc Represents a CommonResult.
     * @implements ICommonResult
     * @constructor
     * @param {ICommonResult=} [properties] Properties to set
     */
    function CommonResult(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CommonResult errCode.
     * @member {number} errCode
     * @memberof CommonResult
     * @instance
     */
    CommonResult.prototype.errCode = 0;

    /**
     * CommonResult errMsg.
     * @member {string} errMsg
     * @memberof CommonResult
     * @instance
     */
    CommonResult.prototype.errMsg = "";

    /**
     * CommonResult flag.
     * @member {string} flag
     * @memberof CommonResult
     * @instance
     */
    CommonResult.prototype.flag = "";

    /**
     * Creates a new CommonResult instance using the specified properties.
     * @function create
     * @memberof CommonResult
     * @static
     * @param {ICommonResult=} [properties] Properties to set
     * @returns {CommonResult} CommonResult instance
     */
    CommonResult.create = function create(properties) {
        return new CommonResult(properties);
    };

    /**
     * Encodes the specified CommonResult message. Does not implicitly {@link CommonResult.verify|verify} messages.
     * @function encode
     * @memberof CommonResult
     * @static
     * @param {ICommonResult} message CommonResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CommonResult.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.errCode != null && Object.hasOwnProperty.call(message, "errCode"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.errCode);
        if (message.errMsg != null && Object.hasOwnProperty.call(message, "errMsg"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.errMsg);
        if (message.flag != null && Object.hasOwnProperty.call(message, "flag"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.flag);
        return writer;
    };

    /**
     * Encodes the specified CommonResult message, length delimited. Does not implicitly {@link CommonResult.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CommonResult
     * @static
     * @param {ICommonResult} message CommonResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CommonResult.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CommonResult message from the specified reader or buffer.
     * @function decode
     * @memberof CommonResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CommonResult} CommonResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CommonResult.decode = function decode(reader, length, error) {
    CommonResult.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CommonResult();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.errCode = reader.int32();
                    break;
                }
            case 2: {
                    message.errMsg = reader.string();
                    break;
                }
            case 3: {
                    message.flag = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CommonResult message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CommonResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CommonResult} CommonResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CommonResult.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CommonResult message.
     * @function verify
     * @memberof CommonResult
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CommonResult.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.errCode != null && message.hasOwnProperty("errCode"))
            if (!$util.isInteger(message.errCode))
                return "errCode: integer expected";
        if (message.errMsg != null && message.hasOwnProperty("errMsg"))
            if (!$util.isString(message.errMsg))
                return "errMsg: string expected";
        if (message.flag != null && message.hasOwnProperty("flag"))
            if (!$util.isString(message.flag))
                return "flag: string expected";
        return null;
    };

    /**
     * Creates a CommonResult message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CommonResult
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CommonResult} CommonResult
     */
    CommonResult.fromObject = function fromObject(object) {
        if (object instanceof $root.CommonResult)
            return object;
        let message = new $root.CommonResult();
        if (object.errCode != null)
            message.errCode = object.errCode | 0;
        if (object.errMsg != null)
            message.errMsg = String(object.errMsg);
        if (object.flag != null)
            message.flag = String(object.flag);
        return message;
    };

    /**
     * Creates a plain object from a CommonResult message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CommonResult
     * @static
     * @param {CommonResult} message CommonResult
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CommonResult.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.errCode = 0;
            object.errMsg = "";
            object.flag = "";
        }
        if (message.errCode != null && message.hasOwnProperty("errCode"))
            object.errCode = message.errCode;
        if (message.errMsg != null && message.hasOwnProperty("errMsg"))
            object.errMsg = message.errMsg;
        if (message.flag != null && message.hasOwnProperty("flag"))
            object.flag = message.flag;
        return object;
    };

    /**
     * Converts this CommonResult to JSON.
     * @function toJSON
     * @memberof CommonResult
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CommonResult.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CommonResult
     * @function getTypeUrl
     * @memberof CommonResult
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CommonResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CommonResult";
    };

    return CommonResult;
})();

export const CommonResultResp = $root.CommonResultResp = (() => {

    /**
     * Properties of a CommonResultResp.
     * @exports ICommonResultResp
     * @interface ICommonResultResp
     * @property {ICommonResult|null} [commonResult] CommonResultResp commonResult
     */

    /**
     * Constructs a new CommonResultResp.
     * @exports CommonResultResp
     * @classdesc Represents a CommonResultResp.
     * @implements ICommonResultResp
     * @constructor
     * @param {ICommonResultResp=} [properties] Properties to set
     */
    function CommonResultResp(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CommonResultResp commonResult.
     * @member {ICommonResult|null|undefined} commonResult
     * @memberof CommonResultResp
     * @instance
     */
    CommonResultResp.prototype.commonResult = null;

    /**
     * Creates a new CommonResultResp instance using the specified properties.
     * @function create
     * @memberof CommonResultResp
     * @static
     * @param {ICommonResultResp=} [properties] Properties to set
     * @returns {CommonResultResp} CommonResultResp instance
     */
    CommonResultResp.create = function create(properties) {
        return new CommonResultResp(properties);
    };

    /**
     * Encodes the specified CommonResultResp message. Does not implicitly {@link CommonResultResp.verify|verify} messages.
     * @function encode
     * @memberof CommonResultResp
     * @static
     * @param {ICommonResultResp} message CommonResultResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CommonResultResp.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.commonResult != null && Object.hasOwnProperty.call(message, "commonResult"))
            $root.CommonResult.encode(message.commonResult, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified CommonResultResp message, length delimited. Does not implicitly {@link CommonResultResp.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CommonResultResp
     * @static
     * @param {ICommonResultResp} message CommonResultResp message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CommonResultResp.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CommonResultResp message from the specified reader or buffer.
     * @function decode
     * @memberof CommonResultResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CommonResultResp} CommonResultResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CommonResultResp.decode = function decode(reader, length, error) {
    CommonResultResp.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CommonResultResp();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.commonResult = $root.CommonResult.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CommonResultResp message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CommonResultResp
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CommonResultResp} CommonResultResp
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CommonResultResp.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CommonResultResp message.
     * @function verify
     * @memberof CommonResultResp
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CommonResultResp.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.commonResult != null && message.hasOwnProperty("commonResult")) {
            let error = $root.CommonResult.verify(message.commonResult);
            if (error)
                return "commonResult." + error;
        }
        return null;
    };

    /**
     * Creates a CommonResultResp message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CommonResultResp
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CommonResultResp} CommonResultResp
     */
    CommonResultResp.fromObject = function fromObject(object) {
        if (object instanceof $root.CommonResultResp)
            return object;
        let message = new $root.CommonResultResp();
        if (object.commonResult != null) {
            if (typeof object.commonResult !== "object")
                throw TypeError(".CommonResultResp.commonResult: object expected");
            message.commonResult = $root.CommonResult.fromObject(object.commonResult);
        }
        return message;
    };

    /**
     * Creates a plain object from a CommonResultResp message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CommonResultResp
     * @static
     * @param {CommonResultResp} message CommonResultResp
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CommonResultResp.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.commonResult = null;
        if (message.commonResult != null && message.hasOwnProperty("commonResult"))
            object.commonResult = $root.CommonResult.toObject(message.commonResult, options);
        return object;
    };

    /**
     * Converts this CommonResultResp to JSON.
     * @function toJSON
     * @memberof CommonResultResp
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CommonResultResp.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CommonResultResp
     * @function getTypeUrl
     * @memberof CommonResultResp
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CommonResultResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CommonResultResp";
    };

    return CommonResultResp;
})();

export const CommonResultReq = $root.CommonResultReq = (() => {

    /**
     * Properties of a CommonResultReq.
     * @exports ICommonResultReq
     * @interface ICommonResultReq
     * @property {IClientInfo|null} [clientInfo] CommonResultReq clientInfo
     */

    /**
     * Constructs a new CommonResultReq.
     * @exports CommonResultReq
     * @classdesc Represents a CommonResultReq.
     * @implements ICommonResultReq
     * @constructor
     * @param {ICommonResultReq=} [properties] Properties to set
     */
    function CommonResultReq(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CommonResultReq clientInfo.
     * @member {IClientInfo|null|undefined} clientInfo
     * @memberof CommonResultReq
     * @instance
     */
    CommonResultReq.prototype.clientInfo = null;

    /**
     * Creates a new CommonResultReq instance using the specified properties.
     * @function create
     * @memberof CommonResultReq
     * @static
     * @param {ICommonResultReq=} [properties] Properties to set
     * @returns {CommonResultReq} CommonResultReq instance
     */
    CommonResultReq.create = function create(properties) {
        return new CommonResultReq(properties);
    };

    /**
     * Encodes the specified CommonResultReq message. Does not implicitly {@link CommonResultReq.verify|verify} messages.
     * @function encode
     * @memberof CommonResultReq
     * @static
     * @param {ICommonResultReq} message CommonResultReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CommonResultReq.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
            $root.ClientInfo.encode(message.clientInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified CommonResultReq message, length delimited. Does not implicitly {@link CommonResultReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CommonResultReq
     * @static
     * @param {ICommonResultReq} message CommonResultReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CommonResultReq.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CommonResultReq message from the specified reader or buffer.
     * @function decode
     * @memberof CommonResultReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CommonResultReq} CommonResultReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CommonResultReq.decode = function decode(reader, length, error) {
    CommonResultReq.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CommonResultReq();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.clientInfo = $root.ClientInfo.decode(reader, reader.uint32());
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CommonResultReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CommonResultReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CommonResultReq} CommonResultReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CommonResultReq.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CommonResultReq message.
     * @function verify
     * @memberof CommonResultReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CommonResultReq.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo")) {
            let error = $root.ClientInfo.verify(message.clientInfo);
            if (error)
                return "clientInfo." + error;
        }
        return null;
    };

    /**
     * Creates a CommonResultReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CommonResultReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CommonResultReq} CommonResultReq
     */
    CommonResultReq.fromObject = function fromObject(object) {
        if (object instanceof $root.CommonResultReq)
            return object;
        let message = new $root.CommonResultReq();
        if (object.clientInfo != null) {
            if (typeof object.clientInfo !== "object")
                throw TypeError(".CommonResultReq.clientInfo: object expected");
            message.clientInfo = $root.ClientInfo.fromObject(object.clientInfo);
        }
        return message;
    };

    /**
     * Creates a plain object from a CommonResultReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CommonResultReq
     * @static
     * @param {CommonResultReq} message CommonResultReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CommonResultReq.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults)
            object.clientInfo = null;
        if (message.clientInfo != null && message.hasOwnProperty("clientInfo"))
            object.clientInfo = $root.ClientInfo.toObject(message.clientInfo, options);
        return object;
    };

    /**
     * Converts this CommonResultReq to JSON.
     * @function toJSON
     * @memberof CommonResultReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CommonResultReq.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CommonResultReq
     * @function getTypeUrl
     * @memberof CommonResultReq
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CommonResultReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CommonResultReq";
    };

    return CommonResultReq;
})();

/**
 * AccountType enum.
 * @exports AccountType
 * @enum {number}
 * @property {number} MOBILE=0 MOBILE value
 * @property {number} EMAIL=1 EMAIL value
 */
export const AccountType = $root.AccountType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "MOBILE"] = 0;
    values[valuesById[1] = "EMAIL"] = 1;
    return values;
})();

export const ValidateCode = $root.ValidateCode = (() => {

    /**
     * Properties of a ValidateCode.
     * @exports IValidateCode
     * @interface IValidateCode
     * @property {string|null} [validateValue] ValidateCode validateValue
     * @property {string|null} [validateAccount] ValidateCode validateAccount
     * @property {string|null} [countryCode] ValidateCode countryCode
     * @property {GetValidateCodeType|null} [validateType] ValidateCode validateType
     */

    /**
     * Constructs a new ValidateCode.
     * @exports ValidateCode
     * @classdesc Represents a ValidateCode.
     * @implements IValidateCode
     * @constructor
     * @param {IValidateCode=} [properties] Properties to set
     */
    function ValidateCode(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * ValidateCode validateValue.
     * @member {string} validateValue
     * @memberof ValidateCode
     * @instance
     */
    ValidateCode.prototype.validateValue = "";

    /**
     * ValidateCode validateAccount.
     * @member {string} validateAccount
     * @memberof ValidateCode
     * @instance
     */
    ValidateCode.prototype.validateAccount = "";

    /**
     * ValidateCode countryCode.
     * @member {string} countryCode
     * @memberof ValidateCode
     * @instance
     */
    ValidateCode.prototype.countryCode = "";

    /**
     * ValidateCode validateType.
     * @member {GetValidateCodeType} validateType
     * @memberof ValidateCode
     * @instance
     */
    ValidateCode.prototype.validateType = 0;

    /**
     * Creates a new ValidateCode instance using the specified properties.
     * @function create
     * @memberof ValidateCode
     * @static
     * @param {IValidateCode=} [properties] Properties to set
     * @returns {ValidateCode} ValidateCode instance
     */
    ValidateCode.create = function create(properties) {
        return new ValidateCode(properties);
    };

    /**
     * Encodes the specified ValidateCode message. Does not implicitly {@link ValidateCode.verify|verify} messages.
     * @function encode
     * @memberof ValidateCode
     * @static
     * @param {IValidateCode} message ValidateCode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ValidateCode.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.validateValue != null && Object.hasOwnProperty.call(message, "validateValue"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.validateValue);
        if (message.validateAccount != null && Object.hasOwnProperty.call(message, "validateAccount"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.validateAccount);
        if (message.countryCode != null && Object.hasOwnProperty.call(message, "countryCode"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.countryCode);
        if (message.validateType != null && Object.hasOwnProperty.call(message, "validateType"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.validateType);
        return writer;
    };

    /**
     * Encodes the specified ValidateCode message, length delimited. Does not implicitly {@link ValidateCode.verify|verify} messages.
     * @function encodeDelimited
     * @memberof ValidateCode
     * @static
     * @param {IValidateCode} message ValidateCode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ValidateCode.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ValidateCode message from the specified reader or buffer.
     * @function decode
     * @memberof ValidateCode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {ValidateCode} ValidateCode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ValidateCode.decode = function decode(reader, length, error) {
    ValidateCode.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.ValidateCode();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.validateValue = reader.string();
                    break;
                }
            case 2: {
                    message.validateAccount = reader.string();
                    break;
                }
            case 3: {
                    message.countryCode = reader.string();
                    break;
                }
            case 4: {
                    message.validateType = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a ValidateCode message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof ValidateCode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {ValidateCode} ValidateCode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ValidateCode.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ValidateCode message.
     * @function verify
     * @memberof ValidateCode
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ValidateCode.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.validateValue != null && message.hasOwnProperty("validateValue"))
            if (!$util.isString(message.validateValue))
                return "validateValue: string expected";
        if (message.validateAccount != null && message.hasOwnProperty("validateAccount"))
            if (!$util.isString(message.validateAccount))
                return "validateAccount: string expected";
        if (message.countryCode != null && message.hasOwnProperty("countryCode"))
            if (!$util.isString(message.countryCode))
                return "countryCode: string expected";
        if (message.validateType != null && message.hasOwnProperty("validateType"))
            switch (message.validateType) {
            default:
                return "validateType: enum value expected";
            case 0:
            case 1:
            case 2:
            case 3:
            case 4:
            case 6:
            case 7:
            case 8:
            case 9:
            case 10:
            case 11:
            case 12:
            case 13:
            case 14:
            case 15:
            case 16:
            case 17:
            case 18:
            case 19:
            case 20:
            case 21:
            case 22:
            case 23:
            case 23:
                break;
            }
        return null;
    };

    /**
     * Creates a ValidateCode message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof ValidateCode
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {ValidateCode} ValidateCode
     */
    ValidateCode.fromObject = function fromObject(object) {
        if (object instanceof $root.ValidateCode)
            return object;
        let message = new $root.ValidateCode();
        if (object.validateValue != null)
            message.validateValue = String(object.validateValue);
        if (object.validateAccount != null)
            message.validateAccount = String(object.validateAccount);
        if (object.countryCode != null)
            message.countryCode = String(object.countryCode);
        switch (object.validateType) {
        default:
            if (typeof object.validateType === "number") {
                message.validateType = object.validateType;
                break;
            }
            break;
        case "REG":
        case 0:
            message.validateType = 0;
            break;
        case "LOGIN":
        case 1:
            message.validateType = 1;
            break;
        case "FIND_PASSWORD":
        case 2:
            message.validateType = 2;
            break;
        case "UPDATE_PASSWORD":
        case 3:
            message.validateType = 3;
            break;
        case "UPDATE_PHONE":
        case 4:
            message.validateType = 4;
            break;
        case "VALIDATE_PASSWORD":
        case 6:
            message.validateType = 6;
            break;
        case "FIND_GESTURE_PASSWORD":
        case 7:
            message.validateType = 7;
            break;
        case "TRADE_PASSWORD":
        case 8:
            message.validateType = 8;
            break;
        case "UPDATE_TRADE_PASSWORD":
        case 9:
            message.validateType = 9;
            break;
        case "BIND_PHONE":
        case 10:
            message.validateType = 10;
            break;
        case "BIND_EMAIL":
        case 11:
            message.validateType = 11;
            break;
        case "UPDATE_EMAIL":
        case 12:
            message.validateType = 12;
            break;
        case "VALIDATE_GOOGLE_AUTH":
        case 13:
            message.validateType = 13;
            break;
        case "UPDATE_SECURE":
        case 14:
            message.validateType = 14;
            break;
        case "CASH_OUT":
        case 15:
            message.validateType = 15;
            break;
        case "VERIFY_EMAIL":
        case 16:
            message.validateType = 16;
            break;
        case "VERIFY_PHONE":
        case 17:
            message.validateType = 17;
            break;
        case "VERIFY_TRADE_PASSWORD":
        case 18:
            message.validateType = 18;
            break;
        case "VERIFY_GOOGLE_CODE":
        case 19:
            message.validateType = 19;
            break;
        case "BIND_ACCOUNT":
        case 20:
            message.validateType = 20;
            break;
        case "VERIFY_LOGIN_PHONE":
        case 21:
            message.validateType = 21;
            break;
        case "VERIFY_LOGIN_EMAIL":
        case 22:
            message.validateType = 22;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.validateType = 23;
            break;
        case "VERIFY_LOGIN_PASSWORD":
        case 23:
            message.validateType = 23;
            break;
        }
        return message;
    };

    /**
     * Creates a plain object from a ValidateCode message. Also converts values to other types if specified.
     * @function toObject
     * @memberof ValidateCode
     * @static
     * @param {ValidateCode} message ValidateCode
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ValidateCode.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.validateValue = "";
            object.validateAccount = "";
            object.countryCode = "";
            object.validateType = options.enums === String ? "REG" : 0;
        }
        if (message.validateValue != null && message.hasOwnProperty("validateValue"))
            object.validateValue = message.validateValue;
        if (message.validateAccount != null && message.hasOwnProperty("validateAccount"))
            object.validateAccount = message.validateAccount;
        if (message.countryCode != null && message.hasOwnProperty("countryCode"))
            object.countryCode = message.countryCode;
        if (message.validateType != null && message.hasOwnProperty("validateType"))
            object.validateType = options.enums === String ? $root.GetValidateCodeType[message.validateType] === undefined ? message.validateType : $root.GetValidateCodeType[message.validateType] : message.validateType;
        return object;
    };

    /**
     * Converts this ValidateCode to JSON.
     * @function toJSON
     * @memberof ValidateCode
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ValidateCode.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ValidateCode
     * @function getTypeUrl
     * @memberof ValidateCode
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ValidateCode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/ValidateCode";
    };

    return ValidateCode;
})();

/**
 * GetValidateCodeType enum.
 * @exports GetValidateCodeType
 * @enum {number}
 * @property {number} REG=0 REG value
 * @property {number} LOGIN=1 LOGIN value
 * @property {number} FIND_PASSWORD=2 FIND_PASSWORD value
 * @property {number} UPDATE_PASSWORD=3 UPDATE_PASSWORD value
 * @property {number} UPDATE_PHONE=4 UPDATE_PHONE value
 * @property {number} VALIDATE_PASSWORD=6 VALIDATE_PASSWORD value
 * @property {number} FIND_GESTURE_PASSWORD=7 FIND_GESTURE_PASSWORD value
 * @property {number} TRADE_PASSWORD=8 TRADE_PASSWORD value
 * @property {number} UPDATE_TRADE_PASSWORD=9 UPDATE_TRADE_PASSWORD value
 * @property {number} BIND_PHONE=10 BIND_PHONE value
 * @property {number} BIND_EMAIL=11 BIND_EMAIL value
 * @property {number} UPDATE_EMAIL=12 UPDATE_EMAIL value
 * @property {number} VALIDATE_GOOGLE_AUTH=13 VALIDATE_GOOGLE_AUTH value
 * @property {number} UPDATE_SECURE=14 UPDATE_SECURE value
 * @property {number} CASH_OUT=15 CASH_OUT value
 * @property {number} VERIFY_EMAIL=16 VERIFY_EMAIL value
 * @property {number} VERIFY_PHONE=17 VERIFY_PHONE value
 * @property {number} VERIFY_TRADE_PASSWORD=18 VERIFY_TRADE_PASSWORD value
 * @property {number} VERIFY_GOOGLE_CODE=19 VERIFY_GOOGLE_CODE value
 * @property {number} BIND_ACCOUNT=20 BIND_ACCOUNT value
 * @property {number} VERIFY_LOGIN_PHONE=21 VERIFY_LOGIN_PHONE value
 * @property {number} VERIFY_LOGIN_EMAIL=22 VERIFY_LOGIN_EMAIL value
 * @property {number} VERIFY_LOGIN_PASSWORD=23 VERIFY_LOGIN_PASSWORD value
 * @property {number} VERIFY_LOGIN_PASSWORD=23 VERIFY_LOGIN_PASSWORD value
 */
export const GetValidateCodeType = $root.GetValidateCodeType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "REG"] = 0;
    values[valuesById[1] = "LOGIN"] = 1;
    values[valuesById[2] = "FIND_PASSWORD"] = 2;
    values[valuesById[3] = "UPDATE_PASSWORD"] = 3;
    values[valuesById[4] = "UPDATE_PHONE"] = 4;
    values[valuesById[6] = "VALIDATE_PASSWORD"] = 6;
    values[valuesById[7] = "FIND_GESTURE_PASSWORD"] = 7;
    values[valuesById[8] = "TRADE_PASSWORD"] = 8;
    values[valuesById[9] = "UPDATE_TRADE_PASSWORD"] = 9;
    values[valuesById[10] = "BIND_PHONE"] = 10;
    values[valuesById[11] = "BIND_EMAIL"] = 11;
    values[valuesById[12] = "UPDATE_EMAIL"] = 12;
    values[valuesById[13] = "VALIDATE_GOOGLE_AUTH"] = 13;
    values[valuesById[14] = "UPDATE_SECURE"] = 14;
    values[valuesById[15] = "CASH_OUT"] = 15;
    values[valuesById[16] = "VERIFY_EMAIL"] = 16;
    values[valuesById[17] = "VERIFY_PHONE"] = 17;
    values[valuesById[18] = "VERIFY_TRADE_PASSWORD"] = 18;
    values[valuesById[19] = "VERIFY_GOOGLE_CODE"] = 19;
    values[valuesById[20] = "BIND_ACCOUNT"] = 20;
    values[valuesById[21] = "VERIFY_LOGIN_PHONE"] = 21;
    values[valuesById[22] = "VERIFY_LOGIN_EMAIL"] = 22;
    values[valuesById[23] = "VERIFY_LOGIN_PASSWORD"] = 23;
    values[valuesById[23] = "VERIFY_LOGIN_PASSWORD"] = 23;
    return values;
})();

/**
 * LoginMode enum.
 * @exports LoginMode
 * @enum {number}
 * @property {number} HAND=0 HAND value
 * @property {number} SYS_AUTO=1 SYS_AUTO value
 */
export const LoginMode = $root.LoginMode = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "HAND"] = 0;
    values[valuesById[1] = "SYS_AUTO"] = 1;
    return values;
})();

/**
 * LoginType enum.
 * @exports LoginType
 * @enum {number}
 * @property {number} SMS_CODE=0 SMS_CODE value
 * @property {number} PASSWORD=1 PASSWORD value
 * @property {number} AUTH_KEY=2 AUTH_KEY value
 * @property {number} GOOGLE_TOKEN=3 GOOGLE_TOKEN value
 * @property {number} APPLE_TOKEN=4 APPLE_TOKEN value
 */
export const LoginType = $root.LoginType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "SMS_CODE"] = 0;
    values[valuesById[1] = "PASSWORD"] = 1;
    values[valuesById[2] = "AUTH_KEY"] = 2;
    values[valuesById[3] = "GOOGLE_TOKEN"] = 3;
    values[valuesById[4] = "APPLE_TOKEN"] = 4;
    return values;
})();

/**
 * Gender enum.
 * @exports Gender
 * @enum {number}
 * @property {number} SECRECY=0 SECRECY value
 * @property {number} MALE=1 MALE value
 * @property {number} FEMALE=2 FEMALE value
 */
export const Gender = $root.Gender = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "SECRECY"] = 0;
    values[valuesById[1] = "MALE"] = 1;
    values[valuesById[2] = "FEMALE"] = 2;
    return values;
})();

/**
 * Platform enum.
 * @exports Platform
 * @enum {number}
 * @property {number} ANDROID=0 ANDROID value
 * @property {number} IPHONE=1 IPHONE value
 * @property {number} UNKOWN=2 UNKOWN value
 * @property {number} MAC=3 MAC value
 * @property {number} WIN=4 WIN value
 * @property {number} HARMONYOS=5 HARMONYOS value
 * @property {number} HARMONYOS=5 HARMONYOS value
 */
export const Platform = $root.Platform = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "ANDROID"] = 0;
    values[valuesById[1] = "IPHONE"] = 1;
    values[valuesById[2] = "UNKOWN"] = 2;
    values[valuesById[3] = "MAC"] = 3;
    values[valuesById[4] = "WIN"] = 4;
    values[valuesById[5] = "HARMONYOS"] = 5;
    values[valuesById[5] = "HARMONYOS"] = 5;
    return values;
})();

/**
 * LastOnlineTimeViewType enum.
 * @exports LastOnlineTimeViewType
 * @enum {number}
 * @property {number} ALL_SHOW=0 ALL_SHOW value
 * @property {number} ONLY_FRIEND=1 ONLY_FRIEND value
 * @property {number} NOT_SHOW=2 NOT_SHOW value
 */
export const LastOnlineTimeViewType = $root.LastOnlineTimeViewType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "ALL_SHOW"] = 0;
    values[valuesById[1] = "ONLY_FRIEND"] = 1;
    values[valuesById[2] = "NOT_SHOW"] = 2;
    return values;
})();

/**
 * SecurityType enum.
 * @exports SecurityType
 * @enum {number}
 * @property {number} OLD_VERSION=0 OLD_VERSION value
 * @property {number} NORMAL_CHECK=1 NORMAL_CHECK value
 * @property {number} SECURITY_CHECK=2 SECURITY_CHECK value
 */
export const SecurityType = $root.SecurityType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "OLD_VERSION"] = 0;
    values[valuesById[1] = "NORMAL_CHECK"] = 1;
    values[valuesById[2] = "SECURITY_CHECK"] = 2;
    return values;
})();

/**
 * GroupMemberType enum.
 * @exports GroupMemberType
 * @enum {number}
 * @property {number} HOST=0 HOST value
 * @property {number} MANAGE=1 MANAGE value
 * @property {number} MEMBER=2 MEMBER value
 */
export const GroupMemberType = $root.GroupMemberType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "HOST"] = 0;
    values[valuesById[1] = "MANAGE"] = 1;
    values[valuesById[2] = "MEMBER"] = 2;
    return values;
})();

/**
 * AttachWorkSpaceType enum.
 * @exports AttachWorkSpaceType
 * @enum {number}
 * @property {number} COMMON=0 COMMON value
 * @property {number} CHAT=1 CHAT value
 */
export const AttachWorkSpaceType = $root.AttachWorkSpaceType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "COMMON"] = 0;
    values[valuesById[1] = "CHAT"] = 1;
    return values;
})();

/**
 * AttachType enum.
 * @exports AttachType
 * @enum {number}
 * @property {number} PIC=0 PIC value
 * @property {number} AUDIO=1 AUDIO value
 * @property {number} VIDEO=2 VIDEO value
 * @property {number} FILE=3 FILE value
 * @property {number} LOG=4 LOG value
 * @property {number} EMOTICON=5 EMOTICON value
 */
export const AttachType = $root.AttachType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "PIC"] = 0;
    values[valuesById[1] = "AUDIO"] = 1;
    values[valuesById[2] = "VIDEO"] = 2;
    values[valuesById[3] = "FILE"] = 3;
    values[valuesById[4] = "LOG"] = 4;
    values[valuesById[5] = "EMOTICON"] = 5;
    return values;
})();

/**
 * QrCodeType enum.
 * @exports QrCodeType
 * @enum {number}
 * @property {number} QR_USER=0 QR_USER value
 * @property {number} QR_GROUP=1 QR_GROUP value
 * @property {number} WEB_LOGIN=2 WEB_LOGIN value
 */
export const QrCodeType = $root.QrCodeType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "QR_USER"] = 0;
    values[valuesById[1] = "QR_GROUP"] = 1;
    values[valuesById[2] = "WEB_LOGIN"] = 2;
    return values;
})();

/**
 * FeedbackType enum.
 * @exports FeedbackType
 * @enum {number}
 * @property {number} SUGGEST=0 SUGGEST value
 * @property {number} MISTAKE=1 MISTAKE value
 * @property {number} OTHER=2 OTHER value
 */
export const FeedbackType = $root.FeedbackType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "SUGGEST"] = 0;
    values[valuesById[1] = "MISTAKE"] = 1;
    values[valuesById[2] = "OTHER"] = 2;
    return values;
})();

/**
 * WebLoginStatus enum.
 * @exports WebLoginStatus
 * @enum {number}
 * @property {number} NOT_SCAN=0 NOT_SCAN value
 * @property {number} SCANNED=1 SCANNED value
 * @property {number} ALREADY_LOGIN=2 ALREADY_LOGIN value
 * @property {number} CANCEL_LOGIN=3 CANCEL_LOGIN value
 */
export const WebLoginStatus = $root.WebLoginStatus = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "NOT_SCAN"] = 0;
    values[valuesById[1] = "SCANNED"] = 1;
    values[valuesById[2] = "ALREADY_LOGIN"] = 2;
    values[valuesById[3] = "CANCEL_LOGIN"] = 3;
    return values;
})();

/**
 * GroupReqStatus enum.
 * @exports GroupReqStatus
 * @enum {number}
 * @property {number} CHECKING=0 CHECKING value
 * @property {number} AGREE=1 AGREE value
 * @property {number} REFUSE=2 REFUSE value
 * @property {number} EXPIRE=3 EXPIRE value
 */
export const GroupReqStatus = $root.GroupReqStatus = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "CHECKING"] = 0;
    values[valuesById[1] = "AGREE"] = 1;
    values[valuesById[2] = "REFUSE"] = 2;
    values[valuesById[3] = "EXPIRE"] = 3;
    return values;
})();

/**
 * GroupReqType enum.
 * @exports GroupReqType
 * @enum {number}
 * @property {number} GROUP_TRANSFER=0 GROUP_TRANSFER value
 * @property {number} GROUP_INVITE=1 GROUP_INVITE value
 * @property {number} GROUP_QR_CODE=2 GROUP_QR_CODE value
 * @property {number} GROUP_OWNER_CHECK_INVITE=3 GROUP_OWNER_CHECK_INVITE value
 * @property {number} GROUP_OWNER_CHECK_QR_CODE=4 GROUP_OWNER_CHECK_QR_CODE value
 * @property {number} GROUP_MEMBER_CHECK=5 GROUP_MEMBER_CHECK value
 * @property {number} GROUP_OWNER_REMOVE_MEMBER=6 GROUP_OWNER_REMOVE_MEMBER value
 * @property {number} GROUP_MEMBER_EXIT=7 GROUP_MEMBER_EXIT value
 * @property {number} GROUP_SET_ADMIN=8 GROUP_SET_ADMIN value
 * @property {number} GROUP_CANCLE_ADMIN=9 GROUP_CANCLE_ADMIN value
 * @property {number} GROUP_ADMIN_UPDATE=10 GROUP_ADMIN_UPDATE value
 * @property {number} GROUP_IS_DISABLED=11 GROUP_IS_DISABLED value
 * @property {number} GROUP_IS_DISABLED=11 GROUP_IS_DISABLED value
 * @property {number} GROUP_MEMBER_SHUTUP=12 GROUP_MEMBER_SHUTUP value
 * @property {number} GROUP_IS_DISBANDED=13 GROUP_IS_DISBANDED value
 * @property {number} GROUP_IS_DISBANDED=13 GROUP_IS_DISBANDED value
 * @property {number} GROUP_LINK=14 GROUP_LINK value
 * @property {number} GROUP_ALIAS=15 GROUP_ALIAS value
 * @property {number} GROUP_IS_ENABLED=16 GROUP_IS_ENABLED value
 * @property {number} GROUP_OBSERVE_ADD=17 GROUP_OBSERVE_ADD value
 * @property {number} GROUP_OBSERVE_REMOVE=18 GROUP_OBSERVE_REMOVE value
 * @property {number} GROUP_IS_ENABLED=16 GROUP_IS_ENABLED value
 * @property {number} GROUP_OBSERVE_ADD=17 GROUP_OBSERVE_ADD value
 * @property {number} GROUP_OBSERVE_REMOVE=18 GROUP_OBSERVE_REMOVE value
 */
export const GroupReqType = $root.GroupReqType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "GROUP_TRANSFER"] = 0;
    values[valuesById[1] = "GROUP_INVITE"] = 1;
    values[valuesById[2] = "GROUP_QR_CODE"] = 2;
    values[valuesById[3] = "GROUP_OWNER_CHECK_INVITE"] = 3;
    values[valuesById[4] = "GROUP_OWNER_CHECK_QR_CODE"] = 4;
    values[valuesById[5] = "GROUP_MEMBER_CHECK"] = 5;
    values[valuesById[6] = "GROUP_OWNER_REMOVE_MEMBER"] = 6;
    values[valuesById[7] = "GROUP_MEMBER_EXIT"] = 7;
    values[valuesById[8] = "GROUP_SET_ADMIN"] = 8;
    values[valuesById[9] = "GROUP_CANCLE_ADMIN"] = 9;
    values[valuesById[10] = "GROUP_ADMIN_UPDATE"] = 10;
    values[valuesById[11] = "GROUP_IS_DISABLED"] = 11;
    values[valuesById[11] = "GROUP_IS_DISABLED"] = 11;
    values[valuesById[12] = "GROUP_MEMBER_SHUTUP"] = 12;
    values[valuesById[13] = "GROUP_IS_DISBANDED"] = 13;
    values[valuesById[13] = "GROUP_IS_DISBANDED"] = 13;
    values[valuesById[14] = "GROUP_LINK"] = 14;
    values[valuesById[15] = "GROUP_ALIAS"] = 15;
    values[valuesById[16] = "GROUP_IS_ENABLED"] = 16;
    values[valuesById[17] = "GROUP_OBSERVE_ADD"] = 17;
    values[valuesById[18] = "GROUP_OBSERVE_REMOVE"] = 18;
    values[valuesById[16] = "GROUP_IS_ENABLED"] = 16;
    values[valuesById[17] = "GROUP_OBSERVE_ADD"] = 17;
    values[valuesById[18] = "GROUP_OBSERVE_REMOVE"] = 18;
    return values;
})();

/**
 * GroupHandleType enum.
 * @exports GroupHandleType
 * @enum {number}
 * @property {number} GROUP_REQ=0 GROUP_REQ value
 * @property {number} GROUP_NAME=1 GROUP_NAME value
 * @property {number} GROUP_PIC=2 GROUP_PIC value
 * @property {number} GROUP_MEMBER_NICKNAME=3 GROUP_MEMBER_NICKNAME value
 * @property {number} GROUP_NOTICE=4 GROUP_NOTICE value
 * @property {number} GROUP_SHUTUP=5 GROUP_SHUTUP value
 * @property {number} GROUP_READ_CANCEL=6 GROUP_READ_CANCEL value
 */
export const GroupHandleType = $root.GroupHandleType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "GROUP_REQ"] = 0;
    values[valuesById[1] = "GROUP_NAME"] = 1;
    values[valuesById[2] = "GROUP_PIC"] = 2;
    values[valuesById[3] = "GROUP_MEMBER_NICKNAME"] = 3;
    values[valuesById[4] = "GROUP_NOTICE"] = 4;
    values[valuesById[5] = "GROUP_SHUTUP"] = 5;
    values[valuesById[6] = "GROUP_READ_CANCEL"] = 6;
    return values;
})();

/**
 * MsgReceiptStatus enum.
 * @exports MsgReceiptStatus
 * @enum {number}
 * @property {number} DELIVERED=0 DELIVERED value
 * @property {number} VIEWED=1 VIEWED value
 * @property {number} PLAYED=2 PLAYED value
 * @property {number} PROCESSED=3 PROCESSED value
 */
export const MsgReceiptStatus = $root.MsgReceiptStatus = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "DELIVERED"] = 0;
    values[valuesById[1] = "VIEWED"] = 1;
    values[valuesById[2] = "PLAYED"] = 2;
    values[valuesById[3] = "PROCESSED"] = 3;
    return values;
})();

/**
 * CheckVersionFlag enum.
 * @exports CheckVersionFlag
 * @enum {number}
 * @property {number} NOT_UP=0 NOT_UP value
 * @property {number} CAN_UP=1 CAN_UP value
 * @property {number} MUST_UP=2 MUST_UP value
 */
export const CheckVersionFlag = $root.CheckVersionFlag = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "NOT_UP"] = 0;
    values[valuesById[1] = "CAN_UP"] = 1;
    values[valuesById[2] = "MUST_UP"] = 2;
    return values;
})();

/**
 * KeyPairType enum.
 * @exports KeyPairType
 * @enum {number}
 * @property {number} KEY_USER=0 KEY_USER value
 * @property {number} KEY_GROUP=1 KEY_GROUP value
 * @property {number} KEY_USER_WEB=2 KEY_USER_WEB value
 * @property {number} KEY_CHANNEL=3 KEY_CHANNEL value
 * @property {number} KEY_CHANNEL=3 KEY_CHANNEL value
 */
export const KeyPairType = $root.KeyPairType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "KEY_USER"] = 0;
    values[valuesById[1] = "KEY_GROUP"] = 1;
    values[valuesById[2] = "KEY_USER_WEB"] = 2;
    values[valuesById[3] = "KEY_CHANNEL"] = 3;
    values[valuesById[3] = "KEY_CHANNEL"] = 3;
    return values;
})();

/**
 * AdminRightType enum.
 * @exports AdminRightType
 * @enum {number}
 * @property {number} ALL_RIGHT=0 ALL_RIGHT value
 * @property {number} UPDATE_DATA=1 UPDATE_DATA value
 * @property {number} CHECK_APPLY=2 CHECK_APPLY value
 * @property {number} PUSH_NOTICE=3 PUSH_NOTICE value
 * @property {number} SET_ADMIN=4 SET_ADMIN value
 * @property {number} SET_JOIN_NOTICE=5 SET_JOIN_NOTICE value
 */
export const AdminRightType = $root.AdminRightType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "ALL_RIGHT"] = 0;
    values[valuesById[1] = "UPDATE_DATA"] = 1;
    values[valuesById[2] = "CHECK_APPLY"] = 2;
    values[valuesById[3] = "PUSH_NOTICE"] = 3;
    values[valuesById[4] = "SET_ADMIN"] = 4;
    values[valuesById[5] = "SET_JOIN_NOTICE"] = 5;
    return values;
})();

/**
 * SearchMapType enum.
 * @exports SearchMapType
 * @enum {number}
 * @property {number} GAODE=0 GAODE value
 * @property {number} GOOGLE=1 GOOGLE value
 * @property {number} BAIDU=2 BAIDU value
 */
export const SearchMapType = $root.SearchMapType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "GAODE"] = 0;
    values[valuesById[1] = "GOOGLE"] = 1;
    values[valuesById[2] = "BAIDU"] = 2;
    return values;
})();

/**
 * ClearTimeType enum.
 * @exports ClearTimeType
 * @enum {number}
 * @property {number} SIX_MONTH=0 SIX_MONTH value
 * @property {number} ONE_MONTH=1 ONE_MONTH value
 * @property {number} THIRD_MONTH=2 THIRD_MONTH value
 * @property {number} TWELVE_MONTH=3 TWELVE_MONTH value
 */
export const ClearTimeType = $root.ClearTimeType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "SIX_MONTH"] = 0;
    values[valuesById[1] = "ONE_MONTH"] = 1;
    values[valuesById[2] = "THIRD_MONTH"] = 2;
    values[valuesById[3] = "TWELVE_MONTH"] = 3;
    return values;
})();

/**
 * InviteLinkType enum.
 * @exports InviteLinkType
 * @enum {number}
 * @property {number} LINK_USER=0 LINK_USER value
 * @property {number} LINK_GROUP=1 LINK_GROUP value
 */
export const InviteLinkType = $root.InviteLinkType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "LINK_USER"] = 0;
    values[valuesById[1] = "LINK_GROUP"] = 1;
    return values;
})();

/**
 * EditType enum.
 * @exports EditType
 * @enum {number}
 * @property {number} UNMODIFIED=0 UNMODIFIED value
 * @property {number} MODIFIED=1 MODIFIED value
 */
export const EditType = $root.EditType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "UNMODIFIED"] = 0;
    values[valuesById[1] = "MODIFIED"] = 1;
    return values;
})();

/**
 * EditType enum.
 * @exports EditType
 * @enum {number}
 * @property {number} UNMODIFIED=0 UNMODIFIED value
 * @property {number} MODIFIED=1 MODIFIED value
 */
export const EditType = $root.EditType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "UNMODIFIED"] = 0;
    values[valuesById[1] = "MODIFIED"] = 1;
    return values;
})();

/**
 * FilterType enum.
 * @exports FilterType
 * @enum {number}
 * @property {number} BE_DEFAULT=0 BE_DEFAULT value
 * @property {number} BE_MEMBER=1 BE_MEMBER value
 * @property {number} BE_ADMIN=2 BE_ADMIN value
 * @property {number} NOT_ONESELF=3 NOT_ONESELF value
 * @property {number} NOT_HOST=4 NOT_HOST value
 * @property {number} NOT_ADMIN=5 NOT_ADMIN value
 */
export const FilterType = $root.FilterType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "BE_DEFAULT"] = 0;
    values[valuesById[1] = "BE_MEMBER"] = 1;
    values[valuesById[2] = "BE_ADMIN"] = 2;
    values[valuesById[3] = "NOT_ONESELF"] = 3;
    values[valuesById[4] = "NOT_HOST"] = 4;
    values[valuesById[5] = "NOT_ADMIN"] = 5;
    return values;
})();

/**
 * SafeSwitchType enum.
 * @exports SafeSwitchType
 * @enum {number}
 * @property {number} EMAIL_TYPE=0 EMAIL_TYPE value
 * @property {number} PHONE_TYPE=1 PHONE_TYPE value
 * @property {number} GOOGLE_AUTH_TYPE=2 GOOGLE_AUTH_TYPE value
 * @property {number} TRADE_PASSWORD_TYPE=3 TRADE_PASSWORD_TYPE value
 */
export const SafeSwitchType = $root.SafeSwitchType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "EMAIL_TYPE"] = 0;
    values[valuesById[1] = "PHONE_TYPE"] = 1;
    values[valuesById[2] = "GOOGLE_AUTH_TYPE"] = 2;
    values[valuesById[3] = "TRADE_PASSWORD_TYPE"] = 3;
    return values;
})();

/**
 * operateType enum.
 * @exports operateType
 * @enum {number}
 * @property {number} UPDATE=0 UPDATE value
 * @property {number} DELETE=1 DELETE value
 */
export const operateType = $root.operateType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "UPDATE"] = 0;
    values[valuesById[1] = "DELETE"] = 1;
    return values;
})();

/**
 * CheckTradePassword enum.
 * @exports CheckTradePassword
 * @enum {number}
 * @property {number} OLD_ERROR=0 OLD_ERROR value
 * @property {number} NEW_THE_SAME=1 NEW_THE_SAME value
 * @property {number} PASS=2 PASS value
 * @property {number} OVER_TIME=3 OVER_TIME value
 */
export const CheckTradePassword = $root.CheckTradePassword = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "OLD_ERROR"] = 0;
    values[valuesById[1] = "NEW_THE_SAME"] = 1;
    values[valuesById[2] = "PASS"] = 2;
    values[valuesById[3] = "OVER_TIME"] = 3;
    return values;
})();

export const GroupAssistantMessageContent = $root.GroupAssistantMessageContent = (() => {

    /**
     * Properties of a GroupAssistantMessageContent.
     * @exports IGroupAssistantMessageContent
     * @interface IGroupAssistantMessageContent
     * @property {Uint8Array|null} [content] GroupAssistantMessageContent content
     * @property {string|null} [attachmentKey] GroupAssistantMessageContent attachmentKey
     */

    /**
     * Constructs a new GroupAssistantMessageContent.
     * @exports GroupAssistantMessageContent
     * @classdesc Represents a GroupAssistantMessageContent.
     * @implements IGroupAssistantMessageContent
     * @constructor
     * @param {IGroupAssistantMessageContent=} [properties] Properties to set
     */
    function GroupAssistantMessageContent(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GroupAssistantMessageContent content.
     * @member {Uint8Array} content
     * @memberof GroupAssistantMessageContent
     * @instance
     */
    GroupAssistantMessageContent.prototype.content = $util.newBuffer([]);

    /**
     * GroupAssistantMessageContent attachmentKey.
     * @member {string} attachmentKey
     * @memberof GroupAssistantMessageContent
     * @instance
     */
    GroupAssistantMessageContent.prototype.attachmentKey = "";

    /**
     * Creates a new GroupAssistantMessageContent instance using the specified properties.
     * @function create
     * @memberof GroupAssistantMessageContent
     * @static
     * @param {IGroupAssistantMessageContent=} [properties] Properties to set
     * @returns {GroupAssistantMessageContent} GroupAssistantMessageContent instance
     */
    GroupAssistantMessageContent.create = function create(properties) {
        return new GroupAssistantMessageContent(properties);
    };

    /**
     * Encodes the specified GroupAssistantMessageContent message. Does not implicitly {@link GroupAssistantMessageContent.verify|verify} messages.
     * @function encode
     * @memberof GroupAssistantMessageContent
     * @static
     * @param {IGroupAssistantMessageContent} message GroupAssistantMessageContent message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GroupAssistantMessageContent.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.content != null && Object.hasOwnProperty.call(message, "content"))
            writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.content);
        if (message.attachmentKey != null && Object.hasOwnProperty.call(message, "attachmentKey"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.attachmentKey);
        return writer;
    };

    /**
     * Encodes the specified GroupAssistantMessageContent message, length delimited. Does not implicitly {@link GroupAssistantMessageContent.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GroupAssistantMessageContent
     * @static
     * @param {IGroupAssistantMessageContent} message GroupAssistantMessageContent message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GroupAssistantMessageContent.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GroupAssistantMessageContent message from the specified reader or buffer.
     * @function decode
     * @memberof GroupAssistantMessageContent
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GroupAssistantMessageContent} GroupAssistantMessageContent
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GroupAssistantMessageContent.decode = function decode(reader, length, error) {
    GroupAssistantMessageContent.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GroupAssistantMessageContent();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.content = reader.bytes();
                    break;
                }
            case 2: {
                    message.attachmentKey = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GroupAssistantMessageContent message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GroupAssistantMessageContent
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GroupAssistantMessageContent} GroupAssistantMessageContent
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GroupAssistantMessageContent.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GroupAssistantMessageContent message.
     * @function verify
     * @memberof GroupAssistantMessageContent
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GroupAssistantMessageContent.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.content != null && message.hasOwnProperty("content"))
            if (!(message.content && typeof message.content.length === "number" || $util.isString(message.content)))
                return "content: buffer expected";
        if (message.attachmentKey != null && message.hasOwnProperty("attachmentKey"))
            if (!$util.isString(message.attachmentKey))
                return "attachmentKey: string expected";
        return null;
    };

    /**
     * Creates a GroupAssistantMessageContent message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GroupAssistantMessageContent
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GroupAssistantMessageContent} GroupAssistantMessageContent
     */
    GroupAssistantMessageContent.fromObject = function fromObject(object) {
        if (object instanceof $root.GroupAssistantMessageContent)
            return object;
        let message = new $root.GroupAssistantMessageContent();
        if (object.content != null)
            if (typeof object.content === "string")
                $util.base64.decode(object.content, message.content = $util.newBuffer($util.base64.length(object.content)), 0);
            else if (object.content.length >= 0)
                message.content = object.content;
        if (object.attachmentKey != null)
            message.attachmentKey = String(object.attachmentKey);
        return message;
    };

    /**
     * Creates a plain object from a GroupAssistantMessageContent message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GroupAssistantMessageContent
     * @static
     * @param {GroupAssistantMessageContent} message GroupAssistantMessageContent
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GroupAssistantMessageContent.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            if (options.bytes === String)
                object.content = "";
            else {
                object.content = [];
                if (options.bytes !== Array)
                    object.content = $util.newBuffer(object.content);
            }
            object.attachmentKey = "";
        }
        if (message.content != null && message.hasOwnProperty("content"))
            object.content = options.bytes === String ? $util.base64.encode(message.content, 0, message.content.length) : options.bytes === Array ? Array.prototype.slice.call(message.content) : message.content;
        if (message.attachmentKey != null && message.hasOwnProperty("attachmentKey"))
            object.attachmentKey = message.attachmentKey;
        return object;
    };

    /**
     * Converts this GroupAssistantMessageContent to JSON.
     * @function toJSON
     * @memberof GroupAssistantMessageContent
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GroupAssistantMessageContent.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GroupAssistantMessageContent
     * @function getTypeUrl
     * @memberof GroupAssistantMessageContent
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GroupAssistantMessageContent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GroupAssistantMessageContent";
    };

    return GroupAssistantMessageContent;
})();

export const UserBase = $root.UserBase = (() => {

    /**
     * Properties of a UserBase.
     * @exports IUserBase
     * @interface IUserBase
     * @property {number|Long|null} [uid] UserBase uid
     * @property {string|null} [nickName] UserBase nickName
     * @property {string|null} [icon] UserBase icon
     * @property {Gender|null} [gender] UserBase gender
     * @property {IFriendRelation|null} [friendRelation] UserBase friendRelation
     * @property {IUserOnOrOffLine|null} [userOnOrOffline] UserBase userOnOrOffline
     * @property {string|null} [signature] UserBase signature
     * @property {string|null} [depict] UserBase depict
     * @property {boolean|null} [bfCancel] UserBase bfCancel
     * @property {boolean|null} [bfBanned] UserBase bfBanned
     * @property {string|null} [identify] UserBase identify
     * @property {string|null} [realName] UserBase realName
     * @property {string|null} [idNumber] UserBase idNumber
     * @property {number|Long|null} [createTime] UserBase createTime
     * @property {number|null} [userType] UserBase userType
     * @property {number|null} [userType] UserBase userType
     */

    /**
     * Constructs a new UserBase.
     * @exports UserBase
     * @classdesc Represents a UserBase.
     * @implements IUserBase
     * @constructor
     * @param {IUserBase=} [properties] Properties to set
     */
    function UserBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * UserBase uid.
     * @member {number|Long} uid
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.uid = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * UserBase nickName.
     * @member {string} nickName
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.nickName = "";

    /**
     * UserBase icon.
     * @member {string} icon
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.icon = "";

    /**
     * UserBase gender.
     * @member {Gender} gender
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.gender = 0;

    /**
     * UserBase friendRelation.
     * @member {IFriendRelation|null|undefined} friendRelation
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.friendRelation = null;

    /**
     * UserBase userOnOrOffline.
     * @member {IUserOnOrOffLine|null|undefined} userOnOrOffline
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.userOnOrOffline = null;

    /**
     * UserBase signature.
     * @member {string} signature
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.signature = "";

    /**
     * UserBase depict.
     * @member {string} depict
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.depict = "";

    /**
     * UserBase bfCancel.
     * @member {boolean} bfCancel
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.bfCancel = false;

    /**
     * UserBase bfBanned.
     * @member {boolean} bfBanned
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.bfBanned = false;

    /**
     * UserBase identify.
     * @member {string} identify
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.identify = "";

    /**
     * UserBase realName.
     * @member {string} realName
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.realName = "";

    /**
     * UserBase idNumber.
     * @member {string} idNumber
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.idNumber = "";

    /**
     * UserBase createTime.
     * @member {number|Long} createTime
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.createTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * UserBase userType.
     * @member {number} userType
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.userType = 0;

    /**
     * UserBase userType.
     * @member {number} userType
     * @memberof UserBase
     * @instance
     */
    UserBase.prototype.userType = 0;

    /**
     * Creates a new UserBase instance using the specified properties.
     * @function create
     * @memberof UserBase
     * @static
     * @param {IUserBase=} [properties] Properties to set
     * @returns {UserBase} UserBase instance
     */
    UserBase.create = function create(properties) {
        return new UserBase(properties);
    };

    /**
     * Encodes the specified UserBase message. Does not implicitly {@link UserBase.verify|verify} messages.
     * @function encode
     * @memberof UserBase
     * @static
     * @param {IUserBase} message UserBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UserBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.uid);
        if (message.nickName != null && Object.hasOwnProperty.call(message, "nickName"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.nickName);
        if (message.icon != null && Object.hasOwnProperty.call(message, "icon"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.icon);
        if (message.gender != null && Object.hasOwnProperty.call(message, "gender"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.gender);
        if (message.friendRelation != null && Object.hasOwnProperty.call(message, "friendRelation"))
            $root.FriendRelation.encode(message.friendRelation, writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
        if (message.userOnOrOffline != null && Object.hasOwnProperty.call(message, "userOnOrOffline"))
            $root.UserOnOrOffLine.encode(message.userOnOrOffline, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
        if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
            writer.uint32(/* id 7, wireType 2 =*/58).string(message.signature);
        if (message.depict != null && Object.hasOwnProperty.call(message, "depict"))
            writer.uint32(/* id 8, wireType 2 =*/66).string(message.depict);
        if (message.bfCancel != null && Object.hasOwnProperty.call(message, "bfCancel"))
            writer.uint32(/* id 9, wireType 0 =*/72).bool(message.bfCancel);
        if (message.bfBanned != null && Object.hasOwnProperty.call(message, "bfBanned"))
            writer.uint32(/* id 10, wireType 0 =*/80).bool(message.bfBanned);
        if (message.identify != null && Object.hasOwnProperty.call(message, "identify"))
            writer.uint32(/* id 11, wireType 2 =*/90).string(message.identify);
        if (message.realName != null && Object.hasOwnProperty.call(message, "realName"))
            writer.uint32(/* id 12, wireType 2 =*/98).string(message.realName);
        if (message.idNumber != null && Object.hasOwnProperty.call(message, "idNumber"))
            writer.uint32(/* id 13, wireType 2 =*/106).string(message.idNumber);
        if (message.createTime != null && Object.hasOwnProperty.call(message, "createTime"))
            writer.uint32(/* id 14, wireType 0 =*/112).int64(message.createTime);
        if (message.userType != null && Object.hasOwnProperty.call(message, "userType"))
            writer.uint32(/* id 15, wireType 0 =*/120).int32(message.userType);
        if (message.userType != null && Object.hasOwnProperty.call(message, "userType"))
            writer.uint32(/* id 15, wireType 0 =*/120).int32(message.userType);
        return writer;
    };

    /**
     * Encodes the specified UserBase message, length delimited. Does not implicitly {@link UserBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof UserBase
     * @static
     * @param {IUserBase} message UserBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UserBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a UserBase message from the specified reader or buffer.
     * @function decode
     * @memberof UserBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {UserBase} UserBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UserBase.decode = function decode(reader, length, error) {
    UserBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.UserBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.uid = reader.int64();
                    break;
                }
            case 2: {
                    message.nickName = reader.string();
                    break;
                }
            case 3: {
                    message.icon = reader.string();
                    break;
                }
            case 4: {
                    message.gender = reader.int32();
                    break;
                }
            case 5: {
                    message.friendRelation = $root.FriendRelation.decode(reader, reader.uint32());
                    break;
                }
            case 6: {
                    message.userOnOrOffline = $root.UserOnOrOffLine.decode(reader, reader.uint32());
                    break;
                }
            case 7: {
                    message.signature = reader.string();
                    break;
                }
            case 8: {
                    message.depict = reader.string();
                    break;
                }
            case 9: {
                    message.bfCancel = reader.bool();
                    break;
                }
            case 10: {
                    message.bfBanned = reader.bool();
                    break;
                }
            case 11: {
                    message.identify = reader.string();
                    break;
                }
            case 12: {
                    message.realName = reader.string();
                    break;
                }
            case 13: {
                    message.idNumber = reader.string();
                    break;
                }
            case 14: {
                    message.createTime = reader.int64();
                    break;
                }
            case 15: {
                    message.userType = reader.int32();
                    break;
                }
            case 15: {
                    message.userType = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a UserBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof UserBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {UserBase} UserBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UserBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a UserBase message.
     * @function verify
     * @memberof UserBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    UserBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.uid != null && message.hasOwnProperty("uid"))
            if (!$util.isInteger(message.uid) && !(message.uid && $util.isInteger(message.uid.low) && $util.isInteger(message.uid.high)))
                return "uid: integer|Long expected";
        if (message.nickName != null && message.hasOwnProperty("nickName"))
            if (!$util.isString(message.nickName))
                return "nickName: string expected";
        if (message.icon != null && message.hasOwnProperty("icon"))
            if (!$util.isString(message.icon))
                return "icon: string expected";
        if (message.gender != null && message.hasOwnProperty("gender"))
            switch (message.gender) {
            default:
                return "gender: enum value expected";
            case 0:
            case 1:
            case 2:
                break;
            }
        if (message.friendRelation != null && message.hasOwnProperty("friendRelation")) {
            let error = $root.FriendRelation.verify(message.friendRelation);
            if (error)
                return "friendRelation." + error;
        }
        if (message.userOnOrOffline != null && message.hasOwnProperty("userOnOrOffline")) {
            let error = $root.UserOnOrOffLine.verify(message.userOnOrOffline);
            if (error)
                return "userOnOrOffline." + error;
        }
        if (message.signature != null && message.hasOwnProperty("signature"))
            if (!$util.isString(message.signature))
                return "signature: string expected";
        if (message.depict != null && message.hasOwnProperty("depict"))
            if (!$util.isString(message.depict))
                return "depict: string expected";
        if (message.bfCancel != null && message.hasOwnProperty("bfCancel"))
            if (typeof message.bfCancel !== "boolean")
                return "bfCancel: boolean expected";
        if (message.bfBanned != null && message.hasOwnProperty("bfBanned"))
            if (typeof message.bfBanned !== "boolean")
                return "bfBanned: boolean expected";
        if (message.identify != null && message.hasOwnProperty("identify"))
            if (!$util.isString(message.identify))
                return "identify: string expected";
        if (message.realName != null && message.hasOwnProperty("realName"))
            if (!$util.isString(message.realName))
                return "realName: string expected";
        if (message.idNumber != null && message.hasOwnProperty("idNumber"))
            if (!$util.isString(message.idNumber))
                return "idNumber: string expected";
        if (message.createTime != null && message.hasOwnProperty("createTime"))
            if (!$util.isInteger(message.createTime) && !(message.createTime && $util.isInteger(message.createTime.low) && $util.isInteger(message.createTime.high)))
                return "createTime: integer|Long expected";
        if (message.userType != null && message.hasOwnProperty("userType"))
            if (!$util.isInteger(message.userType))
                return "userType: integer expected";
        if (message.userType != null && message.hasOwnProperty("userType"))
            if (!$util.isInteger(message.userType))
                return "userType: integer expected";
        return null;
    };

    /**
     * Creates a UserBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof UserBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {UserBase} UserBase
     */
    UserBase.fromObject = function fromObject(object) {
        if (object instanceof $root.UserBase)
            return object;
        let message = new $root.UserBase();
        if (object.uid != null)
            if ($util.Long)
                (message.uid = $util.Long.fromValue(object.uid)).unsigned = false;
            else if (typeof object.uid === "string")
                message.uid = parseInt(object.uid, 10);
            else if (typeof object.uid === "number")
                message.uid = object.uid;
            else if (typeof object.uid === "object")
                message.uid = new $util.LongBits(object.uid.low >>> 0, object.uid.high >>> 0).toNumber();
        if (object.nickName != null)
            message.nickName = String(object.nickName);
        if (object.icon != null)
            message.icon = String(object.icon);
        switch (object.gender) {
        default:
            if (typeof object.gender === "number") {
                message.gender = object.gender;
                break;
            }
            break;
        case "SECRECY":
        case 0:
            message.gender = 0;
            break;
        case "MALE":
        case 1:
            message.gender = 1;
            break;
        case "FEMALE":
        case 2:
            message.gender = 2;
            break;
        }
        if (object.friendRelation != null) {
            if (typeof object.friendRelation !== "object")
                throw TypeError(".UserBase.friendRelation: object expected");
            message.friendRelation = $root.FriendRelation.fromObject(object.friendRelation);
        }
        if (object.userOnOrOffline != null) {
            if (typeof object.userOnOrOffline !== "object")
                throw TypeError(".UserBase.userOnOrOffline: object expected");
            message.userOnOrOffline = $root.UserOnOrOffLine.fromObject(object.userOnOrOffline);
        }
        if (object.signature != null)
            message.signature = String(object.signature);
        if (object.depict != null)
            message.depict = String(object.depict);
        if (object.bfCancel != null)
            message.bfCancel = Boolean(object.bfCancel);
        if (object.bfBanned != null)
            message.bfBanned = Boolean(object.bfBanned);
        if (object.identify != null)
            message.identify = String(object.identify);
        if (object.realName != null)
            message.realName = String(object.realName);
        if (object.idNumber != null)
            message.idNumber = String(object.idNumber);
        if (object.createTime != null)
            if ($util.Long)
                (message.createTime = $util.Long.fromValue(object.createTime)).unsigned = false;
            else if (typeof object.createTime === "string")
                message.createTime = parseInt(object.createTime, 10);
            else if (typeof object.createTime === "number")
                message.createTime = object.createTime;
            else if (typeof object.createTime === "object")
                message.createTime = new $util.LongBits(object.createTime.low >>> 0, object.createTime.high >>> 0).toNumber();
        if (object.userType != null)
            message.userType = object.userType | 0;
        if (object.userType != null)
            message.userType = object.userType | 0;
        return message;
    };

    /**
     * Creates a plain object from a UserBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof UserBase
     * @static
     * @param {UserBase} message UserBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    UserBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.uid = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.uid = options.longs === String ? "0" : 0;
            object.nickName = "";
            object.icon = "";
            object.gender = options.enums === String ? "SECRECY" : 0;
            object.friendRelation = null;
            object.userOnOrOffline = null;
            object.signature = "";
            object.depict = "";
            object.bfCancel = false;
            object.bfBanned = false;
            object.identify = "";
            object.realName = "";
            object.idNumber = "";
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.createTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.createTime = options.longs === String ? "0" : 0;
            object.userType = 0;
            object.userType = 0;
        }
        if (message.uid != null && message.hasOwnProperty("uid"))
            if (typeof message.uid === "number")
                object.uid = options.longs === String ? String(message.uid) : message.uid;
            else
                object.uid = options.longs === String ? $util.Long.prototype.toString.call(message.uid) : options.longs === Number ? new $util.LongBits(message.uid.low >>> 0, message.uid.high >>> 0).toNumber() : message.uid;
        if (message.nickName != null && message.hasOwnProperty("nickName"))
            object.nickName = message.nickName;
        if (message.icon != null && message.hasOwnProperty("icon"))
            object.icon = message.icon;
        if (message.gender != null && message.hasOwnProperty("gender"))
            object.gender = options.enums === String ? $root.Gender[message.gender] === undefined ? message.gender : $root.Gender[message.gender] : message.gender;
        if (message.friendRelation != null && message.hasOwnProperty("friendRelation"))
            object.friendRelation = $root.FriendRelation.toObject(message.friendRelation, options);
        if (message.userOnOrOffline != null && message.hasOwnProperty("userOnOrOffline"))
            object.userOnOrOffline = $root.UserOnOrOffLine.toObject(message.userOnOrOffline, options);
        if (message.signature != null && message.hasOwnProperty("signature"))
            object.signature = message.signature;
        if (message.depict != null && message.hasOwnProperty("depict"))
            object.depict = message.depict;
        if (message.bfCancel != null && message.hasOwnProperty("bfCancel"))
            object.bfCancel = message.bfCancel;
        if (message.bfBanned != null && message.hasOwnProperty("bfBanned"))
            object.bfBanned = message.bfBanned;
        if (message.identify != null && message.hasOwnProperty("identify"))
            object.identify = message.identify;
        if (message.realName != null && message.hasOwnProperty("realName"))
            object.realName = message.realName;
        if (message.idNumber != null && message.hasOwnProperty("idNumber"))
            object.idNumber = message.idNumber;
        if (message.createTime != null && message.hasOwnProperty("createTime"))
            if (typeof message.createTime === "number")
                object.createTime = options.longs === String ? String(message.createTime) : message.createTime;
            else
                object.createTime = options.longs === String ? $util.Long.prototype.toString.call(message.createTime) : options.longs === Number ? new $util.LongBits(message.createTime.low >>> 0, message.createTime.high >>> 0).toNumber() : message.createTime;
        if (message.userType != null && message.hasOwnProperty("userType"))
            object.userType = message.userType;
        if (message.userType != null && message.hasOwnProperty("userType"))
            object.userType = message.userType;
        return object;
    };

    /**
     * Converts this UserBase to JSON.
     * @function toJSON
     * @memberof UserBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    UserBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for UserBase
     * @function getTypeUrl
     * @memberof UserBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    UserBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/UserBase";
    };

    return UserBase;
})();

export const UserSwitch = $root.UserSwitch = (() => {

    /**
     * Properties of a UserSwitch.
     * @exports IUserSwitch
     * @interface IUserSwitch
     * @property {boolean|null} [phoneValidate] UserSwitch phoneValidate
     * @property {boolean|null} [emailValidate] UserSwitch emailValidate
     */

    /**
     * Constructs a new UserSwitch.
     * @exports UserSwitch
     * @classdesc Represents a UserSwitch.
     * @implements IUserSwitch
     * @constructor
     * @param {IUserSwitch=} [properties] Properties to set
     */
    function UserSwitch(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * UserSwitch phoneValidate.
     * @member {boolean} phoneValidate
     * @memberof UserSwitch
     * @instance
     */
    UserSwitch.prototype.phoneValidate = false;

    /**
     * UserSwitch emailValidate.
     * @member {boolean} emailValidate
     * @memberof UserSwitch
     * @instance
     */
    UserSwitch.prototype.emailValidate = false;

    /**
     * Creates a new UserSwitch instance using the specified properties.
     * @function create
     * @memberof UserSwitch
     * @static
     * @param {IUserSwitch=} [properties] Properties to set
     * @returns {UserSwitch} UserSwitch instance
     */
    UserSwitch.create = function create(properties) {
        return new UserSwitch(properties);
    };

    /**
     * Encodes the specified UserSwitch message. Does not implicitly {@link UserSwitch.verify|verify} messages.
     * @function encode
     * @memberof UserSwitch
     * @static
     * @param {IUserSwitch} message UserSwitch message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UserSwitch.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.phoneValidate != null && Object.hasOwnProperty.call(message, "phoneValidate"))
            writer.uint32(/* id 1, wireType 0 =*/8).bool(message.phoneValidate);
        if (message.emailValidate != null && Object.hasOwnProperty.call(message, "emailValidate"))
            writer.uint32(/* id 2, wireType 0 =*/16).bool(message.emailValidate);
        return writer;
    };

    /**
     * Encodes the specified UserSwitch message, length delimited. Does not implicitly {@link UserSwitch.verify|verify} messages.
     * @function encodeDelimited
     * @memberof UserSwitch
     * @static
     * @param {IUserSwitch} message UserSwitch message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UserSwitch.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a UserSwitch message from the specified reader or buffer.
     * @function decode
     * @memberof UserSwitch
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {UserSwitch} UserSwitch
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UserSwitch.decode = function decode(reader, length, error) {
    UserSwitch.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.UserSwitch();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.phoneValidate = reader.bool();
                    break;
                }
            case 2: {
                    message.emailValidate = reader.bool();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a UserSwitch message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof UserSwitch
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {UserSwitch} UserSwitch
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UserSwitch.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a UserSwitch message.
     * @function verify
     * @memberof UserSwitch
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    UserSwitch.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.phoneValidate != null && message.hasOwnProperty("phoneValidate"))
            if (typeof message.phoneValidate !== "boolean")
                return "phoneValidate: boolean expected";
        if (message.emailValidate != null && message.hasOwnProperty("emailValidate"))
            if (typeof message.emailValidate !== "boolean")
                return "emailValidate: boolean expected";
        return null;
    };

    /**
     * Creates a UserSwitch message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof UserSwitch
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {UserSwitch} UserSwitch
     */
    UserSwitch.fromObject = function fromObject(object) {
        if (object instanceof $root.UserSwitch)
            return object;
        let message = new $root.UserSwitch();
        if (object.phoneValidate != null)
            message.phoneValidate = Boolean(object.phoneValidate);
        if (object.emailValidate != null)
            message.emailValidate = Boolean(object.emailValidate);
        return message;
    };

    /**
     * Creates a plain object from a UserSwitch message. Also converts values to other types if specified.
     * @function toObject
     * @memberof UserSwitch
     * @static
     * @param {UserSwitch} message UserSwitch
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    UserSwitch.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.phoneValidate = false;
            object.emailValidate = false;
        }
        if (message.phoneValidate != null && message.hasOwnProperty("phoneValidate"))
            object.phoneValidate = message.phoneValidate;
        if (message.emailValidate != null && message.hasOwnProperty("emailValidate"))
            object.emailValidate = message.emailValidate;
        return object;
    };

    /**
     * Converts this UserSwitch to JSON.
     * @function toJSON
     * @memberof UserSwitch
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    UserSwitch.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for UserSwitch
     * @function getTypeUrl
     * @memberof UserSwitch
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    UserSwitch.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/UserSwitch";
    };

    return UserSwitch;
})();

export const FriendRelation = $root.FriendRelation = (() => {

    /**
     * Properties of a FriendRelation.
     * @exports IFriendRelation
     * @interface IFriendRelation
     * @property {boolean|null} [bfFriend] FriendRelation bfFriend
     * @property {string|null} [remarkName] FriendRelation remarkName
     * @property {number|null} [status] FriendRelation status
     */

    /**
     * Constructs a new FriendRelation.
     * @exports FriendRelation
     * @classdesc Represents a FriendRelation.
     * @implements IFriendRelation
     * @constructor
     * @param {IFriendRelation=} [properties] Properties to set
     */
    function FriendRelation(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * FriendRelation bfFriend.
     * @member {boolean} bfFriend
     * @memberof FriendRelation
     * @instance
     */
    FriendRelation.prototype.bfFriend = false;

    /**
     * FriendRelation remarkName.
     * @member {string} remarkName
     * @memberof FriendRelation
     * @instance
     */
    FriendRelation.prototype.remarkName = "";

    /**
     * FriendRelation status.
     * @member {number} status
     * @memberof FriendRelation
     * @instance
     */
    FriendRelation.prototype.status = 0;

    /**
     * Creates a new FriendRelation instance using the specified properties.
     * @function create
     * @memberof FriendRelation
     * @static
     * @param {IFriendRelation=} [properties] Properties to set
     * @returns {FriendRelation} FriendRelation instance
     */
    FriendRelation.create = function create(properties) {
        return new FriendRelation(properties);
    };

    /**
     * Encodes the specified FriendRelation message. Does not implicitly {@link FriendRelation.verify|verify} messages.
     * @function encode
     * @memberof FriendRelation
     * @static
     * @param {IFriendRelation} message FriendRelation message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FriendRelation.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.bfFriend != null && Object.hasOwnProperty.call(message, "bfFriend"))
            writer.uint32(/* id 1, wireType 0 =*/8).bool(message.bfFriend);
        if (message.remarkName != null && Object.hasOwnProperty.call(message, "remarkName"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.remarkName);
        if (message.status != null && Object.hasOwnProperty.call(message, "status"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.status);
        return writer;
    };

    /**
     * Encodes the specified FriendRelation message, length delimited. Does not implicitly {@link FriendRelation.verify|verify} messages.
     * @function encodeDelimited
     * @memberof FriendRelation
     * @static
     * @param {IFriendRelation} message FriendRelation message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FriendRelation.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a FriendRelation message from the specified reader or buffer.
     * @function decode
     * @memberof FriendRelation
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {FriendRelation} FriendRelation
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FriendRelation.decode = function decode(reader, length, error) {
    FriendRelation.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.FriendRelation();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.bfFriend = reader.bool();
                    break;
                }
            case 2: {
                    message.remarkName = reader.string();
                    break;
                }
            case 3: {
                    message.status = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a FriendRelation message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof FriendRelation
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {FriendRelation} FriendRelation
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FriendRelation.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a FriendRelation message.
     * @function verify
     * @memberof FriendRelation
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    FriendRelation.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.bfFriend != null && message.hasOwnProperty("bfFriend"))
            if (typeof message.bfFriend !== "boolean")
                return "bfFriend: boolean expected";
        if (message.remarkName != null && message.hasOwnProperty("remarkName"))
            if (!$util.isString(message.remarkName))
                return "remarkName: string expected";
        if (message.status != null && message.hasOwnProperty("status"))
            if (!$util.isInteger(message.status))
                return "status: integer expected";
        return null;
    };

    /**
     * Creates a FriendRelation message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof FriendRelation
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {FriendRelation} FriendRelation
     */
    FriendRelation.fromObject = function fromObject(object) {
        if (object instanceof $root.FriendRelation)
            return object;
        let message = new $root.FriendRelation();
        if (object.bfFriend != null)
            message.bfFriend = Boolean(object.bfFriend);
        if (object.remarkName != null)
            message.remarkName = String(object.remarkName);
        if (object.status != null)
            message.status = object.status | 0;
        return message;
    };

    /**
     * Creates a plain object from a FriendRelation message. Also converts values to other types if specified.
     * @function toObject
     * @memberof FriendRelation
     * @static
     * @param {FriendRelation} message FriendRelation
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    FriendRelation.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.bfFriend = false;
            object.remarkName = "";
            object.status = 0;
        }
        if (message.bfFriend != null && message.hasOwnProperty("bfFriend"))
            object.bfFriend = message.bfFriend;
        if (message.remarkName != null && message.hasOwnProperty("remarkName"))
            object.remarkName = message.remarkName;
        if (message.status != null && message.hasOwnProperty("status"))
            object.status = message.status;
        return object;
    };

    /**
     * Converts this FriendRelation to JSON.
     * @function toJSON
     * @memberof FriendRelation
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    FriendRelation.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for FriendRelation
     * @function getTypeUrl
     * @memberof FriendRelation
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    FriendRelation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/FriendRelation";
    };

    return FriendRelation;
})();

export const ArchiveInfo = $root.ArchiveInfo = (() => {

    /**
     * Properties of an ArchiveInfo.
     * @exports IArchiveInfo
     * @interface IArchiveInfo
     * @property {number|Long|null} [target] ArchiveInfo target
     * @property {number|Long|null} [type] ArchiveInfo type
     * @property {number|Long|null} [status] ArchiveInfo status
     */

    /**
     * Constructs a new ArchiveInfo.
     * @exports ArchiveInfo
     * @classdesc Represents an ArchiveInfo.
     * @implements IArchiveInfo
     * @constructor
     * @param {IArchiveInfo=} [properties] Properties to set
     */
    function ArchiveInfo(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * ArchiveInfo target.
     * @member {number|Long} target
     * @memberof ArchiveInfo
     * @instance
     */
    ArchiveInfo.prototype.target = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * ArchiveInfo type.
     * @member {number|Long} type
     * @memberof ArchiveInfo
     * @instance
     */
    ArchiveInfo.prototype.type = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * ArchiveInfo status.
     * @member {number|Long} status
     * @memberof ArchiveInfo
     * @instance
     */
    ArchiveInfo.prototype.status = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * Creates a new ArchiveInfo instance using the specified properties.
     * @function create
     * @memberof ArchiveInfo
     * @static
     * @param {IArchiveInfo=} [properties] Properties to set
     * @returns {ArchiveInfo} ArchiveInfo instance
     */
    ArchiveInfo.create = function create(properties) {
        return new ArchiveInfo(properties);
    };

    /**
     * Encodes the specified ArchiveInfo message. Does not implicitly {@link ArchiveInfo.verify|verify} messages.
     * @function encode
     * @memberof ArchiveInfo
     * @static
     * @param {IArchiveInfo} message ArchiveInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ArchiveInfo.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.target != null && Object.hasOwnProperty.call(message, "target"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.target);
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 2, wireType 0 =*/16).int64(message.type);
        if (message.status != null && Object.hasOwnProperty.call(message, "status"))
            writer.uint32(/* id 3, wireType 0 =*/24).int64(message.status);
        return writer;
    };

    /**
     * Encodes the specified ArchiveInfo message, length delimited. Does not implicitly {@link ArchiveInfo.verify|verify} messages.
     * @function encodeDelimited
     * @memberof ArchiveInfo
     * @static
     * @param {IArchiveInfo} message ArchiveInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ArchiveInfo.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an ArchiveInfo message from the specified reader or buffer.
     * @function decode
     * @memberof ArchiveInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {ArchiveInfo} ArchiveInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ArchiveInfo.decode = function decode(reader, length, error) {
    ArchiveInfo.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.ArchiveInfo();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.target = reader.int64();
                    break;
                }
            case 2: {
                    message.type = reader.int64();
                    break;
                }
            case 3: {
                    message.status = reader.int64();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an ArchiveInfo message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof ArchiveInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {ArchiveInfo} ArchiveInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ArchiveInfo.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an ArchiveInfo message.
     * @function verify
     * @memberof ArchiveInfo
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ArchiveInfo.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.target != null && message.hasOwnProperty("target"))
            if (!$util.isInteger(message.target) && !(message.target && $util.isInteger(message.target.low) && $util.isInteger(message.target.high)))
                return "target: integer|Long expected";
        if (message.type != null && message.hasOwnProperty("type"))
            if (!$util.isInteger(message.type) && !(message.type && $util.isInteger(message.type.low) && $util.isInteger(message.type.high)))
                return "type: integer|Long expected";
        if (message.status != null && message.hasOwnProperty("status"))
            if (!$util.isInteger(message.status) && !(message.status && $util.isInteger(message.status.low) && $util.isInteger(message.status.high)))
                return "status: integer|Long expected";
        return null;
    };

    /**
     * Creates an ArchiveInfo message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof ArchiveInfo
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {ArchiveInfo} ArchiveInfo
     */
    ArchiveInfo.fromObject = function fromObject(object) {
        if (object instanceof $root.ArchiveInfo)
            return object;
        let message = new $root.ArchiveInfo();
        if (object.target != null)
            if ($util.Long)
                (message.target = $util.Long.fromValue(object.target)).unsigned = false;
            else if (typeof object.target === "string")
                message.target = parseInt(object.target, 10);
            else if (typeof object.target === "number")
                message.target = object.target;
            else if (typeof object.target === "object")
                message.target = new $util.LongBits(object.target.low >>> 0, object.target.high >>> 0).toNumber();
        if (object.type != null)
            if ($util.Long)
                (message.type = $util.Long.fromValue(object.type)).unsigned = false;
            else if (typeof object.type === "string")
                message.type = parseInt(object.type, 10);
            else if (typeof object.type === "number")
                message.type = object.type;
            else if (typeof object.type === "object")
                message.type = new $util.LongBits(object.type.low >>> 0, object.type.high >>> 0).toNumber();
        if (object.status != null)
            if ($util.Long)
                (message.status = $util.Long.fromValue(object.status)).unsigned = false;
            else if (typeof object.status === "string")
                message.status = parseInt(object.status, 10);
            else if (typeof object.status === "number")
                message.status = object.status;
            else if (typeof object.status === "object")
                message.status = new $util.LongBits(object.status.low >>> 0, object.status.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from an ArchiveInfo message. Also converts values to other types if specified.
     * @function toObject
     * @memberof ArchiveInfo
     * @static
     * @param {ArchiveInfo} message ArchiveInfo
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ArchiveInfo.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.target = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.target = options.longs === String ? "0" : 0;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.type = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.type = options.longs === String ? "0" : 0;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.status = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.status = options.longs === String ? "0" : 0;
        }
        if (message.target != null && message.hasOwnProperty("target"))
            if (typeof message.target === "number")
                object.target = options.longs === String ? String(message.target) : message.target;
            else
                object.target = options.longs === String ? $util.Long.prototype.toString.call(message.target) : options.longs === Number ? new $util.LongBits(message.target.low >>> 0, message.target.high >>> 0).toNumber() : message.target;
        if (message.type != null && message.hasOwnProperty("type"))
            if (typeof message.type === "number")
                object.type = options.longs === String ? String(message.type) : message.type;
            else
                object.type = options.longs === String ? $util.Long.prototype.toString.call(message.type) : options.longs === Number ? new $util.LongBits(message.type.low >>> 0, message.type.high >>> 0).toNumber() : message.type;
        if (message.status != null && message.hasOwnProperty("status"))
            if (typeof message.status === "number")
                object.status = options.longs === String ? String(message.status) : message.status;
            else
                object.status = options.longs === String ? $util.Long.prototype.toString.call(message.status) : options.longs === Number ? new $util.LongBits(message.status.low >>> 0, message.status.high >>> 0).toNumber() : message.status;
        return object;
    };

    /**
     * Converts this ArchiveInfo to JSON.
     * @function toJSON
     * @memberof ArchiveInfo
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ArchiveInfo.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ArchiveInfo
     * @function getTypeUrl
     * @memberof ArchiveInfo
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ArchiveInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/ArchiveInfo";
    };

    return ArchiveInfo;
})();

export const GroupMemberBase = $root.GroupMemberBase = (() => {

    /**
     * Properties of a GroupMemberBase.
     * @exports IGroupMemberBase
     * @interface IGroupMemberBase
     * @property {IUserBase|null} [user] GroupMemberBase user
     * @property {number|Long|null} [groupId] GroupMemberBase groupId
     * @property {GroupMemberType|null} [type] GroupMemberBase type
     * @property {string|null} [groupNickName] GroupMemberBase groupNickName
     * @property {number|Long|null} [score] GroupMemberBase score
     * @property {IAdminRightBase|null} [right] GroupMemberBase right
     * @property {boolean|null} [bfMyBlack] GroupMemberBase bfMyBlack
     * @property {number|null} [labelType] GroupMemberBase labelType
     */

    /**
     * Constructs a new GroupMemberBase.
     * @exports GroupMemberBase
     * @classdesc Represents a GroupMemberBase.
     * @implements IGroupMemberBase
     * @constructor
     * @param {IGroupMemberBase=} [properties] Properties to set
     */
    function GroupMemberBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GroupMemberBase user.
     * @member {IUserBase|null|undefined} user
     * @memberof GroupMemberBase
     * @instance
     */
    GroupMemberBase.prototype.user = null;

    /**
     * GroupMemberBase groupId.
     * @member {number|Long} groupId
     * @memberof GroupMemberBase
     * @instance
     */
    GroupMemberBase.prototype.groupId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * GroupMemberBase type.
     * @member {GroupMemberType} type
     * @memberof GroupMemberBase
     * @instance
     */
    GroupMemberBase.prototype.type = 0;

    /**
     * GroupMemberBase groupNickName.
     * @member {string} groupNickName
     * @memberof GroupMemberBase
     * @instance
     */
    GroupMemberBase.prototype.groupNickName = "";

    /**
     * GroupMemberBase score.
     * @member {number|Long} score
     * @memberof GroupMemberBase
     * @instance
     */
    GroupMemberBase.prototype.score = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * GroupMemberBase right.
     * @member {IAdminRightBase|null|undefined} right
     * @memberof GroupMemberBase
     * @instance
     */
    GroupMemberBase.prototype.right = null;

    /**
     * GroupMemberBase bfMyBlack.
     * @member {boolean} bfMyBlack
     * @memberof GroupMemberBase
     * @instance
     */
    GroupMemberBase.prototype.bfMyBlack = false;

    /**
     * GroupMemberBase labelType.
     * @member {number} labelType
     * @memberof GroupMemberBase
     * @instance
     */
    GroupMemberBase.prototype.labelType = 0;

    /**
     * Creates a new GroupMemberBase instance using the specified properties.
     * @function create
     * @memberof GroupMemberBase
     * @static
     * @param {IGroupMemberBase=} [properties] Properties to set
     * @returns {GroupMemberBase} GroupMemberBase instance
     */
    GroupMemberBase.create = function create(properties) {
        return new GroupMemberBase(properties);
    };

    /**
     * Encodes the specified GroupMemberBase message. Does not implicitly {@link GroupMemberBase.verify|verify} messages.
     * @function encode
     * @memberof GroupMemberBase
     * @static
     * @param {IGroupMemberBase} message GroupMemberBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GroupMemberBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.user != null && Object.hasOwnProperty.call(message, "user"))
            $root.UserBase.encode(message.user, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.groupId != null && Object.hasOwnProperty.call(message, "groupId"))
            writer.uint32(/* id 2, wireType 0 =*/16).int64(message.groupId);
        if (message.type != null && Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.type);
        if (message.groupNickName != null && Object.hasOwnProperty.call(message, "groupNickName"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.groupNickName);
        if (message.score != null && Object.hasOwnProperty.call(message, "score"))
            writer.uint32(/* id 5, wireType 0 =*/40).int64(message.score);
        if (message.right != null && Object.hasOwnProperty.call(message, "right"))
            $root.AdminRightBase.encode(message.right, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
        if (message.bfMyBlack != null && Object.hasOwnProperty.call(message, "bfMyBlack"))
            writer.uint32(/* id 7, wireType 0 =*/56).bool(message.bfMyBlack);
        if (message.labelType != null && Object.hasOwnProperty.call(message, "labelType"))
            writer.uint32(/* id 8, wireType 0 =*/64).int32(message.labelType);
        return writer;
    };

    /**
     * Encodes the specified GroupMemberBase message, length delimited. Does not implicitly {@link GroupMemberBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GroupMemberBase
     * @static
     * @param {IGroupMemberBase} message GroupMemberBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GroupMemberBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GroupMemberBase message from the specified reader or buffer.
     * @function decode
     * @memberof GroupMemberBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GroupMemberBase} GroupMemberBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GroupMemberBase.decode = function decode(reader, length, error) {
    GroupMemberBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GroupMemberBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.user = $root.UserBase.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.groupId = reader.int64();
                    break;
                }
            case 3: {
                    message.type = reader.int32();
                    break;
                }
            case 4: {
                    message.groupNickName = reader.string();
                    break;
                }
            case 5: {
                    message.score = reader.int64();
                    break;
                }
            case 6: {
                    message.right = $root.AdminRightBase.decode(reader, reader.uint32());
                    break;
                }
            case 7: {
                    message.bfMyBlack = reader.bool();
                    break;
                }
            case 8: {
                    message.labelType = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GroupMemberBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GroupMemberBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GroupMemberBase} GroupMemberBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GroupMemberBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GroupMemberBase message.
     * @function verify
     * @memberof GroupMemberBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GroupMemberBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.user != null && message.hasOwnProperty("user")) {
            let error = $root.UserBase.verify(message.user);
            if (error)
                return "user." + error;
        }
        if (message.groupId != null && message.hasOwnProperty("groupId"))
            if (!$util.isInteger(message.groupId) && !(message.groupId && $util.isInteger(message.groupId.low) && $util.isInteger(message.groupId.high)))
                return "groupId: integer|Long expected";
        if (message.type != null && message.hasOwnProperty("type"))
            switch (message.type) {
            default:
                return "type: enum value expected";
            case 0:
            case 1:
            case 2:
                break;
            }
        if (message.groupNickName != null && message.hasOwnProperty("groupNickName"))
            if (!$util.isString(message.groupNickName))
                return "groupNickName: string expected";
        if (message.score != null && message.hasOwnProperty("score"))
            if (!$util.isInteger(message.score) && !(message.score && $util.isInteger(message.score.low) && $util.isInteger(message.score.high)))
                return "score: integer|Long expected";
        if (message.right != null && message.hasOwnProperty("right")) {
            let error = $root.AdminRightBase.verify(message.right);
            if (error)
                return "right." + error;
        }
        if (message.bfMyBlack != null && message.hasOwnProperty("bfMyBlack"))
            if (typeof message.bfMyBlack !== "boolean")
                return "bfMyBlack: boolean expected";
        if (message.labelType != null && message.hasOwnProperty("labelType"))
            if (!$util.isInteger(message.labelType))
                return "labelType: integer expected";
        return null;
    };

    /**
     * Creates a GroupMemberBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GroupMemberBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GroupMemberBase} GroupMemberBase
     */
    GroupMemberBase.fromObject = function fromObject(object) {
        if (object instanceof $root.GroupMemberBase)
            return object;
        let message = new $root.GroupMemberBase();
        if (object.user != null) {
            if (typeof object.user !== "object")
                throw TypeError(".GroupMemberBase.user: object expected");
            message.user = $root.UserBase.fromObject(object.user);
        }
        if (object.groupId != null)
            if ($util.Long)
                (message.groupId = $util.Long.fromValue(object.groupId)).unsigned = false;
            else if (typeof object.groupId === "string")
                message.groupId = parseInt(object.groupId, 10);
            else if (typeof object.groupId === "number")
                message.groupId = object.groupId;
            else if (typeof object.groupId === "object")
                message.groupId = new $util.LongBits(object.groupId.low >>> 0, object.groupId.high >>> 0).toNumber();
        switch (object.type) {
        default:
            if (typeof object.type === "number") {
                message.type = object.type;
                break;
            }
            break;
        case "HOST":
        case 0:
            message.type = 0;
            break;
        case "MANAGE":
        case 1:
            message.type = 1;
            break;
        case "MEMBER":
        case 2:
            message.type = 2;
            break;
        }
        if (object.groupNickName != null)
            message.groupNickName = String(object.groupNickName);
        if (object.score != null)
            if ($util.Long)
                (message.score = $util.Long.fromValue(object.score)).unsigned = false;
            else if (typeof object.score === "string")
                message.score = parseInt(object.score, 10);
            else if (typeof object.score === "number")
                message.score = object.score;
            else if (typeof object.score === "object")
                message.score = new $util.LongBits(object.score.low >>> 0, object.score.high >>> 0).toNumber();
        if (object.right != null) {
            if (typeof object.right !== "object")
                throw TypeError(".GroupMemberBase.right: object expected");
            message.right = $root.AdminRightBase.fromObject(object.right);
        }
        if (object.bfMyBlack != null)
            message.bfMyBlack = Boolean(object.bfMyBlack);
        if (object.labelType != null)
            message.labelType = object.labelType | 0;
        return message;
    };

    /**
     * Creates a plain object from a GroupMemberBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GroupMemberBase
     * @static
     * @param {GroupMemberBase} message GroupMemberBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GroupMemberBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.user = null;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.groupId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.groupId = options.longs === String ? "0" : 0;
            object.type = options.enums === String ? "HOST" : 0;
            object.groupNickName = "";
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.score = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.score = options.longs === String ? "0" : 0;
            object.right = null;
            object.bfMyBlack = false;
            object.labelType = 0;
        }
        if (message.user != null && message.hasOwnProperty("user"))
            object.user = $root.UserBase.toObject(message.user, options);
        if (message.groupId != null && message.hasOwnProperty("groupId"))
            if (typeof message.groupId === "number")
                object.groupId = options.longs === String ? String(message.groupId) : message.groupId;
            else
                object.groupId = options.longs === String ? $util.Long.prototype.toString.call(message.groupId) : options.longs === Number ? new $util.LongBits(message.groupId.low >>> 0, message.groupId.high >>> 0).toNumber() : message.groupId;
        if (message.type != null && message.hasOwnProperty("type"))
            object.type = options.enums === String ? $root.GroupMemberType[message.type] === undefined ? message.type : $root.GroupMemberType[message.type] : message.type;
        if (message.groupNickName != null && message.hasOwnProperty("groupNickName"))
            object.groupNickName = message.groupNickName;
        if (message.score != null && message.hasOwnProperty("score"))
            if (typeof message.score === "number")
                object.score = options.longs === String ? String(message.score) : message.score;
            else
                object.score = options.longs === String ? $util.Long.prototype.toString.call(message.score) : options.longs === Number ? new $util.LongBits(message.score.low >>> 0, message.score.high >>> 0).toNumber() : message.score;
        if (message.right != null && message.hasOwnProperty("right"))
            object.right = $root.AdminRightBase.toObject(message.right, options);
        if (message.bfMyBlack != null && message.hasOwnProperty("bfMyBlack"))
            object.bfMyBlack = message.bfMyBlack;
        if (message.labelType != null && message.hasOwnProperty("labelType"))
            object.labelType = message.labelType;
        return object;
    };

    /**
     * Converts this GroupMemberBase to JSON.
     * @function toJSON
     * @memberof GroupMemberBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GroupMemberBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GroupMemberBase
     * @function getTypeUrl
     * @memberof GroupMemberBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GroupMemberBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GroupMemberBase";
    };

    return GroupMemberBase;
})();

export const ContactsDetailBase = $root.ContactsDetailBase = (() => {

    /**
     * Properties of a ContactsDetailBase.
     * @exports IContactsDetailBase
     * @interface IContactsDetailBase
     * @property {IUserBase|null} [userInfo] ContactsDetailBase userInfo
     * @property {string|null} [depict] ContactsDetailBase depict
     * @property {boolean|null} [bfStar] ContactsDetailBase bfStar
     * @property {boolean|null} [bfDisturb] ContactsDetailBase bfDisturb
     * @property {boolean|null} [bfMyBlack] ContactsDetailBase bfMyBlack
     * @property {string|null} [letter] ContactsDetailBase letter
     * @property {boolean|null} [bfTop] ContactsDetailBase bfTop
     * @property {boolean|null} [bfVerify] ContactsDetailBase bfVerify
     * @property {string|null} [signature] ContactsDetailBase signature
     * @property {string|null} [groupNickName] ContactsDetailBase groupNickName
     * @property {string|null} [phone] ContactsDetailBase phone
     * @property {boolean|null} [bfReadCancel] ContactsDetailBase bfReadCancel
     * @property {number|null} [msgCancelTime] ContactsDetailBase msgCancelTime
     * @property {boolean|null} [bfScreenshot] ContactsDetailBase bfScreenshot
     * @property {number|null} [commonGroupNum] ContactsDetailBase commonGroupNum
     * @property {boolean|null} [bfReadReceipt] ContactsDetailBase bfReadReceipt
     * @property {number|null} [groupShutupTime] ContactsDetailBase groupShutupTime
     * @property {number|null} [searchType] ContactsDetailBase searchType
     * @property {string|null} [addToken] ContactsDetailBase addToken
     * @property {boolean|null} [bfIdSearch] ContactsDetailBase bfIdSearch
     * @property {boolean|null} [bfIdSearch] ContactsDetailBase bfIdSearch
     */

    /**
     * Constructs a new ContactsDetailBase.
     * @exports ContactsDetailBase
     * @classdesc Represents a ContactsDetailBase.
     * @implements IContactsDetailBase
     * @constructor
     * @param {IContactsDetailBase=} [properties] Properties to set
     */
    function ContactsDetailBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * ContactsDetailBase userInfo.
     * @member {IUserBase|null|undefined} userInfo
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.userInfo = null;

    /**
     * ContactsDetailBase depict.
     * @member {string} depict
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.depict = "";

    /**
     * ContactsDetailBase bfStar.
     * @member {boolean} bfStar
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.bfStar = false;

    /**
     * ContactsDetailBase bfDisturb.
     * @member {boolean} bfDisturb
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.bfDisturb = false;

    /**
     * ContactsDetailBase bfMyBlack.
     * @member {boolean} bfMyBlack
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.bfMyBlack = false;

    /**
     * ContactsDetailBase letter.
     * @member {string} letter
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.letter = "";

    /**
     * ContactsDetailBase bfTop.
     * @member {boolean} bfTop
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.bfTop = false;

    /**
     * ContactsDetailBase bfVerify.
     * @member {boolean} bfVerify
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.bfVerify = false;

    /**
     * ContactsDetailBase signature.
     * @member {string} signature
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.signature = "";

    /**
     * ContactsDetailBase groupNickName.
     * @member {string} groupNickName
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.groupNickName = "";

    /**
     * ContactsDetailBase phone.
     * @member {string} phone
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.phone = "";

    /**
     * ContactsDetailBase bfReadCancel.
     * @member {boolean} bfReadCancel
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.bfReadCancel = false;

    /**
     * ContactsDetailBase msgCancelTime.
     * @member {number} msgCancelTime
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.msgCancelTime = 0;

    /**
     * ContactsDetailBase bfScreenshot.
     * @member {boolean} bfScreenshot
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.bfScreenshot = false;

    /**
     * ContactsDetailBase commonGroupNum.
     * @member {number} commonGroupNum
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.commonGroupNum = 0;

    /**
     * ContactsDetailBase bfReadReceipt.
     * @member {boolean} bfReadReceipt
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.bfReadReceipt = false;

    /**
     * ContactsDetailBase groupShutupTime.
     * @member {number} groupShutupTime
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.groupShutupTime = 0;

    /**
     * ContactsDetailBase searchType.
     * @member {number} searchType
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.searchType = 0;

    /**
     * ContactsDetailBase addToken.
     * @member {string} addToken
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.addToken = "";

    /**
     * ContactsDetailBase bfIdSearch.
     * @member {boolean} bfIdSearch
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.bfIdSearch = false;

    /**
     * ContactsDetailBase bfIdSearch.
     * @member {boolean} bfIdSearch
     * @memberof ContactsDetailBase
     * @instance
     */
    ContactsDetailBase.prototype.bfIdSearch = false;

    /**
     * Creates a new ContactsDetailBase instance using the specified properties.
     * @function create
     * @memberof ContactsDetailBase
     * @static
     * @param {IContactsDetailBase=} [properties] Properties to set
     * @returns {ContactsDetailBase} ContactsDetailBase instance
     */
    ContactsDetailBase.create = function create(properties) {
        return new ContactsDetailBase(properties);
    };

    /**
     * Encodes the specified ContactsDetailBase message. Does not implicitly {@link ContactsDetailBase.verify|verify} messages.
     * @function encode
     * @memberof ContactsDetailBase
     * @static
     * @param {IContactsDetailBase} message ContactsDetailBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ContactsDetailBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.userInfo != null && Object.hasOwnProperty.call(message, "userInfo"))
            $root.UserBase.encode(message.userInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.depict != null && Object.hasOwnProperty.call(message, "depict"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.depict);
        if (message.bfStar != null && Object.hasOwnProperty.call(message, "bfStar"))
            writer.uint32(/* id 3, wireType 0 =*/24).bool(message.bfStar);
        if (message.bfDisturb != null && Object.hasOwnProperty.call(message, "bfDisturb"))
            writer.uint32(/* id 4, wireType 0 =*/32).bool(message.bfDisturb);
        if (message.bfMyBlack != null && Object.hasOwnProperty.call(message, "bfMyBlack"))
            writer.uint32(/* id 5, wireType 0 =*/40).bool(message.bfMyBlack);
        if (message.letter != null && Object.hasOwnProperty.call(message, "letter"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.letter);
        if (message.bfTop != null && Object.hasOwnProperty.call(message, "bfTop"))
            writer.uint32(/* id 7, wireType 0 =*/56).bool(message.bfTop);
        if (message.bfVerify != null && Object.hasOwnProperty.call(message, "bfVerify"))
            writer.uint32(/* id 8, wireType 0 =*/64).bool(message.bfVerify);
        if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
            writer.uint32(/* id 9, wireType 2 =*/74).string(message.signature);
        if (message.groupNickName != null && Object.hasOwnProperty.call(message, "groupNickName"))
            writer.uint32(/* id 10, wireType 2 =*/82).string(message.groupNickName);
        if (message.phone != null && Object.hasOwnProperty.call(message, "phone"))
            writer.uint32(/* id 11, wireType 2 =*/90).string(message.phone);
        if (message.bfReadCancel != null && Object.hasOwnProperty.call(message, "bfReadCancel"))
            writer.uint32(/* id 12, wireType 0 =*/96).bool(message.bfReadCancel);
        if (message.msgCancelTime != null && Object.hasOwnProperty.call(message, "msgCancelTime"))
            writer.uint32(/* id 13, wireType 0 =*/104).int32(message.msgCancelTime);
        if (message.bfScreenshot != null && Object.hasOwnProperty.call(message, "bfScreenshot"))
            writer.uint32(/* id 14, wireType 0 =*/112).bool(message.bfScreenshot);
        if (message.commonGroupNum != null && Object.hasOwnProperty.call(message, "commonGroupNum"))
            writer.uint32(/* id 15, wireType 0 =*/120).int32(message.commonGroupNum);
        if (message.bfReadReceipt != null && Object.hasOwnProperty.call(message, "bfReadReceipt"))
            writer.uint32(/* id 16, wireType 0 =*/128).bool(message.bfReadReceipt);
        if (message.groupShutupTime != null && Object.hasOwnProperty.call(message, "groupShutupTime"))
            writer.uint32(/* id 17, wireType 0 =*/136).int32(message.groupShutupTime);
        if (message.searchType != null && Object.hasOwnProperty.call(message, "searchType"))
            writer.uint32(/* id 18, wireType 0 =*/144).int32(message.searchType);
        if (message.addToken != null && Object.hasOwnProperty.call(message, "addToken"))
            writer.uint32(/* id 19, wireType 2 =*/154).string(message.addToken);
        if (message.bfIdSearch != null && Object.hasOwnProperty.call(message, "bfIdSearch"))
            writer.uint32(/* id 20, wireType 0 =*/160).bool(message.bfIdSearch);
        if (message.bfIdSearch != null && Object.hasOwnProperty.call(message, "bfIdSearch"))
            writer.uint32(/* id 20, wireType 0 =*/160).bool(message.bfIdSearch);
        return writer;
    };

    /**
     * Encodes the specified ContactsDetailBase message, length delimited. Does not implicitly {@link ContactsDetailBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof ContactsDetailBase
     * @static
     * @param {IContactsDetailBase} message ContactsDetailBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ContactsDetailBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ContactsDetailBase message from the specified reader or buffer.
     * @function decode
     * @memberof ContactsDetailBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {ContactsDetailBase} ContactsDetailBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ContactsDetailBase.decode = function decode(reader, length, error) {
    ContactsDetailBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.ContactsDetailBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.userInfo = $root.UserBase.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.depict = reader.string();
                    break;
                }
            case 3: {
                    message.bfStar = reader.bool();
                    break;
                }
            case 4: {
                    message.bfDisturb = reader.bool();
                    break;
                }
            case 5: {
                    message.bfMyBlack = reader.bool();
                    break;
                }
            case 6: {
                    message.letter = reader.string();
                    break;
                }
            case 7: {
                    message.bfTop = reader.bool();
                    break;
                }
            case 8: {
                    message.bfVerify = reader.bool();
                    break;
                }
            case 9: {
                    message.signature = reader.string();
                    break;
                }
            case 10: {
                    message.groupNickName = reader.string();
                    break;
                }
            case 11: {
                    message.phone = reader.string();
                    break;
                }
            case 12: {
                    message.bfReadCancel = reader.bool();
                    break;
                }
            case 13: {
                    message.msgCancelTime = reader.int32();
                    break;
                }
            case 14: {
                    message.bfScreenshot = reader.bool();
                    break;
                }
            case 15: {
                    message.commonGroupNum = reader.int32();
                    break;
                }
            case 16: {
                    message.bfReadReceipt = reader.bool();
                    break;
                }
            case 17: {
                    message.groupShutupTime = reader.int32();
                    break;
                }
            case 18: {
                    message.searchType = reader.int32();
                    break;
                }
            case 19: {
                    message.addToken = reader.string();
                    break;
                }
            case 20: {
                    message.bfIdSearch = reader.bool();
                    break;
                }
            case 20: {
                    message.bfIdSearch = reader.bool();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a ContactsDetailBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof ContactsDetailBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {ContactsDetailBase} ContactsDetailBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ContactsDetailBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ContactsDetailBase message.
     * @function verify
     * @memberof ContactsDetailBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ContactsDetailBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.userInfo != null && message.hasOwnProperty("userInfo")) {
            let error = $root.UserBase.verify(message.userInfo);
            if (error)
                return "userInfo." + error;
        }
        if (message.depict != null && message.hasOwnProperty("depict"))
            if (!$util.isString(message.depict))
                return "depict: string expected";
        if (message.bfStar != null && message.hasOwnProperty("bfStar"))
            if (typeof message.bfStar !== "boolean")
                return "bfStar: boolean expected";
        if (message.bfDisturb != null && message.hasOwnProperty("bfDisturb"))
            if (typeof message.bfDisturb !== "boolean")
                return "bfDisturb: boolean expected";
        if (message.bfMyBlack != null && message.hasOwnProperty("bfMyBlack"))
            if (typeof message.bfMyBlack !== "boolean")
                return "bfMyBlack: boolean expected";
        if (message.letter != null && message.hasOwnProperty("letter"))
            if (!$util.isString(message.letter))
                return "letter: string expected";
        if (message.bfTop != null && message.hasOwnProperty("bfTop"))
            if (typeof message.bfTop !== "boolean")
                return "bfTop: boolean expected";
        if (message.bfVerify != null && message.hasOwnProperty("bfVerify"))
            if (typeof message.bfVerify !== "boolean")
                return "bfVerify: boolean expected";
        if (message.signature != null && message.hasOwnProperty("signature"))
            if (!$util.isString(message.signature))
                return "signature: string expected";
        if (message.groupNickName != null && message.hasOwnProperty("groupNickName"))
            if (!$util.isString(message.groupNickName))
                return "groupNickName: string expected";
        if (message.phone != null && message.hasOwnProperty("phone"))
            if (!$util.isString(message.phone))
                return "phone: string expected";
        if (message.bfReadCancel != null && message.hasOwnProperty("bfReadCancel"))
            if (typeof message.bfReadCancel !== "boolean")
                return "bfReadCancel: boolean expected";
        if (message.msgCancelTime != null && message.hasOwnProperty("msgCancelTime"))
            if (!$util.isInteger(message.msgCancelTime))
                return "msgCancelTime: integer expected";
        if (message.bfScreenshot != null && message.hasOwnProperty("bfScreenshot"))
            if (typeof message.bfScreenshot !== "boolean")
                return "bfScreenshot: boolean expected";
        if (message.commonGroupNum != null && message.hasOwnProperty("commonGroupNum"))
            if (!$util.isInteger(message.commonGroupNum))
                return "commonGroupNum: integer expected";
        if (message.bfReadReceipt != null && message.hasOwnProperty("bfReadReceipt"))
            if (typeof message.bfReadReceipt !== "boolean")
                return "bfReadReceipt: boolean expected";
        if (message.groupShutupTime != null && message.hasOwnProperty("groupShutupTime"))
            if (!$util.isInteger(message.groupShutupTime))
                return "groupShutupTime: integer expected";
        if (message.searchType != null && message.hasOwnProperty("searchType"))
            if (!$util.isInteger(message.searchType))
                return "searchType: integer expected";
        if (message.addToken != null && message.hasOwnProperty("addToken"))
            if (!$util.isString(message.addToken))
                return "addToken: string expected";
        if (message.bfIdSearch != null && message.hasOwnProperty("bfIdSearch"))
            if (typeof message.bfIdSearch !== "boolean")
                return "bfIdSearch: boolean expected";
        if (message.bfIdSearch != null && message.hasOwnProperty("bfIdSearch"))
            if (typeof message.bfIdSearch !== "boolean")
                return "bfIdSearch: boolean expected";
        return null;
    };

    /**
     * Creates a ContactsDetailBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof ContactsDetailBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {ContactsDetailBase} ContactsDetailBase
     */
    ContactsDetailBase.fromObject = function fromObject(object) {
        if (object instanceof $root.ContactsDetailBase)
            return object;
        let message = new $root.ContactsDetailBase();
        if (object.userInfo != null) {
            if (typeof object.userInfo !== "object")
                throw TypeError(".ContactsDetailBase.userInfo: object expected");
            message.userInfo = $root.UserBase.fromObject(object.userInfo);
        }
        if (object.depict != null)
            message.depict = String(object.depict);
        if (object.bfStar != null)
            message.bfStar = Boolean(object.bfStar);
        if (object.bfDisturb != null)
            message.bfDisturb = Boolean(object.bfDisturb);
        if (object.bfMyBlack != null)
            message.bfMyBlack = Boolean(object.bfMyBlack);
        if (object.letter != null)
            message.letter = String(object.letter);
        if (object.bfTop != null)
            message.bfTop = Boolean(object.bfTop);
        if (object.bfVerify != null)
            message.bfVerify = Boolean(object.bfVerify);
        if (object.signature != null)
            message.signature = String(object.signature);
        if (object.groupNickName != null)
            message.groupNickName = String(object.groupNickName);
        if (object.phone != null)
            message.phone = String(object.phone);
        if (object.bfReadCancel != null)
            message.bfReadCancel = Boolean(object.bfReadCancel);
        if (object.msgCancelTime != null)
            message.msgCancelTime = object.msgCancelTime | 0;
        if (object.bfScreenshot != null)
            message.bfScreenshot = Boolean(object.bfScreenshot);
        if (object.commonGroupNum != null)
            message.commonGroupNum = object.commonGroupNum | 0;
        if (object.bfReadReceipt != null)
            message.bfReadReceipt = Boolean(object.bfReadReceipt);
        if (object.groupShutupTime != null)
            message.groupShutupTime = object.groupShutupTime | 0;
        if (object.searchType != null)
            message.searchType = object.searchType | 0;
        if (object.addToken != null)
            message.addToken = String(object.addToken);
        if (object.bfIdSearch != null)
            message.bfIdSearch = Boolean(object.bfIdSearch);
        if (object.bfIdSearch != null)
            message.bfIdSearch = Boolean(object.bfIdSearch);
        return message;
    };

    /**
     * Creates a plain object from a ContactsDetailBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof ContactsDetailBase
     * @static
     * @param {ContactsDetailBase} message ContactsDetailBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ContactsDetailBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.userInfo = null;
            object.depict = "";
            object.bfStar = false;
            object.bfDisturb = false;
            object.bfMyBlack = false;
            object.letter = "";
            object.bfTop = false;
            object.bfVerify = false;
            object.signature = "";
            object.groupNickName = "";
            object.phone = "";
            object.bfReadCancel = false;
            object.msgCancelTime = 0;
            object.bfScreenshot = false;
            object.commonGroupNum = 0;
            object.bfReadReceipt = false;
            object.groupShutupTime = 0;
            object.searchType = 0;
            object.addToken = "";
            object.bfIdSearch = false;
            object.bfIdSearch = false;
        }
        if (message.userInfo != null && message.hasOwnProperty("userInfo"))
            object.userInfo = $root.UserBase.toObject(message.userInfo, options);
        if (message.depict != null && message.hasOwnProperty("depict"))
            object.depict = message.depict;
        if (message.bfStar != null && message.hasOwnProperty("bfStar"))
            object.bfStar = message.bfStar;
        if (message.bfDisturb != null && message.hasOwnProperty("bfDisturb"))
            object.bfDisturb = message.bfDisturb;
        if (message.bfMyBlack != null && message.hasOwnProperty("bfMyBlack"))
            object.bfMyBlack = message.bfMyBlack;
        if (message.letter != null && message.hasOwnProperty("letter"))
            object.letter = message.letter;
        if (message.bfTop != null && message.hasOwnProperty("bfTop"))
            object.bfTop = message.bfTop;
        if (message.bfVerify != null && message.hasOwnProperty("bfVerify"))
            object.bfVerify = message.bfVerify;
        if (message.signature != null && message.hasOwnProperty("signature"))
            object.signature = message.signature;
        if (message.groupNickName != null && message.hasOwnProperty("groupNickName"))
            object.groupNickName = message.groupNickName;
        if (message.phone != null && message.hasOwnProperty("phone"))
            object.phone = message.phone;
        if (message.bfReadCancel != null && message.hasOwnProperty("bfReadCancel"))
            object.bfReadCancel = message.bfReadCancel;
        if (message.msgCancelTime != null && message.hasOwnProperty("msgCancelTime"))
            object.msgCancelTime = message.msgCancelTime;
        if (message.bfScreenshot != null && message.hasOwnProperty("bfScreenshot"))
            object.bfScreenshot = message.bfScreenshot;
        if (message.commonGroupNum != null && message.hasOwnProperty("commonGroupNum"))
            object.commonGroupNum = message.commonGroupNum;
        if (message.bfReadReceipt != null && message.hasOwnProperty("bfReadReceipt"))
            object.bfReadReceipt = message.bfReadReceipt;
        if (message.groupShutupTime != null && message.hasOwnProperty("groupShutupTime"))
            object.groupShutupTime = message.groupShutupTime;
        if (message.searchType != null && message.hasOwnProperty("searchType"))
            object.searchType = message.searchType;
        if (message.addToken != null && message.hasOwnProperty("addToken"))
            object.addToken = message.addToken;
        if (message.bfIdSearch != null && message.hasOwnProperty("bfIdSearch"))
            object.bfIdSearch = message.bfIdSearch;
        if (message.bfIdSearch != null && message.hasOwnProperty("bfIdSearch"))
            object.bfIdSearch = message.bfIdSearch;
        return object;
    };

    /**
     * Converts this ContactsDetailBase to JSON.
     * @function toJSON
     * @memberof ContactsDetailBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ContactsDetailBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ContactsDetailBase
     * @function getTypeUrl
     * @memberof ContactsDetailBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ContactsDetailBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/ContactsDetailBase";
    };

    return ContactsDetailBase;
})();

export const MsgReceiptStatusBase = $root.MsgReceiptStatusBase = (() => {

    /**
     * Properties of a MsgReceiptStatusBase.
     * @exports IMsgReceiptStatusBase
     * @interface IMsgReceiptStatusBase
     * @property {MsgReceiptStatus|null} [status] MsgReceiptStatusBase status
     * @property {number|Long|null} [time] MsgReceiptStatusBase time
     */

    /**
     * Constructs a new MsgReceiptStatusBase.
     * @exports MsgReceiptStatusBase
     * @classdesc Represents a MsgReceiptStatusBase.
     * @implements IMsgReceiptStatusBase
     * @constructor
     * @param {IMsgReceiptStatusBase=} [properties] Properties to set
     */
    function MsgReceiptStatusBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * MsgReceiptStatusBase status.
     * @member {MsgReceiptStatus} status
     * @memberof MsgReceiptStatusBase
     * @instance
     */
    MsgReceiptStatusBase.prototype.status = 0;

    /**
     * MsgReceiptStatusBase time.
     * @member {number|Long} time
     * @memberof MsgReceiptStatusBase
     * @instance
     */
    MsgReceiptStatusBase.prototype.time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * Creates a new MsgReceiptStatusBase instance using the specified properties.
     * @function create
     * @memberof MsgReceiptStatusBase
     * @static
     * @param {IMsgReceiptStatusBase=} [properties] Properties to set
     * @returns {MsgReceiptStatusBase} MsgReceiptStatusBase instance
     */
    MsgReceiptStatusBase.create = function create(properties) {
        return new MsgReceiptStatusBase(properties);
    };

    /**
     * Encodes the specified MsgReceiptStatusBase message. Does not implicitly {@link MsgReceiptStatusBase.verify|verify} messages.
     * @function encode
     * @memberof MsgReceiptStatusBase
     * @static
     * @param {IMsgReceiptStatusBase} message MsgReceiptStatusBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    MsgReceiptStatusBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.status != null && Object.hasOwnProperty.call(message, "status"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
        if (message.time != null && Object.hasOwnProperty.call(message, "time"))
            writer.uint32(/* id 2, wireType 0 =*/16).int64(message.time);
        return writer;
    };

    /**
     * Encodes the specified MsgReceiptStatusBase message, length delimited. Does not implicitly {@link MsgReceiptStatusBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof MsgReceiptStatusBase
     * @static
     * @param {IMsgReceiptStatusBase} message MsgReceiptStatusBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    MsgReceiptStatusBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a MsgReceiptStatusBase message from the specified reader or buffer.
     * @function decode
     * @memberof MsgReceiptStatusBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {MsgReceiptStatusBase} MsgReceiptStatusBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    MsgReceiptStatusBase.decode = function decode(reader, length, error) {
    MsgReceiptStatusBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.MsgReceiptStatusBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.status = reader.int32();
                    break;
                }
            case 2: {
                    message.time = reader.int64();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a MsgReceiptStatusBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof MsgReceiptStatusBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {MsgReceiptStatusBase} MsgReceiptStatusBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    MsgReceiptStatusBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a MsgReceiptStatusBase message.
     * @function verify
     * @memberof MsgReceiptStatusBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    MsgReceiptStatusBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.status != null && message.hasOwnProperty("status"))
            switch (message.status) {
            default:
                return "status: enum value expected";
            case 0:
            case 1:
            case 2:
            case 3:
                break;
            }
        if (message.time != null && message.hasOwnProperty("time"))
            if (!$util.isInteger(message.time) && !(message.time && $util.isInteger(message.time.low) && $util.isInteger(message.time.high)))
                return "time: integer|Long expected";
        return null;
    };

    /**
     * Creates a MsgReceiptStatusBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof MsgReceiptStatusBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {MsgReceiptStatusBase} MsgReceiptStatusBase
     */
    MsgReceiptStatusBase.fromObject = function fromObject(object) {
        if (object instanceof $root.MsgReceiptStatusBase)
            return object;
        let message = new $root.MsgReceiptStatusBase();
        switch (object.status) {
        default:
            if (typeof object.status === "number") {
                message.status = object.status;
                break;
            }
            break;
        case "DELIVERED":
        case 0:
            message.status = 0;
            break;
        case "VIEWED":
        case 1:
            message.status = 1;
            break;
        case "PLAYED":
        case 2:
            message.status = 2;
            break;
        case "PROCESSED":
        case 3:
            message.status = 3;
            break;
        }
        if (object.time != null)
            if ($util.Long)
                (message.time = $util.Long.fromValue(object.time)).unsigned = false;
            else if (typeof object.time === "string")
                message.time = parseInt(object.time, 10);
            else if (typeof object.time === "number")
                message.time = object.time;
            else if (typeof object.time === "object")
                message.time = new $util.LongBits(object.time.low >>> 0, object.time.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a MsgReceiptStatusBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof MsgReceiptStatusBase
     * @static
     * @param {MsgReceiptStatusBase} message MsgReceiptStatusBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    MsgReceiptStatusBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.status = options.enums === String ? "DELIVERED" : 0;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.time = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.time = options.longs === String ? "0" : 0;
        }
        if (message.status != null && message.hasOwnProperty("status"))
            object.status = options.enums === String ? $root.MsgReceiptStatus[message.status] === undefined ? message.status : $root.MsgReceiptStatus[message.status] : message.status;
        if (message.time != null && message.hasOwnProperty("time"))
            if (typeof message.time === "number")
                object.time = options.longs === String ? String(message.time) : message.time;
            else
                object.time = options.longs === String ? $util.Long.prototype.toString.call(message.time) : options.longs === Number ? new $util.LongBits(message.time.low >>> 0, message.time.high >>> 0).toNumber() : message.time;
        return object;
    };

    /**
     * Converts this MsgReceiptStatusBase to JSON.
     * @function toJSON
     * @memberof MsgReceiptStatusBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    MsgReceiptStatusBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for MsgReceiptStatusBase
     * @function getTypeUrl
     * @memberof MsgReceiptStatusBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    MsgReceiptStatusBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/MsgReceiptStatusBase";
    };

    return MsgReceiptStatusBase;
})();

export const MsgReceiptBase = $root.MsgReceiptBase = (() => {

    /**
     * Properties of a MsgReceiptBase.
     * @exports IMsgReceiptBase
     * @interface IMsgReceiptBase
     * @property {IUserBase|null} [targetUser] MsgReceiptBase targetUser
     * @property {Array.<IMsgReceiptStatusBase>|null} [statusList] MsgReceiptBase statusList
     */

    /**
     * Constructs a new MsgReceiptBase.
     * @exports MsgReceiptBase
     * @classdesc Represents a MsgReceiptBase.
     * @implements IMsgReceiptBase
     * @constructor
     * @param {IMsgReceiptBase=} [properties] Properties to set
     */
    function MsgReceiptBase(properties) {
        this.statusList = [];
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * MsgReceiptBase targetUser.
     * @member {IUserBase|null|undefined} targetUser
     * @memberof MsgReceiptBase
     * @instance
     */
    MsgReceiptBase.prototype.targetUser = null;

    /**
     * MsgReceiptBase statusList.
     * @member {Array.<IMsgReceiptStatusBase>} statusList
     * @memberof MsgReceiptBase
     * @instance
     */
    MsgReceiptBase.prototype.statusList = $util.emptyArray;

    /**
     * Creates a new MsgReceiptBase instance using the specified properties.
     * @function create
     * @memberof MsgReceiptBase
     * @static
     * @param {IMsgReceiptBase=} [properties] Properties to set
     * @returns {MsgReceiptBase} MsgReceiptBase instance
     */
    MsgReceiptBase.create = function create(properties) {
        return new MsgReceiptBase(properties);
    };

    /**
     * Encodes the specified MsgReceiptBase message. Does not implicitly {@link MsgReceiptBase.verify|verify} messages.
     * @function encode
     * @memberof MsgReceiptBase
     * @static
     * @param {IMsgReceiptBase} message MsgReceiptBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    MsgReceiptBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.targetUser != null && Object.hasOwnProperty.call(message, "targetUser"))
            $root.UserBase.encode(message.targetUser, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.statusList != null && message.statusList.length)
            for (let i = 0; i < message.statusList.length; ++i)
                $root.MsgReceiptStatusBase.encode(message.statusList[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified MsgReceiptBase message, length delimited. Does not implicitly {@link MsgReceiptBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof MsgReceiptBase
     * @static
     * @param {IMsgReceiptBase} message MsgReceiptBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    MsgReceiptBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a MsgReceiptBase message from the specified reader or buffer.
     * @function decode
     * @memberof MsgReceiptBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {MsgReceiptBase} MsgReceiptBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    MsgReceiptBase.decode = function decode(reader, length, error) {
    MsgReceiptBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.MsgReceiptBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.targetUser = $root.UserBase.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    if (!(message.statusList && message.statusList.length))
                        message.statusList = [];
                    message.statusList.push($root.MsgReceiptStatusBase.decode(reader, reader.uint32()));
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a MsgReceiptBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof MsgReceiptBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {MsgReceiptBase} MsgReceiptBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    MsgReceiptBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a MsgReceiptBase message.
     * @function verify
     * @memberof MsgReceiptBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    MsgReceiptBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.targetUser != null && message.hasOwnProperty("targetUser")) {
            let error = $root.UserBase.verify(message.targetUser);
            if (error)
                return "targetUser." + error;
        }
        if (message.statusList != null && message.hasOwnProperty("statusList")) {
            if (!Array.isArray(message.statusList))
                return "statusList: array expected";
            for (let i = 0; i < message.statusList.length; ++i) {
                let error = $root.MsgReceiptStatusBase.verify(message.statusList[i]);
                if (error)
                    return "statusList." + error;
            }
        }
        return null;
    };

    /**
     * Creates a MsgReceiptBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof MsgReceiptBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {MsgReceiptBase} MsgReceiptBase
     */
    MsgReceiptBase.fromObject = function fromObject(object) {
        if (object instanceof $root.MsgReceiptBase)
            return object;
        let message = new $root.MsgReceiptBase();
        if (object.targetUser != null) {
            if (typeof object.targetUser !== "object")
                throw TypeError(".MsgReceiptBase.targetUser: object expected");
            message.targetUser = $root.UserBase.fromObject(object.targetUser);
        }
        if (object.statusList) {
            if (!Array.isArray(object.statusList))
                throw TypeError(".MsgReceiptBase.statusList: array expected");
            message.statusList = [];
            for (let i = 0; i < object.statusList.length; ++i) {
                if (typeof object.statusList[i] !== "object")
                    throw TypeError(".MsgReceiptBase.statusList: object expected");
                message.statusList[i] = $root.MsgReceiptStatusBase.fromObject(object.statusList[i]);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a MsgReceiptBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof MsgReceiptBase
     * @static
     * @param {MsgReceiptBase} message MsgReceiptBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    MsgReceiptBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.arrays || options.defaults)
            object.statusList = [];
        if (options.defaults)
            object.targetUser = null;
        if (message.targetUser != null && message.hasOwnProperty("targetUser"))
            object.targetUser = $root.UserBase.toObject(message.targetUser, options);
        if (message.statusList && message.statusList.length) {
            object.statusList = [];
            for (let j = 0; j < message.statusList.length; ++j)
                object.statusList[j] = $root.MsgReceiptStatusBase.toObject(message.statusList[j], options);
        }
        return object;
    };

    /**
     * Converts this MsgReceiptBase to JSON.
     * @function toJSON
     * @memberof MsgReceiptBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    MsgReceiptBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for MsgReceiptBase
     * @function getTypeUrl
     * @memberof MsgReceiptBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    MsgReceiptBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/MsgReceiptBase";
    };

    return MsgReceiptBase;
})();

export const UserOnOrOffLine = $root.UserOnOrOffLine = (() => {

    /**
     * Properties of a UserOnOrOffLine.
     * @exports IUserOnOrOffLine
     * @interface IUserOnOrOffLine
     * @property {number|Long|null} [uid] UserOnOrOffLine uid
     * @property {boolean|null} [online] UserOnOrOffLine online
     * @property {number|Long|null} [createTime] UserOnOrOffLine createTime
     * @property {boolean|null} [bfShow] UserOnOrOffLine bfShow
     */

    /**
     * Constructs a new UserOnOrOffLine.
     * @exports UserOnOrOffLine
     * @classdesc Represents a UserOnOrOffLine.
     * @implements IUserOnOrOffLine
     * @constructor
     * @param {IUserOnOrOffLine=} [properties] Properties to set
     */
    function UserOnOrOffLine(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * UserOnOrOffLine uid.
     * @member {number|Long} uid
     * @memberof UserOnOrOffLine
     * @instance
     */
    UserOnOrOffLine.prototype.uid = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * UserOnOrOffLine online.
     * @member {boolean} online
     * @memberof UserOnOrOffLine
     * @instance
     */
    UserOnOrOffLine.prototype.online = false;

    /**
     * UserOnOrOffLine createTime.
     * @member {number|Long} createTime
     * @memberof UserOnOrOffLine
     * @instance
     */
    UserOnOrOffLine.prototype.createTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * UserOnOrOffLine bfShow.
     * @member {boolean} bfShow
     * @memberof UserOnOrOffLine
     * @instance
     */
    UserOnOrOffLine.prototype.bfShow = false;

    /**
     * Creates a new UserOnOrOffLine instance using the specified properties.
     * @function create
     * @memberof UserOnOrOffLine
     * @static
     * @param {IUserOnOrOffLine=} [properties] Properties to set
     * @returns {UserOnOrOffLine} UserOnOrOffLine instance
     */
    UserOnOrOffLine.create = function create(properties) {
        return new UserOnOrOffLine(properties);
    };

    /**
     * Encodes the specified UserOnOrOffLine message. Does not implicitly {@link UserOnOrOffLine.verify|verify} messages.
     * @function encode
     * @memberof UserOnOrOffLine
     * @static
     * @param {IUserOnOrOffLine} message UserOnOrOffLine message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UserOnOrOffLine.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.uid);
        if (message.online != null && Object.hasOwnProperty.call(message, "online"))
            writer.uint32(/* id 2, wireType 0 =*/16).bool(message.online);
        if (message.createTime != null && Object.hasOwnProperty.call(message, "createTime"))
            writer.uint32(/* id 3, wireType 0 =*/24).int64(message.createTime);
        if (message.bfShow != null && Object.hasOwnProperty.call(message, "bfShow"))
            writer.uint32(/* id 4, wireType 0 =*/32).bool(message.bfShow);
        return writer;
    };

    /**
     * Encodes the specified UserOnOrOffLine message, length delimited. Does not implicitly {@link UserOnOrOffLine.verify|verify} messages.
     * @function encodeDelimited
     * @memberof UserOnOrOffLine
     * @static
     * @param {IUserOnOrOffLine} message UserOnOrOffLine message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    UserOnOrOffLine.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a UserOnOrOffLine message from the specified reader or buffer.
     * @function decode
     * @memberof UserOnOrOffLine
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {UserOnOrOffLine} UserOnOrOffLine
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UserOnOrOffLine.decode = function decode(reader, length, error) {
    UserOnOrOffLine.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.UserOnOrOffLine();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.uid = reader.int64();
                    break;
                }
            case 2: {
                    message.online = reader.bool();
                    break;
                }
            case 3: {
                    message.createTime = reader.int64();
                    break;
                }
            case 4: {
                    message.bfShow = reader.bool();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a UserOnOrOffLine message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof UserOnOrOffLine
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {UserOnOrOffLine} UserOnOrOffLine
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    UserOnOrOffLine.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a UserOnOrOffLine message.
     * @function verify
     * @memberof UserOnOrOffLine
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    UserOnOrOffLine.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.uid != null && message.hasOwnProperty("uid"))
            if (!$util.isInteger(message.uid) && !(message.uid && $util.isInteger(message.uid.low) && $util.isInteger(message.uid.high)))
                return "uid: integer|Long expected";
        if (message.online != null && message.hasOwnProperty("online"))
            if (typeof message.online !== "boolean")
                return "online: boolean expected";
        if (message.createTime != null && message.hasOwnProperty("createTime"))
            if (!$util.isInteger(message.createTime) && !(message.createTime && $util.isInteger(message.createTime.low) && $util.isInteger(message.createTime.high)))
                return "createTime: integer|Long expected";
        if (message.bfShow != null && message.hasOwnProperty("bfShow"))
            if (typeof message.bfShow !== "boolean")
                return "bfShow: boolean expected";
        return null;
    };

    /**
     * Creates a UserOnOrOffLine message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof UserOnOrOffLine
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {UserOnOrOffLine} UserOnOrOffLine
     */
    UserOnOrOffLine.fromObject = function fromObject(object) {
        if (object instanceof $root.UserOnOrOffLine)
            return object;
        let message = new $root.UserOnOrOffLine();
        if (object.uid != null)
            if ($util.Long)
                (message.uid = $util.Long.fromValue(object.uid)).unsigned = false;
            else if (typeof object.uid === "string")
                message.uid = parseInt(object.uid, 10);
            else if (typeof object.uid === "number")
                message.uid = object.uid;
            else if (typeof object.uid === "object")
                message.uid = new $util.LongBits(object.uid.low >>> 0, object.uid.high >>> 0).toNumber();
        if (object.online != null)
            message.online = Boolean(object.online);
        if (object.createTime != null)
            if ($util.Long)
                (message.createTime = $util.Long.fromValue(object.createTime)).unsigned = false;
            else if (typeof object.createTime === "string")
                message.createTime = parseInt(object.createTime, 10);
            else if (typeof object.createTime === "number")
                message.createTime = object.createTime;
            else if (typeof object.createTime === "object")
                message.createTime = new $util.LongBits(object.createTime.low >>> 0, object.createTime.high >>> 0).toNumber();
        if (object.bfShow != null)
            message.bfShow = Boolean(object.bfShow);
        return message;
    };

    /**
     * Creates a plain object from a UserOnOrOffLine message. Also converts values to other types if specified.
     * @function toObject
     * @memberof UserOnOrOffLine
     * @static
     * @param {UserOnOrOffLine} message UserOnOrOffLine
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    UserOnOrOffLine.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.uid = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.uid = options.longs === String ? "0" : 0;
            object.online = false;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.createTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.createTime = options.longs === String ? "0" : 0;
            object.bfShow = false;
        }
        if (message.uid != null && message.hasOwnProperty("uid"))
            if (typeof message.uid === "number")
                object.uid = options.longs === String ? String(message.uid) : message.uid;
            else
                object.uid = options.longs === String ? $util.Long.prototype.toString.call(message.uid) : options.longs === Number ? new $util.LongBits(message.uid.low >>> 0, message.uid.high >>> 0).toNumber() : message.uid;
        if (message.online != null && message.hasOwnProperty("online"))
            object.online = message.online;
        if (message.createTime != null && message.hasOwnProperty("createTime"))
            if (typeof message.createTime === "number")
                object.createTime = options.longs === String ? String(message.createTime) : message.createTime;
            else
                object.createTime = options.longs === String ? $util.Long.prototype.toString.call(message.createTime) : options.longs === Number ? new $util.LongBits(message.createTime.low >>> 0, message.createTime.high >>> 0).toNumber() : message.createTime;
        if (message.bfShow != null && message.hasOwnProperty("bfShow"))
            object.bfShow = message.bfShow;
        return object;
    };

    /**
     * Converts this UserOnOrOffLine to JSON.
     * @function toJSON
     * @memberof UserOnOrOffLine
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    UserOnOrOffLine.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for UserOnOrOffLine
     * @function getTypeUrl
     * @memberof UserOnOrOffLine
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    UserOnOrOffLine.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/UserOnOrOffLine";
    };

    return UserOnOrOffLine;
})();

export const KeyPairBase = $root.KeyPairBase = (() => {

    /**
     * Properties of a KeyPairBase.
     * @exports IKeyPairBase
     * @interface IKeyPairBase
     * @property {string|null} [publicKey] KeyPairBase publicKey
     * @property {string|null} [privateKey] KeyPairBase privateKey
     * @property {string|null} [msgKey] KeyPairBase msgKey
     * @property {number|null} [keyVersion] KeyPairBase keyVersion
     */

    /**
     * Constructs a new KeyPairBase.
     * @exports KeyPairBase
     * @classdesc Represents a KeyPairBase.
     * @implements IKeyPairBase
     * @constructor
     * @param {IKeyPairBase=} [properties] Properties to set
     */
    function KeyPairBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * KeyPairBase publicKey.
     * @member {string} publicKey
     * @memberof KeyPairBase
     * @instance
     */
    KeyPairBase.prototype.publicKey = "";

    /**
     * KeyPairBase privateKey.
     * @member {string} privateKey
     * @memberof KeyPairBase
     * @instance
     */
    KeyPairBase.prototype.privateKey = "";

    /**
     * KeyPairBase msgKey.
     * @member {string} msgKey
     * @memberof KeyPairBase
     * @instance
     */
    KeyPairBase.prototype.msgKey = "";

    /**
     * KeyPairBase keyVersion.
     * @member {number} keyVersion
     * @memberof KeyPairBase
     * @instance
     */
    KeyPairBase.prototype.keyVersion = 0;

    /**
     * Creates a new KeyPairBase instance using the specified properties.
     * @function create
     * @memberof KeyPairBase
     * @static
     * @param {IKeyPairBase=} [properties] Properties to set
     * @returns {KeyPairBase} KeyPairBase instance
     */
    KeyPairBase.create = function create(properties) {
        return new KeyPairBase(properties);
    };

    /**
     * Encodes the specified KeyPairBase message. Does not implicitly {@link KeyPairBase.verify|verify} messages.
     * @function encode
     * @memberof KeyPairBase
     * @static
     * @param {IKeyPairBase} message KeyPairBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    KeyPairBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.publicKey);
        if (message.privateKey != null && Object.hasOwnProperty.call(message, "privateKey"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.privateKey);
        if (message.msgKey != null && Object.hasOwnProperty.call(message, "msgKey"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.msgKey);
        if (message.keyVersion != null && Object.hasOwnProperty.call(message, "keyVersion"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.keyVersion);
        return writer;
    };

    /**
     * Encodes the specified KeyPairBase message, length delimited. Does not implicitly {@link KeyPairBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof KeyPairBase
     * @static
     * @param {IKeyPairBase} message KeyPairBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    KeyPairBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a KeyPairBase message from the specified reader or buffer.
     * @function decode
     * @memberof KeyPairBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {KeyPairBase} KeyPairBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    KeyPairBase.decode = function decode(reader, length, error) {
    KeyPairBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.KeyPairBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.publicKey = reader.string();
                    break;
                }
            case 2: {
                    message.privateKey = reader.string();
                    break;
                }
            case 3: {
                    message.msgKey = reader.string();
                    break;
                }
            case 4: {
                    message.keyVersion = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a KeyPairBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof KeyPairBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {KeyPairBase} KeyPairBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    KeyPairBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a KeyPairBase message.
     * @function verify
     * @memberof KeyPairBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    KeyPairBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.publicKey != null && message.hasOwnProperty("publicKey"))
            if (!$util.isString(message.publicKey))
                return "publicKey: string expected";
        if (message.privateKey != null && message.hasOwnProperty("privateKey"))
            if (!$util.isString(message.privateKey))
                return "privateKey: string expected";
        if (message.msgKey != null && message.hasOwnProperty("msgKey"))
            if (!$util.isString(message.msgKey))
                return "msgKey: string expected";
        if (message.keyVersion != null && message.hasOwnProperty("keyVersion"))
            if (!$util.isInteger(message.keyVersion))
                return "keyVersion: integer expected";
        return null;
    };

    /**
     * Creates a KeyPairBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof KeyPairBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {KeyPairBase} KeyPairBase
     */
    KeyPairBase.fromObject = function fromObject(object) {
        if (object instanceof $root.KeyPairBase)
            return object;
        let message = new $root.KeyPairBase();
        if (object.publicKey != null)
            message.publicKey = String(object.publicKey);
        if (object.privateKey != null)
            message.privateKey = String(object.privateKey);
        if (object.msgKey != null)
            message.msgKey = String(object.msgKey);
        if (object.keyVersion != null)
            message.keyVersion = object.keyVersion | 0;
        return message;
    };

    /**
     * Creates a plain object from a KeyPairBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof KeyPairBase
     * @static
     * @param {KeyPairBase} message KeyPairBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    KeyPairBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.publicKey = "";
            object.privateKey = "";
            object.msgKey = "";
            object.keyVersion = 0;
        }
        if (message.publicKey != null && message.hasOwnProperty("publicKey"))
            object.publicKey = message.publicKey;
        if (message.privateKey != null && message.hasOwnProperty("privateKey"))
            object.privateKey = message.privateKey;
        if (message.msgKey != null && message.hasOwnProperty("msgKey"))
            object.msgKey = message.msgKey;
        if (message.keyVersion != null && message.hasOwnProperty("keyVersion"))
            object.keyVersion = message.keyVersion;
        return object;
    };

    /**
     * Converts this KeyPairBase to JSON.
     * @function toJSON
     * @memberof KeyPairBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    KeyPairBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for KeyPairBase
     * @function getTypeUrl
     * @memberof KeyPairBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    KeyPairBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/KeyPairBase";
    };

    return KeyPairBase;
})();

export const AdminRightBase = $root.AdminRightBase = (() => {

    /**
     * Properties of an AdminRightBase.
     * @exports IAdminRightBase
     * @interface IAdminRightBase
     * @property {boolean|null} [bfUpdateData] AdminRightBase bfUpdateData
     * @property {boolean|null} [bfJoinCheck] AdminRightBase bfJoinCheck
     * @property {boolean|null} [bfPushNotice] AdminRightBase bfPushNotice
     * @property {boolean|null} [bfSetAdmin] AdminRightBase bfSetAdmin
     * @property {boolean|null} [bfResetQrcode] AdminRightBase bfResetQrcode
     * @property {boolean|null} [bfSetJoinNotice] AdminRightBase bfSetJoinNotice
     */

    /**
     * Constructs a new AdminRightBase.
     * @exports AdminRightBase
     * @classdesc Represents an AdminRightBase.
     * @implements IAdminRightBase
     * @constructor
     * @param {IAdminRightBase=} [properties] Properties to set
     */
    function AdminRightBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * AdminRightBase bfUpdateData.
     * @member {boolean} bfUpdateData
     * @memberof AdminRightBase
     * @instance
     */
    AdminRightBase.prototype.bfUpdateData = false;

    /**
     * AdminRightBase bfJoinCheck.
     * @member {boolean} bfJoinCheck
     * @memberof AdminRightBase
     * @instance
     */
    AdminRightBase.prototype.bfJoinCheck = false;

    /**
     * AdminRightBase bfPushNotice.
     * @member {boolean} bfPushNotice
     * @memberof AdminRightBase
     * @instance
     */
    AdminRightBase.prototype.bfPushNotice = false;

    /**
     * AdminRightBase bfSetAdmin.
     * @member {boolean} bfSetAdmin
     * @memberof AdminRightBase
     * @instance
     */
    AdminRightBase.prototype.bfSetAdmin = false;

    /**
     * AdminRightBase bfResetQrcode.
     * @member {boolean} bfResetQrcode
     * @memberof AdminRightBase
     * @instance
     */
    AdminRightBase.prototype.bfResetQrcode = false;

    /**
     * AdminRightBase bfSetJoinNotice.
     * @member {boolean} bfSetJoinNotice
     * @memberof AdminRightBase
     * @instance
     */
    AdminRightBase.prototype.bfSetJoinNotice = false;

    /**
     * Creates a new AdminRightBase instance using the specified properties.
     * @function create
     * @memberof AdminRightBase
     * @static
     * @param {IAdminRightBase=} [properties] Properties to set
     * @returns {AdminRightBase} AdminRightBase instance
     */
    AdminRightBase.create = function create(properties) {
        return new AdminRightBase(properties);
    };

    /**
     * Encodes the specified AdminRightBase message. Does not implicitly {@link AdminRightBase.verify|verify} messages.
     * @function encode
     * @memberof AdminRightBase
     * @static
     * @param {IAdminRightBase} message AdminRightBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AdminRightBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.bfUpdateData != null && Object.hasOwnProperty.call(message, "bfUpdateData"))
            writer.uint32(/* id 1, wireType 0 =*/8).bool(message.bfUpdateData);
        if (message.bfJoinCheck != null && Object.hasOwnProperty.call(message, "bfJoinCheck"))
            writer.uint32(/* id 2, wireType 0 =*/16).bool(message.bfJoinCheck);
        if (message.bfPushNotice != null && Object.hasOwnProperty.call(message, "bfPushNotice"))
            writer.uint32(/* id 3, wireType 0 =*/24).bool(message.bfPushNotice);
        if (message.bfSetAdmin != null && Object.hasOwnProperty.call(message, "bfSetAdmin"))
            writer.uint32(/* id 4, wireType 0 =*/32).bool(message.bfSetAdmin);
        if (message.bfResetQrcode != null && Object.hasOwnProperty.call(message, "bfResetQrcode"))
            writer.uint32(/* id 5, wireType 0 =*/40).bool(message.bfResetQrcode);
        if (message.bfSetJoinNotice != null && Object.hasOwnProperty.call(message, "bfSetJoinNotice"))
            writer.uint32(/* id 6, wireType 0 =*/48).bool(message.bfSetJoinNotice);
        return writer;
    };

    /**
     * Encodes the specified AdminRightBase message, length delimited. Does not implicitly {@link AdminRightBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof AdminRightBase
     * @static
     * @param {IAdminRightBase} message AdminRightBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AdminRightBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an AdminRightBase message from the specified reader or buffer.
     * @function decode
     * @memberof AdminRightBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {AdminRightBase} AdminRightBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AdminRightBase.decode = function decode(reader, length, error) {
    AdminRightBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.AdminRightBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.bfUpdateData = reader.bool();
                    break;
                }
            case 2: {
                    message.bfJoinCheck = reader.bool();
                    break;
                }
            case 3: {
                    message.bfPushNotice = reader.bool();
                    break;
                }
            case 4: {
                    message.bfSetAdmin = reader.bool();
                    break;
                }
            case 5: {
                    message.bfResetQrcode = reader.bool();
                    break;
                }
            case 6: {
                    message.bfSetJoinNotice = reader.bool();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an AdminRightBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof AdminRightBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {AdminRightBase} AdminRightBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AdminRightBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an AdminRightBase message.
     * @function verify
     * @memberof AdminRightBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    AdminRightBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.bfUpdateData != null && message.hasOwnProperty("bfUpdateData"))
            if (typeof message.bfUpdateData !== "boolean")
                return "bfUpdateData: boolean expected";
        if (message.bfJoinCheck != null && message.hasOwnProperty("bfJoinCheck"))
            if (typeof message.bfJoinCheck !== "boolean")
                return "bfJoinCheck: boolean expected";
        if (message.bfPushNotice != null && message.hasOwnProperty("bfPushNotice"))
            if (typeof message.bfPushNotice !== "boolean")
                return "bfPushNotice: boolean expected";
        if (message.bfSetAdmin != null && message.hasOwnProperty("bfSetAdmin"))
            if (typeof message.bfSetAdmin !== "boolean")
                return "bfSetAdmin: boolean expected";
        if (message.bfResetQrcode != null && message.hasOwnProperty("bfResetQrcode"))
            if (typeof message.bfResetQrcode !== "boolean")
                return "bfResetQrcode: boolean expected";
        if (message.bfSetJoinNotice != null && message.hasOwnProperty("bfSetJoinNotice"))
            if (typeof message.bfSetJoinNotice !== "boolean")
                return "bfSetJoinNotice: boolean expected";
        return null;
    };

    /**
     * Creates an AdminRightBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof AdminRightBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {AdminRightBase} AdminRightBase
     */
    AdminRightBase.fromObject = function fromObject(object) {
        if (object instanceof $root.AdminRightBase)
            return object;
        let message = new $root.AdminRightBase();
        if (object.bfUpdateData != null)
            message.bfUpdateData = Boolean(object.bfUpdateData);
        if (object.bfJoinCheck != null)
            message.bfJoinCheck = Boolean(object.bfJoinCheck);
        if (object.bfPushNotice != null)
            message.bfPushNotice = Boolean(object.bfPushNotice);
        if (object.bfSetAdmin != null)
            message.bfSetAdmin = Boolean(object.bfSetAdmin);
        if (object.bfResetQrcode != null)
            message.bfResetQrcode = Boolean(object.bfResetQrcode);
        if (object.bfSetJoinNotice != null)
            message.bfSetJoinNotice = Boolean(object.bfSetJoinNotice);
        return message;
    };

    /**
     * Creates a plain object from an AdminRightBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof AdminRightBase
     * @static
     * @param {AdminRightBase} message AdminRightBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    AdminRightBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.bfUpdateData = false;
            object.bfJoinCheck = false;
            object.bfPushNotice = false;
            object.bfSetAdmin = false;
            object.bfResetQrcode = false;
            object.bfSetJoinNotice = false;
        }
        if (message.bfUpdateData != null && message.hasOwnProperty("bfUpdateData"))
            object.bfUpdateData = message.bfUpdateData;
        if (message.bfJoinCheck != null && message.hasOwnProperty("bfJoinCheck"))
            object.bfJoinCheck = message.bfJoinCheck;
        if (message.bfPushNotice != null && message.hasOwnProperty("bfPushNotice"))
            object.bfPushNotice = message.bfPushNotice;
        if (message.bfSetAdmin != null && message.hasOwnProperty("bfSetAdmin"))
            object.bfSetAdmin = message.bfSetAdmin;
        if (message.bfResetQrcode != null && message.hasOwnProperty("bfResetQrcode"))
            object.bfResetQrcode = message.bfResetQrcode;
        if (message.bfSetJoinNotice != null && message.hasOwnProperty("bfSetJoinNotice"))
            object.bfSetJoinNotice = message.bfSetJoinNotice;
        return object;
    };

    /**
     * Converts this AdminRightBase to JSON.
     * @function toJSON
     * @memberof AdminRightBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    AdminRightBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for AdminRightBase
     * @function getTypeUrl
     * @memberof AdminRightBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    AdminRightBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/AdminRightBase";
    };

    return AdminRightBase;
})();

export const GroupNoticeBase = $root.GroupNoticeBase = (() => {

    /**
     * Properties of a GroupNoticeBase.
     * @exports IGroupNoticeBase
     * @interface IGroupNoticeBase
     * @property {IGroupMemberBase|null} [editUser] GroupNoticeBase editUser
     * @property {string|null} [notice] GroupNoticeBase notice
     * @property {number|Long|null} [releaseTime] GroupNoticeBase releaseTime
     * @property {number|Long|null} [noticeId] GroupNoticeBase noticeId
     * @property {boolean|null} [bfRemind] GroupNoticeBase bfRemind
     */

    /**
     * Constructs a new GroupNoticeBase.
     * @exports GroupNoticeBase
     * @classdesc Represents a GroupNoticeBase.
     * @implements IGroupNoticeBase
     * @constructor
     * @param {IGroupNoticeBase=} [properties] Properties to set
     */
    function GroupNoticeBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * GroupNoticeBase editUser.
     * @member {IGroupMemberBase|null|undefined} editUser
     * @memberof GroupNoticeBase
     * @instance
     */
    GroupNoticeBase.prototype.editUser = null;

    /**
     * GroupNoticeBase notice.
     * @member {string} notice
     * @memberof GroupNoticeBase
     * @instance
     */
    GroupNoticeBase.prototype.notice = "";

    /**
     * GroupNoticeBase releaseTime.
     * @member {number|Long} releaseTime
     * @memberof GroupNoticeBase
     * @instance
     */
    GroupNoticeBase.prototype.releaseTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * GroupNoticeBase noticeId.
     * @member {number|Long} noticeId
     * @memberof GroupNoticeBase
     * @instance
     */
    GroupNoticeBase.prototype.noticeId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * GroupNoticeBase bfRemind.
     * @member {boolean} bfRemind
     * @memberof GroupNoticeBase
     * @instance
     */
    GroupNoticeBase.prototype.bfRemind = false;

    /**
     * Creates a new GroupNoticeBase instance using the specified properties.
     * @function create
     * @memberof GroupNoticeBase
     * @static
     * @param {IGroupNoticeBase=} [properties] Properties to set
     * @returns {GroupNoticeBase} GroupNoticeBase instance
     */
    GroupNoticeBase.create = function create(properties) {
        return new GroupNoticeBase(properties);
    };

    /**
     * Encodes the specified GroupNoticeBase message. Does not implicitly {@link GroupNoticeBase.verify|verify} messages.
     * @function encode
     * @memberof GroupNoticeBase
     * @static
     * @param {IGroupNoticeBase} message GroupNoticeBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GroupNoticeBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.editUser != null && Object.hasOwnProperty.call(message, "editUser"))
            $root.GroupMemberBase.encode(message.editUser, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
        if (message.notice != null && Object.hasOwnProperty.call(message, "notice"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.notice);
        if (message.releaseTime != null && Object.hasOwnProperty.call(message, "releaseTime"))
            writer.uint32(/* id 3, wireType 0 =*/24).int64(message.releaseTime);
        if (message.noticeId != null && Object.hasOwnProperty.call(message, "noticeId"))
            writer.uint32(/* id 4, wireType 0 =*/32).int64(message.noticeId);
        if (message.bfRemind != null && Object.hasOwnProperty.call(message, "bfRemind"))
            writer.uint32(/* id 5, wireType 0 =*/40).bool(message.bfRemind);
        return writer;
    };

    /**
     * Encodes the specified GroupNoticeBase message, length delimited. Does not implicitly {@link GroupNoticeBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof GroupNoticeBase
     * @static
     * @param {IGroupNoticeBase} message GroupNoticeBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GroupNoticeBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a GroupNoticeBase message from the specified reader or buffer.
     * @function decode
     * @memberof GroupNoticeBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {GroupNoticeBase} GroupNoticeBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GroupNoticeBase.decode = function decode(reader, length, error) {
    GroupNoticeBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.GroupNoticeBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.editUser = $root.GroupMemberBase.decode(reader, reader.uint32());
                    break;
                }
            case 2: {
                    message.notice = reader.string();
                    break;
                }
            case 3: {
                    message.releaseTime = reader.int64();
                    break;
                }
            case 4: {
                    message.noticeId = reader.int64();
                    break;
                }
            case 5: {
                    message.bfRemind = reader.bool();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a GroupNoticeBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof GroupNoticeBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {GroupNoticeBase} GroupNoticeBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GroupNoticeBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GroupNoticeBase message.
     * @function verify
     * @memberof GroupNoticeBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GroupNoticeBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.editUser != null && message.hasOwnProperty("editUser")) {
            let error = $root.GroupMemberBase.verify(message.editUser);
            if (error)
                return "editUser." + error;
        }
        if (message.notice != null && message.hasOwnProperty("notice"))
            if (!$util.isString(message.notice))
                return "notice: string expected";
        if (message.releaseTime != null && message.hasOwnProperty("releaseTime"))
            if (!$util.isInteger(message.releaseTime) && !(message.releaseTime && $util.isInteger(message.releaseTime.low) && $util.isInteger(message.releaseTime.high)))
                return "releaseTime: integer|Long expected";
        if (message.noticeId != null && message.hasOwnProperty("noticeId"))
            if (!$util.isInteger(message.noticeId) && !(message.noticeId && $util.isInteger(message.noticeId.low) && $util.isInteger(message.noticeId.high)))
                return "noticeId: integer|Long expected";
        if (message.bfRemind != null && message.hasOwnProperty("bfRemind"))
            if (typeof message.bfRemind !== "boolean")
                return "bfRemind: boolean expected";
        return null;
    };

    /**
     * Creates a GroupNoticeBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof GroupNoticeBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {GroupNoticeBase} GroupNoticeBase
     */
    GroupNoticeBase.fromObject = function fromObject(object) {
        if (object instanceof $root.GroupNoticeBase)
            return object;
        let message = new $root.GroupNoticeBase();
        if (object.editUser != null) {
            if (typeof object.editUser !== "object")
                throw TypeError(".GroupNoticeBase.editUser: object expected");
            message.editUser = $root.GroupMemberBase.fromObject(object.editUser);
        }
        if (object.notice != null)
            message.notice = String(object.notice);
        if (object.releaseTime != null)
            if ($util.Long)
                (message.releaseTime = $util.Long.fromValue(object.releaseTime)).unsigned = false;
            else if (typeof object.releaseTime === "string")
                message.releaseTime = parseInt(object.releaseTime, 10);
            else if (typeof object.releaseTime === "number")
                message.releaseTime = object.releaseTime;
            else if (typeof object.releaseTime === "object")
                message.releaseTime = new $util.LongBits(object.releaseTime.low >>> 0, object.releaseTime.high >>> 0).toNumber();
        if (object.noticeId != null)
            if ($util.Long)
                (message.noticeId = $util.Long.fromValue(object.noticeId)).unsigned = false;
            else if (typeof object.noticeId === "string")
                message.noticeId = parseInt(object.noticeId, 10);
            else if (typeof object.noticeId === "number")
                message.noticeId = object.noticeId;
            else if (typeof object.noticeId === "object")
                message.noticeId = new $util.LongBits(object.noticeId.low >>> 0, object.noticeId.high >>> 0).toNumber();
        if (object.bfRemind != null)
            message.bfRemind = Boolean(object.bfRemind);
        return message;
    };

    /**
     * Creates a plain object from a GroupNoticeBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof GroupNoticeBase
     * @static
     * @param {GroupNoticeBase} message GroupNoticeBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GroupNoticeBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.editUser = null;
            object.notice = "";
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.releaseTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.releaseTime = options.longs === String ? "0" : 0;
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.noticeId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.noticeId = options.longs === String ? "0" : 0;
            object.bfRemind = false;
        }
        if (message.editUser != null && message.hasOwnProperty("editUser"))
            object.editUser = $root.GroupMemberBase.toObject(message.editUser, options);
        if (message.notice != null && message.hasOwnProperty("notice"))
            object.notice = message.notice;
        if (message.releaseTime != null && message.hasOwnProperty("releaseTime"))
            if (typeof message.releaseTime === "number")
                object.releaseTime = options.longs === String ? String(message.releaseTime) : message.releaseTime;
            else
                object.releaseTime = options.longs === String ? $util.Long.prototype.toString.call(message.releaseTime) : options.longs === Number ? new $util.LongBits(message.releaseTime.low >>> 0, message.releaseTime.high >>> 0).toNumber() : message.releaseTime;
        if (message.noticeId != null && message.hasOwnProperty("noticeId"))
            if (typeof message.noticeId === "number")
                object.noticeId = options.longs === String ? String(message.noticeId) : message.noticeId;
            else
                object.noticeId = options.longs === String ? $util.Long.prototype.toString.call(message.noticeId) : options.longs === Number ? new $util.LongBits(message.noticeId.low >>> 0, message.noticeId.high >>> 0).toNumber() : message.noticeId;
        if (message.bfRemind != null && message.hasOwnProperty("bfRemind"))
            object.bfRemind = message.bfRemind;
        return object;
    };

    /**
     * Converts this GroupNoticeBase to JSON.
     * @function toJSON
     * @memberof GroupNoticeBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GroupNoticeBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GroupNoticeBase
     * @function getTypeUrl
     * @memberof GroupNoticeBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GroupNoticeBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/GroupNoticeBase";
    };

    return GroupNoticeBase;
})();

export const EmoticonBase = $root.EmoticonBase = (() => {

    /**
     * Properties of an EmoticonBase.
     * @exports IEmoticonBase
     * @interface IEmoticonBase
     * @property {number|Long|null} [emoticonId] EmoticonBase emoticonId
     * @property {string|null} [emoticonUrl] EmoticonBase emoticonUrl
     * @property {number|null} [height] EmoticonBase height
     * @property {number|null} [width] EmoticonBase width
     */

    /**
     * Constructs a new EmoticonBase.
     * @exports EmoticonBase
     * @classdesc Represents an EmoticonBase.
     * @implements IEmoticonBase
     * @constructor
     * @param {IEmoticonBase=} [properties] Properties to set
     */
    function EmoticonBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * EmoticonBase emoticonId.
     * @member {number|Long} emoticonId
     * @memberof EmoticonBase
     * @instance
     */
    EmoticonBase.prototype.emoticonId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * EmoticonBase emoticonUrl.
     * @member {string} emoticonUrl
     * @memberof EmoticonBase
     * @instance
     */
    EmoticonBase.prototype.emoticonUrl = "";

    /**
     * EmoticonBase height.
     * @member {number} height
     * @memberof EmoticonBase
     * @instance
     */
    EmoticonBase.prototype.height = 0;

    /**
     * EmoticonBase width.
     * @member {number} width
     * @memberof EmoticonBase
     * @instance
     */
    EmoticonBase.prototype.width = 0;

    /**
     * Creates a new EmoticonBase instance using the specified properties.
     * @function create
     * @memberof EmoticonBase
     * @static
     * @param {IEmoticonBase=} [properties] Properties to set
     * @returns {EmoticonBase} EmoticonBase instance
     */
    EmoticonBase.create = function create(properties) {
        return new EmoticonBase(properties);
    };

    /**
     * Encodes the specified EmoticonBase message. Does not implicitly {@link EmoticonBase.verify|verify} messages.
     * @function encode
     * @memberof EmoticonBase
     * @static
     * @param {IEmoticonBase} message EmoticonBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    EmoticonBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.emoticonId != null && Object.hasOwnProperty.call(message, "emoticonId"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.emoticonId);
        if (message.emoticonUrl != null && Object.hasOwnProperty.call(message, "emoticonUrl"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.emoticonUrl);
        if (message.height != null && Object.hasOwnProperty.call(message, "height"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.height);
        if (message.width != null && Object.hasOwnProperty.call(message, "width"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.width);
        return writer;
    };

    /**
     * Encodes the specified EmoticonBase message, length delimited. Does not implicitly {@link EmoticonBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof EmoticonBase
     * @static
     * @param {IEmoticonBase} message EmoticonBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    EmoticonBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an EmoticonBase message from the specified reader or buffer.
     * @function decode
     * @memberof EmoticonBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {EmoticonBase} EmoticonBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    EmoticonBase.decode = function decode(reader, length, error) {
    EmoticonBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.EmoticonBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.emoticonId = reader.int64();
                    break;
                }
            case 2: {
                    message.emoticonUrl = reader.string();
                    break;
                }
            case 3: {
                    message.height = reader.int32();
                    break;
                }
            case 4: {
                    message.width = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an EmoticonBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof EmoticonBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {EmoticonBase} EmoticonBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    EmoticonBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an EmoticonBase message.
     * @function verify
     * @memberof EmoticonBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    EmoticonBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.emoticonId != null && message.hasOwnProperty("emoticonId"))
            if (!$util.isInteger(message.emoticonId) && !(message.emoticonId && $util.isInteger(message.emoticonId.low) && $util.isInteger(message.emoticonId.high)))
                return "emoticonId: integer|Long expected";
        if (message.emoticonUrl != null && message.hasOwnProperty("emoticonUrl"))
            if (!$util.isString(message.emoticonUrl))
                return "emoticonUrl: string expected";
        if (message.height != null && message.hasOwnProperty("height"))
            if (!$util.isInteger(message.height))
                return "height: integer expected";
        if (message.width != null && message.hasOwnProperty("width"))
            if (!$util.isInteger(message.width))
                return "width: integer expected";
        return null;
    };

    /**
     * Creates an EmoticonBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof EmoticonBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {EmoticonBase} EmoticonBase
     */
    EmoticonBase.fromObject = function fromObject(object) {
        if (object instanceof $root.EmoticonBase)
            return object;
        let message = new $root.EmoticonBase();
        if (object.emoticonId != null)
            if ($util.Long)
                (message.emoticonId = $util.Long.fromValue(object.emoticonId)).unsigned = false;
            else if (typeof object.emoticonId === "string")
                message.emoticonId = parseInt(object.emoticonId, 10);
            else if (typeof object.emoticonId === "number")
                message.emoticonId = object.emoticonId;
            else if (typeof object.emoticonId === "object")
                message.emoticonId = new $util.LongBits(object.emoticonId.low >>> 0, object.emoticonId.high >>> 0).toNumber();
        if (object.emoticonUrl != null)
            message.emoticonUrl = String(object.emoticonUrl);
        if (object.height != null)
            message.height = object.height | 0;
        if (object.width != null)
            message.width = object.width | 0;
        return message;
    };

    /**
     * Creates a plain object from an EmoticonBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof EmoticonBase
     * @static
     * @param {EmoticonBase} message EmoticonBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    EmoticonBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.emoticonId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.emoticonId = options.longs === String ? "0" : 0;
            object.emoticonUrl = "";
            object.height = 0;
            object.width = 0;
        }
        if (message.emoticonId != null && message.hasOwnProperty("emoticonId"))
            if (typeof message.emoticonId === "number")
                object.emoticonId = options.longs === String ? String(message.emoticonId) : message.emoticonId;
            else
                object.emoticonId = options.longs === String ? $util.Long.prototype.toString.call(message.emoticonId) : options.longs === Number ? new $util.LongBits(message.emoticonId.low >>> 0, message.emoticonId.high >>> 0).toNumber() : message.emoticonId;
        if (message.emoticonUrl != null && message.hasOwnProperty("emoticonUrl"))
            object.emoticonUrl = message.emoticonUrl;
        if (message.height != null && message.hasOwnProperty("height"))
            object.height = message.height;
        if (message.width != null && message.hasOwnProperty("width"))
            object.width = message.width;
        return object;
    };

    /**
     * Converts this EmoticonBase to JSON.
     * @function toJSON
     * @memberof EmoticonBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    EmoticonBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for EmoticonBase
     * @function getTypeUrl
     * @memberof EmoticonBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    EmoticonBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/EmoticonBase";
    };

    return EmoticonBase;
})();

export const CoinLinkBase = $root.CoinLinkBase = (() => {

    /**
     * Properties of a CoinLinkBase.
     * @exports ICoinLinkBase
     * @interface ICoinLinkBase
     * @property {number|null} [coinId] CoinLinkBase coinId
     * @property {string|null} [linkName] CoinLinkBase linkName
     * @property {string|null} [minCash] CoinLinkBase minCash
     * @property {string|null} [maxCash] CoinLinkBase maxCash
     * @property {boolean|null} [isOpenCash] CoinLinkBase isOpenCash
     * @property {boolean|null} [isOpenRecharge] CoinLinkBase isOpenRecharge
     * @property {string|null} [minRecharge] CoinLinkBase minRecharge
     * @property {number|null} [tagType] CoinLinkBase tagType
     * @property {string|null} [tagName] CoinLinkBase tagName
     * @property {string|null} [blockConfirm] CoinLinkBase blockConfirm
     * @property {string|null} [cashTotal] CoinLinkBase cashTotal
     * @property {string|null} [feeCoin] CoinLinkBase feeCoin
     * @property {number|null} [showDecimal] CoinLinkBase showDecimal
     */

    /**
     * Constructs a new CoinLinkBase.
     * @exports CoinLinkBase
     * @classdesc Represents a CoinLinkBase.
     * @implements ICoinLinkBase
     * @constructor
     * @param {ICoinLinkBase=} [properties] Properties to set
     */
    function CoinLinkBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CoinLinkBase coinId.
     * @member {number} coinId
     * @memberof CoinLinkBase
     * @instance
     */
    CoinLinkBase.prototype.coinId = 0;

    /**
     * CoinLinkBase linkName.
     * @member {string} linkName
     * @memberof CoinLinkBase
     * @instance
     */
    CoinLinkBase.prototype.linkName = "";

    /**
     * CoinLinkBase minCash.
     * @member {string} minCash
     * @memberof CoinLinkBase
     * @instance
     */
    CoinLinkBase.prototype.minCash = "";

    /**
     * CoinLinkBase maxCash.
     * @member {string} maxCash
     * @memberof CoinLinkBase
     * @instance
     */
    CoinLinkBase.prototype.maxCash = "";

    /**
     * CoinLinkBase isOpenCash.
     * @member {boolean} isOpenCash
     * @memberof CoinLinkBase
     * @instance
     */
    CoinLinkBase.prototype.isOpenCash = false;

    /**
     * CoinLinkBase isOpenRecharge.
     * @member {boolean} isOpenRecharge
     * @memberof CoinLinkBase
     * @instance
     */
    CoinLinkBase.prototype.isOpenRecharge = false;

    /**
     * CoinLinkBase minRecharge.
     * @member {string} minRecharge
     * @memberof CoinLinkBase
     * @instance
     */
    CoinLinkBase.prototype.minRecharge = "";

    /**
     * CoinLinkBase tagType.
     * @member {number} tagType
     * @memberof CoinLinkBase
     * @instance
     */
    CoinLinkBase.prototype.tagType = 0;

    /**
     * CoinLinkBase tagName.
     * @member {string} tagName
     * @memberof CoinLinkBase
     * @instance
     */
    CoinLinkBase.prototype.tagName = "";

    /**
     * CoinLinkBase blockConfirm.
     * @member {string} blockConfirm
     * @memberof CoinLinkBase
     * @instance
     */
    CoinLinkBase.prototype.blockConfirm = "";

    /**
     * CoinLinkBase cashTotal.
     * @member {string} cashTotal
     * @memberof CoinLinkBase
     * @instance
     */
    CoinLinkBase.prototype.cashTotal = "";

    /**
     * CoinLinkBase feeCoin.
     * @member {string} feeCoin
     * @memberof CoinLinkBase
     * @instance
     */
    CoinLinkBase.prototype.feeCoin = "";

    /**
     * CoinLinkBase showDecimal.
     * @member {number} showDecimal
     * @memberof CoinLinkBase
     * @instance
     */
    CoinLinkBase.prototype.showDecimal = 0;

    /**
     * Creates a new CoinLinkBase instance using the specified properties.
     * @function create
     * @memberof CoinLinkBase
     * @static
     * @param {ICoinLinkBase=} [properties] Properties to set
     * @returns {CoinLinkBase} CoinLinkBase instance
     */
    CoinLinkBase.create = function create(properties) {
        return new CoinLinkBase(properties);
    };

    /**
     * Encodes the specified CoinLinkBase message. Does not implicitly {@link CoinLinkBase.verify|verify} messages.
     * @function encode
     * @memberof CoinLinkBase
     * @static
     * @param {ICoinLinkBase} message CoinLinkBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CoinLinkBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.coinId != null && Object.hasOwnProperty.call(message, "coinId"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.coinId);
        if (message.linkName != null && Object.hasOwnProperty.call(message, "linkName"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.linkName);
        if (message.minCash != null && Object.hasOwnProperty.call(message, "minCash"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.minCash);
        if (message.maxCash != null && Object.hasOwnProperty.call(message, "maxCash"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.maxCash);
        if (message.isOpenCash != null && Object.hasOwnProperty.call(message, "isOpenCash"))
            writer.uint32(/* id 5, wireType 0 =*/40).bool(message.isOpenCash);
        if (message.isOpenRecharge != null && Object.hasOwnProperty.call(message, "isOpenRecharge"))
            writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isOpenRecharge);
        if (message.minRecharge != null && Object.hasOwnProperty.call(message, "minRecharge"))
            writer.uint32(/* id 7, wireType 2 =*/58).string(message.minRecharge);
        if (message.tagType != null && Object.hasOwnProperty.call(message, "tagType"))
            writer.uint32(/* id 8, wireType 0 =*/64).int32(message.tagType);
        if (message.tagName != null && Object.hasOwnProperty.call(message, "tagName"))
            writer.uint32(/* id 9, wireType 2 =*/74).string(message.tagName);
        if (message.blockConfirm != null && Object.hasOwnProperty.call(message, "blockConfirm"))
            writer.uint32(/* id 10, wireType 2 =*/82).string(message.blockConfirm);
        if (message.cashTotal != null && Object.hasOwnProperty.call(message, "cashTotal"))
            writer.uint32(/* id 11, wireType 2 =*/90).string(message.cashTotal);
        if (message.feeCoin != null && Object.hasOwnProperty.call(message, "feeCoin"))
            writer.uint32(/* id 12, wireType 2 =*/98).string(message.feeCoin);
        if (message.showDecimal != null && Object.hasOwnProperty.call(message, "showDecimal"))
            writer.uint32(/* id 13, wireType 0 =*/104).int32(message.showDecimal);
        return writer;
    };

    /**
     * Encodes the specified CoinLinkBase message, length delimited. Does not implicitly {@link CoinLinkBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CoinLinkBase
     * @static
     * @param {ICoinLinkBase} message CoinLinkBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CoinLinkBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CoinLinkBase message from the specified reader or buffer.
     * @function decode
     * @memberof CoinLinkBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CoinLinkBase} CoinLinkBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CoinLinkBase.decode = function decode(reader, length, error) {
    CoinLinkBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CoinLinkBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.coinId = reader.int32();
                    break;
                }
            case 2: {
                    message.linkName = reader.string();
                    break;
                }
            case 3: {
                    message.minCash = reader.string();
                    break;
                }
            case 4: {
                    message.maxCash = reader.string();
                    break;
                }
            case 5: {
                    message.isOpenCash = reader.bool();
                    break;
                }
            case 6: {
                    message.isOpenRecharge = reader.bool();
                    break;
                }
            case 7: {
                    message.minRecharge = reader.string();
                    break;
                }
            case 8: {
                    message.tagType = reader.int32();
                    break;
                }
            case 9: {
                    message.tagName = reader.string();
                    break;
                }
            case 10: {
                    message.blockConfirm = reader.string();
                    break;
                }
            case 11: {
                    message.cashTotal = reader.string();
                    break;
                }
            case 12: {
                    message.feeCoin = reader.string();
                    break;
                }
            case 13: {
                    message.showDecimal = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CoinLinkBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CoinLinkBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CoinLinkBase} CoinLinkBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CoinLinkBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CoinLinkBase message.
     * @function verify
     * @memberof CoinLinkBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CoinLinkBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.coinId != null && message.hasOwnProperty("coinId"))
            if (!$util.isInteger(message.coinId))
                return "coinId: integer expected";
        if (message.linkName != null && message.hasOwnProperty("linkName"))
            if (!$util.isString(message.linkName))
                return "linkName: string expected";
        if (message.minCash != null && message.hasOwnProperty("minCash"))
            if (!$util.isString(message.minCash))
                return "minCash: string expected";
        if (message.maxCash != null && message.hasOwnProperty("maxCash"))
            if (!$util.isString(message.maxCash))
                return "maxCash: string expected";
        if (message.isOpenCash != null && message.hasOwnProperty("isOpenCash"))
            if (typeof message.isOpenCash !== "boolean")
                return "isOpenCash: boolean expected";
        if (message.isOpenRecharge != null && message.hasOwnProperty("isOpenRecharge"))
            if (typeof message.isOpenRecharge !== "boolean")
                return "isOpenRecharge: boolean expected";
        if (message.minRecharge != null && message.hasOwnProperty("minRecharge"))
            if (!$util.isString(message.minRecharge))
                return "minRecharge: string expected";
        if (message.tagType != null && message.hasOwnProperty("tagType"))
            if (!$util.isInteger(message.tagType))
                return "tagType: integer expected";
        if (message.tagName != null && message.hasOwnProperty("tagName"))
            if (!$util.isString(message.tagName))
                return "tagName: string expected";
        if (message.blockConfirm != null && message.hasOwnProperty("blockConfirm"))
            if (!$util.isString(message.blockConfirm))
                return "blockConfirm: string expected";
        if (message.cashTotal != null && message.hasOwnProperty("cashTotal"))
            if (!$util.isString(message.cashTotal))
                return "cashTotal: string expected";
        if (message.feeCoin != null && message.hasOwnProperty("feeCoin"))
            if (!$util.isString(message.feeCoin))
                return "feeCoin: string expected";
        if (message.showDecimal != null && message.hasOwnProperty("showDecimal"))
            if (!$util.isInteger(message.showDecimal))
                return "showDecimal: integer expected";
        return null;
    };

    /**
     * Creates a CoinLinkBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CoinLinkBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CoinLinkBase} CoinLinkBase
     */
    CoinLinkBase.fromObject = function fromObject(object) {
        if (object instanceof $root.CoinLinkBase)
            return object;
        let message = new $root.CoinLinkBase();
        if (object.coinId != null)
            message.coinId = object.coinId | 0;
        if (object.linkName != null)
            message.linkName = String(object.linkName);
        if (object.minCash != null)
            message.minCash = String(object.minCash);
        if (object.maxCash != null)
            message.maxCash = String(object.maxCash);
        if (object.isOpenCash != null)
            message.isOpenCash = Boolean(object.isOpenCash);
        if (object.isOpenRecharge != null)
            message.isOpenRecharge = Boolean(object.isOpenRecharge);
        if (object.minRecharge != null)
            message.minRecharge = String(object.minRecharge);
        if (object.tagType != null)
            message.tagType = object.tagType | 0;
        if (object.tagName != null)
            message.tagName = String(object.tagName);
        if (object.blockConfirm != null)
            message.blockConfirm = String(object.blockConfirm);
        if (object.cashTotal != null)
            message.cashTotal = String(object.cashTotal);
        if (object.feeCoin != null)
            message.feeCoin = String(object.feeCoin);
        if (object.showDecimal != null)
            message.showDecimal = object.showDecimal | 0;
        return message;
    };

    /**
     * Creates a plain object from a CoinLinkBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CoinLinkBase
     * @static
     * @param {CoinLinkBase} message CoinLinkBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CoinLinkBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.coinId = 0;
            object.linkName = "";
            object.minCash = "";
            object.maxCash = "";
            object.isOpenCash = false;
            object.isOpenRecharge = false;
            object.minRecharge = "";
            object.tagType = 0;
            object.tagName = "";
            object.blockConfirm = "";
            object.cashTotal = "";
            object.feeCoin = "";
            object.showDecimal = 0;
        }
        if (message.coinId != null && message.hasOwnProperty("coinId"))
            object.coinId = message.coinId;
        if (message.linkName != null && message.hasOwnProperty("linkName"))
            object.linkName = message.linkName;
        if (message.minCash != null && message.hasOwnProperty("minCash"))
            object.minCash = message.minCash;
        if (message.maxCash != null && message.hasOwnProperty("maxCash"))
            object.maxCash = message.maxCash;
        if (message.isOpenCash != null && message.hasOwnProperty("isOpenCash"))
            object.isOpenCash = message.isOpenCash;
        if (message.isOpenRecharge != null && message.hasOwnProperty("isOpenRecharge"))
            object.isOpenRecharge = message.isOpenRecharge;
        if (message.minRecharge != null && message.hasOwnProperty("minRecharge"))
            object.minRecharge = message.minRecharge;
        if (message.tagType != null && message.hasOwnProperty("tagType"))
            object.tagType = message.tagType;
        if (message.tagName != null && message.hasOwnProperty("tagName"))
            object.tagName = message.tagName;
        if (message.blockConfirm != null && message.hasOwnProperty("blockConfirm"))
            object.blockConfirm = message.blockConfirm;
        if (message.cashTotal != null && message.hasOwnProperty("cashTotal"))
            object.cashTotal = message.cashTotal;
        if (message.feeCoin != null && message.hasOwnProperty("feeCoin"))
            object.feeCoin = message.feeCoin;
        if (message.showDecimal != null && message.hasOwnProperty("showDecimal"))
            object.showDecimal = message.showDecimal;
        return object;
    };

    /**
     * Converts this CoinLinkBase to JSON.
     * @function toJSON
     * @memberof CoinLinkBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CoinLinkBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CoinLinkBase
     * @function getTypeUrl
     * @memberof CoinLinkBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CoinLinkBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CoinLinkBase";
    };

    return CoinLinkBase;
})();

export const CoinTypeBase = $root.CoinTypeBase = (() => {

    /**
     * Properties of a CoinTypeBase.
     * @exports ICoinTypeBase
     * @interface ICoinTypeBase
     * @property {number|null} [id] CoinTypeBase id
     * @property {string|null} [name] CoinTypeBase name
     * @property {string|null} [desc] CoinTypeBase desc
     * @property {string|null} [icon] CoinTypeBase icon
     * @property {string|null} [letter] CoinTypeBase letter
     * @property {Array.<ICoinLinkBase>|null} [link] CoinTypeBase link
     * @property {string|null} [minCash] CoinTypeBase minCash
     * @property {string|null} [maxCash] CoinTypeBase maxCash
     * @property {boolean|null} [isOpenCash] CoinTypeBase isOpenCash
     * @property {boolean|null} [isOpenRecharge] CoinTypeBase isOpenRecharge
     * @property {string|null} [minRecharge] CoinTypeBase minRecharge
     * @property {number|null} [tagType] CoinTypeBase tagType
     * @property {string|null} [tagName] CoinTypeBase tagName
     * @property {string|null} [blockConfirm] CoinTypeBase blockConfirm
     * @property {string|null} [cashTotal] CoinTypeBase cashTotal
     * @property {string|null} [feeCoin] CoinTypeBase feeCoin
     * @property {number|null} [showDecimal] CoinTypeBase showDecimal
     * @property {string|null} [symbol] CoinTypeBase symbol
     * @property {string|null} [alias] CoinTypeBase alias
     */

    /**
     * Constructs a new CoinTypeBase.
     * @exports CoinTypeBase
     * @classdesc Represents a CoinTypeBase.
     * @implements ICoinTypeBase
     * @constructor
     * @param {ICoinTypeBase=} [properties] Properties to set
     */
    function CoinTypeBase(properties) {
        this.link = [];
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CoinTypeBase id.
     * @member {number} id
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.id = 0;

    /**
     * CoinTypeBase name.
     * @member {string} name
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.name = "";

    /**
     * CoinTypeBase desc.
     * @member {string} desc
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.desc = "";

    /**
     * CoinTypeBase icon.
     * @member {string} icon
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.icon = "";

    /**
     * CoinTypeBase letter.
     * @member {string} letter
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.letter = "";

    /**
     * CoinTypeBase link.
     * @member {Array.<ICoinLinkBase>} link
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.link = $util.emptyArray;

    /**
     * CoinTypeBase minCash.
     * @member {string} minCash
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.minCash = "";

    /**
     * CoinTypeBase maxCash.
     * @member {string} maxCash
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.maxCash = "";

    /**
     * CoinTypeBase isOpenCash.
     * @member {boolean} isOpenCash
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.isOpenCash = false;

    /**
     * CoinTypeBase isOpenRecharge.
     * @member {boolean} isOpenRecharge
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.isOpenRecharge = false;

    /**
     * CoinTypeBase minRecharge.
     * @member {string} minRecharge
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.minRecharge = "";

    /**
     * CoinTypeBase tagType.
     * @member {number} tagType
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.tagType = 0;

    /**
     * CoinTypeBase tagName.
     * @member {string} tagName
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.tagName = "";

    /**
     * CoinTypeBase blockConfirm.
     * @member {string} blockConfirm
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.blockConfirm = "";

    /**
     * CoinTypeBase cashTotal.
     * @member {string} cashTotal
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.cashTotal = "";

    /**
     * CoinTypeBase feeCoin.
     * @member {string} feeCoin
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.feeCoin = "";

    /**
     * CoinTypeBase showDecimal.
     * @member {number} showDecimal
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.showDecimal = 0;

    /**
     * CoinTypeBase symbol.
     * @member {string} symbol
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.symbol = "";

    /**
     * CoinTypeBase alias.
     * @member {string} alias
     * @memberof CoinTypeBase
     * @instance
     */
    CoinTypeBase.prototype.alias = "";

    /**
     * Creates a new CoinTypeBase instance using the specified properties.
     * @function create
     * @memberof CoinTypeBase
     * @static
     * @param {ICoinTypeBase=} [properties] Properties to set
     * @returns {CoinTypeBase} CoinTypeBase instance
     */
    CoinTypeBase.create = function create(properties) {
        return new CoinTypeBase(properties);
    };

    /**
     * Encodes the specified CoinTypeBase message. Does not implicitly {@link CoinTypeBase.verify|verify} messages.
     * @function encode
     * @memberof CoinTypeBase
     * @static
     * @param {ICoinTypeBase} message CoinTypeBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CoinTypeBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.id != null && Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
        if (message.name != null && Object.hasOwnProperty.call(message, "name"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
        if (message.desc != null && Object.hasOwnProperty.call(message, "desc"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.desc);
        if (message.icon != null && Object.hasOwnProperty.call(message, "icon"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.icon);
        if (message.letter != null && Object.hasOwnProperty.call(message, "letter"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.letter);
        if (message.link != null && message.link.length)
            for (let i = 0; i < message.link.length; ++i)
                $root.CoinLinkBase.encode(message.link[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
        if (message.minCash != null && Object.hasOwnProperty.call(message, "minCash"))
            writer.uint32(/* id 7, wireType 2 =*/58).string(message.minCash);
        if (message.maxCash != null && Object.hasOwnProperty.call(message, "maxCash"))
            writer.uint32(/* id 8, wireType 2 =*/66).string(message.maxCash);
        if (message.isOpenCash != null && Object.hasOwnProperty.call(message, "isOpenCash"))
            writer.uint32(/* id 9, wireType 0 =*/72).bool(message.isOpenCash);
        if (message.isOpenRecharge != null && Object.hasOwnProperty.call(message, "isOpenRecharge"))
            writer.uint32(/* id 10, wireType 0 =*/80).bool(message.isOpenRecharge);
        if (message.minRecharge != null && Object.hasOwnProperty.call(message, "minRecharge"))
            writer.uint32(/* id 11, wireType 2 =*/90).string(message.minRecharge);
        if (message.tagType != null && Object.hasOwnProperty.call(message, "tagType"))
            writer.uint32(/* id 12, wireType 0 =*/96).int32(message.tagType);
        if (message.tagName != null && Object.hasOwnProperty.call(message, "tagName"))
            writer.uint32(/* id 13, wireType 2 =*/106).string(message.tagName);
        if (message.blockConfirm != null && Object.hasOwnProperty.call(message, "blockConfirm"))
            writer.uint32(/* id 14, wireType 2 =*/114).string(message.blockConfirm);
        if (message.cashTotal != null && Object.hasOwnProperty.call(message, "cashTotal"))
            writer.uint32(/* id 15, wireType 2 =*/122).string(message.cashTotal);
        if (message.feeCoin != null && Object.hasOwnProperty.call(message, "feeCoin"))
            writer.uint32(/* id 16, wireType 2 =*/130).string(message.feeCoin);
        if (message.showDecimal != null && Object.hasOwnProperty.call(message, "showDecimal"))
            writer.uint32(/* id 17, wireType 0 =*/136).int32(message.showDecimal);
        if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
            writer.uint32(/* id 18, wireType 2 =*/146).string(message.symbol);
        if (message.alias != null && Object.hasOwnProperty.call(message, "alias"))
            writer.uint32(/* id 19, wireType 2 =*/154).string(message.alias);
        return writer;
    };

    /**
     * Encodes the specified CoinTypeBase message, length delimited. Does not implicitly {@link CoinTypeBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CoinTypeBase
     * @static
     * @param {ICoinTypeBase} message CoinTypeBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CoinTypeBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CoinTypeBase message from the specified reader or buffer.
     * @function decode
     * @memberof CoinTypeBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CoinTypeBase} CoinTypeBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CoinTypeBase.decode = function decode(reader, length, error) {
    CoinTypeBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CoinTypeBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.id = reader.int32();
                    break;
                }
            case 2: {
                    message.name = reader.string();
                    break;
                }
            case 3: {
                    message.desc = reader.string();
                    break;
                }
            case 4: {
                    message.icon = reader.string();
                    break;
                }
            case 5: {
                    message.letter = reader.string();
                    break;
                }
            case 6: {
                    if (!(message.link && message.link.length))
                        message.link = [];
                    message.link.push($root.CoinLinkBase.decode(reader, reader.uint32()));
                    break;
                }
            case 7: {
                    message.minCash = reader.string();
                    break;
                }
            case 8: {
                    message.maxCash = reader.string();
                    break;
                }
            case 9: {
                    message.isOpenCash = reader.bool();
                    break;
                }
            case 10: {
                    message.isOpenRecharge = reader.bool();
                    break;
                }
            case 11: {
                    message.minRecharge = reader.string();
                    break;
                }
            case 12: {
                    message.tagType = reader.int32();
                    break;
                }
            case 13: {
                    message.tagName = reader.string();
                    break;
                }
            case 14: {
                    message.blockConfirm = reader.string();
                    break;
                }
            case 15: {
                    message.cashTotal = reader.string();
                    break;
                }
            case 16: {
                    message.feeCoin = reader.string();
                    break;
                }
            case 17: {
                    message.showDecimal = reader.int32();
                    break;
                }
            case 18: {
                    message.symbol = reader.string();
                    break;
                }
            case 19: {
                    message.alias = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CoinTypeBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CoinTypeBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CoinTypeBase} CoinTypeBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CoinTypeBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CoinTypeBase message.
     * @function verify
     * @memberof CoinTypeBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CoinTypeBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.id != null && message.hasOwnProperty("id"))
            if (!$util.isInteger(message.id))
                return "id: integer expected";
        if (message.name != null && message.hasOwnProperty("name"))
            if (!$util.isString(message.name))
                return "name: string expected";
        if (message.desc != null && message.hasOwnProperty("desc"))
            if (!$util.isString(message.desc))
                return "desc: string expected";
        if (message.icon != null && message.hasOwnProperty("icon"))
            if (!$util.isString(message.icon))
                return "icon: string expected";
        if (message.letter != null && message.hasOwnProperty("letter"))
            if (!$util.isString(message.letter))
                return "letter: string expected";
        if (message.link != null && message.hasOwnProperty("link")) {
            if (!Array.isArray(message.link))
                return "link: array expected";
            for (let i = 0; i < message.link.length; ++i) {
                let error = $root.CoinLinkBase.verify(message.link[i]);
                if (error)
                    return "link." + error;
            }
        }
        if (message.minCash != null && message.hasOwnProperty("minCash"))
            if (!$util.isString(message.minCash))
                return "minCash: string expected";
        if (message.maxCash != null && message.hasOwnProperty("maxCash"))
            if (!$util.isString(message.maxCash))
                return "maxCash: string expected";
        if (message.isOpenCash != null && message.hasOwnProperty("isOpenCash"))
            if (typeof message.isOpenCash !== "boolean")
                return "isOpenCash: boolean expected";
        if (message.isOpenRecharge != null && message.hasOwnProperty("isOpenRecharge"))
            if (typeof message.isOpenRecharge !== "boolean")
                return "isOpenRecharge: boolean expected";
        if (message.minRecharge != null && message.hasOwnProperty("minRecharge"))
            if (!$util.isString(message.minRecharge))
                return "minRecharge: string expected";
        if (message.tagType != null && message.hasOwnProperty("tagType"))
            if (!$util.isInteger(message.tagType))
                return "tagType: integer expected";
        if (message.tagName != null && message.hasOwnProperty("tagName"))
            if (!$util.isString(message.tagName))
                return "tagName: string expected";
        if (message.blockConfirm != null && message.hasOwnProperty("blockConfirm"))
            if (!$util.isString(message.blockConfirm))
                return "blockConfirm: string expected";
        if (message.cashTotal != null && message.hasOwnProperty("cashTotal"))
            if (!$util.isString(message.cashTotal))
                return "cashTotal: string expected";
        if (message.feeCoin != null && message.hasOwnProperty("feeCoin"))
            if (!$util.isString(message.feeCoin))
                return "feeCoin: string expected";
        if (message.showDecimal != null && message.hasOwnProperty("showDecimal"))
            if (!$util.isInteger(message.showDecimal))
                return "showDecimal: integer expected";
        if (message.symbol != null && message.hasOwnProperty("symbol"))
            if (!$util.isString(message.symbol))
                return "symbol: string expected";
        if (message.alias != null && message.hasOwnProperty("alias"))
            if (!$util.isString(message.alias))
                return "alias: string expected";
        return null;
    };

    /**
     * Creates a CoinTypeBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CoinTypeBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CoinTypeBase} CoinTypeBase
     */
    CoinTypeBase.fromObject = function fromObject(object) {
        if (object instanceof $root.CoinTypeBase)
            return object;
        let message = new $root.CoinTypeBase();
        if (object.id != null)
            message.id = object.id | 0;
        if (object.name != null)
            message.name = String(object.name);
        if (object.desc != null)
            message.desc = String(object.desc);
        if (object.icon != null)
            message.icon = String(object.icon);
        if (object.letter != null)
            message.letter = String(object.letter);
        if (object.link) {
            if (!Array.isArray(object.link))
                throw TypeError(".CoinTypeBase.link: array expected");
            message.link = [];
            for (let i = 0; i < object.link.length; ++i) {
                if (typeof object.link[i] !== "object")
                    throw TypeError(".CoinTypeBase.link: object expected");
                message.link[i] = $root.CoinLinkBase.fromObject(object.link[i]);
            }
        }
        if (object.minCash != null)
            message.minCash = String(object.minCash);
        if (object.maxCash != null)
            message.maxCash = String(object.maxCash);
        if (object.isOpenCash != null)
            message.isOpenCash = Boolean(object.isOpenCash);
        if (object.isOpenRecharge != null)
            message.isOpenRecharge = Boolean(object.isOpenRecharge);
        if (object.minRecharge != null)
            message.minRecharge = String(object.minRecharge);
        if (object.tagType != null)
            message.tagType = object.tagType | 0;
        if (object.tagName != null)
            message.tagName = String(object.tagName);
        if (object.blockConfirm != null)
            message.blockConfirm = String(object.blockConfirm);
        if (object.cashTotal != null)
            message.cashTotal = String(object.cashTotal);
        if (object.feeCoin != null)
            message.feeCoin = String(object.feeCoin);
        if (object.showDecimal != null)
            message.showDecimal = object.showDecimal | 0;
        if (object.symbol != null)
            message.symbol = String(object.symbol);
        if (object.alias != null)
            message.alias = String(object.alias);
        return message;
    };

    /**
     * Creates a plain object from a CoinTypeBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CoinTypeBase
     * @static
     * @param {CoinTypeBase} message CoinTypeBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CoinTypeBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.arrays || options.defaults)
            object.link = [];
        if (options.defaults) {
            object.id = 0;
            object.name = "";
            object.desc = "";
            object.icon = "";
            object.letter = "";
            object.minCash = "";
            object.maxCash = "";
            object.isOpenCash = false;
            object.isOpenRecharge = false;
            object.minRecharge = "";
            object.tagType = 0;
            object.tagName = "";
            object.blockConfirm = "";
            object.cashTotal = "";
            object.feeCoin = "";
            object.showDecimal = 0;
            object.symbol = "";
            object.alias = "";
        }
        if (message.id != null && message.hasOwnProperty("id"))
            object.id = message.id;
        if (message.name != null && message.hasOwnProperty("name"))
            object.name = message.name;
        if (message.desc != null && message.hasOwnProperty("desc"))
            object.desc = message.desc;
        if (message.icon != null && message.hasOwnProperty("icon"))
            object.icon = message.icon;
        if (message.letter != null && message.hasOwnProperty("letter"))
            object.letter = message.letter;
        if (message.link && message.link.length) {
            object.link = [];
            for (let j = 0; j < message.link.length; ++j)
                object.link[j] = $root.CoinLinkBase.toObject(message.link[j], options);
        }
        if (message.minCash != null && message.hasOwnProperty("minCash"))
            object.minCash = message.minCash;
        if (message.maxCash != null && message.hasOwnProperty("maxCash"))
            object.maxCash = message.maxCash;
        if (message.isOpenCash != null && message.hasOwnProperty("isOpenCash"))
            object.isOpenCash = message.isOpenCash;
        if (message.isOpenRecharge != null && message.hasOwnProperty("isOpenRecharge"))
            object.isOpenRecharge = message.isOpenRecharge;
        if (message.minRecharge != null && message.hasOwnProperty("minRecharge"))
            object.minRecharge = message.minRecharge;
        if (message.tagType != null && message.hasOwnProperty("tagType"))
            object.tagType = message.tagType;
        if (message.tagName != null && message.hasOwnProperty("tagName"))
            object.tagName = message.tagName;
        if (message.blockConfirm != null && message.hasOwnProperty("blockConfirm"))
            object.blockConfirm = message.blockConfirm;
        if (message.cashTotal != null && message.hasOwnProperty("cashTotal"))
            object.cashTotal = message.cashTotal;
        if (message.feeCoin != null && message.hasOwnProperty("feeCoin"))
            object.feeCoin = message.feeCoin;
        if (message.showDecimal != null && message.hasOwnProperty("showDecimal"))
            object.showDecimal = message.showDecimal;
        if (message.symbol != null && message.hasOwnProperty("symbol"))
            object.symbol = message.symbol;
        if (message.alias != null && message.hasOwnProperty("alias"))
            object.alias = message.alias;
        return object;
    };

    /**
     * Converts this CoinTypeBase to JSON.
     * @function toJSON
     * @memberof CoinTypeBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CoinTypeBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CoinTypeBase
     * @function getTypeUrl
     * @memberof CoinTypeBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CoinTypeBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CoinTypeBase";
    };

    return CoinTypeBase;
})();

export const NewsCategoryBase = $root.NewsCategoryBase = (() => {

    /**
     * Properties of a NewsCategoryBase.
     * @exports INewsCategoryBase
     * @interface INewsCategoryBase
     * @property {number|null} [categoryId] NewsCategoryBase categoryId
     * @property {string|null} [name] NewsCategoryBase name
     * @property {number|null} [sort] NewsCategoryBase sort
     */

    /**
     * Constructs a new NewsCategoryBase.
     * @exports NewsCategoryBase
     * @classdesc Represents a NewsCategoryBase.
     * @implements INewsCategoryBase
     * @constructor
     * @param {INewsCategoryBase=} [properties] Properties to set
     */
    function NewsCategoryBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * NewsCategoryBase categoryId.
     * @member {number} categoryId
     * @memberof NewsCategoryBase
     * @instance
     */
    NewsCategoryBase.prototype.categoryId = 0;

    /**
     * NewsCategoryBase name.
     * @member {string} name
     * @memberof NewsCategoryBase
     * @instance
     */
    NewsCategoryBase.prototype.name = "";

    /**
     * NewsCategoryBase sort.
     * @member {number} sort
     * @memberof NewsCategoryBase
     * @instance
     */
    NewsCategoryBase.prototype.sort = 0;

    /**
     * Creates a new NewsCategoryBase instance using the specified properties.
     * @function create
     * @memberof NewsCategoryBase
     * @static
     * @param {INewsCategoryBase=} [properties] Properties to set
     * @returns {NewsCategoryBase} NewsCategoryBase instance
     */
    NewsCategoryBase.create = function create(properties) {
        return new NewsCategoryBase(properties);
    };

    /**
     * Encodes the specified NewsCategoryBase message. Does not implicitly {@link NewsCategoryBase.verify|verify} messages.
     * @function encode
     * @memberof NewsCategoryBase
     * @static
     * @param {INewsCategoryBase} message NewsCategoryBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NewsCategoryBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.categoryId != null && Object.hasOwnProperty.call(message, "categoryId"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.categoryId);
        if (message.name != null && Object.hasOwnProperty.call(message, "name"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
        if (message.sort != null && Object.hasOwnProperty.call(message, "sort"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.sort);
        return writer;
    };

    /**
     * Encodes the specified NewsCategoryBase message, length delimited. Does not implicitly {@link NewsCategoryBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof NewsCategoryBase
     * @static
     * @param {INewsCategoryBase} message NewsCategoryBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NewsCategoryBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a NewsCategoryBase message from the specified reader or buffer.
     * @function decode
     * @memberof NewsCategoryBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {NewsCategoryBase} NewsCategoryBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NewsCategoryBase.decode = function decode(reader, length, error) {
    NewsCategoryBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.NewsCategoryBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.categoryId = reader.int32();
                    break;
                }
            case 2: {
                    message.name = reader.string();
                    break;
                }
            case 3: {
                    message.sort = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a NewsCategoryBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof NewsCategoryBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {NewsCategoryBase} NewsCategoryBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NewsCategoryBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a NewsCategoryBase message.
     * @function verify
     * @memberof NewsCategoryBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    NewsCategoryBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.categoryId != null && message.hasOwnProperty("categoryId"))
            if (!$util.isInteger(message.categoryId))
                return "categoryId: integer expected";
        if (message.name != null && message.hasOwnProperty("name"))
            if (!$util.isString(message.name))
                return "name: string expected";
        if (message.sort != null && message.hasOwnProperty("sort"))
            if (!$util.isInteger(message.sort))
                return "sort: integer expected";
        return null;
    };

    /**
     * Creates a NewsCategoryBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof NewsCategoryBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {NewsCategoryBase} NewsCategoryBase
     */
    NewsCategoryBase.fromObject = function fromObject(object) {
        if (object instanceof $root.NewsCategoryBase)
            return object;
        let message = new $root.NewsCategoryBase();
        if (object.categoryId != null)
            message.categoryId = object.categoryId | 0;
        if (object.name != null)
            message.name = String(object.name);
        if (object.sort != null)
            message.sort = object.sort | 0;
        return message;
    };

    /**
     * Creates a plain object from a NewsCategoryBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof NewsCategoryBase
     * @static
     * @param {NewsCategoryBase} message NewsCategoryBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    NewsCategoryBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.categoryId = 0;
            object.name = "";
            object.sort = 0;
        }
        if (message.categoryId != null && message.hasOwnProperty("categoryId"))
            object.categoryId = message.categoryId;
        if (message.name != null && message.hasOwnProperty("name"))
            object.name = message.name;
        if (message.sort != null && message.hasOwnProperty("sort"))
            object.sort = message.sort;
        return object;
    };

    /**
     * Converts this NewsCategoryBase to JSON.
     * @function toJSON
     * @memberof NewsCategoryBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    NewsCategoryBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for NewsCategoryBase
     * @function getTypeUrl
     * @memberof NewsCategoryBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    NewsCategoryBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/NewsCategoryBase";
    };

    return NewsCategoryBase;
})();

export const NewsBase = $root.NewsBase = (() => {

    /**
     * Properties of a NewsBase.
     * @exports INewsBase
     * @interface INewsBase
     * @property {number|Long|null} [newsId] NewsBase newsId
     * @property {string|null} [title] NewsBase title
     * @property {string|null} [pic] NewsBase pic
     * @property {number|Long|null} [time] NewsBase time
     * @property {string|null} [detailLink] NewsBase detailLink
     */

    /**
     * Constructs a new NewsBase.
     * @exports NewsBase
     * @classdesc Represents a NewsBase.
     * @implements INewsBase
     * @constructor
     * @param {INewsBase=} [properties] Properties to set
     */
    function NewsBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * NewsBase newsId.
     * @member {number|Long} newsId
     * @memberof NewsBase
     * @instance
     */
    NewsBase.prototype.newsId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * NewsBase title.
     * @member {string} title
     * @memberof NewsBase
     * @instance
     */
    NewsBase.prototype.title = "";

    /**
     * NewsBase pic.
     * @member {string} pic
     * @memberof NewsBase
     * @instance
     */
    NewsBase.prototype.pic = "";

    /**
     * NewsBase time.
     * @member {number|Long} time
     * @memberof NewsBase
     * @instance
     */
    NewsBase.prototype.time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * NewsBase detailLink.
     * @member {string} detailLink
     * @memberof NewsBase
     * @instance
     */
    NewsBase.prototype.detailLink = "";

    /**
     * Creates a new NewsBase instance using the specified properties.
     * @function create
     * @memberof NewsBase
     * @static
     * @param {INewsBase=} [properties] Properties to set
     * @returns {NewsBase} NewsBase instance
     */
    NewsBase.create = function create(properties) {
        return new NewsBase(properties);
    };

    /**
     * Encodes the specified NewsBase message. Does not implicitly {@link NewsBase.verify|verify} messages.
     * @function encode
     * @memberof NewsBase
     * @static
     * @param {INewsBase} message NewsBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NewsBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.newsId != null && Object.hasOwnProperty.call(message, "newsId"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.newsId);
        if (message.title != null && Object.hasOwnProperty.call(message, "title"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.title);
        if (message.pic != null && Object.hasOwnProperty.call(message, "pic"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.pic);
        if (message.time != null && Object.hasOwnProperty.call(message, "time"))
            writer.uint32(/* id 4, wireType 0 =*/32).int64(message.time);
        if (message.detailLink != null && Object.hasOwnProperty.call(message, "detailLink"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.detailLink);
        return writer;
    };

    /**
     * Encodes the specified NewsBase message, length delimited. Does not implicitly {@link NewsBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof NewsBase
     * @static
     * @param {INewsBase} message NewsBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NewsBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a NewsBase message from the specified reader or buffer.
     * @function decode
     * @memberof NewsBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {NewsBase} NewsBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NewsBase.decode = function decode(reader, length, error) {
    NewsBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.NewsBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.newsId = reader.int64();
                    break;
                }
            case 2: {
                    message.title = reader.string();
                    break;
                }
            case 3: {
                    message.pic = reader.string();
                    break;
                }
            case 4: {
                    message.time = reader.int64();
                    break;
                }
            case 5: {
                    message.detailLink = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a NewsBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof NewsBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {NewsBase} NewsBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NewsBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a NewsBase message.
     * @function verify
     * @memberof NewsBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    NewsBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.newsId != null && message.hasOwnProperty("newsId"))
            if (!$util.isInteger(message.newsId) && !(message.newsId && $util.isInteger(message.newsId.low) && $util.isInteger(message.newsId.high)))
                return "newsId: integer|Long expected";
        if (message.title != null && message.hasOwnProperty("title"))
            if (!$util.isString(message.title))
                return "title: string expected";
        if (message.pic != null && message.hasOwnProperty("pic"))
            if (!$util.isString(message.pic))
                return "pic: string expected";
        if (message.time != null && message.hasOwnProperty("time"))
            if (!$util.isInteger(message.time) && !(message.time && $util.isInteger(message.time.low) && $util.isInteger(message.time.high)))
                return "time: integer|Long expected";
        if (message.detailLink != null && message.hasOwnProperty("detailLink"))
            if (!$util.isString(message.detailLink))
                return "detailLink: string expected";
        return null;
    };

    /**
     * Creates a NewsBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof NewsBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {NewsBase} NewsBase
     */
    NewsBase.fromObject = function fromObject(object) {
        if (object instanceof $root.NewsBase)
            return object;
        let message = new $root.NewsBase();
        if (object.newsId != null)
            if ($util.Long)
                (message.newsId = $util.Long.fromValue(object.newsId)).unsigned = false;
            else if (typeof object.newsId === "string")
                message.newsId = parseInt(object.newsId, 10);
            else if (typeof object.newsId === "number")
                message.newsId = object.newsId;
            else if (typeof object.newsId === "object")
                message.newsId = new $util.LongBits(object.newsId.low >>> 0, object.newsId.high >>> 0).toNumber();
        if (object.title != null)
            message.title = String(object.title);
        if (object.pic != null)
            message.pic = String(object.pic);
        if (object.time != null)
            if ($util.Long)
                (message.time = $util.Long.fromValue(object.time)).unsigned = false;
            else if (typeof object.time === "string")
                message.time = parseInt(object.time, 10);
            else if (typeof object.time === "number")
                message.time = object.time;
            else if (typeof object.time === "object")
                message.time = new $util.LongBits(object.time.low >>> 0, object.time.high >>> 0).toNumber();
        if (object.detailLink != null)
            message.detailLink = String(object.detailLink);
        return message;
    };

    /**
     * Creates a plain object from a NewsBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof NewsBase
     * @static
     * @param {NewsBase} message NewsBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    NewsBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.newsId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.newsId = options.longs === String ? "0" : 0;
            object.title = "";
            object.pic = "";
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.time = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.time = options.longs === String ? "0" : 0;
            object.detailLink = "";
        }
        if (message.newsId != null && message.hasOwnProperty("newsId"))
            if (typeof message.newsId === "number")
                object.newsId = options.longs === String ? String(message.newsId) : message.newsId;
            else
                object.newsId = options.longs === String ? $util.Long.prototype.toString.call(message.newsId) : options.longs === Number ? new $util.LongBits(message.newsId.low >>> 0, message.newsId.high >>> 0).toNumber() : message.newsId;
        if (message.title != null && message.hasOwnProperty("title"))
            object.title = message.title;
        if (message.pic != null && message.hasOwnProperty("pic"))
            object.pic = message.pic;
        if (message.time != null && message.hasOwnProperty("time"))
            if (typeof message.time === "number")
                object.time = options.longs === String ? String(message.time) : message.time;
            else
                object.time = options.longs === String ? $util.Long.prototype.toString.call(message.time) : options.longs === Number ? new $util.LongBits(message.time.low >>> 0, message.time.high >>> 0).toNumber() : message.time;
        if (message.detailLink != null && message.hasOwnProperty("detailLink"))
            object.detailLink = message.detailLink;
        return object;
    };

    /**
     * Converts this NewsBase to JSON.
     * @function toJSON
     * @memberof NewsBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    NewsBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for NewsBase
     * @function getTypeUrl
     * @memberof NewsBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    NewsBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/NewsBase";
    };

    return NewsBase;
})();

export const PaymentBase = $root.PaymentBase = (() => {

    /**
     * Properties of a PaymentBase.
     * @exports IPaymentBase
     * @interface IPaymentBase
     * @property {number|null} [paymentId] PaymentBase paymentId
     * @property {number|null} [paymentType] PaymentBase paymentType
     * @property {string|null} [accountName] PaymentBase accountName
     * @property {string|null} [cardNo] PaymentBase cardNo
     * @property {string|null} [bankName] PaymentBase bankName
     * @property {string|null} [subBankName] PaymentBase subBankName
     */

    /**
     * Constructs a new PaymentBase.
     * @exports PaymentBase
     * @classdesc Represents a PaymentBase.
     * @implements IPaymentBase
     * @constructor
     * @param {IPaymentBase=} [properties] Properties to set
     */
    function PaymentBase(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * PaymentBase paymentId.
     * @member {number} paymentId
     * @memberof PaymentBase
     * @instance
     */
    PaymentBase.prototype.paymentId = 0;

    /**
     * PaymentBase paymentType.
     * @member {number} paymentType
     * @memberof PaymentBase
     * @instance
     */
    PaymentBase.prototype.paymentType = 0;

    /**
     * PaymentBase accountName.
     * @member {string} accountName
     * @memberof PaymentBase
     * @instance
     */
    PaymentBase.prototype.accountName = "";

    /**
     * PaymentBase cardNo.
     * @member {string} cardNo
     * @memberof PaymentBase
     * @instance
     */
    PaymentBase.prototype.cardNo = "";

    /**
     * PaymentBase bankName.
     * @member {string} bankName
     * @memberof PaymentBase
     * @instance
     */
    PaymentBase.prototype.bankName = "";

    /**
     * PaymentBase subBankName.
     * @member {string} subBankName
     * @memberof PaymentBase
     * @instance
     */
    PaymentBase.prototype.subBankName = "";

    /**
     * Creates a new PaymentBase instance using the specified properties.
     * @function create
     * @memberof PaymentBase
     * @static
     * @param {IPaymentBase=} [properties] Properties to set
     * @returns {PaymentBase} PaymentBase instance
     */
    PaymentBase.create = function create(properties) {
        return new PaymentBase(properties);
    };

    /**
     * Encodes the specified PaymentBase message. Does not implicitly {@link PaymentBase.verify|verify} messages.
     * @function encode
     * @memberof PaymentBase
     * @static
     * @param {IPaymentBase} message PaymentBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PaymentBase.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.paymentId != null && Object.hasOwnProperty.call(message, "paymentId"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.paymentId);
        if (message.paymentType != null && Object.hasOwnProperty.call(message, "paymentType"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.paymentType);
        if (message.accountName != null && Object.hasOwnProperty.call(message, "accountName"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.accountName);
        if (message.cardNo != null && Object.hasOwnProperty.call(message, "cardNo"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.cardNo);
        if (message.bankName != null && Object.hasOwnProperty.call(message, "bankName"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.bankName);
        if (message.subBankName != null && Object.hasOwnProperty.call(message, "subBankName"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.subBankName);
        return writer;
    };

    /**
     * Encodes the specified PaymentBase message, length delimited. Does not implicitly {@link PaymentBase.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PaymentBase
     * @static
     * @param {IPaymentBase} message PaymentBase message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PaymentBase.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a PaymentBase message from the specified reader or buffer.
     * @function decode
     * @memberof PaymentBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PaymentBase} PaymentBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PaymentBase.decode = function decode(reader, length, error) {
    PaymentBase.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.PaymentBase();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.paymentId = reader.int32();
                    break;
                }
            case 2: {
                    message.paymentType = reader.int32();
                    break;
                }
            case 3: {
                    message.accountName = reader.string();
                    break;
                }
            case 4: {
                    message.cardNo = reader.string();
                    break;
                }
            case 5: {
                    message.bankName = reader.string();
                    break;
                }
            case 6: {
                    message.subBankName = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a PaymentBase message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PaymentBase
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PaymentBase} PaymentBase
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PaymentBase.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PaymentBase message.
     * @function verify
     * @memberof PaymentBase
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PaymentBase.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.paymentId != null && message.hasOwnProperty("paymentId"))
            if (!$util.isInteger(message.paymentId))
                return "paymentId: integer expected";
        if (message.paymentType != null && message.hasOwnProperty("paymentType"))
            if (!$util.isInteger(message.paymentType))
                return "paymentType: integer expected";
        if (message.accountName != null && message.hasOwnProperty("accountName"))
            if (!$util.isString(message.accountName))
                return "accountName: string expected";
        if (message.cardNo != null && message.hasOwnProperty("cardNo"))
            if (!$util.isString(message.cardNo))
                return "cardNo: string expected";
        if (message.bankName != null && message.hasOwnProperty("bankName"))
            if (!$util.isString(message.bankName))
                return "bankName: string expected";
        if (message.subBankName != null && message.hasOwnProperty("subBankName"))
            if (!$util.isString(message.subBankName))
                return "subBankName: string expected";
        return null;
    };

    /**
     * Creates a PaymentBase message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PaymentBase
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PaymentBase} PaymentBase
     */
    PaymentBase.fromObject = function fromObject(object) {
        if (object instanceof $root.PaymentBase)
            return object;
        let message = new $root.PaymentBase();
        if (object.paymentId != null)
            message.paymentId = object.paymentId | 0;
        if (object.paymentType != null)
            message.paymentType = object.paymentType | 0;
        if (object.accountName != null)
            message.accountName = String(object.accountName);
        if (object.cardNo != null)
            message.cardNo = String(object.cardNo);
        if (object.bankName != null)
            message.bankName = String(object.bankName);
        if (object.subBankName != null)
            message.subBankName = String(object.subBankName);
        return message;
    };

    /**
     * Creates a plain object from a PaymentBase message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PaymentBase
     * @static
     * @param {PaymentBase} message PaymentBase
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PaymentBase.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.paymentId = 0;
            object.paymentType = 0;
            object.accountName = "";
            object.cardNo = "";
            object.bankName = "";
            object.subBankName = "";
        }
        if (message.paymentId != null && message.hasOwnProperty("paymentId"))
            object.paymentId = message.paymentId;
        if (message.paymentType != null && message.hasOwnProperty("paymentType"))
            object.paymentType = message.paymentType;
        if (message.accountName != null && message.hasOwnProperty("accountName"))
            object.accountName = message.accountName;
        if (message.cardNo != null && message.hasOwnProperty("cardNo"))
            object.cardNo = message.cardNo;
        if (message.bankName != null && message.hasOwnProperty("bankName"))
            object.bankName = message.bankName;
        if (message.subBankName != null && message.hasOwnProperty("subBankName"))
            object.subBankName = message.subBankName;
        return object;
    };

    /**
     * Converts this PaymentBase to JSON.
     * @function toJSON
     * @memberof PaymentBase
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PaymentBase.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PaymentBase
     * @function getTypeUrl
     * @memberof PaymentBase
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PaymentBase.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/PaymentBase";
    };

    return PaymentBase;
})();

export const PaymentBasics = $root.PaymentBasics = (() => {

    /**
     * Properties of a PaymentBasics.
     * @exports IPaymentBasics
     * @interface IPaymentBasics
     * @property {number|Long|null} [paymentId] PaymentBasics paymentId
     * @property {number|null} [paymentType] PaymentBasics paymentType
     */

    /**
     * Constructs a new PaymentBasics.
     * @exports PaymentBasics
     * @classdesc Represents a PaymentBasics.
     * @implements IPaymentBasics
     * @constructor
     * @param {IPaymentBasics=} [properties] Properties to set
     */
    function PaymentBasics(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * PaymentBasics paymentId.
     * @member {number|Long} paymentId
     * @memberof PaymentBasics
     * @instance
     */
    PaymentBasics.prototype.paymentId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

    /**
     * PaymentBasics paymentType.
     * @member {number} paymentType
     * @memberof PaymentBasics
     * @instance
     */
    PaymentBasics.prototype.paymentType = 0;

    /**
     * Creates a new PaymentBasics instance using the specified properties.
     * @function create
     * @memberof PaymentBasics
     * @static
     * @param {IPaymentBasics=} [properties] Properties to set
     * @returns {PaymentBasics} PaymentBasics instance
     */
    PaymentBasics.create = function create(properties) {
        return new PaymentBasics(properties);
    };

    /**
     * Encodes the specified PaymentBasics message. Does not implicitly {@link PaymentBasics.verify|verify} messages.
     * @function encode
     * @memberof PaymentBasics
     * @static
     * @param {IPaymentBasics} message PaymentBasics message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PaymentBasics.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.paymentId != null && Object.hasOwnProperty.call(message, "paymentId"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.paymentId);
        if (message.paymentType != null && Object.hasOwnProperty.call(message, "paymentType"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.paymentType);
        return writer;
    };

    /**
     * Encodes the specified PaymentBasics message, length delimited. Does not implicitly {@link PaymentBasics.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PaymentBasics
     * @static
     * @param {IPaymentBasics} message PaymentBasics message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PaymentBasics.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a PaymentBasics message from the specified reader or buffer.
     * @function decode
     * @memberof PaymentBasics
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PaymentBasics} PaymentBasics
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PaymentBasics.decode = function decode(reader, length, error) {
    PaymentBasics.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.PaymentBasics();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.paymentId = reader.int64();
                    break;
                }
            case 2: {
                    message.paymentType = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a PaymentBasics message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PaymentBasics
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PaymentBasics} PaymentBasics
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PaymentBasics.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PaymentBasics message.
     * @function verify
     * @memberof PaymentBasics
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PaymentBasics.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.paymentId != null && message.hasOwnProperty("paymentId"))
            if (!$util.isInteger(message.paymentId) && !(message.paymentId && $util.isInteger(message.paymentId.low) && $util.isInteger(message.paymentId.high)))
                return "paymentId: integer|Long expected";
        if (message.paymentType != null && message.hasOwnProperty("paymentType"))
            if (!$util.isInteger(message.paymentType))
                return "paymentType: integer expected";
        return null;
    };

    /**
     * Creates a PaymentBasics message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PaymentBasics
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PaymentBasics} PaymentBasics
     */
    PaymentBasics.fromObject = function fromObject(object) {
        if (object instanceof $root.PaymentBasics)
            return object;
        let message = new $root.PaymentBasics();
        if (object.paymentId != null)
            if ($util.Long)
                (message.paymentId = $util.Long.fromValue(object.paymentId)).unsigned = false;
            else if (typeof object.paymentId === "string")
                message.paymentId = parseInt(object.paymentId, 10);
            else if (typeof object.paymentId === "number")
                message.paymentId = object.paymentId;
            else if (typeof object.paymentId === "object")
                message.paymentId = new $util.LongBits(object.paymentId.low >>> 0, object.paymentId.high >>> 0).toNumber();
        if (object.paymentType != null)
            message.paymentType = object.paymentType | 0;
        return message;
    };

    /**
     * Creates a plain object from a PaymentBasics message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PaymentBasics
     * @static
     * @param {PaymentBasics} message PaymentBasics
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PaymentBasics.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            if ($util.Long) {
                let long = new $util.Long(0, 0, false);
                object.paymentId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
            } else
                object.paymentId = options.longs === String ? "0" : 0;
            object.paymentType = 0;
        }
        if (message.paymentId != null && message.hasOwnProperty("paymentId"))
            if (typeof message.paymentId === "number")
                object.paymentId = options.longs === String ? String(message.paymentId) : message.paymentId;
            else
                object.paymentId = options.longs === String ? $util.Long.prototype.toString.call(message.paymentId) : options.longs === Number ? new $util.LongBits(message.paymentId.low >>> 0, message.paymentId.high >>> 0).toNumber() : message.paymentId;
        if (message.paymentType != null && message.hasOwnProperty("paymentType"))
            object.paymentType = message.paymentType;
        return object;
    };

    /**
     * Converts this PaymentBasics to JSON.
     * @function toJSON
     * @memberof PaymentBasics
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PaymentBasics.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PaymentBasics
     * @function getTypeUrl
     * @memberof PaymentBasics
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PaymentBasics.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/PaymentBasics";
    };

    return PaymentBasics;
})();

export const TranslationInfo = $root.TranslationInfo = (() => {

    /**
     * Properties of a TranslationInfo.
     * @exports ITranslationInfo
     * @interface ITranslationInfo
     * @property {number|null} [autoTranslateStatus] TranslationInfo autoTranslateStatus
     * @property {number|null} [receiveLanguage] TranslationInfo receiveLanguage
     * @property {number|null} [sendLanguage] TranslationInfo sendLanguage
     * @property {string|null} [key] TranslationInfo key
     * @property {string|null} [region] TranslationInfo region
     */

    /**
     * Constructs a new TranslationInfo.
     * @exports TranslationInfo
     * @classdesc Represents a TranslationInfo.
     * @implements ITranslationInfo
     * @constructor
     * @param {ITranslationInfo=} [properties] Properties to set
     */
    function TranslationInfo(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * TranslationInfo autoTranslateStatus.
     * @member {number} autoTranslateStatus
     * @memberof TranslationInfo
     * @instance
     */
    TranslationInfo.prototype.autoTranslateStatus = 0;

    /**
     * TranslationInfo receiveLanguage.
     * @member {number} receiveLanguage
     * @memberof TranslationInfo
     * @instance
     */
    TranslationInfo.prototype.receiveLanguage = 0;

    /**
     * TranslationInfo sendLanguage.
     * @member {number} sendLanguage
     * @memberof TranslationInfo
     * @instance
     */
    TranslationInfo.prototype.sendLanguage = 0;

    /**
     * TranslationInfo key.
     * @member {string} key
     * @memberof TranslationInfo
     * @instance
     */
    TranslationInfo.prototype.key = "";

    /**
     * TranslationInfo region.
     * @member {string} region
     * @memberof TranslationInfo
     * @instance
     */
    TranslationInfo.prototype.region = "";

    /**
     * Creates a new TranslationInfo instance using the specified properties.
     * @function create
     * @memberof TranslationInfo
     * @static
     * @param {ITranslationInfo=} [properties] Properties to set
     * @returns {TranslationInfo} TranslationInfo instance
     */
    TranslationInfo.create = function create(properties) {
        return new TranslationInfo(properties);
    };

    /**
     * Encodes the specified TranslationInfo message. Does not implicitly {@link TranslationInfo.verify|verify} messages.
     * @function encode
     * @memberof TranslationInfo
     * @static
     * @param {ITranslationInfo} message TranslationInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    TranslationInfo.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.autoTranslateStatus != null && Object.hasOwnProperty.call(message, "autoTranslateStatus"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.autoTranslateStatus);
        if (message.receiveLanguage != null && Object.hasOwnProperty.call(message, "receiveLanguage"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.receiveLanguage);
        if (message.sendLanguage != null && Object.hasOwnProperty.call(message, "sendLanguage"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.sendLanguage);
        if (message.key != null && Object.hasOwnProperty.call(message, "key"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.key);
        if (message.region != null && Object.hasOwnProperty.call(message, "region"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.region);
        return writer;
    };

    /**
     * Encodes the specified TranslationInfo message, length delimited. Does not implicitly {@link TranslationInfo.verify|verify} messages.
     * @function encodeDelimited
     * @memberof TranslationInfo
     * @static
     * @param {ITranslationInfo} message TranslationInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    TranslationInfo.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a TranslationInfo message from the specified reader or buffer.
     * @function decode
     * @memberof TranslationInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {TranslationInfo} TranslationInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    TranslationInfo.decode = function decode(reader, length, error) {
    TranslationInfo.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.TranslationInfo();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 2: {
                    message.autoTranslateStatus = reader.int32();
                    break;
                }
            case 3: {
                    message.receiveLanguage = reader.int32();
                    break;
                }
            case 4: {
                    message.sendLanguage = reader.int32();
                    break;
                }
            case 5: {
                    message.key = reader.string();
                    break;
                }
            case 6: {
                    message.region = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a TranslationInfo message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof TranslationInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {TranslationInfo} TranslationInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    TranslationInfo.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a TranslationInfo message.
     * @function verify
     * @memberof TranslationInfo
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    TranslationInfo.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.autoTranslateStatus != null && message.hasOwnProperty("autoTranslateStatus"))
            if (!$util.isInteger(message.autoTranslateStatus))
                return "autoTranslateStatus: integer expected";
        if (message.receiveLanguage != null && message.hasOwnProperty("receiveLanguage"))
            if (!$util.isInteger(message.receiveLanguage))
                return "receiveLanguage: integer expected";
        if (message.sendLanguage != null && message.hasOwnProperty("sendLanguage"))
            if (!$util.isInteger(message.sendLanguage))
                return "sendLanguage: integer expected";
        if (message.key != null && message.hasOwnProperty("key"))
            if (!$util.isString(message.key))
                return "key: string expected";
        if (message.region != null && message.hasOwnProperty("region"))
            if (!$util.isString(message.region))
                return "region: string expected";
        return null;
    };

    /**
     * Creates a TranslationInfo message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof TranslationInfo
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {TranslationInfo} TranslationInfo
     */
    TranslationInfo.fromObject = function fromObject(object) {
        if (object instanceof $root.TranslationInfo)
            return object;
        let message = new $root.TranslationInfo();
        if (object.autoTranslateStatus != null)
            message.autoTranslateStatus = object.autoTranslateStatus | 0;
        if (object.receiveLanguage != null)
            message.receiveLanguage = object.receiveLanguage | 0;
        if (object.sendLanguage != null)
            message.sendLanguage = object.sendLanguage | 0;
        if (object.key != null)
            message.key = String(object.key);
        if (object.region != null)
            message.region = String(object.region);
        return message;
    };

    /**
     * Creates a plain object from a TranslationInfo message. Also converts values to other types if specified.
     * @function toObject
     * @memberof TranslationInfo
     * @static
     * @param {TranslationInfo} message TranslationInfo
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    TranslationInfo.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.autoTranslateStatus = 0;
            object.receiveLanguage = 0;
            object.sendLanguage = 0;
            object.key = "";
            object.region = "";
        }
        if (message.autoTranslateStatus != null && message.hasOwnProperty("autoTranslateStatus"))
            object.autoTranslateStatus = message.autoTranslateStatus;
        if (message.receiveLanguage != null && message.hasOwnProperty("receiveLanguage"))
            object.receiveLanguage = message.receiveLanguage;
        if (message.sendLanguage != null && message.hasOwnProperty("sendLanguage"))
            object.sendLanguage = message.sendLanguage;
        if (message.key != null && message.hasOwnProperty("key"))
            object.key = message.key;
        if (message.region != null && message.hasOwnProperty("region"))
            object.region = message.region;
        return object;
    };

    /**
     * Converts this TranslationInfo to JSON.
     * @function toJSON
     * @memberof TranslationInfo
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    TranslationInfo.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for TranslationInfo
     * @function getTypeUrl
     * @memberof TranslationInfo
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    TranslationInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/TranslationInfo";
    };

    return TranslationInfo;
})();

export const BotGameInfo = $root.BotGameInfo = (() => {

    /**
     * Properties of a BotGameInfo.
     * @exports IBotGameInfo
     * @interface IBotGameInfo
     * @property {string|null} [gameId] BotGameInfo gameId
     * @property {string|null} [gameName] BotGameInfo gameName
     */

    /**
     * Constructs a new BotGameInfo.
     * @exports BotGameInfo
     * @classdesc Represents a BotGameInfo.
     * @implements IBotGameInfo
     * @constructor
     * @param {IBotGameInfo=} [properties] Properties to set
     */
    function BotGameInfo(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * BotGameInfo gameId.
     * @member {string} gameId
     * @memberof BotGameInfo
     * @instance
     */
    BotGameInfo.prototype.gameId = "";

    /**
     * BotGameInfo gameName.
     * @member {string} gameName
     * @memberof BotGameInfo
     * @instance
     */
    BotGameInfo.prototype.gameName = "";

    /**
     * Creates a new BotGameInfo instance using the specified properties.
     * @function create
     * @memberof BotGameInfo
     * @static
     * @param {IBotGameInfo=} [properties] Properties to set
     * @returns {BotGameInfo} BotGameInfo instance
     */
    BotGameInfo.create = function create(properties) {
        return new BotGameInfo(properties);
    };

    /**
     * Encodes the specified BotGameInfo message. Does not implicitly {@link BotGameInfo.verify|verify} messages.
     * @function encode
     * @memberof BotGameInfo
     * @static
     * @param {IBotGameInfo} message BotGameInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BotGameInfo.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.gameId != null && Object.hasOwnProperty.call(message, "gameId"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.gameId);
        if (message.gameName != null && Object.hasOwnProperty.call(message, "gameName"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.gameName);
        return writer;
    };

    /**
     * Encodes the specified BotGameInfo message, length delimited. Does not implicitly {@link BotGameInfo.verify|verify} messages.
     * @function encodeDelimited
     * @memberof BotGameInfo
     * @static
     * @param {IBotGameInfo} message BotGameInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BotGameInfo.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a BotGameInfo message from the specified reader or buffer.
     * @function decode
     * @memberof BotGameInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {BotGameInfo} BotGameInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BotGameInfo.decode = function decode(reader, length, error) {
    BotGameInfo.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.BotGameInfo();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.gameId = reader.string();
                    break;
                }
            case 2: {
                    message.gameName = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a BotGameInfo message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof BotGameInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {BotGameInfo} BotGameInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BotGameInfo.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a BotGameInfo message.
     * @function verify
     * @memberof BotGameInfo
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    BotGameInfo.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.gameId != null && message.hasOwnProperty("gameId"))
            if (!$util.isString(message.gameId))
                return "gameId: string expected";
        if (message.gameName != null && message.hasOwnProperty("gameName"))
            if (!$util.isString(message.gameName))
                return "gameName: string expected";
        return null;
    };

    /**
     * Creates a BotGameInfo message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof BotGameInfo
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {BotGameInfo} BotGameInfo
     */
    BotGameInfo.fromObject = function fromObject(object) {
        if (object instanceof $root.BotGameInfo)
            return object;
        let message = new $root.BotGameInfo();
        if (object.gameId != null)
            message.gameId = String(object.gameId);
        if (object.gameName != null)
            message.gameName = String(object.gameName);
        return message;
    };

    /**
     * Creates a plain object from a BotGameInfo message. Also converts values to other types if specified.
     * @function toObject
     * @memberof BotGameInfo
     * @static
     * @param {BotGameInfo} message BotGameInfo
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    BotGameInfo.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.gameId = "";
            object.gameName = "";
        }
        if (message.gameId != null && message.hasOwnProperty("gameId"))
            object.gameId = message.gameId;
        if (message.gameName != null && message.hasOwnProperty("gameName"))
            object.gameName = message.gameName;
        return object;
    };

    /**
     * Converts this BotGameInfo to JSON.
     * @function toJSON
     * @memberof BotGameInfo
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    BotGameInfo.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for BotGameInfo
     * @function getTypeUrl
     * @memberof BotGameInfo
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    BotGameInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/BotGameInfo";
    };

    return BotGameInfo;
})();

export const BotAgentInfo = $root.BotAgentInfo = (() => {

    /**
     * Properties of a BotAgentInfo.
     * @exports IBotAgentInfo
     * @interface IBotAgentInfo
     * @property {number|null} [agentId] BotAgentInfo agentId
     * @property {string|null} [agentName] BotAgentInfo agentName
     */

    /**
     * Constructs a new BotAgentInfo.
     * @exports BotAgentInfo
     * @classdesc Represents a BotAgentInfo.
     * @implements IBotAgentInfo
     * @constructor
     * @param {IBotAgentInfo=} [properties] Properties to set
     */
    function BotAgentInfo(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * BotAgentInfo agentId.
     * @member {number} agentId
     * @memberof BotAgentInfo
     * @instance
     */
    BotAgentInfo.prototype.agentId = 0;

    /**
     * BotAgentInfo agentName.
     * @member {string} agentName
     * @memberof BotAgentInfo
     * @instance
     */
    BotAgentInfo.prototype.agentName = "";

    /**
     * Creates a new BotAgentInfo instance using the specified properties.
     * @function create
     * @memberof BotAgentInfo
     * @static
     * @param {IBotAgentInfo=} [properties] Properties to set
     * @returns {BotAgentInfo} BotAgentInfo instance
     */
    BotAgentInfo.create = function create(properties) {
        return new BotAgentInfo(properties);
    };

    /**
     * Encodes the specified BotAgentInfo message. Does not implicitly {@link BotAgentInfo.verify|verify} messages.
     * @function encode
     * @memberof BotAgentInfo
     * @static
     * @param {IBotAgentInfo} message BotAgentInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BotAgentInfo.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.agentId != null && Object.hasOwnProperty.call(message, "agentId"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.agentId);
        if (message.agentName != null && Object.hasOwnProperty.call(message, "agentName"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.agentName);
        return writer;
    };

    /**
     * Encodes the specified BotAgentInfo message, length delimited. Does not implicitly {@link BotAgentInfo.verify|verify} messages.
     * @function encodeDelimited
     * @memberof BotAgentInfo
     * @static
     * @param {IBotAgentInfo} message BotAgentInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BotAgentInfo.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a BotAgentInfo message from the specified reader or buffer.
     * @function decode
     * @memberof BotAgentInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {BotAgentInfo} BotAgentInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BotAgentInfo.decode = function decode(reader, length, error) {
    BotAgentInfo.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.BotAgentInfo();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.agentId = reader.int32();
                    break;
                }
            case 2: {
                    message.agentName = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a BotAgentInfo message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof BotAgentInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {BotAgentInfo} BotAgentInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BotAgentInfo.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a BotAgentInfo message.
     * @function verify
     * @memberof BotAgentInfo
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    BotAgentInfo.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.agentId != null && message.hasOwnProperty("agentId"))
            if (!$util.isInteger(message.agentId))
                return "agentId: integer expected";
        if (message.agentName != null && message.hasOwnProperty("agentName"))
            if (!$util.isString(message.agentName))
                return "agentName: string expected";
        return null;
    };

    /**
     * Creates a BotAgentInfo message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof BotAgentInfo
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {BotAgentInfo} BotAgentInfo
     */
    BotAgentInfo.fromObject = function fromObject(object) {
        if (object instanceof $root.BotAgentInfo)
            return object;
        let message = new $root.BotAgentInfo();
        if (object.agentId != null)
            message.agentId = object.agentId | 0;
        if (object.agentName != null)
            message.agentName = String(object.agentName);
        return message;
    };

    /**
     * Creates a plain object from a BotAgentInfo message. Also converts values to other types if specified.
     * @function toObject
     * @memberof BotAgentInfo
     * @static
     * @param {BotAgentInfo} message BotAgentInfo
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    BotAgentInfo.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.agentId = 0;
            object.agentName = "";
        }
        if (message.agentId != null && message.hasOwnProperty("agentId"))
            object.agentId = message.agentId;
        if (message.agentName != null && message.hasOwnProperty("agentName"))
            object.agentName = message.agentName;
        return object;
    };

    /**
     * Converts this BotAgentInfo to JSON.
     * @function toJSON
     * @memberof BotAgentInfo
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    BotAgentInfo.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for BotAgentInfo
     * @function getTypeUrl
     * @memberof BotAgentInfo
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    BotAgentInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/BotAgentInfo";
    };

    return BotAgentInfo;
})();

export const AwsConfig = $root.AwsConfig = (() => {

    /**
     * Properties of an AwsConfig.
     * @exports IAwsConfig
     * @interface IAwsConfig
     * @property {string|null} [accessKey] AwsConfig accessKey
     * @property {string|null} [secretKey] AwsConfig secretKey
     * @property {string|null} [regionName] AwsConfig regionName
     * @property {string|null} [sessionId] AwsConfig sessionId
     */

    /**
     * Constructs a new AwsConfig.
     * @exports AwsConfig
     * @classdesc Represents an AwsConfig.
     * @implements IAwsConfig
     * @constructor
     * @param {IAwsConfig=} [properties] Properties to set
     */
    function AwsConfig(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * AwsConfig accessKey.
     * @member {string} accessKey
     * @memberof AwsConfig
     * @instance
     */
    AwsConfig.prototype.accessKey = "";

    /**
     * AwsConfig secretKey.
     * @member {string} secretKey
     * @memberof AwsConfig
     * @instance
     */
    AwsConfig.prototype.secretKey = "";

    /**
     * AwsConfig regionName.
     * @member {string} regionName
     * @memberof AwsConfig
     * @instance
     */
    AwsConfig.prototype.regionName = "";

    /**
     * AwsConfig sessionId.
     * @member {string} sessionId
     * @memberof AwsConfig
     * @instance
     */
    AwsConfig.prototype.sessionId = "";

    /**
     * Creates a new AwsConfig instance using the specified properties.
     * @function create
     * @memberof AwsConfig
     * @static
     * @param {IAwsConfig=} [properties] Properties to set
     * @returns {AwsConfig} AwsConfig instance
     */
    AwsConfig.create = function create(properties) {
        return new AwsConfig(properties);
    };

    /**
     * Encodes the specified AwsConfig message. Does not implicitly {@link AwsConfig.verify|verify} messages.
     * @function encode
     * @memberof AwsConfig
     * @static
     * @param {IAwsConfig} message AwsConfig message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AwsConfig.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.accessKey != null && Object.hasOwnProperty.call(message, "accessKey"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.accessKey);
        if (message.secretKey != null && Object.hasOwnProperty.call(message, "secretKey"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.secretKey);
        if (message.regionName != null && Object.hasOwnProperty.call(message, "regionName"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.regionName);
        if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.sessionId);
        return writer;
    };

    /**
     * Encodes the specified AwsConfig message, length delimited. Does not implicitly {@link AwsConfig.verify|verify} messages.
     * @function encodeDelimited
     * @memberof AwsConfig
     * @static
     * @param {IAwsConfig} message AwsConfig message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AwsConfig.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an AwsConfig message from the specified reader or buffer.
     * @function decode
     * @memberof AwsConfig
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {AwsConfig} AwsConfig
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AwsConfig.decode = function decode(reader, length, error) {
    AwsConfig.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.AwsConfig();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 2: {
                    message.accessKey = reader.string();
                    break;
                }
            case 3: {
                    message.secretKey = reader.string();
                    break;
                }
            case 4: {
                    message.regionName = reader.string();
                    break;
                }
            case 5: {
                    message.sessionId = reader.string();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes an AwsConfig message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof AwsConfig
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {AwsConfig} AwsConfig
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AwsConfig.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an AwsConfig message.
     * @function verify
     * @memberof AwsConfig
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    AwsConfig.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.accessKey != null && message.hasOwnProperty("accessKey"))
            if (!$util.isString(message.accessKey))
                return "accessKey: string expected";
        if (message.secretKey != null && message.hasOwnProperty("secretKey"))
            if (!$util.isString(message.secretKey))
                return "secretKey: string expected";
        if (message.regionName != null && message.hasOwnProperty("regionName"))
            if (!$util.isString(message.regionName))
                return "regionName: string expected";
        if (message.sessionId != null && message.hasOwnProperty("sessionId"))
            if (!$util.isString(message.sessionId))
                return "sessionId: string expected";
        return null;
    };

    /**
     * Creates an AwsConfig message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof AwsConfig
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {AwsConfig} AwsConfig
     */
    AwsConfig.fromObject = function fromObject(object) {
        if (object instanceof $root.AwsConfig)
            return object;
        let message = new $root.AwsConfig();
        if (object.accessKey != null)
            message.accessKey = String(object.accessKey);
        if (object.secretKey != null)
            message.secretKey = String(object.secretKey);
        if (object.regionName != null)
            message.regionName = String(object.regionName);
        if (object.sessionId != null)
            message.sessionId = String(object.sessionId);
        return message;
    };

    /**
     * Creates a plain object from an AwsConfig message. Also converts values to other types if specified.
     * @function toObject
     * @memberof AwsConfig
     * @static
     * @param {AwsConfig} message AwsConfig
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    AwsConfig.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.accessKey = "";
            object.secretKey = "";
            object.regionName = "";
            object.sessionId = "";
        }
        if (message.accessKey != null && message.hasOwnProperty("accessKey"))
            object.accessKey = message.accessKey;
        if (message.secretKey != null && message.hasOwnProperty("secretKey"))
            object.secretKey = message.secretKey;
        if (message.regionName != null && message.hasOwnProperty("regionName"))
            object.regionName = message.regionName;
        if (message.sessionId != null && message.hasOwnProperty("sessionId"))
            object.sessionId = message.sessionId;
        return object;
    };

    /**
     * Converts this AwsConfig to JSON.
     * @function toJSON
     * @memberof AwsConfig
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    AwsConfig.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for AwsConfig
     * @function getTypeUrl
     * @memberof AwsConfig
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    AwsConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/AwsConfig";
    };

    return AwsConfig;
})();

/**
 * PageSort enum.
 * @exports PageSort
 * @enum {number}
 * @property {number} ASC=0 ASC value
 * @property {number} DESC=1 DESC value
 */
export const PageSort = $root.PageSort = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "ASC"] = 0;
    values[valuesById[1] = "DESC"] = 1;
    return values;
})();

/**
 * UploadChannelType enum.
 * @exports UploadChannelType
 * @enum {number}
 * @property {number} OSS_DEFAULT=0 OSS_DEFAULT value
 * @property {number} OSS_CHAT=1 OSS_CHAT value
 * @property {number} OSS_LOW_RATE=2 OSS_LOW_RATE value
 */
export const UploadChannelType = $root.UploadChannelType = (() => {
    const valuesById = {}, values = Object.create(valuesById);
    values[valuesById[0] = "OSS_DEFAULT"] = 0;
    values[valuesById[1] = "OSS_CHAT"] = 1;
    values[valuesById[2] = "OSS_LOW_RATE"] = 2;
    return values;
})();

export const LinkObj = $root.LinkObj = (() => {

    /**
     * Properties of a LinkObj.
     * @exports ILinkObj
     * @interface ILinkObj
     * @property {string|null} [link] LinkObj link
     * @property {number|null} [location] LinkObj location
     * @property {number|null} [length] LinkObj length
     */

    /**
     * Constructs a new LinkObj.
     * @exports LinkObj
     * @classdesc Represents a LinkObj.
     * @implements ILinkObj
     * @constructor
     * @param {ILinkObj=} [properties] Properties to set
     */
    function LinkObj(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * LinkObj link.
     * @member {string} link
     * @memberof LinkObj
     * @instance
     */
    LinkObj.prototype.link = "";

    /**
     * LinkObj location.
     * @member {number} location
     * @memberof LinkObj
     * @instance
     */
    LinkObj.prototype.location = 0;

    /**
     * LinkObj length.
     * @member {number} length
     * @memberof LinkObj
     * @instance
     */
    LinkObj.prototype.length = 0;

    /**
     * Creates a new LinkObj instance using the specified properties.
     * @function create
     * @memberof LinkObj
     * @static
     * @param {ILinkObj=} [properties] Properties to set
     * @returns {LinkObj} LinkObj instance
     */
    LinkObj.create = function create(properties) {
        return new LinkObj(properties);
    };

    /**
     * Encodes the specified LinkObj message. Does not implicitly {@link LinkObj.verify|verify} messages.
     * @function encode
     * @memberof LinkObj
     * @static
     * @param {ILinkObj} message LinkObj message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LinkObj.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.link != null && Object.hasOwnProperty.call(message, "link"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.link);
        if (message.location != null && Object.hasOwnProperty.call(message, "location"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.location);
        if (message.length != null && Object.hasOwnProperty.call(message, "length"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.length);
        return writer;
    };

    /**
     * Encodes the specified LinkObj message, length delimited. Does not implicitly {@link LinkObj.verify|verify} messages.
     * @function encodeDelimited
     * @memberof LinkObj
     * @static
     * @param {ILinkObj} message LinkObj message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LinkObj.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a LinkObj message from the specified reader or buffer.
     * @function decode
     * @memberof LinkObj
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {LinkObj} LinkObj
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LinkObj.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.LinkObj();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.link = reader.string();
                    break;
                }
            case 2: {
                    message.location = reader.int32();
                    break;
                }
            case 3: {
                    message.length = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a LinkObj message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof LinkObj
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {LinkObj} LinkObj
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LinkObj.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a LinkObj message.
     * @function verify
     * @memberof LinkObj
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    LinkObj.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.link != null && message.hasOwnProperty("link"))
            if (!$util.isString(message.link))
                return "link: string expected";
        if (message.location != null && message.hasOwnProperty("location"))
            if (!$util.isInteger(message.location))
                return "location: integer expected";
        if (message.length != null && message.hasOwnProperty("length"))
            if (!$util.isInteger(message.length))
                return "length: integer expected";
        return null;
    };

    /**
     * Creates a LinkObj message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof LinkObj
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {LinkObj} LinkObj
     */
    LinkObj.fromObject = function fromObject(object) {
        if (object instanceof $root.LinkObj)
            return object;
        let message = new $root.LinkObj();
        if (object.link != null)
            message.link = String(object.link);
        if (object.location != null)
            message.location = object.location | 0;
        if (object.length != null)
            message.length = object.length | 0;
        return message;
    };

    /**
     * Creates a plain object from a LinkObj message. Also converts values to other types if specified.
     * @function toObject
     * @memberof LinkObj
     * @static
     * @param {LinkObj} message LinkObj
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    LinkObj.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.link = "";
            object.location = 0;
            object.length = 0;
        }
        if (message.link != null && message.hasOwnProperty("link"))
            object.link = message.link;
        if (message.location != null && message.hasOwnProperty("location"))
            object.location = message.location;
        if (message.length != null && message.hasOwnProperty("length"))
            object.length = message.length;
        return object;
    };

    /**
     * Converts this LinkObj to JSON.
     * @function toJSON
     * @memberof LinkObj
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    LinkObj.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for LinkObj
     * @function getTypeUrl
     * @memberof LinkObj
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    LinkObj.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/LinkObj";
    };

    return LinkObj;
})();

export const FeeConfig = $root.FeeConfig = (() => {

    /**
     * Properties of a FeeConfig.
     * @exports IFeeConfig
     * @interface IFeeConfig
     * @property {number|null} [scenesType] FeeConfig scenesType
     * @property {string|null} [scenesName] FeeConfig scenesName
     * @property {Array.<ICoinFeeConfig>|null} [feeConfigs] FeeConfig feeConfigs
     */

    /**
     * Constructs a new FeeConfig.
     * @exports FeeConfig
     * @classdesc Represents a FeeConfig.
     * @implements IFeeConfig
     * @constructor
     * @param {IFeeConfig=} [properties] Properties to set
     */
    function FeeConfig(properties) {
        this.feeConfigs = [];
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * FeeConfig scenesType.
     * @member {number} scenesType
     * @memberof FeeConfig
     * @instance
     */
    FeeConfig.prototype.scenesType = 0;

    /**
     * FeeConfig scenesName.
     * @member {string} scenesName
     * @memberof FeeConfig
     * @instance
     */
    FeeConfig.prototype.scenesName = "";

    /**
     * FeeConfig feeConfigs.
     * @member {Array.<ICoinFeeConfig>} feeConfigs
     * @memberof FeeConfig
     * @instance
     */
    FeeConfig.prototype.feeConfigs = $util.emptyArray;

    /**
     * Creates a new FeeConfig instance using the specified properties.
     * @function create
     * @memberof FeeConfig
     * @static
     * @param {IFeeConfig=} [properties] Properties to set
     * @returns {FeeConfig} FeeConfig instance
     */
    FeeConfig.create = function create(properties) {
        return new FeeConfig(properties);
    };

    /**
     * Encodes the specified FeeConfig message. Does not implicitly {@link FeeConfig.verify|verify} messages.
     * @function encode
     * @memberof FeeConfig
     * @static
     * @param {IFeeConfig} message FeeConfig message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FeeConfig.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.scenesType != null && Object.hasOwnProperty.call(message, "scenesType"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.scenesType);
        if (message.scenesName != null && Object.hasOwnProperty.call(message, "scenesName"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.scenesName);
        if (message.feeConfigs != null && message.feeConfigs.length)
            for (let i = 0; i < message.feeConfigs.length; ++i)
                $root.CoinFeeConfig.encode(message.feeConfigs[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
        return writer;
    };

    /**
     * Encodes the specified FeeConfig message, length delimited. Does not implicitly {@link FeeConfig.verify|verify} messages.
     * @function encodeDelimited
     * @memberof FeeConfig
     * @static
     * @param {IFeeConfig} message FeeConfig message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FeeConfig.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a FeeConfig message from the specified reader or buffer.
     * @function decode
     * @memberof FeeConfig
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {FeeConfig} FeeConfig
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FeeConfig.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.FeeConfig();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 2: {
                    message.scenesType = reader.int32();
                    break;
                }
            case 3: {
                    message.scenesName = reader.string();
                    break;
                }
            case 4: {
                    if (!(message.feeConfigs && message.feeConfigs.length))
                        message.feeConfigs = [];
                    message.feeConfigs.push($root.CoinFeeConfig.decode(reader, reader.uint32()));
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a FeeConfig message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof FeeConfig
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {FeeConfig} FeeConfig
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FeeConfig.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a FeeConfig message.
     * @function verify
     * @memberof FeeConfig
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    FeeConfig.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.scenesType != null && message.hasOwnProperty("scenesType"))
            if (!$util.isInteger(message.scenesType))
                return "scenesType: integer expected";
        if (message.scenesName != null && message.hasOwnProperty("scenesName"))
            if (!$util.isString(message.scenesName))
                return "scenesName: string expected";
        if (message.feeConfigs != null && message.hasOwnProperty("feeConfigs")) {
            if (!Array.isArray(message.feeConfigs))
                return "feeConfigs: array expected";
            for (let i = 0; i < message.feeConfigs.length; ++i) {
                let error = $root.CoinFeeConfig.verify(message.feeConfigs[i]);
                if (error)
                    return "feeConfigs." + error;
            }
        }
        return null;
    };

    /**
     * Creates a FeeConfig message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof FeeConfig
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {FeeConfig} FeeConfig
     */
    FeeConfig.fromObject = function fromObject(object) {
        if (object instanceof $root.FeeConfig)
            return object;
        let message = new $root.FeeConfig();
        if (object.scenesType != null)
            message.scenesType = object.scenesType | 0;
        if (object.scenesName != null)
            message.scenesName = String(object.scenesName);
        if (object.feeConfigs) {
            if (!Array.isArray(object.feeConfigs))
                throw TypeError(".FeeConfig.feeConfigs: array expected");
            message.feeConfigs = [];
            for (let i = 0; i < object.feeConfigs.length; ++i) {
                if (typeof object.feeConfigs[i] !== "object")
                    throw TypeError(".FeeConfig.feeConfigs: object expected");
                message.feeConfigs[i] = $root.CoinFeeConfig.fromObject(object.feeConfigs[i]);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a FeeConfig message. Also converts values to other types if specified.
     * @function toObject
     * @memberof FeeConfig
     * @static
     * @param {FeeConfig} message FeeConfig
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    FeeConfig.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.arrays || options.defaults)
            object.feeConfigs = [];
        if (options.defaults) {
            object.scenesType = 0;
            object.scenesName = "";
        }
        if (message.scenesType != null && message.hasOwnProperty("scenesType"))
            object.scenesType = message.scenesType;
        if (message.scenesName != null && message.hasOwnProperty("scenesName"))
            object.scenesName = message.scenesName;
        if (message.feeConfigs && message.feeConfigs.length) {
            object.feeConfigs = [];
            for (let j = 0; j < message.feeConfigs.length; ++j)
                object.feeConfigs[j] = $root.CoinFeeConfig.toObject(message.feeConfigs[j], options);
        }
        return object;
    };

    /**
     * Converts this FeeConfig to JSON.
     * @function toJSON
     * @memberof FeeConfig
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    FeeConfig.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for FeeConfig
     * @function getTypeUrl
     * @memberof FeeConfig
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    FeeConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/FeeConfig";
    };

    return FeeConfig;
})();

export const CoinFeeConfig = $root.CoinFeeConfig = (() => {

    /**
     * Properties of a CoinFeeConfig.
     * @exports ICoinFeeConfig
     * @interface ICoinFeeConfig
     * @property {string|null} [coinName] CoinFeeConfig coinName
     * @property {string|null} [minFee] CoinFeeConfig minFee
     * @property {string|null} [maxFee] CoinFeeConfig maxFee
     * @property {string|null} [baseFee] CoinFeeConfig baseFee
     * @property {string|null} [feeRate] CoinFeeConfig feeRate
     * @property {string|null} [fixedFee] CoinFeeConfig fixedFee
     * @property {number|null} [mathType] CoinFeeConfig mathType
     * @property {number|null} [feeScale] CoinFeeConfig feeScale
     * @property {number|null} [roundingModeType] CoinFeeConfig roundingModeType
     */

    /**
     * Constructs a new CoinFeeConfig.
     * @exports CoinFeeConfig
     * @classdesc Represents a CoinFeeConfig.
     * @implements ICoinFeeConfig
     * @constructor
     * @param {ICoinFeeConfig=} [properties] Properties to set
     */
    function CoinFeeConfig(properties) {
        if (properties)
            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null)
                    this[keys[i]] = properties[keys[i]];
    }

    /**
     * CoinFeeConfig coinName.
     * @member {string} coinName
     * @memberof CoinFeeConfig
     * @instance
     */
    CoinFeeConfig.prototype.coinName = "";

    /**
     * CoinFeeConfig minFee.
     * @member {string} minFee
     * @memberof CoinFeeConfig
     * @instance
     */
    CoinFeeConfig.prototype.minFee = "";

    /**
     * CoinFeeConfig maxFee.
     * @member {string} maxFee
     * @memberof CoinFeeConfig
     * @instance
     */
    CoinFeeConfig.prototype.maxFee = "";

    /**
     * CoinFeeConfig baseFee.
     * @member {string} baseFee
     * @memberof CoinFeeConfig
     * @instance
     */
    CoinFeeConfig.prototype.baseFee = "";

    /**
     * CoinFeeConfig feeRate.
     * @member {string} feeRate
     * @memberof CoinFeeConfig
     * @instance
     */
    CoinFeeConfig.prototype.feeRate = "";

    /**
     * CoinFeeConfig fixedFee.
     * @member {string} fixedFee
     * @memberof CoinFeeConfig
     * @instance
     */
    CoinFeeConfig.prototype.fixedFee = "";

    /**
     * CoinFeeConfig mathType.
     * @member {number} mathType
     * @memberof CoinFeeConfig
     * @instance
     */
    CoinFeeConfig.prototype.mathType = 0;

    /**
     * CoinFeeConfig feeScale.
     * @member {number} feeScale
     * @memberof CoinFeeConfig
     * @instance
     */
    CoinFeeConfig.prototype.feeScale = 0;

    /**
     * CoinFeeConfig roundingModeType.
     * @member {number} roundingModeType
     * @memberof CoinFeeConfig
     * @instance
     */
    CoinFeeConfig.prototype.roundingModeType = 0;

    /**
     * Creates a new CoinFeeConfig instance using the specified properties.
     * @function create
     * @memberof CoinFeeConfig
     * @static
     * @param {ICoinFeeConfig=} [properties] Properties to set
     * @returns {CoinFeeConfig} CoinFeeConfig instance
     */
    CoinFeeConfig.create = function create(properties) {
        return new CoinFeeConfig(properties);
    };

    /**
     * Encodes the specified CoinFeeConfig message. Does not implicitly {@link CoinFeeConfig.verify|verify} messages.
     * @function encode
     * @memberof CoinFeeConfig
     * @static
     * @param {ICoinFeeConfig} message CoinFeeConfig message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CoinFeeConfig.encode = function encode(message, writer) {
        if (!writer)
            writer = $Writer.create();
        if (message.coinName != null && Object.hasOwnProperty.call(message, "coinName"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.coinName);
        if (message.minFee != null && Object.hasOwnProperty.call(message, "minFee"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.minFee);
        if (message.maxFee != null && Object.hasOwnProperty.call(message, "maxFee"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.maxFee);
        if (message.baseFee != null && Object.hasOwnProperty.call(message, "baseFee"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.baseFee);
        if (message.feeRate != null && Object.hasOwnProperty.call(message, "feeRate"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.feeRate);
        if (message.fixedFee != null && Object.hasOwnProperty.call(message, "fixedFee"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.fixedFee);
        if (message.mathType != null && Object.hasOwnProperty.call(message, "mathType"))
            writer.uint32(/* id 7, wireType 0 =*/56).int32(message.mathType);
        if (message.feeScale != null && Object.hasOwnProperty.call(message, "feeScale"))
            writer.uint32(/* id 8, wireType 0 =*/64).int32(message.feeScale);
        if (message.roundingModeType != null && Object.hasOwnProperty.call(message, "roundingModeType"))
            writer.uint32(/* id 9, wireType 0 =*/72).int32(message.roundingModeType);
        return writer;
    };

    /**
     * Encodes the specified CoinFeeConfig message, length delimited. Does not implicitly {@link CoinFeeConfig.verify|verify} messages.
     * @function encodeDelimited
     * @memberof CoinFeeConfig
     * @static
     * @param {ICoinFeeConfig} message CoinFeeConfig message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CoinFeeConfig.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CoinFeeConfig message from the specified reader or buffer.
     * @function decode
     * @memberof CoinFeeConfig
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {CoinFeeConfig} CoinFeeConfig
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CoinFeeConfig.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length, message = new $root.CoinFeeConfig();
        while (reader.pos < end) {
            let tag = reader.uint32();
            if (tag === error)
                break;
            switch (tag >>> 3) {
            case 1: {
                    message.coinName = reader.string();
                    break;
                }
            case 2: {
                    message.minFee = reader.string();
                    break;
                }
            case 3: {
                    message.maxFee = reader.string();
                    break;
                }
            case 4: {
                    message.baseFee = reader.string();
                    break;
                }
            case 5: {
                    message.feeRate = reader.string();
                    break;
                }
            case 6: {
                    message.fixedFee = reader.string();
                    break;
                }
            case 7: {
                    message.mathType = reader.int32();
                    break;
                }
            case 8: {
                    message.feeScale = reader.int32();
                    break;
                }
            case 9: {
                    message.roundingModeType = reader.int32();
                    break;
                }
            default:
                reader.skipType(tag & 7);
                break;
            }
        }
        return message;
    };

    /**
     * Decodes a CoinFeeConfig message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof CoinFeeConfig
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {CoinFeeConfig} CoinFeeConfig
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CoinFeeConfig.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CoinFeeConfig message.
     * @function verify
     * @memberof CoinFeeConfig
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CoinFeeConfig.verify = function verify(message) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (message.coinName != null && message.hasOwnProperty("coinName"))
            if (!$util.isString(message.coinName))
                return "coinName: string expected";
        if (message.minFee != null && message.hasOwnProperty("minFee"))
            if (!$util.isString(message.minFee))
                return "minFee: string expected";
        if (message.maxFee != null && message.hasOwnProperty("maxFee"))
            if (!$util.isString(message.maxFee))
                return "maxFee: string expected";
        if (message.baseFee != null && message.hasOwnProperty("baseFee"))
            if (!$util.isString(message.baseFee))
                return "baseFee: string expected";
        if (message.feeRate != null && message.hasOwnProperty("feeRate"))
            if (!$util.isString(message.feeRate))
                return "feeRate: string expected";
        if (message.fixedFee != null && message.hasOwnProperty("fixedFee"))
            if (!$util.isString(message.fixedFee))
                return "fixedFee: string expected";
        if (message.mathType != null && message.hasOwnProperty("mathType"))
            if (!$util.isInteger(message.mathType))
                return "mathType: integer expected";
        if (message.feeScale != null && message.hasOwnProperty("feeScale"))
            if (!$util.isInteger(message.feeScale))
                return "feeScale: integer expected";
        if (message.roundingModeType != null && message.hasOwnProperty("roundingModeType"))
            if (!$util.isInteger(message.roundingModeType))
                return "roundingModeType: integer expected";
        return null;
    };

    /**
     * Creates a CoinFeeConfig message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof CoinFeeConfig
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {CoinFeeConfig} CoinFeeConfig
     */
    CoinFeeConfig.fromObject = function fromObject(object) {
        if (object instanceof $root.CoinFeeConfig)
            return object;
        let message = new $root.CoinFeeConfig();
        if (object.coinName != null)
            message.coinName = String(object.coinName);
        if (object.minFee != null)
            message.minFee = String(object.minFee);
        if (object.maxFee != null)
            message.maxFee = String(object.maxFee);
        if (object.baseFee != null)
            message.baseFee = String(object.baseFee);
        if (object.feeRate != null)
            message.feeRate = String(object.feeRate);
        if (object.fixedFee != null)
            message.fixedFee = String(object.fixedFee);
        if (object.mathType != null)
            message.mathType = object.mathType | 0;
        if (object.feeScale != null)
            message.feeScale = object.feeScale | 0;
        if (object.roundingModeType != null)
            message.roundingModeType = object.roundingModeType | 0;
        return message;
    };

    /**
     * Creates a plain object from a CoinFeeConfig message. Also converts values to other types if specified.
     * @function toObject
     * @memberof CoinFeeConfig
     * @static
     * @param {CoinFeeConfig} message CoinFeeConfig
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CoinFeeConfig.toObject = function toObject(message, options) {
        if (!options)
            options = {};
        let object = {};
        if (options.defaults) {
            object.coinName = "";
            object.minFee = "";
            object.maxFee = "";
            object.baseFee = "";
            object.feeRate = "";
            object.fixedFee = "";
            object.mathType = 0;
            object.feeScale = 0;
            object.roundingModeType = 0;
        }
        if (message.coinName != null && message.hasOwnProperty("coinName"))
            object.coinName = message.coinName;
        if (message.minFee != null && message.hasOwnProperty("minFee"))
            object.minFee = message.minFee;
        if (message.maxFee != null && message.hasOwnProperty("maxFee"))
            object.maxFee = message.maxFee;
        if (message.baseFee != null && message.hasOwnProperty("baseFee"))
            object.baseFee = message.baseFee;
        if (message.feeRate != null && message.hasOwnProperty("feeRate"))
            object.feeRate = message.feeRate;
        if (message.fixedFee != null && message.hasOwnProperty("fixedFee"))
            object.fixedFee = message.fixedFee;
        if (message.mathType != null && message.hasOwnProperty("mathType"))
            object.mathType = message.mathType;
        if (message.feeScale != null && message.hasOwnProperty("feeScale"))
            object.feeScale = message.feeScale;
        if (message.roundingModeType != null && message.hasOwnProperty("roundingModeType"))
            object.roundingModeType = message.roundingModeType;
        return object;
    };

    /**
     * Converts this CoinFeeConfig to JSON.
     * @function toJSON
     * @memberof CoinFeeConfig
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CoinFeeConfig.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CoinFeeConfig
     * @function getTypeUrl
     * @memberof CoinFeeConfig
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CoinFeeConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
            typeUrlPrefix = "type.googleapis.com";
        }
        return typeUrlPrefix + "/CoinFeeConfig";
    };

    return CoinFeeConfig;
})();

export { $root as default };
